# Production upstream token storage verified

1,739 ID tests pass with zero failures, 20,506 assertions and 100% line/function coverage (`/private/tmp/id-upstream-storage-verified-full.log`, 313.15 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-upstream-storage-verified-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-upstream-storage-build.log`, 17.645 seconds). Production createAuth now uses dedicated versioned account transforms for access/refresh/ID tokens. Unit, real adapter and restricted native SSO tests cover encrypted first/repeat login, null imports, partial/bulk/transaction writes, rollback with expected-value read-back, old/new key rotation and refusal of missing keys/plaintext/corruption. The synthetic transform fixture and cloned provider setup were removed. The only first full-run failure was three generated OpenAPI readOnly annotations; exporter and drift checks fixed it. Earlier wrong error-form test expectations and the strengthened storage assertion are recorded in the custody/execution reports. No production key provisioning, legacy conversion, migration or deployment occurred. Next: explicit legacy state, bounded repeat-safe conversion and interruption/restore proofs. The complete F0–F7 goal remains active.

# Enterprise foundation planning progress

## Execution: provider seam and initial machine identity

Execution is now authorised under the active goal. F0 and F1 are partially implemented; F2–F7 remain pending. The planning notes below are historical.

- Added provider integration evidence using Better Auth 1.7.2's supported `authenticateClient`/`issueTokens` API and transaction-bound adapter. Six tests cover successful extension writes, policy/signing rollback, rejected credentials, single-use assertion consumption and the machine-only request boundary. A generic whole-endpoint transaction wrapper was deleted after review because denied issuance must not roll back authentication replay markers.
- Added immutable machine claim/version verification and positive version column migration. Five token-identity tests first reproduced failures, then passed: claims, owner mutation, client-ID recreation, disable/re-enable and rotation. A sixth regression first proved resource custom claims could replace house identity; the database now rejects those keys while allowing ordinary claims.
- OpenAPI was regenerated through the export script. Schema/README/specification distinguish this partial implementation from the remaining foundation contracts.
- Earlier provider-only full run: 1,150 ID tests passed with 100% coverage; root typecheck/lint/build and 54 web tests passed. First identity full run: 1,154 passed, one generated OpenAPI snapshot mismatch; regenerated subsequently. Final current-slice gates are running; do not treat these earlier counts as validation of the final tree.

Administrative ownership changes now fail with `ownership_conflict`; unchanged owner requests return the registration and record `client.owner_unchanged` with `changed: false`. This includes denying initial ownership adoption of an unowned registration. Service and HTTP tests were updated from the former transfer contract. The subsequent full run passed 1,157 ID tests at 100% coverage and all root gates, including 54 web tests. The initial web build sandbox port-binding failure was resolved by rerunning the build with the required permission.

A subsequent regression reproduced stale admin authority after resource unlink. The principal query now joins the client-resource link and enabled resource, and the scope calculation intersects the current resource ceiling. This uses one existing database query, avoiding a separate policy read. Final gates for this addition passed: 1,158 ID tests, 100% application line/function coverage; 54 web tests; root typecheck, lint and build; `git diff --check`. Logs are `/private/tmp/id-resource-final-{tests,typecheck,lint,build,web}.log`. No production database migration or release was performed.

## Execution: database identity guards

The previous goal turn made concrete progress: strict token/resource checks and all repository gates passed. This continuation adds migration `0005_immutable_security_identity` and direct SQL regressions. The three tests first failed: identity could change, authorisation version could decrease, and credential changes did not advance it.

PostgreSQL UPDATE triggers now reject changes to client instance/public ID/owner and resource instance/public identifier, reject decreasing client versions, and advance the version on disable or changes to secret digest, JWK set, JWK URL or authentication method. Unchanged credentials, display edits and re-enable do not advance the version. Explicit unowned-to-owned adoption is also tested.

First-principles review: removed the unused ownership-assignment query and its custom exception; deleted service/query version-increment logic because the database now enforces the same rule across administrative, provider and raw writes. No parallel ownership store or second version counter was introduced. Tests that depended on transferring ownership now create their intended fixtures directly.

Validation: 1,161 ID tests passed with 100% line/function coverage; root typecheck, lint and build and 54 web tests passed. Rebuilding the explicitly disposable test schema and replaying every migration also succeeded. The final full suite on that fresh schema also passed: 1,161 tests, 7,974 assertions and 100% application line/function coverage, including the added unowned-adoption assertion. Evidence: `/private/tmp/id-db-identity-fresh-tests.log` and `/private/tmp/id-db-identity-{migration,typecheck,lint,build,web}.log`. No production database was migrated.

## Execution: permanent identifier reservations

The previous goal turn made concrete progress: database identity/version guards, fresh migration rehearsal and repository gates passed. The next regression reproduced reuse of an erased client identifier. Migration 0006 introduces one three-column `security_identifiers` table with unique public identifiers and instance UUIDs per client/resource namespace. It backfills current records and reserves new records with AFTER INSERT triggers in the same transaction. UPDATE/DELETE of reservations are rejected; erasure of live records retains reservations. Rolled-back creation leaves none. No secrets, owner copies or live-record foreign keys are stored.

Tests cover public identifier and instance reuse, resource reuse, rollback, immutable reservation records, and concurrent creation with a two-connection pool. Test fixture resets now explicitly clear this durable table alongside their domain tables. The initial full run passed 1,163 tests with one test-harness failure (Bun's rejection matcher needed a native Promise around Drizzle's lazy raw query); that harness was corrected. Final fresh-schema gates passed: 1,165 ID tests, 7,988 assertions and 100% application line/function coverage; root typecheck/lint/build and 54 web tests. The final public documentation build also passed. Logs: `/private/tmp/id-reservation-final-{tests,typecheck,lint,build,web}.log`, `/private/tmp/id-reservation-migration.log` and `/private/tmp/id-reservation-doc-{build,web}.log`.

The API maps reservation uniqueness failures to `409 identifier_reserved`; generated OpenAPI and public error guidance were updated. First-principles review kept one unique-constraint-based reservation mechanism, with no separate service lookup, advisory-lock protocol, timestamps, credential copies or background worker. Unrelated formatting changes were removed.

Migration limit: only current records can be backfilled from this database. Identifiers erased before the migration require an explicit historical inventory before production cutover; the migration cannot reconstruct missing history.

## Execution: immutable system bindings

The previous goal turn made concrete progress: permanent reservations, fresh migrations and all repository gates passed. Three new bootstrap regressions then reproduced name adoption and platform reassignment after a configured slug change.

Migration 0007 adds the immutable singleton `system_bindings` row for the platform organisation, admin resource and admin group. Restrictive and composite foreign keys protect these records and the group's owner. Bootstrap uses the binding after first creation and refuses existing names when no reviewed binding exists. It also rejects a changed admin audience. The migration deliberately performs no name-derived backfill.

Added `scripts/bind-system.ts` and the transactional `bindExistingSystem` service for existing deployments. Operators supply reviewed UUIDs; the service validates references/group ownership/audience, permits identical repeats, refuses replacement, and rolls binding creation back if audit insertion fails. No production import was executed.

Platform authorisation, root lockout and platform summaries now use the persisted organisation ID. Grants expose a database-derived `isPlatform`; the old slug lookups were deleted. The HTTP regression renames the platform, gives its former slug and platform-named scopes to a tenant, then proves that human/machine platform access still works, tenant platform access is denied, and root remains locked. Query/HTTP contracts, generated OpenAPI and documentation were updated.

First-principles review retained one binding row and the existing bootstrap transaction lock. No second authority cache, name alias system, or transfer API was added. The CLI is an explicit migration path rather than automatic adoption of existing records.

Validation after a fresh disposable-schema migration: 1,171 ID tests, 8,018 assertions, 100% application line/function coverage; 54 web tests; root typecheck, lint and build; `git diff --check`. Logs: `/private/tmp/id-system-final-{migration,tests,typecheck,lint,build,web}.log`.

F1's identity mechanisms are implemented, but its minimal issuance audit assertions and final slice audit remain pending. F0 still needs the tenant/offboarding regressions delivered with their behaviour changes. Durable audit/journal, tenant lifecycle isolation, capability ceilings and the full release gates remain unfinished; do not mark the overall goal complete.

## Execution: durable audit subject history

The previous goal turn made concrete progress with immutable system bindings and verified gates. This turn's regression reproduced loss of person history immediately after membership deletion.

Migration 0008 removes the audit tenant FK that nulled historical attribution and adds `audit_event_subjects`. Audit insertion captures actor/target references and related users for existing member/group-member/session event shapes in the same SQL statement. Person-history reads now use indexed retained references instead of live membership joins. User/organisation history remains queryable after erasure; unknown IDs without history still return 404. Organisation erasure itself retains its tenant UUID.

Backfill derives only available references and labels them `legacy_derived`. A migration-path test proves a removed membership with no user evidence gains no invented user reference. A subject-write fault test proves the event insertion rolls back. Full before/after/effect manifests, typed event envelope, append-only runtime permissions and token-issuance audit are still pending.

First-principles review removed the live membership/session lookup branches from the history query and the organisation service's special null-attribution erasure flag. One atomic database capture path avoids an extra application transaction per event. Test resets now explicitly clear audit rows where they previously relied on the removed FK's TRUNCATE cascade; ordinary erasure preserves records.

Fresh migration and final gates passed: 1,174 ID tests, 8,024 assertions, 100% application line/function coverage; 54 web tests; root typecheck/lint/build and `git diff --check`. Evidence: `/private/tmp/id-audit-history-final-{tests,typecheck,lint,build,web}.log` and `/private/tmp/id-audit-history-migration.log`. Docs and generated OpenAPI now describe retained-history reads. No production migration or erasure was executed.

Planning-only task. Read the existing evidence report, design and source. Read planning-with-files skill and checked session catchup (no recovered changes). Consulted Lee Robinson's documentation principles, PostgreSQL row security documentation and OAuth security BCP. Asked one optional product question about identity retention after erasure.

Completed the proposed specification at docs/05-id-enterprise-foundation.md, covering F0–F7 slices, explicit acceptance tests, migration/rollback, provider seams and documentation requirements. Linked it from docs/02-plan.md and docs/03-answerable-id.md. Updated docs/04-answerable-id-schema.md to distinguish observed machine issuance/admin checks and known audit/session limitations from planned guarantees. No runtime behaviour changed.

Validation complete: all relative file/anchor links in the four touched design documents resolved; git diff --check passed. The specification includes explicit proposed status and does not claim new tests pass. Existing 1,144-pass result is historical evidence from the review. Planning completed; implementation remains pending. Identity retention is an explicit product gate, not silently assumed.

## Execution: restricted runtime database role

Migration 0009 makes subject/reservation capture run through owner-executed triggers with fixed search paths and revokes direct invocation. Added transactional runtime-role provisioning and a production startup guard. Migration tooling now uses DATABASE_MIGRATION_URL independently of authentication settings; the application uses DATABASE_URL. Provisioning intentionally leaves login credentials to deployment tooling and must be repeated after migrations add tables.

An actual separate test login proves bootstrap, client/resource creation, token issuance and admin reads work. Negative tests deny audit mutation/truncation, direct subject/reservation writes, trigger alteration, schema creation, capture-function invocation and owner-role switching. Provisioning rejects privileged and owning roles. Startup refuses unsafe permissions before seed/listen and closes the pool.

First-principles review shared role-attribute and ownership predicates between provisioning and startup and removed migration tooling's dependency on the complete authentication environment. No runtime superuser fallback or role-switching protocol was added. This protects evidence against the runtime credential, not database administrators, and does not implement tenant RLS.

Fresh migration and final gates passed: 1,178 ID tests, 8,048 assertions, 100% application line/function coverage; 54 web tests; root typecheck/lint/build and git diff --check. Logs: /private/tmp/id-runtime-role-final-{tests,typecheck,lint,build,web}.log and /private/tmp/id-runtime-role-migration.log. README/schema/specification now describe the migration credential transition and deployment steps. No production migration or permission change was executed. The overall F0–F7 goal remains active and incomplete.

## Execution: transactional operation journal primitive

The previous goal turn made progress by validating the runtime-role slice and correcting its deployment documentation. Migration 0010 adds permanent completed-operation reservations, unique by actor instance, authority scope, operation name and key digest. The record stores the canonical request fingerprint, applied/no-op outcome, status and result reference, with no live identity foreign keys or secret payloads. Database triggers prevent completed-record changes; runtime permissions additionally deny truncation.

executeOperation takes a transaction advisory lock on the scoped key, invokes the caller's current-authority check, then replays an identical completed command or runs the mutation and records its result in the same transaction. Concurrent duplicates receive retryable operation_in_progress; different-input reuse receives non-retryable idempotency_key_reused. The primitive never persists a running row. Tests cover canonical object order, isolated actor/tenant/operation namespaces, revoked replay, immutable records, concurrent connections, and rollback of a separate data change, audit event and reservation on mutation/journal failure.

First-principles review retained a single transaction and key table. No background worker, lease, checkpoint state or plaintext response store was introduced. Domain defaults/set ordering and transactional authority/lock policy belong to the typed calling service. HTTP adoption, operation-status reads, encrypted secret-result recovery and revisions are still required; this helper alone does not make the administrative API idempotent.

The first full suite exposed three expected catalog-contract mismatches for the added table; explicit column/constraint/table assertions were updated. Final fresh-schema validation passed: 1,182 ID tests, 8,080 assertions, 100% application line/function coverage; root typecheck/lint/build and 54 web tests passed. Evidence: /private/tmp/id-operations-verified-{tests,typecheck,lint}.log, /private/tmp/id-operations-final-{build,web}.log and /private/tmp/id-operations-migration.log. No production migration or release was performed. The full F0–F7 goal remains active.

## Execution: operation-status read boundary

The previous goal turn made progress with a verified transactional journal primitive. This continuation adds GET /operations/{operationId} for platform audit readers and GET /organizations/{organizationId}/operations/{operationId} for the same tenant actor or a platform audit reader. Queries filter actor/scope in SQL and return only outcome, original status, timestamp and result reference. Request/key digests and any response secrets are excluded. Reads do not depend on a live target. Missing and inaccessible operations return not_found; that is not a claim that no concurrent transaction exists.

HTTP tests cover original actor, another actor, another tenant, platform reader, platform machine, tenant machine, invalid/unknown IDs and membership expiry, in addition to the generated negative route suite. The tenant machine uses an actually issued client-credentials token. Actor names reuse actorFromContext; client public identifiers are immutable and permanently reserved, and user actors use their stable UUIDs. No second principal parser was retained.

First-principles review removed the duplicate user/client/root identity switch and retained one explicit response projection. No polling worker or artificial running/failed journal state was added. Administrative writes still do not use the journal; encrypted result recovery, revisions and all mutation-family integration remain required.

OpenAPI was regenerated with the export command and the approved path inventory updated. Public management guidance and implementation/specification docs describe the new read boundary and its limits. Final verification passed: 1,203 ID tests, 8,198 assertions, 100% application line/function coverage; root typecheck/lint/build, 54 web tests and git diff --check. Evidence: /private/tmp/id-operation-status-verified-tests.log, /private/tmp/id-operation-status-final-{typecheck,lint}.log and /private/tmp/id-operation-status-{build,web}.log. No production change was made. The full foundation goal remains active.

## Execution: encrypted operation result recovery

The previous goal turn made progress with verified status-read isolation. This continuation adds operation-bound JWE encryption using the existing JOSE dependency and dedicated versioned 256-bit key inputs. Tests started with a missing implementation, then proved exact JSON recovery, random ciphertext, key rotation, wrong-operation rejection, tamper/wrong-key rejection, malformed input and a 1 MiB plaintext bound. Non-finite JSON response numbers are rejected so serialisation cannot silently change the result.

Migration 0011 adds replay_expires_at to the immutable reservation and an independent admin_operation_results ciphertext row. executeOperation can now commit encrypted result storage with the local mutation/audit/journal and recover the original JSON after fresh authorisation. Secret responses are available for at most 24 hours; ordinary responses for seven days. Missing/expired ciphertext returns non-retryable operation_result_expired without releasing or rerunning the key. Missing configuration or decryption failure returns retryable operation_replay_unavailable. Encryption failure rolls back the journal, audit and a separate data mutation. Runtime permissions deny ciphertext mutation/deletion/truncation.

Status reads expose stored-payload availability and expiry without decrypting data. Public schema, generated OpenAPI and docs were updated. Availability does not establish key availability. Physical payload purging, deployment key configuration and actual administrative mutation integration remain pending; encrypted payloads must not be described as physically purged merely because replay expires.

First-principles review used the installed JOSE implementation instead of custom cryptography and separated purgeable ciphertext from permanent keys rather than weakening reservation immutability. No worker, key service or automatic rotation infrastructure was added. Final fresh-schema validation passed: 1,208 ID tests, 8,240 assertions, 100% application line/function coverage; root typecheck/lint/build and 54 web tests passed, as did git diff --check. Evidence: /private/tmp/id-operation-recovery-{migration,tests,typecheck,lint,build,web}.log. No production migration, credential provisioning or release was performed. The overall F0–F7 goal remains active.

## Execution: replay deployment configuration and restricted retention

The previous goal turn made progress with verified encrypted replay. This continuation adds OPERATION_REPLAY_CONFIG with dedicated versioned keys. Environment loading validates structure, canonical 256-bit key encoding and the active key without echoing values in errors. A regression first showed the configuration was ignored, then passed after implementation. Configuration remains optional while mutation replay is not exposed.

Migration 0012 adds one fixed-search-path SECURITY DEFINER purge function. It checks a 1–1,000 batch bound, uses database-time expiry, skips locked ciphertext, and commits exact removed IDs/count to operation.results_purged atomically with deletion. Empty runs create no event. Permanent keys and fresh payloads are never removed. A real restricted retention login cannot read or mutate tables directly; the runtime cannot invoke retention. Collision of the audit ID reproduces audit failure and proves deletion rollback; successive calls prove bounded cleanup without releasing reservations.

Role provisioning shares the existing ownership/privilege checks. Added configure-retention-role.ts using migration credentials and purge-operation-results.ts using separate retention credentials. The latter runs one bounded batch. No scheduler, worker service, new cryptography or automatic key provisioning was introduced. Docs explain role setup, scheduling, separate secret storage and the distinction between replay expiry and physical deletion. Production provisioning/scheduling remain unexecuted.

The first database approval review timed out without a safety finding. Its permitted single retry succeeded. Final fresh-schema validation passed: 1,211 ID tests, 8,273 assertions, 100% application line/function coverage; root typecheck/lint/build and 54 web tests passed, as did git diff --check. Evidence: /private/tmp/id-retention-{migration,tests,typecheck,lint,build,web}.log. No production migration or credential change was made. The full F0–F7 goal remains active.

## Execution: client creation and rotation replay

The previous goal turn made progress with verified retention/key configuration. The HTTP regression first reproduced a conflict on repeated client creation. Client creation and secret rotation now require Idempotency-Key and configured replay encryption. They normalise validated scope sets/defaulted redirect arrays, retain platform-scoped actor/key fingerprints, and recover the exact original JSON for 24 hours. The response carries Operation-Id, Idempotency-Replayed and Cache-Control: no-store. CORS permits the request key and exposes the response headers.

Both services can receive the existing command transaction; their transaction/savepoint work, audit and encrypted result commit together. The command callback re-evaluates current root lockout, user/session authority or verified machine identity/version/current ceilings before replay or mutation. User/session rows are held with shared locks. A common grant/revocation ordering protocol across every security mutation is still F4 work; these checks do not prove that larger concurrency boundary.

HTTP tests prove canonical duplicate creation, unchanged secret/version on rotation replay, concurrent two-connection creation, changed-target conflicts, missing-key/configuration errors, purged-result refusal, audit-failure rollback and retry, creation replay after actual client deletion without recreation, and denial after session revocation. Direct transaction tests reject stale user, root and machine authority. The response is checked against the strict public client schema, which now includes its existing authorisation version field.

First-principles review reuses the service mutations and existing actor mapping. It verifies credentials once and carries verified bearer claims to the transactional check without parsing another token. The complete shared grant evaluator remains F5 work. Response serialisation is shared with encrypted recovery. No generic HTTP interception or replay of OAuth grants was added.

Generated OpenAPI, management/onboarding examples, README and implementation docs describe the required header/configuration transition and limits. The first full suite found the old CORS header assertion; it was updated, along with browser response-header assertions. Final validation passed: 1,217 ID tests, 8,313 assertions, 100% application line/function coverage; root typecheck/lint/build and 54 web tests passed, as did git diff --check. Evidence: /private/tmp/id-client-replay-verified-{tests,typecheck,lint}.log, /private/tmp/id-client-replay-final-build.log and /private/tmp/id-client-replay-web.log. No production release was performed. All other write families, configuration revisions, audit envelopes/effect manifests and tenant isolation remain unfinished.

## Execution: operation-linked audit envelopes and client snapshots

The previous goal turn made progress with verified client creation/rotation replay. The new regression first showed their audit events lacked the operation ID. Migration 0013 adds an indexed nullable operation_id and a deferred FK to the permanent journal. Events may precede their operation inside one transaction, but an orphan rejects commit and rolls back its local data/audit. Existing records retain null links. Envelope schema_version is 0 for legacy rows and defaults to 1 for new writes. A test executes the committed migration SQL in an isolated schema with a legacy row, proving both defaults without invented links.

The command wrapper passes an explicit operation-bearing Actor to the two journalled client services. Audit API responses expose the link/version and platform/organisation queries can filter by operationId. Client creation/update audit data now uses selected security snapshots instead of copying raw input/patches; JWK configuration and credential material are omitted. requestedFields records patch intent without claiming each submitted value changed. Rotation records actual before/after authorisation versions and credential-change evidence. These records retain their owning tenant UUID.

First-principles review deleted the broad audit input copies and uses a database FK instead of application lookups or timestamp correlation. No ambient operation context or separate effect table was added. Full policy/removal manifests and the remaining action-specific payload contracts remain pending.

The first full run found two fixtures performing DDL while deferred FK checks were pending, plus an expected envelope-shape assertion. The fixtures now flush constraints before DDL cleanup, and the shape assertion includes the new fields. The next full run encountered transient connection/hook timeouts in previously passing identity/schema tests. Database inspection found no blocking queries and connections subsequently responded promptly; that process was allowed to finish before a tests-only retry. No test timeout or production setting was changed.

The unstable run ended with timeout/pool errors. Approval review for the fresh retry timed out before execution; the explicitly permitted single retry succeeded. Final fresh-schema validation passed: 1,221 ID tests, 8,335 assertions, 100% application line/function coverage; root typecheck/lint/build, 54 web tests and git diff --check passed. Evidence: /private/tmp/id-operation-audit-final-{migration,tests}.log, /private/tmp/id-operation-audit-verified-{typecheck,lint}.log and /private/tmp/id-operation-audit-{build,web}.log. No production migration or release was performed. The full F0–F7 goal remains active.

## Execution: client configuration revisions and PATCH recovery

The next regression reproduced a missing ETag on client reads. Migration 0014 adds a positive, database-controlled revision; changed client rows advance it after the identity/version guard. Resource-link creation, deletion and target movement touch affected clients, including resource-deletion cascades. Duplicate links and metadata-only link updates leave the public configuration revision unchanged. GET takes a shared client lock while reading a stable sorted resource list, so its tag and representation belong together.

Client PATCH now requires Idempotency-Key and a strong If-Match instance/revision tag. The journal rechecks authority and recovers committed results before invoking the stale revision check. New stale writes return 412; missing and malformed preconditions return 428/400. A meaningful change advances the revision; an unchanged patch skips UPDATE, records client.update_unchanged with retained tenant/operation attribution, and commits a noop result. Ordinary patch responses recover for seven days. OpenAPI, browser CORS headers and operator/public documentation describe this compatibility change and its boundaries.

Tests exercise concurrent competing keys (one commit, one stale rejection), old-tag replay, unrelated/weak/missing/malformed tags, unchanged timestamps/revisions, noop audit attribution/retention, resource-link ETags, raw SQL changes and forged revision writes. The full run identified two schema snapshot expectations and a resource fixture missing required scopes; these were corrected. First-principles review removed duplicated client SQL by extending the existing lock query to support shared reads; no new workflow infrastructure was introduced.

Verified fresh-schema ID suite: 1,227 pass, 8,390 assertions, 100% application line/function coverage. Root typecheck/lint and 54 web tests passed. Logs: /private/tmp/id-client-revision-verified-{migration,tests}.log, /private/tmp/id-client-revision-clean-{typecheck,lint}.log, /private/tmp/id-client-revision-web-tests.log. Root production build also passed after the ID suite (/private/tmp/id-client-revision-build.log), as did git diff --check. No production migration or deployment has run. Remaining write families, tenant lifecycle/isolation, paired capability evaluator, complete audit effects and release rehearsal remain unfinished; the full goal stays active.

## Execution: every client lifecycle command uses operation recovery

The previous goal turn made verified progress on client revisions. The next regression confirmed lifecycle commands had no operation receipt. Enable/disable, immutable owner verification, resource link/unlink and client erasure now require Idempotency-Key and share the existing transaction journal with seven-day ordinary recovery. All client mutation routes are now connected. The shared HTTP helper preserves a mutation's dynamic status and bodyless 204 responses; replay can return an erased client's original receipt without loading the deleted row.

New desired-state commands record noop outcomes for already enabled/disabled clients and present/absent resource links. No-op client state changes skip UPDATE and revocation, preserving revision/timestamps. Owner verification remains immutable. Audits retain operation/tenant attribution and selected before/after configuration or link state. Disable captures actual revoked counts; complete token IDs/removal manifests remain separate unfinished F2/F6 work.

Tests prove historical disable/link replay cannot reverse a subsequent enable/unlink, link creation replays its original 201 while a distinct duplicate command returns 200/noop, unlink/erase replay empty 204 after deletion, owner verification replays after deletion, changed confirmation conflicts with the original operation fingerprint, and audit failure rolls back both lifecycle state and journal reservation before a successful retry. Existing tests were updated for explicit no-op events instead of ambiguous already-state conflicts.

First-principles review reused the current service transactions and operation helper. Only dynamic status and empty-response handling were added to the helper; no lifecycle table, workflow engine or additional operation state was necessary. The helper always rechecks current authority; complete cross-family grant/revocation lock ordering remains F4 work.

Final verification: fresh-schema ID suite 1,230 pass, 8,467 assertions, 100% line/function coverage; root typecheck, lint and production build pass; 54 web tests and git diff --check pass. A focused contract check after wording updates passed all 20 tests (its command exits nonzero because a focused run does not meet whole-application coverage). Evidence: /private/tmp/id-client-lifecycle-{migration,tests,final-typecheck,final-lint,build,web,contract,openapi}.log. Generated API contracts, public/operator docs and environment comments now describe all client mutation keys and replay windows. No production migration/deployment was run. Other administrative families, tenant isolation/lifecycle, paired capability evaluator, complete audit effects and release rehearsal remain unfinished. Goal active.

## Execution: resource revisions and operation recovery

The previous turn made verified progress across client lifecycle commands. The next regression reproduced duplicate resource creation returning 409 rather than recovering its original registration. Every resource mutation now requires Idempotency-Key and uses seven-day ordinary result recovery. Resource PATCH additionally requires If-Match, returns 412 for new stale commands, and recovers committed requests before checking the old revision. Unchanged patches and enable/disable commands record explicit noops and preserve row timestamps/revisions. Erasure replays bodyless 204 after deletion; creation replay does not recreate an erased resource.

Migration 0015 adds a positive resource configuration revision and reuses the existing database revision guard. The existing client-resource link trigger now touches both endpoint revisions on visible link changes, including client-deletion cascades. Resource GET takes a shared row lock while reading a sorted client list. Resource services now write operation-linked selected before/after audit snapshots; resources are global and do not acquire fabricated tenant ownership. PolicyVersion is still distinct from configuration revision; complete issuance policy versioning and grant/revocation ordering remain unfinished.

Tests cover canonical scope-set creation replay, stale/replayed/noop patches, malformed and missing tags, simultaneous edits (one commit, one 412), link/noop/client-erasure revision changes, raw SQL revision protection, link movement, lifecycle historical replay, and erasure/creation replay after deletion. Existing audit-failure tests still roll back every resource mutation. Schema expectations and older already-state assertions were updated to the new contract.

First-principles review reused the journal, HTTP revision parser, lock query and database trigger function; no new table or workflow abstraction was needed. Selected resource snapshots cover exposed mutable configuration without broadly copying raw metadata/custom claims. The common all-writer locking protocol and complete deletion-effect manifests remain separate requirements, not guarantees from these tests.

Fresh-schema ID verification: 1,235 pass, 8,523 assertions, 100% line/function coverage. Root typecheck/lint and 54 web tests pass; git diff --check passes. Evidence: /private/tmp/id-resource-final-{migration,tests,typecheck,lint}.log and /private/tmp/id-resource-web.log. Root production build also passed after the ID suite (/private/tmp/id-resource-build.log). OpenAPI, operator/public docs, schema/foundation status and environment comments describe resource keys/revisions and remaining boundaries. No production migration/deployment was performed; goal remains active.

## Execution: durable tenant membership revocation and explicit reinstatement

The previous turn made verified progress on resource administration. The new real-SSO regression proved that deleting a member allowed the next valid corporate login to provision it again. Migration 0016 adds active/revoked membership state, a checked revocation timestamp and immutable membership/user/organisation identity. The shared isEffective predicate now excludes revoked memberships; the explicit summary predicate was updated as well.

Removal now retains the membership UUID, removes its direct entitlements/group memberships in the same transaction, and records pre-removal effective access plus actual deleted rows. Repeat removal preserves its timestamp and distinguishes a no-change event from actual cleanup. New direct/group assignments to revoked members are rejected by the services. The new tenant/platform-authorised reinstateMember route explicitly activates the existing identity/window without restoring removed assignments; audit failure rolls that transition back. Member projections distinguish membershipStatus/revokedAt from the existing global user status.

Federation rejects revoked exact and imported identities before activation or account updates. Tests prove valid SSO after revocation is denied, explicit reinstatement allows the same membership UUID, and tenant A revocation preserves tenant B's effective access for the same person. A separate regression caught sign-in diagnostics still reporting would_sign_in; diagnostics now reports membership_revoked too.

First-principles review reused the membership row and existing effective-state predicate instead of adding a denial table. Assignment cleanup lives in the query module, with its returned rows used as evidence. Complete grant/session context revocation, tenant-local session routes, RLS/typed contexts, tenant command replay and common concurrent grant/revocation ordering remain unfinished. Previous hard-deleted memberships cannot be inferred by the migration; historical offboarding reconciliation remains a cutover requirement. Global user/organisation erasure still removes identity linkage as designed.

The full-suite launch initially timed out in automatic approval before starting; its permitted single retry succeeded. Full runs exposed old deletion/audit assertions and the new OpenAPI path inventory; those expectations were updated, including successful audit attribution for repeat removal/reinstatement. Final fresh-schema verification passed: 1,251 ID tests, 8,671 assertions, 100% line/function coverage. Root typecheck/lint/build, 54 web tests and git diff --check passed. Evidence: /private/tmp/id-membership-clean-{migration,tests}.log, /private/tmp/id-membership-final-clean-{typecheck,lint}.log, /private/tmp/id-membership-{web,build,openapi}.log. No production migration or deployment performed; full foundation goal remains active.


## Execution: organisation disable isolates global state and invalidates old machine epochs

Two regressions first failed: disabling tenant A deleted a shared person's global session, and disabling/re-enabling the owner allowed an old machine JWT again. Migration 0017 adds a positive monotonic organisation authorization version with a database transition trigger. Machine issuance binds organization_authorization_version; admin authentication and journal command authority compare it with current organisation state. Client credential versions remain separate, and resource custom claims cannot override the new identity claim. Pre-epoch JWTs must be reissued.

Organisation disable now preserves global sessions and unbound user grants. It revokes only stored machine access-token rows (null userId) whose client belongs to the organisation. The transaction records actual revoked token IDs and before/after organisation status/version. Tests prove that tenant B's machine/user tokens and the shared person's global session survive A's disable, unbound user grants through A-owned apps survive too, raw SQL cannot roll back the epoch, re-enable cannot revive an old JWT, and audit failure rolls back the epoch and actual machine revocations.

First-principles review deleted both unused organisation user/client enumeration helpers and removed the global user revocation loops. One version column and existing token rows suffice; no new revocation table or workflow was added. Complete tenant user-grant contexts, tenant member-session route replacement and common issuance/revocation ordering are still unfinished F4 requirements. Offline verifiers still need expiry or online invalidation to observe changes. Public docs and generated OpenAPI explicitly describe these boundaries.

Fresh-schema verification: 1,254 ID tests pass, 8,688 assertions, 100% line/function coverage. Root typecheck/lint, 54 web tests and git diff --check pass. Evidence: /private/tmp/id-org-epoch-clean-{migration,tests}.log, /private/tmp/id-org-epoch-final-{typecheck,lint}.log and /private/tmp/id-org-epoch-{web,openapi}.log. Root production build also passed after the standalone database suite (/private/tmp/id-org-epoch-build.log). No production migration or deployment performed. Full foundation goal remains active.


## Execution: remove tenant access to global browser sessions

The previous turn made verified progress on organisation machine epochs. A real-SSO regression then reproduced the tenant session alias exposing a global browser session (200 instead of the required absent route). The GET/DELETE member-session aliases, their forwarding services and member-to-global-user lookup are now removed. Platform global user-session routes remain. The organisation summary no longer reads/returns members' global active-session counts; the platform summary retains its global count.

The regression uses a person shared across two organisations, checks both removed methods return 404 even for platform callers, verifies their real browser session and access/refresh rows remain live, confirms tenant callers cannot use platform user-session endpoints, and asserts the tenant summary omits sessions. Existing platform revocation/rollback/pagination tests remain; removed route tests no longer describe global revocation as tenant isolation. The initial full suite found the explicit OpenAPI path inventory still expected the removed route; that expectation was corrected.

First-principles review deleted the unsafe forwarding paths and folded the remaining one-use revokeAll helper into the global operation. The audit helper no longer accepts a tenant argument. No empty pretend tenant-session endpoint, activeOrganizationId filter or unconsumed grant table was added. This is the deletion step, not completion of F4: actual tenant-bound authorization/delegation records and their views still require implementation before user OAuth opens. Public/operator/schema docs and generated contracts document the breaking removal, platform-only global operations and the membership-revocation offboarding path.

Final fresh-schema verification: 1,233 ID tests pass, 8,529 assertions, 100% line/function coverage. The test count decreases because the two removed routes and their generated route cases no longer exist; the new real-SSO tenant isolation regression passes. Root typecheck/lint, 54 web tests and git diff --check pass. Evidence: /private/tmp/id-session-final-{migration,tests}.log, /private/tmp/id-session-verified-{typecheck,lint}.log, /private/tmp/id-session-{web,openapi}.log. Root production build also passed after the standalone database suite (/private/tmp/id-session-build.log). No production deployment or migration was performed; full foundation goal remains active.


## Execution: machine issuance and lifecycle commit ordering

The previous turn made verified progress by deleting unsafe tenant session aliases. Inspection showed that machine identity read current state without locking it against lifecycle writes. A small authDatabaseAdapter wrapper now exposes the actual SQL transaction through a WeakMap keyed by the active Better Auth transaction adapter. The entry is removed in finally, and access with an expired/unknown adapter throws. The supported Drizzle adapter still supplies model/field transforms and native transaction metadata; no node_modules code or private async-storage implementation was changed.

Machine identity now locks organisation then client with shared row locks before rereading current identity/status/version. It explicitly verifies the locked instance ID against the authenticated instance. Organisation/client lifecycle writes already take those rows exclusively. Barrier tests query pg_blocking_pids to prove a lifecycle writer waits on the issuing transaction, then verify the signed JWT remains rejected after re-enable. Reverse ordering denies issuance and rolls back a transaction-local marker. Tests also prove shared readers overlap, a different tenant can change, and expired adapter handles cannot expose a transaction.

The initial fixture wrongly expected a stored row for a JWT; inspection confirmed the provider's normal JWT path is stateless. The proof now checks actual version-based rejection instead of invented stored-token revocation. The full suite then caught the wrapper initially advertising no native transactions through adapter metadata, which made SSO reject user resolution. That metadata was restored; subsequent full federation/root tests passed. Existing policy/signing rollback and consumed-assertion tests now run through the wrapper too.

First-principles review retained one small adapter seam and reused existing row-lock queries. The old duplicate adapter lookups in machine identity were removed. No new transaction engine, grant table or revocation ledger was introduced. This is partial ordering only: resource/capability/assignment writes, bounded waits, user grant contexts and production lock-duration/load measurement remain required. Shared locks span local signing and commit, and offline verifiers retain the documented expiry/current-state limitation.

A later full run exceeded the management fixture hook’s five-second runtime default, causing overlapping cleanup. The file now uses setDefaultTimeout(15_000), matching the repository test limit. The first per-hook argument attempt was rejected by Bun 1.3.1 despite its installed types; a separate 5.1-second hook probe verified the supported file-level API (/private/tmp/id-hooktimeout.log). The dedicated PostgreSQL blocker assertion still has its own five-second deadline. Final fresh-schema verification passed: 1,239 ID tests, 8,551 assertions, 100% line/function coverage. Root typecheck/lint and the current 62 web tests pass, as does git diff --check. Evidence: /private/tmp/id-grant-lock-release-{migration,tests,typecheck,lint,web}.log. Root production build passed after the standalone database suite (/private/tmp/id-grant-lock-build.log). Separate web edits arrived while gates ran; initial root typechecks saw incomplete metadata/icon files, then typecheck/lint passed against the newer files without modifying that work. No production migration or deployment was performed. Full foundation goal remains active.


## Execution: bounded machine issuance lock waits

The previous turn made verified progress on organisation/client lifecycle ordering. The provider now sets transaction-local PostgreSQL lock_timeout to two seconds before issuance. Drizzle's lock-timeout error maps to OAuth 503 temporarily_unavailable with Retry-After: 1 after transaction rollback. Other errors retain their original handling; no blanket retry or swallowed failure was introduced.

A real PostgreSQL regression holds the organisation exclusively, pauses an authenticated issuance with a transaction-local marker, resumes it into contention, and verifies the 503/body/header, marker rollback and expiry of the exposed transaction handle. After the writer releases, a fresh request succeeds. The test leases all four pool connections to verify their timeout settings match the pre-request baseline after rollback and commit. Existing signing/policy failure and single-use assertion tests still pass; assertion authentication remains outside the rolled-back issuance transaction.

First-principles review uses the database's transaction-local setting rather than an application timer, worker or new environment setting. The API schema reuses the existing OAuth error shape. Public docs explain that the two-second limit applies per lock wait, not to signing, pool acquisition or total request duration. Callers use bounded backoff and new private_key_jwt assertions; this is fresh OAuth issuance, not journal replay. Administrative wait limits, the full resource/capability/assignment protocol and total request budgets remain unfinished.

Final fresh-schema verification: 1,240 ID tests pass, 8,559 assertions, 100% line/function coverage. Root typecheck/lint, 62 web tests and git diff --check pass. Evidence: /private/tmp/id-lock-timeout-{migration,tests,web,openapi}.log and /private/tmp/id-lock-timeout-final-{typecheck,lint}.log. Root production build passed after the standalone ID suite (/private/tmp/id-lock-timeout-build.log). No production deployment, database migration or operational job was performed. Full foundation goal remains active.


## Execution: resource policy ordering and explicit unlink-before-erasure

The previous turn made verified progress on bounded machine lock waits. Source inspection showed that provider resource policy reads preceded claim-time locks. prepareMachineGrant now takes organisation, client and resource shared locks before provider issuance, verifies the authenticated client instance/owner/credential version, and reloads current client configuration through the supported adapter. Scope/default-scope checks then use that current configuration. A further review of getOAuthProviderApi.issueTokens showed it does not repeat grant-type authorization, so the current client_credentials grant permission is checked explicitly too. Grant-type mutation is not exposed by admin PATCH; its interleaving regression uses out-of-band SQL.

Barrier tests cover committed client/resource scope restrictions, out-of-band grant removal, a resource writer blocked behind issuance, later-token TTL, and unlinking ordered after an issuance. The earlier lifecycle/timeout tests now pause at the adapter transaction boundary when they need to precede all policy locks. Test cleanup awaits resumed requests before the next fixture, preventing a failed setup mutation from leaving work running into a truncate.

Resource erasure previously acquired the resource and cascaded into client revision updates, reversing issuance/link lock order. Migration 0018 changes the link FK to ON DELETE RESTRICT. The service returns 409 resource_has_clients before deletion; an indexed existence query reads at most one link. PostgreSQL also rejects raw erasure with links. Explicit unlinking retains its normal journal/audit/revision effects; client erasure still removes its own links. A HTTP test proves failed resource erasure leaves no operation reservation, then the same key succeeds and replays after unlinking. The old cascading-deletion assertion was replaced with the new rejection/unlink contract.

First-principles review moved locking to the point before policy reads rather than adding a second policy cache. The adapter refresh preserves the provider's field transformations while retaining comparison with the original authenticated identity. Requiring explicit unlinking deletes the reverse cascade path instead of adding a more complex catalog-lock coordinator. Existing links survive migration; no production migration was performed. Capability/assignment policy versions, remaining administrative ordering/waits, tenant user-grant contexts and production load measurement remain unfinished.

Verification: full fresh-schema ID suite 1,247 pass, 8,595 assertions, 100% line/function coverage. The final SQL form of the grant-revocation test also passed separately (the focused command exits nonzero solely for whole-application coverage). Root typecheck/lint, 62 web tests and git diff --check pass. Evidence: /private/tmp/id-resource-order-final-{migration,tests,web}.log, /private/tmp/id-resource-order-final2-{typecheck,lint}.log, /private/tmp/id-resource-order-grant-final.log and /private/tmp/id-resource-order-openapi.log. Root production build passed after the standalone database checks (/private/tmp/id-resource-order-build.log). Docs and generated contracts describe the new erasure error and current policy read boundary. Full foundation goal remains active.


## Execution: committed machine issuance evidence

The preceding planning turn made progress by mapping current source gaps to the remaining execution packages and correcting the roadmap's stale all-unimplemented classification. Execution resumed with the unfinished successful-issuance audit slice, preserving the full F0–F7 objective.

Machine issuance now records oauth.token.issued inside the existing provider transaction before returning the signed token. Evidence contains immutable client/tenant/resource references, token jti, configuration revisions, current identity epochs, requested/granted scopes and issuance/expiry seconds. It extracts allowlisted fields from our newly signed JWT and current locked configuration; caller metadata and bearer/client secrets are excluded. This decode is evidence extraction, not authentication. Audit insertion failure produces retryable OAuth 503 and rolls back extension writes. Authentication assertion consumption remains outside the issuance rollback boundary. Repeated correlation IDs produce distinct OAuth tokens/events, not administrative operation replay.

Tests prove attributed success, secret/metadata exclusion, preserved audit subjects, audit-write rejection with no token release and marker rollback, recovery, distinct issuances and mismatched attribution rejection. Provider policy/signing/credential rejection cases assert no successful issuance event. The real restricted runtime role now asserts the exact issuance action. The first full run found an audit-list test comparing a snapshot taken before minting against one taken afterwards. The corrected test explicitly requires one new event with the minted token jti, preserves the older events, and compares staff/machine readers against the same committed state. Pagination checks remain intact.

First-principles review retained the existing audit store and provider transaction, with no new table, worker, replay layer or outbox. Removing the newly required audit event to satisfy an obsolete test would weaken the objective; the incorrect snapshot assumption was removed instead. Configuration revisions are not represented as complete capability policy versions. A committed issuance event does not prove response delivery or remote consumption. Denial/user-grant auditing, complete capability evidence, monitoring, tenant contexts/RLS and remaining command adoption are still unfinished.

Verification: fresh disposable migrations and full ID suite pass 1,251 tests, 8,627 assertions, 100% line/function coverage. Root typecheck, lint, production build, 62 web tests and git diff --check pass. Logs: /private/tmp/id-audit-close-{migration,tests,typecheck,lint,build,web}.log. Earlier focused proofs: /private/tmp/id-issuance-audit-focused.log. Generated OpenAPI and relevant README/schema/foundation/public sign-in documentation include the audit commit/failure contract. No production migration, deployment or scheduled job was performed. The full foundation goal remains active; next packages are mutation inventory and explicit tenant boundaries.


## Execution: administrative mutation contract inventory

The preceding turn made verified progress by completing successful machine issuance evidence and restoring a green full baseline. Current route/service inspection identified 46 administrative mutations: 14 client/resource commands declare replay; 32 others do not. Existing tenant write scopes are limited to member update/reinstate/remove, while group/entitlement/domain/SSO/organisation writes remain platform-only. The source-backed inventory is reports/answerable-id-mutation-inventory.md, covering exact operation IDs, authority, transaction boundaries, existing audit payloads and remaining evidence gaps.

The existing routes.test.ts now pins the exact remaining missing Idempotency-Key declarations and configuration preconditions. Adopted routes must describe successful Operation-Id/Idempotency-Replayed headers and stable recovery/conflict error statuses. Missing PATCH preconditions are tracked, along with the two mutable PUT replacements (SSO provider and group membership windows); immutable owner verification and link creation remain explicit desired-state exceptions. A new missing contract or an adopted-route regression fails the inventory test. This is declaration coverage, not proof of runtime replay or audit semantics; entries must only be retired with service/HTTP/fault tests. Expected audit/effect runtime coverage and full tenant contexts remain unfinished.

First-principles review used existing route parameters and response metadata rather than adding a second runtime metadata object, registry or command framework. A PATCH-only check was too weak: the two replace-style PUT routes can overwrite configuration too, so six missing preconditions are now recorded. The temporary backlog arrays should disappear after adoption, leaving the general contract assertions. No production behaviour changed in this inventory slice.

Verification: fresh disposable migrations and the complete ID suite pass 1,252 tests, 8,733 assertions, 100% line/function coverage. Root typecheck/lint/build and 62 web tests pass. The build completed both tasks, reusing one cached result. All 24 inventory source links resolve; git diff --check passes. Logs: /private/tmp/id-command-inventory-{migration,tests,typecheck,lint,build,web}.log. No production migration or deployment occurred. Full F0–F7 goal remains active; trusted tenant command contexts and member mutation adoption are next.


## Execution: transactional authority for member writes

The preceding goal turn made verified progress by inventorying administrative contracts and adding regression guards. The current member HTTP routes relied on middleware authority captured before the service transaction. A deterministic regression revoked the acting tenant member after middleware admission but before the transaction began: PATCH still returned 200 (/private/tmp/id-tenant-command-red.log).

All three member mutations now enter withTenantMemberCommand. The callback receives a readonly tenant member context containing the actual SQL transaction and validated target organisation. The transaction locks that organisation before re-reading current authority; member/group/assignment services use the same organisation lock. The existing command authoriser now accepts explicit required scopes and reuses current user/session checks, effective grants and machine identity/epoch/scope checks. Client/resource callers keep the platform:write wrapper; member writes require platform:users or org:users in the exact target organisation. Missing target existence is reported only after authority is checked. Member services accept Executor so their existing mutation/audit savepoints remain inside the outer authority transaction.

The regression now covers PATCH, DELETE and reinstatement POST: revocation after middleware admission returns insufficient_scope, leaves the target unchanged and records no successful mutation event. A real tenant-owned client with org:users can update its own tenant member but receives 404 for another tenant. Existing platform human/machine, users-only tenant, missing-target and role-negative cases remain green.

First-principles review reused the existing grant/client/session authoriser instead of duplicating policy or building a new command registry. The small callback context keeps target tenant and transaction together. No schema, worker, new permission system or receipt mechanism was added. This is an initial HTTP member-write boundary, not full TenantContext enforcement: reads and direct internal services still accept IDs, member replay/revisions and RLS remain pending, and the complete platform/client/resource writer protocol plus bounded waits are not yet proven. Denial audit coverage remains a separate unfinished requirement.

Verification: fresh disposable migration and full suite pass 1,256 ID tests, 8,759 assertions, 100% line/function coverage (/private/tmp/id-tenant-command-{migration,tests}.log). Typecheck found missing requestId fields in two new test setup actors; after correction, all 62 member integration tests passed again with 437 assertions (/private/tmp/id-tenant-command-final-members.log; focused exit1 is the repository-wide coverage threshold only). Final typecheck/lint pass (/private/tmp/id-tenant-command-final-{typecheck,lint}.log), as do production build, 62 web tests and git diff --check (/private/tmp/id-tenant-command-{build,web}.log). README, public management guide, foundation status, inventory and task plan now state the exact implemented boundary and remaining work. No production migration/deployment occurred; the full foundation goal remains active.


## Execution: tenant-scoped member operation replay

The preceding goal turn made verified progress by closing stale middleware authority for member writes. The new replay regression initially failed because member commands returned no Operation-Id (/private/tmp/id-member-replay-red.log). Update, removal and reinstatement now require Idempotency-Key and use the existing immutable journal plus encrypted seven-day ordinary recovery. Scope is tenant:<organisation UUID> and actor instance, including for platform actors acting on that tenant; canonical input includes member ID and normalised window input. Audit receives the operation ID and commits with the mutation and receipt. New-key unchanged state records a noop; same-key replay returns the original response without another successful mutation fact.

The journal now owns the authority/mutation transaction, replacing the extra outer tenant transaction wrapper. authorizeTenantMemberCommand returns the validated tenant context inside that transaction before both mutation and replay. HTTP response/key/cipher handling is shared between the explicit platform and tenant-member wrappers. The first implementation captured the authorised context in a mutable variable; first-principles review removed that state by letting executeOperation pass its authoriser result directly to the mutation callback. No new tables, worker, retention mechanism or parallel command framework was introduced.

Tests exercise all three member commands for identical result recovery, one audit fact, tenant receipt scope, changed-target conflicts and new-key noops. Additional tests cover simultaneous duplicate removal with one committed audit effect, current authority revoked between middleware admission and journal replay, missing keys and purged recovery data returning 410 without repeating the effect. The existing platform/client/resource replay, identity, audit rollback and route tests remain green. The inventory guard now lists 29 missing replay contracts (17 of 46 adopted); six revision-precondition gaps remain, including member PATCH. Complete tenant service/read contexts, RLS, authority writer ordering, bounded administrative waits, typed audit manifests and other families remain unfinished.

Verification: two full fresh-schema runs pass 1,259 ID tests, 8,831 assertions and 100% line/function coverage. The final run includes the direct context handoff simplification: /private/tmp/id-member-replay-final-{migration,tests}.log. Final typecheck/lint pass (/private/tmp/id-member-replay-final-{typecheck,lint}.log), as do production build, 62 web tests and git diff --check (/private/tmp/id-member-replay-{build,web}.log). OpenAPI was regenerated using the exporter (/private/tmp/id-member-replay-openapi.log). README, implemented schema, foundation status, operation-status descriptions, public management examples/error table, inventory and task plan describe member replay and retain the explicit missing-revision limitation. No production migration, deployment or scheduled job occurred. Full F0–F7 goal remains active; member revision preconditions are next in this family.


## Execution: member configuration revisions

The preceding goal turn made verified progress by adopting the journal for all three member mutations. Current member details combine global profile fields, groups and clock-dependent effective status, so a membership-row revision would not be a correct strong validator for that full representation. Added a small getMemberConfiguration read under platform:users / org:users that returns only stable membership identity, lifecycle and window fields with an ETag. Rich member details include the numeric revision but no configuration-only ETag.

Migration 0019 (generated schema snapshot plus explicit trigger) adds a positive member revision and reuses protect_configuration_revision. Row changes, including revocation/reinstatement, advance it; unchanged values preserve it; direct revision edits fail. PATCH now requires Idempotency-Key and If-Match, fingerprints the expected identity/revision, and compares it under organisation and member row locks. Audit includes allowlisted before/after configuration. Journal replay is evaluated before the old precondition. Previously committed PATCH receipts without a precondition remain retained but cannot match the new input contract; the README documents status inspection and reviewed transition rather than automatic re-execution.

The new regression initially saw the absent configuration route. Focused HTTP checks then proved stale/new writes, replay, noops and route authority; its raw guard assertion needed .execute() because Bun's rejects matcher requires an executed promise rather than a Drizzle query builder. The completed test also proves rejected direct revision edits, display-name independence and old-tag rejection after removal/reinstatement. Existing member/replay/management tests now read the configuration tag for deliberate new patches and preserve the original tag on replay. Schema column/constraint and API route-list expectations were reviewed and updated for exactly the new revision, CHECK and GET route. The inventory now has five missing preconditions, while replay remains implemented for 17 of 46 mutations.

First-principles review avoided user/group cascade triggers by keeping the validator's representation narrow. It reused the existing revision trigger/parser and removed the redundant full member/group read from PATCH no-op detection: the checked resulting revision supplies that answer. A row lock before comparison also protects against writers outside the organisation-lock convention. No new workflow, cache or version service was introduced. Other family revisions, complete tenant service/read contexts, RLS, policy ceilings/pairs and broader foundation work remain unfinished.

Verification: final fresh-schema run after cleanup passes 1,271 ID tests, 8,914 assertions and 100% line/function coverage (/private/tmp/id-member-revision-final2-{migration,tests}.log). An earlier corrected full run also passed (/private/tmp/id-member-revision-release-tests.log). Final typecheck/lint, 62 web tests, production build and git diff --check pass (/private/tmp/id-member-revision-final2-{typecheck,lint}.log and /private/tmp/id-member-revision-{web,build}.log). Migration generation and OpenAPI export evidence: /private/tmp/id-member-revision-{generate,openapi}.log. README, schema/foundation status, mutation inventory, task plan and public cURL/precondition guidance are updated. No production migration/deployment occurred. Full F0–F7 goal remains active.


## Execution: domain command recovery

Domain create, enable, disable and delete now use the existing platform command journal with seven-day encrypted recovery. Normalised creation input is fingerprinted with the organisation UUID; lifecycle/deletion fingerprints include the immutable domain row UUID. Identical authorised retries recover the committed response and operation ID without another mutation fact, including after deletion. Changed input conflicts. New-key enable/disable of the current state records a noop and preserves timestamps. Services capture allowlisted domain identity and before/after routing state under organisation/domain locks; audit insertion failure rolls back the assignment and operation reservation together. Existing domain uniqueness and platform-only write authority are preserved.

The initial replay regression failed on missing Operation-Id. Focused tests then exposed the root fixture's missing encryption configuration; the fixture now supplies a test key, and bootstrap instructions require deployment replay keys rather than introducing a root bypass. Tests cover creation/deletion replay, normalised input, changed-input conflicts, lifecycle replay/noops, unchanged timestamps and audit-failure rollback with a successful same-key retry. This is not proof of complete cross-family writer ordering or DNS ownership verification.

First-principles review reused the journal, cipher and platform authority check with no new schema, worker or command framework. Audit uses an explicit field allowlist instead of copying a potentially secret-bearing future row. No-op transitions avoid unnecessary writes. Inventory and route guard now track 21 of 46 mutations with replay and 25 remaining; five replacement preconditions remain missing. Full tenant service/read contexts, targeted RLS, capability/pair policy and other foundation requirements are still unfinished.

Verification: final disposable migration and full suite pass 1,274 ID tests, 8,975 assertions and 100% line/function coverage (/private/tmp/id-domain-replay-final-{migration,tests}.log). The first full run exposed a test clock boundary and a five-second access fixture timeout. Expiry now uses database time, and that fixture applies the existing 15-second hook allowance; both pass in the final run. Final typecheck/lint, 62 web tests, production build and git diff --check pass (/private/tmp/id-domain-replay-final2-{typecheck,lint}.log, /private/tmp/id-domain-replay-final-web.log, /private/tmp/id-domain-replay-build.log). OpenAPI was regenerated by the exporter (/private/tmp/id-domain-replay-openapi.log). README, schema/foundation status, mutation inventory, task plan and public setup/retry examples now describe the implemented contract. No production migration, deployment, key provisioning or scheduled job occurred. Full F0–F7 goal remains active.


## Execution: organisation command recovery

The preceding goal turn made verified progress on domain recovery. This slice routes all five organisation mutations through the existing platform journal with seven-day encrypted responses and current-authority checks before replay. The initial HTTP regression failed on the absent Operation-Id (/private/tmp/id-org-replay-red.log). Identical retries now return the original response and operation ID, including creation replay after erasure, without recreating the row or repeating its mutation fact. New-key unchanged PATCH/enable/disable records a noop and preserves timestamps. No-op disable neither advances the organisation epoch nor repeats machine-token revocation. Existing global browser session and unbound-user-grant preservation remains enforced.

Organisation create/update/enable/erase facts now carry allowlisted before/after configuration; disable retains epoch and actual revoked-machine-row evidence. Arbitrary metadata is no longer copied from submitted input into these facts; update records only whether it changed. Erasure still needs a complete cascade manifest, and PATCH still needs a configuration revision. Tests prove changed-input conflicts, historical replay, lifecycle and PATCH noops, operation outcomes, concurrent duplicate creation, atomic audit failure/rollback with same-key retry, and loss of platform authority between middleware admission and replay. Focused checks pass 97 tests/510 assertions; the later full run includes the extra noop receipt and concurrency-error assertions.

First-principles review reused the journal, cipher and authority boundary without adding schema or workers. It removed needless no-op writes and avoided unrestricted metadata snapshots. Full-suite validation also exposed five-second fixture hooks in group and entitlement suites. An isolated Bun 1.3.1 probe proved --timeout 15000 applies to hooks (/private/tmp/id-hook-timeout-probe/result.log). The shared test:coverage command now sets that timeout, replacing the ineffective bunfig entry and both per-file setDefaultTimeout exceptions. CI already invokes this command. Explicit shorter concurrency barriers remain intact; focused test instructions now include the runner flag.

Verification: final fresh disposable migration and complete suite pass 1,278 ID tests, 9,063 assertions and 100% line/function coverage (/private/tmp/id-org-replay-final2-{migration,tests}.log). Typecheck/lint pass (/private/tmp/id-org-replay-final3-{typecheck,lint}.log), as do 62 web tests, production build and git diff --check (/private/tmp/id-org-replay-{web,build}.log). OpenAPI was regenerated through the exporter (/private/tmp/id-org-replay-openapi.log). README, schema/foundation status, inventory and public setup/offboarding/retry guidance are updated, including correction of the old claim that organisation disable ends every member's global sessions. Replay coverage is now 26 of 46 commands, with 20 remaining; five replacement preconditions remain missing. No production migration, deployment, credentials or scheduled jobs were executed. The full F0–F7 goal remains active; organisation revisions, tenant contexts/RLS, capability/pair policy, other command families and release controls remain unfinished.


## Execution: organisation configuration revisions

The preceding goal turn made verified progress on organisation command recovery. This slice closes stale PATCH overwrites with migration 0020: a positive server-controlled organisation revision using the existing protect_configuration_revision trigger. The generated Drizzle snapshot/journal and SQL add exactly the revision column, CHECK and trigger. The initial integration regression failed because GET had no ETag (/private/tmp/id-org-revision-red.log).

getOrganization returns only stored row fields, so its existing response can supply a correct strong ETag without another configuration endpoint. PATCH now requires If-Match and the operation key, fingerprints the expected identity/revision, and checks it under the existing organisation lock inside the journal mutation. Recovery runs before that old precondition. Missing tags return 428, malformed tags 400, and stale/wrong-instance tags 412. Audit snapshots include the revision; unchanged PATCH and identical raw row updates preserve it. Lifecycle, epoch and metadata updates invalidate older tags. Existing integration helpers read tags for new edits and preserve the original tag per replay key.

Tests cover stale overwrites, wrong instance, missing/weak tags, same-key replay after revision advance, changed-precondition conflicts, stable noops and one operation-linked audit fact. Direct revision edits fail without changing the row. Lifecycle and raw SQL metadata writes advance the revision; a raw same-value write does not. Competing edits from one expected revision produce one success and one 412. Schema introspection was updated for exactly the new column/CHECK. First-principles review reused the existing trigger/parser and organisation lock, with no new endpoint, worker or revision subsystem.

Verification: focused service/API/schema checks pass 121 tests and 697 assertions (/private/tmp/id-org-revision-focused.log; focused exit1 is the global coverage threshold). The final fresh-schema full suite includes the additional raw SQL/concurrent-edit checks and passes 1,279 ID tests, 9,097 assertions and 100% line/function coverage (/private/tmp/id-org-revision-final-{migration,tests}.log). Final typecheck/lint pass (/private/tmp/id-org-revision-final-{typecheck,lint}.log), as do 62 web tests, production build and git diff --check (/private/tmp/id-org-revision-{web,build}.log). Migration generation/OpenAPI export evidence is in /private/tmp/id-org-revision-{generate,openapi}.log. README, schema/foundation progress, public ETag/cURL/retry guidance and the inventory are updated, including the transition for older PATCH receipts without preconditions. Four replacement-precondition gaps and 20 mutation-replay gaps remain. Tenant contexts/RLS, capability/pair policy, erasure manifests and remaining F0–F7 requirements are not complete. No production migration or deployment occurred; the full goal remains active.


## Execution: keyed request fingerprints before SSO adoption

The preceding goal turn made verified progress on organisation revisions. Tracing the next SSO command family exposed a prerequisite: executeOperation persisted a plain SHA-256 of canonical input, and SSO PUT accepts a supplied clientSecret. New encrypted journal records now use versioned HMAC-SHA-256 fingerprints bound to canonical actor/scope/operation/key identity and input. The MAC key is domain-separated from the AES key using the existing dedicated replay master-key ring. The stored fingerprint carries its original key ID for verification after rotation; no new schema or deployment key service is needed. The initial unit regression failed on the absent fingerprint method (/private/tmp/id-keyed-fingerprint-red.log).

Current authority still precedes replay. Missing live fingerprint keys return 503 without mutation. Expired or purged payloads return 410 before fingerprint comparison, including changed-input retries, so old keys can be retired without ever permitting an old completed operation to execute again. New active keys do not replace old material behind the same key ID. Existing 64-hex SHA-256 receipts retain their original verification and are not rewritten; this does not retroactively protect those digests. Internal reference-only operations without a cipher retain SHA-256 and must not accept secret-bearing input. SSO routes themselves have not yet adopted the journal.

Unit/integration tests cover changed secrets, original-key rotation, missing/retired keys, mismatched key material, malformed fingerprints, current-authority failure before recovery, expired reservations after retirement and legacy encrypted receipt recovery. The journal reuses its immutable fingerprint field and existing encrypted-result retention path. First-principles review avoided a parallel key configuration/table, reused canonicalisation and removed duplicate verification checks. No migration, network action, production key provisioning or worker was added.

Verification: focused cipher/journal tests pass 12 tests and 91 assertions with both changed modules at 100% line/function coverage (/private/tmp/id-keyed-fingerprint-focused.log; focused exit1 reflects the global coverage threshold). Fresh disposable migration and full suite pass 1,282 ID tests, 9,122 assertions and 100% line/function coverage (/private/tmp/id-keyed-fingerprint-final-{migration,tests}.log). Typecheck/lint, 62 web tests, production build and git diff --check pass (/private/tmp/id-keyed-fingerprint-{typecheck,lint,web,build}.log). README, schema/foundation status and public expiry/key-rotation semantics are updated. API shapes and generated OpenAPI are unchanged. The full goal remains active: SSO command adoption, 20 pending replay contracts, four replacement preconditions, tenant contexts/RLS, capability/pair policy and other F0–F7 requirements are unfinished.


## Execution: SSO command recovery

The preceding goal turn made verified progress by introducing keyed fingerprints before accepting SSO secrets into the journal. PUT and delete now require Idempotency-Key and use platform-scoped seven-day encrypted recovery of their redacted responses. The initial HTTP regression failed on missing Operation-Id (/private/tmp/id-sso-replay-red.log). PUT fingerprints normalised hostnames, explicit defaults and deduplicated/sorted scope sets. Omitted clientSecret retains the current secret on a new execution, but replay returns its original result instead of reapplying that instruction to newer configuration. Delete records the original immutable provider UUID; replay cannot delete a provider recreated afterwards.

The service reads the provider under a row lock after its organisation lock, compares parsed effective configuration after secret preservation/defaults, and skips an equivalent update. New-key noops preserve timestamps and have explicit operation/audit outcomes. Audit now records redacted before/after settings and a credential-change flag; raw OIDC configuration and client secrets are not copied. Delete snapshots the actual deleted row. The existing redaction, serializer, journal and key configuration are reused; no schema or worker is added. First-principles review avoided object-key-order writes and removed an unnecessary non-null assertion from row selection.

Tests cover historical PUT replay after a later credential change, delete replay after recreation, normalisation of hostname/defaults/scope order, noops, keyed fingerprints, changed-secret conflicts, secret preservation/redaction, rollback of both credential update and journal reservation on audit failure, concurrent duplicate creation, and current platform authority revoked between middleware admission and replay. Existing root bootstrap uses its configured replay test key without a bypass. Focused checks pass 52 tests/340 assertions (/private/tmp/id-sso-replay-focused2.log; global coverage threshold causes focused exit1).

Verification: fresh disposable migration and full suite pass 1,287 ID tests, 9,188 assertions and 100% line/function coverage (/private/tmp/id-sso-replay-final-{migration,tests}.log), including the final scope/default canonicalisation case. Typecheck/lint, 62 web tests, production build and git diff --check pass (/private/tmp/id-sso-replay-final-{typecheck,lint}.log and /private/tmp/id-sso-replay-{web,build}.log). OpenAPI was regenerated through the exporter (/private/tmp/id-sso-replay-openapi.log). README, schema/foundation progress, public bootstrap/configuration/retry examples and the inventory are updated. Replay is adopted for 28 of 46 mutations; 18 remain. Four replacement preconditions remain, including SSO PUT. Full in-flight grant invalidation, tenant contexts/RLS, capability/pair policy and other F0–F7 requirements are unfinished. No production migration, key provisioning or deployment occurred; the full goal remains active.


## Execution: SSO creation and replacement preconditions

The preceding goal turn made verified progress on SSO command recovery. Migration 0021 now adds a positive server-controlled provider revision using the existing protect_configuration_revision trigger; Drizzle generated the snapshot/journal and column/CHECK SQL before the trigger was appended. GET and PUT return revision and a strong ETag. PUT requires exactly one precondition: If-None-Match: * to assert absence, or If-Match with the existing provider's immutable instance/revision. The initial regression expected 428 but received 201 (/private/tmp/id-sso-revision-red.log).

The expected absence or revision is fingerprinted and checked under the existing organisation/provider locks inside the journal mutation. Committed replay runs first, so creation retries can recover 201 after the row exists, and replacement retries can recover after their old revision changes. Missing headers return 428; malformed/conflicting headers return 400; existing-on-create, missing-on-replace and stale/wrong-instance state return 412. Secret changes advance the revision while the response remains redacted. Old tags cannot adopt a row recreated under the same organisation URL. Audit projections now include revision.

Tests cover the header alternatives, historical replay, noops, changed-precondition conflicts, rejected direct revision edits, deletion/recreation, competing replacements and raw configuration changes. Existing test helpers read preconditions for new commands and retain the original precondition per retry key. The concurrent-create test explicitly pins expected absence, eliminating a helper read race. Root setup uses If-None-Match: * with its operation key. The route inventory guard checks both conditional header declarations and 412/428 responses for SSO, rather than incorrectly requiring If-Match on creation. No new endpoint or revision subsystem was introduced; first-principles review reused the trigger, parser, journal and locks.

Verification: focused API/service/bootstrap/schema checks pass 77 tests and 559 assertions (/private/tmp/id-sso-revision-focused.log; focused exit1 is the global coverage threshold). Fresh disposable migrations and the final complete suite pass 1,288 ID tests, 9,219 assertions and 100% line/function coverage (/private/tmp/id-sso-revision-final-{migration,tests}.log). Typecheck/lint, 62 web tests, production build and git diff --check pass (/private/tmp/id-sso-revision-final-{typecheck,lint}.log and /private/tmp/id-sso-revision-{web,build}.log). Migration generation/OpenAPI export evidence: /private/tmp/id-sso-revision-{generate,openapi}.log. README, schema/foundation status, public bootstrap/replacement examples and the inventory describe the new contract, including older receipts without preconditions. Three replacement-precondition gaps and 18 mutation-replay gaps remain. Full in-flight grant invalidation, tenant contexts/RLS, capability/pair policy and other F0–F7 requirements are unfinished. No production migration or deployment occurred; the full goal remains active.


## Execution: group command recovery

The preceding goal turn made verified progress on SSO creation/replacement preconditions. All seven group mutations now require Idempotency-Key and use the existing platform journal with seven-day encrypted response recovery. The initial HTTP regression failed on absent Operation-Id (/private/tmp/id-group-replay-red.log). Fingerprints include organisation/group/member targets and normalised window dates. New-key unchanged group configuration/lifecycle or assignment windows records a noop and skips its update/upsert; omitted window fields preserve their existing values. Directory-managed membership restrictions and revoked-member checks remain enforced.

Group and assignment reads now take row locks after the organisation lock before capturing allowlisted before/after audit state. Audit/receipt/mutation remain atomic. Assignment result references identify the composite group/member pair. Historical removal replay cannot remove a later re-added assignment. Group erasure snapshots the group but still lacks its full cascade manifest. First-principles review reused the journal, queries and authority checks, with no new schema or worker, and avoided unnecessary no-op writes. Group PATCH/window PUT revisions and complete policy writer ordering remain separate unfinished requirements.

Tests cover all seven commands for identical result recovery and one operation-linked fact, changed-input conflicts, noops, historical removal after re-addition, audit rollback with same-key retry, concurrent duplicate creation and revoked platform authority between middleware admission and replay. Existing lifecycle/audit assertions were updated for explicit noops and snapshots. Focused checks pass 120 tests/774 assertions (/private/tmp/id-group-replay-focused2.log; focused exit1 is the global coverage threshold). The first full run caught a missing OpenAPI operation-header declaration on the membership-create 201 response; the declaration was corrected and OpenAPI regenerated rather than relaxing the guard.

Verification: the final fresh disposable migration and full suite pass 1,293 ID tests, 9,356 assertions and 100% line/function coverage (/private/tmp/id-group-replay-final2-{migration,tests}.log). Final typecheck/lint pass (/private/tmp/id-group-replay-final2-{typecheck,lint}.log), as do 62 web tests, production build and git diff --check (/private/tmp/id-group-replay-{web,build}.log). OpenAPI exporter evidence: /private/tmp/id-group-replay-openapi.log. README, schema/foundation status, public bootstrap/group examples and mutation inventory are updated. Replay is adopted for 35 of 46 mutations, with 11 remaining and three replacement-precondition gaps. Tenant contexts/RLS, capability/pair policy, complete cascade/invalidation evidence and other F0–F7 requirements remain unfinished. No production migration or deployment occurred; the full goal remains active.

## Execution: group configuration revisions

Migration 0022 adds a positive server-controlled group revision using the existing configuration trigger. GET exposes a strong ETag; PATCH requires If-Match and Idempotency-Key. Expected instance/revision participates in the fingerprint and is checked under the existing organisation/group locks after committed replay. Noops preserve state and revision. Lifecycle and raw configuration changes advance the revision; audit snapshots include it. Recreating the same slug cannot satisfy the previous group's tag.

The regression covers missing/weak/stale tags, changed-precondition conflicts, historical replay, noops, direct revision tampering, lifecycle/raw writes, competing edits and deletion/recreation. The first full run caught the stale updateGroup entry in the explicit contract backlog; removed it after the route regression passed. The final fresh-schema full suite passes 1,294 ID tests, 9,382 assertions and 100% line/function coverage (/private/tmp/id-group-revision-final2-{migration,tests}.log). Typecheck/lint and 62 web tests pass (/private/tmp/id-group-revision-final-{typecheck,lint}.log and /private/tmp/id-group-revision-web.log). OpenAPI was exported and public examples, README, schema/foundation status and mutation inventory updated.

First-principles review reuses the trigger, parser, journal and locks without another mechanism. Group configuration revisions deliberately do not version assignment windows: the composite group/member key can be reused after deletion, so assignment preconditions need a distinct instance identity to reject old edits after recreation. That next slice remains unimplemented. Two replacement preconditions and 11 replay contracts remain, along with tenant contexts/RLS, capability/pair policy, complete audit/cascade/invalidation and remaining F0–F7 work. No production migration or deployment occurred; the full goal remains active.

Final slice verification: production build passes (/private/tmp/id-group-revision-build.log), and git diff --check passes. All repository gates for this slice are green; this is not completion of the enterprise foundation.

## Execution: group assignment identity prerequisite

The previous goal turn made verified progress on group configuration revisions. Migration 0023 now retains the composite group/member primary key and tenant foreign keys while adding a unique immutable UUID and positive server-controlled revision. Existing rows receive UUIDv7 IDs once during migration; both production query insertion paths explicitly use createId, with no database default. Conflict updates retain the original ID, unchanged windows retain the revision, and raw configuration changes advance it. Identity/ownership changes and caller-controlled revision resets fail. The parent group's revision is independent.

Tests prove assignment update/recreation behaviour, direct SQL guards, and migration over populated legacy-shaped rows inside a rolled-back transaction, including preserved windows/timestamps and rejection of inserts without IDs. The first focused run exposed a test matcher receiving Drizzle's thenable rather than a Promise; executing the raw query corrected the assertion. The next focused suite passes 147 tests/990 assertions (/private/tmp/id-assignment-focused2.log; focused exit1 is the repository-wide coverage threshold). Fresh disposable migration and the full suite pass 1,296 ID tests/9,404 assertions with 100% line/function coverage (/private/tmp/id-assignment-final-{migration,tests}.log). Typecheck/lint and 62 web tests pass (/private/tmp/id-assignment-final-{typecheck,lint}.log, /private/tmp/id-assignment-web.log).

Assignment mutation responses and audit snapshots now include ID/revision. OpenAPI was regenerated through the exporter (/private/tmp/id-assignment-openapi.log); README, public management guidance, schema and foundation status document the prerequisite without claiming a completed HTTP guard. Drizzle generated the schema snapshot/journal before the backfill and trigger SQL were added (/private/tmp/id-assignment-generate.log). First-principles review retains the existing composite uniqueness and revision trigger, with no second assignment table or worker. Membership PUT still needs an exact stored-assignment read and creation/replacement preconditions using the new instance/revision; two precondition gaps and 11 replay gaps remain. Tenant contexts/RLS, capability/pair policy, full cascade/invalidation and remaining F0–F7 work are unfinished. No production migration or deployment occurred; the full goal remains active.

Final verification: production build and git diff --check pass (/private/tmp/id-assignment-build.log). All repository gates pass for this prerequisite; the full foundation goal is not complete.

## Execution: conditional group assignment commands

The previous goal turn made verified progress on assignment UUIDs and database revisions. getGroupMember now returns the stored assignment with an instance/revision ETag, under existing org:read/platform:read authority and cross-tenant non-disclosure. PUT requires exactly one condition: If-None-Match: * for absence or If-Match for replacement. The expectation enters the keyed fingerprint, and the service checks it under existing organisation/group/assignment locks after committed replay. Noops preserve revision. Missing conditions return 428, conflicting/malformed conditions 400, and stale/missing/recreated replacement state 412. Historical retries recover the original body/ETag without changing a newer assignment.

First-principles review extracted the identical SSO header parser into requirePutRevision rather than duplicating it, and kept the assignment GET limited to stored fields so its strong ETag does not claim to version time-derived effectiveness or a global profile. No migration or extra revision mechanism was needed. Existing group tests retain the original condition per retry key, while new commands read their current target. Root bootstrap explicitly asserts absence. The route contract inventory now tracks only entitlement PATCH as missing a revision precondition; 11 replay gaps remain.

The new HTTP regression covers creation, missing/conflicting/weak headers, tenant reads, stale replacements, noops, changed-condition key conflicts, concurrent creation and replacement, deletion/recreation and historical replay. Its initial assertion expected 403 for an outsider; inspection of authorize.ts established the existing 404 non-disclosure contract, and the assertion was corrected. Focused assignment checks pass 1 test/34 assertions (/private/tmp/id-assignment-http-focused2.log); the preceding wider focused run passed all 129 other tests. The fresh-schema complete suite passes 1,306 ID tests/9,491 assertions with 100% line/function coverage (/private/tmp/id-assignment-http-final-{migration,tests}.log). Typecheck/lint and 62 web tests pass (/private/tmp/id-assignment-http-final-{typecheck,lint}.log and /private/tmp/id-assignment-http-web.log).

OpenAPI was regenerated through the exporter (/private/tmp/id-assignment-http-openapi.log). README, schema/foundation status, mutation inventory and public onboarding/management examples describe expected absence, replacement, replay and transition for older receipts without conditions. Full tenant contexts/RLS, capability/pair policy, complete audit/cascade/invalidation, remaining replay contracts and other F0–F7 requirements are unfinished. No production migration or deployment occurred; the full goal remains active.

Final slice verification: production build and git diff --check pass (/private/tmp/id-assignment-http-build.log). All repository gates are green for the assignment HTTP contract; the enterprise foundation remains incomplete.

## Execution: entitlement command recovery

The previous goal turn made verified progress on conditional assignment writes. All five entitlement mutations now require Idempotency-Key and use the existing platform journal with seven-day encrypted response recovery. Canonical inputs include organisation/entitlement identity, normalised scope sets and window dates. The service normalises scope writes and compares effective sets; unchanged PATCH/windows and repeated lifecycle commands skip writes and record explicit noop outcomes. The query locks the entitlement after its organisation before capturing allowlisted principal/target/scopes/status/windows before and after. Mutation, audit and journal result remain atomic; deletion replay recovers history without recreation.

Tests cover all five historical responses after removal, scope order/duplicate normalisation, changed-input conflicts, noops, concurrent duplicate creation, audit rollback and current authority revoked between middleware and replay. The new cases initially shared a grant and collided with its uniqueness constraint; each case now clears only its own test-resource grants. The fault test explicitly requires constraint_violation to prove it reaches its injected audit constraint. Focused existing checks pass 88 tests/2,144 assertions (/private/tmp/id-entitlement-replay-focused.log); new focused checks pass 5 tests/52 assertions before the final stricter fault assertion (/private/tmp/id-entitlement-replay-newtests2.log). The first full suite passed 1,310 tests and failed one access-view assertion expecting the former audit shape; it now checks the operation ID and before/after snapshot.

First-principles review reuses the journal, organisation locking, optional query row lock and existing retention configuration, with no migration or worker. Scope-set comparison avoids writes caused only by ordering/duplicates. README, schema/foundation status, mutation inventory and public retry/bootstrap examples describe the implemented behaviour; obsolete entitlement_already_* error rows were removed. OpenAPI was regenerated through the exporter (/private/tmp/id-entitlement-replay-openapi.log). Replay is adopted for 40 of 46 mutations; the six remaining commands are global users/sessions. Entitlement PATCH still needs its revision precondition, and tenant contexts/RLS, capability/pair policy, complete audit/cascade/invalidation and remaining F0–F7 work are unfinished. No production migration or deployment occurred; the full goal remains active.

Final fresh-schema validation passes 1,311 ID tests/9,593 assertions and 100% line/function coverage (/private/tmp/id-entitlement-replay-final2-{migration,tests}.log). Final typecheck/lint pass (/private/tmp/id-entitlement-replay-final2-{typecheck,lint}.log), as do 62 web tests (/private/tmp/id-entitlement-replay-web.log). The full foundation is not complete.

Production build and git diff --check pass (/private/tmp/id-entitlement-replay-build.log). All repository gates are green for entitlement replay; the goal remains active with the original F0–F7 scope.

## Execution: entitlement revisions

The previous goal turn made verified progress on entitlement command recovery. Migration 0024 adds a positive server-controlled revision using the existing configuration guard. getEntitlement supplies a stored-row ETag, and PATCH requires If-Match with its operation key. Expected instance/revision participates in the fingerprint; committed replay precedes the check under existing organisation/entitlement locks. Noops preserve revisions, and lifecycle/raw-row changes advance them. Audit snapshots now include revision.

The regression covers missing/weak/stale tags, changed-condition key conflicts, historical body/ETag replay, noops, direct revision tampering, lifecycle/raw changes, competing edits and rejection of an old tag after recreating the principal/target grant with a new UUID. Existing retry helpers retain the original condition per key; new PATCH commands read the current target. Focused schema/HTTP/replay checks pass 113 tests/808 assertions (/private/tmp/id-entitlement-revision-focused.log; focused exit1 reflects the repository-wide coverage threshold).

Drizzle generated migration 0024 and its snapshot/journal before the existing trigger was attached (/private/tmp/id-entitlement-revision-generate.log). OpenAPI was regenerated through the exporter (/private/tmp/id-entitlement-revision-openapi.log). README, schema/foundation status, public management/onboarding examples and mutation inventory document the condition and older-receipt transition. First-principles review reuses the revision trigger, parser, journal and locks with no additional mechanism. The current replacement-precondition backlog is empty; six global user/session replay commands remain. Those require platform:users, which must remain the authority requirement when they adopt the journal. Full tenant contexts/RLS, capability/pair policy, complete audit/cascade/invalidation and remaining F0–F7 requirements are unfinished. No production migration or deployment occurred; the full goal remains active.

Final fresh-schema verification passes 1,312 ID tests/9,619 assertions with 100% line/function coverage (/private/tmp/id-entitlement-revision-final-{migration,tests}.log). Typecheck/lint and 62 web tests pass (/private/tmp/id-entitlement-revision-final-{typecheck,lint}.log and /private/tmp/id-entitlement-revision-web.log).

Production build and git diff --check pass (/private/tmp/id-entitlement-revision-build.log). All repository gates are green for this slice. The full foundation goal remains active and incomplete.

## Execution: global session command recovery

The previous goal turn made verified progress on entitlement revisions. Both global session revocation commands now require Idempotency-Key and use seven-day platform-scoped recovery. platformUsersCommand invokes the existing current-authority evaluator with platform:users; it does not replace that requirement with platform:write. Replays recover the original count/204 without revoking later sessions or requiring the target user/session to remain present. New revoke-all commands record noop only if neither sessions nor active user tokens changed; token-only effects record applied even with revoked: 0.

The deletion query now returns sorted actual session IDs, while its existing count helper delegates to it for other callers. Single revocation records a limited stored-session snapshot, deleted ID and actual token counts without bearer token/IP/user-agent data. Revoke-all facts target the user instead of mislabelling its UUID as a session ID and include actual deleted IDs. Token IDs and complete issuance/revocation ordering remain pending. Mutation, audit and journal result remain atomic. First-principles review reuses the journal, authorisation evaluator and deletion statement; no migration, extra table or worker is introduced.

Tests cover historical recovery, preservation of later sessions, recovery after target erasure, empty/token-only effects, injected audit failure rollback, concurrent retries and users-only authority revoked after middleware admission. Existing service/query/HTTP checks pass 41 tests/1,761 assertions (/private/tmp/id-session-replay-focused.log); new focused checks pass 4 tests/33 assertions before adding erased-user recovery (/private/tmp/id-session-replay-newtests.log). The final fresh-schema suite passes 1,316 ID tests/9,668 assertions with 100% line/function coverage (/private/tmp/id-session-replay-final-{migration,tests}.log). Typecheck/lint and 62 web tests pass (/private/tmp/id-session-replay-final-{typecheck,lint}.log and /private/tmp/id-session-replay-web.log).

OpenAPI was regenerated through the exporter (/private/tmp/id-session-replay-openapi.log). README, schema/foundation status, mutation inventory and public retry guidance are updated. Replay is adopted for 42 of 46 mutations, with four user operations remaining and no replacement-precondition gaps. Correction to the preceding turn's scope summary: disableUser, enableUser and retireUserEmail require platform:users; eraseUser requires platform:write. The current users.ts route declarations and mutation inventory establish that distinction. Separate operation-status GET still requires platform:read; users-only principals can retry commands but that read-policy gap remains explicit. Tenant contexts/RLS, capability/pair policy, complete audit/cascade/invalidation and remaining F0–F7 requirements remain unfinished. No production migration or deployment occurred; the full goal remains active.

Production build and git diff --check pass (/private/tmp/id-session-replay-build.log). All repository gates are green for this slice; the original foundation goal remains incomplete and active.

## Execution: global user command recovery

The previous goal turn made verified progress on global session recovery. The remaining four user mutations now use the platform journal and seven-day encrypted response recovery. Disable/enable/retire-email preserve platform:users; eraseUser preserves platform:write and its confirmation check. Current authority precedes replay even after the target has been erased. New-key satisfied lifecycle commands record noops and preserve timestamps; disable noops do not repeat cleanup. Inert activation and retired-email re-enable restrictions remain.

New audit facts contain UUID/status/disabled time/email-retired flag rather than names/emails. Disable includes actual deleted session IDs and token counts; retirement no longer copies the old email into new immutable audit JSON. Erasure snapshots limited lifecycle state but still lacks its full cascade manifest. The existing profile response shape is encrypted for the ordinary seven-day recovery window and may remain recoverable after operational erasure. This does not resolve the explicit UUID-only versus restricted named-history decision, old identifying audit payloads or backup/key retention. Historical facts are not rewritten.

Tests cover all four historical results after erasure, noops, changed confirmation conflicts, injected audit rollback, concurrent retries, absence of identifying fields in new audit facts and both authority scopes revoked between middleware and replay. The first existing HTTP assertion accidentally expected audit-shaped data in the profile response and was corrected to the unchanged response contract. Two new fixtures initially inherited the database's inert default; they now explicitly create active users, preserving the inert restriction. Focused new checks pass 4 tests/68 assertions (/private/tmp/id-user-replay-focused3.log). The complete fresh-schema suite passes 1,320 ID tests/9,764 assertions with 100% line/function coverage (/private/tmp/id-user-replay-final-{migration,tests}.log). Typecheck/lint and 62 web tests pass (/private/tmp/id-user-replay-final-{typecheck,lint}.log and /private/tmp/id-user-replay-web.log).

OpenAPI was regenerated through the exporter (/private/tmp/id-user-replay-openapi.log). README, schema/foundation status, public lifecycle/retry/retention guidance and mutation inventory are updated; obsolete already-active/disabled/retired conflict rows were removed. First-principles review reuses the existing wrappers, journal, locks, deletion IDs and encryption; it removes identifying audit copies and avoids no-op writes without adding schema/workers. All 46 current administrative mutations now have replay contracts and all current replacement routes have preconditions. This does not complete the full foundation: operation-status read policy, full effect/cascade/token-ID evidence, tenant contexts/RLS, capability/pair policy, fresh authentication, common writer ordering and operational release proof remain unfinished. No production migration or deployment occurred; the original F0–F7 goal remains active.

Production build and git diff --check pass (/private/tmp/id-user-replay-build.log). All repository gates are green for this slice; completion of the full enterprise foundation remains unproven and the goal stays active.

## Execution: owned receipt inspection

The previous goal turn made verified progress by completing current mutation-family journal adoption. GET /me/operations/{operationId} now permits only receipts belonging to the authenticated actor, derived inside the service from the principal rather than supplied as a separate actor argument. It checks the immutable receipt context and re-evaluates current administrative authority inside a read transaction. Platform read/write/users scopes permit owned platform/tenant receipts; matching org read/write/users scopes also permit owned tenant receipts. Other actors and unsupported contexts return 404. No grants or lost contextual authority fail; replay still requires each mutation's own scope.

The endpoint reuses the existing reference-only projection and adds no-store. It never decrypts response bodies or returns fingerprints/keys. Existing audit-wide readers retain their permissions and visibility. The shared authority evaluator now accepts explicit alternative scope lists while existing commands still pass their single required scope. The own-receipt alternatives are shared constants used by both evaluator and OpenAPI. Self routes advertise no fixed x-scopes, with handler-checked alternatives where applicable; /me remains authenticated even without grants. The explicit open-route allowlist admits only /me and this handler-authorised self route. Existing generic audit-reader tests stay scoped to their two routes; dedicated own-route tests cover authentication, origin, ownership, contexts and current authority.

Tests cover users-only inspection without audit access, foreign actor/tenant exclusion, unsupported contexts, no grants, membership revoked at transaction entry, exact private-field exclusion, and platform/tenant machine and root identities. Focused checks pass 27 tests/1,705 assertions (/private/tmp/id-own-operation-focused.log). The first full run found the missing new path in app.test.ts's explicit OpenAPI inventory; the expected path was added. Review also corrected misleading fixed-scope metadata on self routes rather than leaving description/contract disagreement. The final fresh-schema suite passes 1,324 ID tests/9,888 assertions with 100% line/function coverage (/private/tmp/id-own-operation-final2-{migration,tests}.log). Typecheck/lint and 62 web tests pass (/private/tmp/id-own-operation-final2-{typecheck,lint}.log and /private/tmp/id-own-operation-web2.log).

OpenAPI was regenerated through the exporter (/private/tmp/id-own-operation-openapi3.log). README, schema/foundation progress, mutation inventory and public own-receipt/error/permission guidance are updated. First-principles review removed the separate caller-supplied actor parameter, reused the projection/evaluator, and shared the scope constants. No migration, worker or audit-access expansion was introduced. Full reader/service TenantContexts, RLS, capability/pair policy, complete effect/cascade/token evidence, fresh authentication, common writer ordering and operational release proof remain unfinished. No production deployment occurred; the full F0–F7 goal remains active.

Production build and git diff --check pass (/private/tmp/id-own-operation-build.log). All repository gates are green for owned receipt reads; the original enterprise foundation remains incomplete and active.

## Member service tenant context boundary

Member window updates, removal and reinstatement now require the authorised TenantMemberContext directly instead of accepting a raw executor and organisation ID. The runtime registry rejects copied contexts; the HTTP command wrapper invalidates its context after callback success or failure. Services reuse the organisation lock held by the factory, removing three duplicate lock calls while retaining atomic service audit writes. Service and SSO regression fixtures use the real authorisation factory inside a transaction. New regression evidence covers copied contexts, use after successful/failed callbacks and attempted wrapper reuse; existing tests retain cross-tenant, authority revocation, replay and SSO admission coverage.

Validation: 1,325 ID tests, 9,896 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check pass. Logs: /private/tmp/id-tenant-context-{tests,typecheck,lint,web,build}.log. No schema migration or public HTTP contract change was required. README, foundation specification and task tracking describe the new boundary and its limits.

First-principles review retained the existing transaction/authority primitives, removed duplicate organisation locking and used one private weak registry for runtime provenance/lifetime checks. This is a bounded internal API change, not complete tenant isolation: context factories must still be called inside transactions with the callback wrapper, actor attribution remains a separate service input, and reads, other tenant services, query-layer contexts, RLS, capability/pair policy, complete effect evidence and operational release proof remain pending. No production deployment occurred; the F0–F7 goal remains active.

## Member read contexts and current authority

Member list/detail and configuration services now require scoped contexts. The read wrapper owns the transaction, takes a shared organisation lock, rechecks the current principal using the existing authority evaluator, and expires the context on callback success or failure. Directory and configuration permissions remain distinct (platform:read/org:read versus platform:users/org:users); runtime checks reject casts into another projection or a write context. HTTP responses use no-store. Reinstatement now reads only configuration for its prior status, removing an unnecessary profile/group read. Query-layer functions and other tenant services remain outside this context adoption.

New tests cover copied/expired contexts, failed callback cleanup, projection separation and attempted writes from read contexts. Every member GET has a regression that revokes the actor after middleware but before the read transaction. The existing PATCH revocation test was corrected to fetch its ETag before installing the transaction hook; otherwise the new configuration transaction consumed the hook prematurely. Focused validation: 83 tests, 628 assertions (/private/tmp/id-member-reads-focused2.log). Full ID validation: 1,327 tests, 9,926 assertions, 100% line/function coverage (/private/tmp/id-member-reads-tests.log). Typecheck, lint and 62 web tests pass (/private/tmp/id-member-reads-{typecheck,lint,web}.log).

README, public member guidance, foundation specification and task plan reflect the boundary and remaining gaps. The remaining execution report also corrects its stale claim that member mutations lack replay: the current inventory has all 46 replay contracts and no missing replacement preconditions. First-principles review reused the authority evaluator and weak registry, removed the redundant organisation existence query from listMembers and narrowed the reinstatement pre-read. No migration, new dependency or public permission expansion. Complete tenant/query contexts, RLS, capabilities/pairs, effect evidence and operational release proof remain pending; the full goal stays active.

Next relevant boundary: access.ts still accepts raw database/organisation arguments. Preserve its existing asymmetric getMemberAccess permission (platform:read or org:users); it must not inherit member configuration's platform:users requirement by accident.

Production build and git diff --check also pass (/private/tmp/id-member-reads-build.log). All repository gates are green for the member read context slice. No production deployment occurred.

## Access-view tenant contexts

Both access services now require a validated read context rather than raw database/organisation inputs. The shared read boundary owns the transaction, acquires the existing shared organisation lock, evaluates current authority and invalidates the context on callback completion. Member access preserves platform:read or org:users; target access preserves platform:read or org:read. Services reject copied, expired and differently scoped contexts. Both HTTP views return no-store. The existing context module moved from services/tenant-command.ts to services/tenant-context.ts; member and access HTTP readers now share http/admin/tenant-read.ts instead of duplicating transaction/authority plumbing.

First-principles review removed redundant organisation existence reads and changed member-access existence checking from a profile/group projection to membership configuration. The existing evaluator and runtime registry are reused; no new framework, migration or dependency was added. Focused tests pass 104 tests/774 assertions (/private/tmp/id-access-context-focused.log), including current-membership revocation between middleware and each access transaction, platform-reader preservation, foreign targets and context misuse. Typecheck, lint and 62 web tests pass (/private/tmp/id-access-context-{typecheck,lint,web}.log). README, public guidance, foundation specification and remaining execution plan are updated.

This does not complete the access policy: target existence is still global, resource ownership/classification and exact client-resource capability evaluation remain pending. Other tenant services/query functions, targeted RLS, complete effect evidence and operational release proof remain unfinished. The full F0–F7 goal remains active. Next context adoption can batch group/domain/entitlement tenant directory reads with the existing org:read/platform:read context while keeping their platform-only writes and platform-wide lists distinct.

Full verification: the initial run had one unexpected PostgreSQL connection termination in the unchanged db/queries/access.integration.test.ts; 1,328 tests passed and that query test failed (/private/tmp/id-access-context-tests.log). No assertion or new context test failed. The complete repeat passes 1,329 tests, 9,942 assertions, 100% line/function coverage (/private/tmp/id-access-context-tests2.log). The connection failure did not recur; no test expectation or database policy was weakened to obtain the pass.

Production build and git diff --check pass (/private/tmp/id-access-context-build.log). All repository gates are green for the access-context slice. No production deployment occurred; the complete foundation goal remains active.

## Group, domain and entitlement directory contexts

Seven additional tenant read services now require the existing directory context: group list/detail, group-member list/detail, domain listing and tenant entitlement list/detail. Their HTTP handlers use tenantRead, which owns the read transaction, takes a shared organisation lock, rechecks platform:read or org:read and expires the issued context on callback completion. All return no-store. Platform-wide lists and platform-only mutations remain separate. The directory validator was renamed requireTenantDirectoryContext to reflect its shared consumers.

The new directory-context.integration.test.ts exercises every service with valid, copied, expired and incorrectly scoped contexts, then revokes membership between middleware and each of the seven HTTP read transactions. Existing tests retain tenant isolation, missing-target, pagination, revision and command regression coverage. Focused validation passes 280 tests/1,750 assertions (/private/tmp/id-directory-context-focused.log). Typecheck, lint and 62 web tests pass (/private/tmp/id-directory-context-{typecheck,lint,web}.log).

First-principles review removed the repeated organisation existence lookups from list services; the context factory already validates and locks that row. No new permission type, context registry, dependency, migration or public permission expansion was introduced. README, public management guidance, foundation specification and execution tracking are updated. Complete service/query contexts, platform contexts, targeted RLS, capabilities/pairs, complete effect evidence and operational release proof remain unfinished; the F0–F7 goal stays active.

Next compatible read adoption: organisation detail/summary and redacted SSO provider reads use platform:read or org:read. Historical audit reads require separate treatment because erased tenants have retained history and cannot require a live organisation row. Platform-wide lists remain explicitly distinct.

Full ID verification passes 1,331 tests and 9,998 assertions with 100% line/function coverage (/private/tmp/id-directory-context-tests.log). No failed tests in this run.

Production build and git diff --check pass (/private/tmp/id-directory-context-build.log). All repository gates pass for this directory-read slice. No production deployment occurred; the full foundation remains active.

## Organisation, summary and SSO read contexts

Organisation detail, tenant summary and redacted SSO provider reads now require the existing directory context and use tenantRead in HTTP. They preserve platform:read or org:read, recheck current authority in a shared-lock transaction, reject invalid context provenance/permission/lifetime and return no-store. The directory regression matrix now covers ten services/routes. Removed SSO's redundant organisation lookup and summary's unreachable missing-organisation branch: the read context validates and holds the live organisation row throughout the callback. Platform summary and platform-wide organisation listing remain separate.

Focused tests pass 154 tests/932 assertions across eight files (/private/tmp/id-organization-context-focused.log), including service projections, SSO redaction, HTTP revocation, lifecycle, replay and ETags. Typecheck, lint and 62 web tests pass (/private/tmp/id-organization-context-{typecheck,lint,web}.log). README, public management guidance, foundation specification and execution tracking are updated. No migration, new permission, framework or dependency. Complete platform/query contexts, historical audit boundaries, targeted RLS, capability policy and release proof remain pending.

Next concrete isolation gap found in services/diagnostics.ts: diagnoseSignIn accepts raw organisation/DB arguments, looks up any email globally and returns the global user's UUID/status/retired-email flag, all account issuers/directory IDs and the routed organisation UUID/slug even when the email belongs to another tenant. The HTTP route permits org:users (platform alternative platform:read). Fix its tenant projection and transaction context together; merely adding a context would preserve this cross-tenant disclosure. Keep any platform-only diagnostic evidence explicit, do not infer platform visibility from a tenant scope, and preserve truthful unknown/token-required outcomes when tenant authority cannot establish global identity facts. Historical audit reads remain a separate problem because an erased tenant has no live row to lock.

Full ID verification passes 1,331 tests/10,022 assertions with 100% line/function coverage (/private/tmp/id-organization-context-tests.log). The expanded ten-route context matrix passes; no failed tests in this run.

Production build and git diff --check pass (/private/tmp/id-organization-context-build.log). All repository gates pass for the organisation/summary/SSO read-context slice. No production deployment occurred. The full goal remains active; address the recorded diagnostic cross-tenant projection next.

## Tenant diagnostic privacy boundary

A regression first reproduced the foreign identity disclosure: org:users diagnosis returned a foreign global user's UUID/status/retired-email state (/private/tmp/id-diagnosis-before.log). diagnoseSignIn now requires the existing member-access read context (platform:read or org:users), rechecks authority inside the transaction and returns no-store. It replaces the global user/account lookup with the tenant-filtered member query. Foreign routing UUIDs/slugs are never returned. Accounts and user.retiredEmail were removed from the response for every caller, including staff, avoiding an unnecessary second diagnostic implementation; staff retain separately authorised global user APIs.

The response no longer predicts global account creation or successful sign-in. authentication_required means local configuration/membership checks found no blocker; a null user means no matching membership here, not global email availability. identity_conflict moved to requiresToken because real authentication must establish identity. Disabled local users and revoked local memberships remain visible blockers; membership windows remain grant conditions. This is an explicit API contract change and its consumer update requirement is documented.

Focused checks pass 26 tests/110 assertions (/private/tmp/id-diagnosis-focused.log). Tests compare unknown and foreign identities, show that creating a disabled user with accounts/membership in B does not alter A's response, reject copied/expired/wrong-scope contexts, recheck authority after middleware, and keep staff and tenant projections identical. Typecheck, lint and 62 web tests pass (/private/tmp/id-diagnosis-{typecheck,lint,web}.log). OpenAPI regenerated only via exporter (/private/tmp/id-diagnosis-openapi.log); onboarding docs, README, foundation and task tracking updated.

First-principles review deleted global account enumeration and unsupported predictions instead of introducing platform/tenant diagnostic forks or guessing identity from email. It reuses existing scoped reads and context lifecycle. The full foundation remains unfinished: historical audit/operation contexts, platform/query boundaries, targeted RLS, capabilities/pairs, effect evidence and operational release proof remain pending.

Full ID verification passes 1,335 tests/10,039 assertions with 100% line/function coverage (/private/tmp/id-diagnosis-tests.log). No failed tests; the original disclosure regression now passes.

Production build and git diff --check pass (/private/tmp/id-diagnosis-build.log). All repository gates pass for diagnostic isolation. No production deployment occurred; consumer compatibility remains a release gate and the full foundation goal remains active.

## Current-authority tenant audit history

The before-fix regression returned 200 instead of 403 when authority was revoked between middleware and the tenant audit read (/private/tmp/id-history-context-before.log). Organisation audit services now require an issued history context; HTTP enters it through tenantRead and returns no-store. The transaction locks a live organisation if present and rechecks platform:read or org:read. Only history contexts permit a missing live organisation; the service retains its explicit live-row-or-audit-evidence check and unknown history returns 404. The context still fixes the organisation filter, and cannot be copied, reused or substituted for live directory access.

The read permission mapping is now explicit for directory, configuration, memberAccess and history, avoiding default scope branches as contexts grow. No new storage, worker, dependency or audit copy was introduced. Focused tests pass 35 tests/236 assertions (/private/tmp/id-history-context-focused.log), including stale authority, retained staff history after erasure, foreign/unknown history rejection and invalid contexts. Existing person/tenant erasure-history regression also passes with the real factory. Typecheck, lint and 62 web tests pass (/private/tmp/id-history-context-{typecheck,lint,web}.log). README, public management docs, foundation and execution tracking are updated.

Platform-wide/user audit reads and operation status still require further context adoption. In particular, getOrganizationOperation currently selects actor-wide versus platform-wide receipt visibility from middleware tier; the next change must derive that distinction from current authority inside its read transaction, not merely add an org:read check while retaining cached platform visibility. Preserve erased-tenant receipts and actor filtering. Platform/query contexts, RLS, capabilities/pairs, complete audit effects and operational release proof remain unfinished; the full goal stays active.

Full ID verification passes 1,338 tests/10,050 assertions with 100% line/function coverage (/private/tmp/id-history-context-tests.log). The stale-authority regression now passes and retained history remains readable after erasure. No failed tests.

Production build and git diff --check pass (/private/tmp/id-history-context-build.log). All repository gates pass for current-authority tenant history reads. No production deployment occurred; the full foundation remains active.

## Current authority controls receipt visibility

A before-fix regression returned 200 after platform membership was revoked at transaction entry (/private/tmp/id-operation-authority-before.log). Both audit-wide operation-status routes now call getAuditOperationStatus, which owns the read transaction, rechecks current platform:read or matching org:read, and derives visibility from the result. authorizeCommand now returns the admitting platform/tenant tier; root and current platform grants retain priority, while current tenant grants restrict the receipt to the authenticated actor and tenant. Middleware's cached tier and a caller-supplied actor/access selector no longer determine HTTP receipt visibility. A live tenant row is shared-locked when present; erased tenants' receipts remain readable with current platform authority.

Tests exercise loss of all platform authority, downgrade from platform to tenant while reading another actor's receipt (404), continued access to one's own tenant receipt (200), erased-tenant receipts and existing user/client/root own-receipt behaviour. The first focused run found missing no-store on the downgrade 404; all status handlers now set it before invoking the service. Focused validation passes 28 tests/125 assertions (/private/tmp/id-operation-authority-focused2.log). Typecheck, lint and 62 web tests pass (/private/tmp/id-operation-authority-{typecheck,lint,web}.log). README, public management guidance, foundation and task tracking updated; no public response-body/schema change.

First-principles review removed cached-tier branching and reused the existing evaluator and reference-only projection. A single authenticated actor helper is shared with own-receipt reads. No table, worker or dependency was added. Platform-wide/user audit context adoption, other platform/query boundaries, targeted RLS, capability/pair policy, complete audit effects and operational release proof remain unfinished. The full F0–F7 goal stays active.

Full ID verification passes 1,341 tests/10,058 assertions with 100% line/function coverage (/private/tmp/id-operation-authority-tests.log). Existing command/replay authorisation tests pass with the evaluator returning its current tier. No failed tests.

Production build and git diff --check pass (/private/tmp/id-operation-authority-build.log). All repository gates pass for current-tier receipt visibility. No production deployment occurred; the full foundation remains active.

## Explicit platform audit read contexts

The before-fix test returned 200 when platform membership was revoked after middleware admission (/private/tmp/id-platform-audit-before.log). Fleet-wide and user audit services now require PlatformReadContext, created only after current platform:read succeeds inside its owned transaction. The fixed permission has no tenant alternative. A private weak registry rejects copied/expired handles and tenant-context substitution; callback success and failure both invalidate the handle. The HTTP platformRead wrapper sets no-store before the service call. Retained user history after erasure and unknown-subject 404 behaviour remain unchanged.

Focused validation passes 40 tests/496 assertions (/private/tmp/id-platform-audit-focused.log), including current authority on both routes, erased-user history, service context misuse and failed callback cleanup. Typecheck, lint and 62 web tests pass (/private/tmp/id-platform-audit-{typecheck,lint,web}.log). README, public management guidance, foundation and execution tracking are updated.

First-principles review kept a minimal separate platform read context with no optional tenant filter, scope override or write authority. No schema, worker, dependency or response-body change. Other platform service families still need adoption, followed by query/RLS boundaries, capability/pair policy, full audit effects and operational release proof. The full F0–F7 goal remains active. Next useful batch is pure platform reads (global users/sessions, client/resource configuration, platform summary and platform-wide lists); keep SSO discovery/network calls outside database transactions.

Full ID verification passes 1,343 tests/10,066 assertions with 100% line/function coverage (/private/tmp/id-platform-audit-tests.log). Both platform stale-authority paths and retained history regressions pass. No failed tests.

Production build and git diff --check pass (/private/tmp/id-platform-audit-build.log). All repository gates pass for explicit platform audit contexts. No production deployment occurred; the full foundation remains active.

## Global user and session read contexts

The before-fix regression returned 200 after platform authority was revoked between middleware and a global identity read (/private/tmp/id-global-identity-before.log). User list/detail and global session listing now require an issued PlatformReadContext and enter through platformRead in HTTP. Current platform:read is rechecked inside the transaction; tenant contexts, copies and expired handles cannot invoke the services. All three successful response paths are checked for no-store. Existing user/session mutations retain their independent command permissions and replay contracts.

Focused validation passes 113 tests/729 assertions (/private/tmp/id-global-identity-focused.log), covering the read boundary, platform revocation, invalid service contexts and existing user/session privacy, pagination, lifecycle and replay. The subsequent first-principles review found session listing loaded a complete profile/identity graph only to check existence. Added one ID-only userExists query and reused it in session listing and user audit history, removing unnecessary profile/account/membership/count queries from both paths while retaining unknown-user behaviour. No new context type, schema or dependency. Typecheck, lint and 62 web tests pass (/private/tmp/id-global-identity-{typecheck,lint,web}.log).

README, public management guidance, foundation and task tracking are updated. Other platform service families and query/RLS boundaries remain pending, followed by capabilities/pairs, complete audit effects and release proof. The full F0–F7 goal remains active; no production deployment occurred.

Full ID verification passes 1,345 tests/10,087 assertions with 100% line/function coverage (/private/tmp/id-global-identity-tests.log). The simplified existence query preserves session and erased-user audit behaviour; no failed tests.

Production build and git diff --check pass (/private/tmp/id-global-identity-build.log). All repository gates pass for global identity read contexts and ID-only existence checks. No production deployment occurred; the full foundation remains active.


## Fleet directory service boundary

Platform summary, organisation listing and fleet-wide entitlement listing now consume the existing PlatformReadContext and HTTP wrapper. Before the fix, revocation after middleware still returned 200 (expected 403): `/private/tmp/id-fleet-context-before.log`. Regression tests cover current-authority revocation, valid/copy/expired/tenant contexts, and no-store responses. Existing filters and response shapes are preserved. Verification: 195 focused tests pass. Full gates pass: 1,347 ID tests, 10,108 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-fleet-context-{focused,tests,typecheck,lint,web,build}.log`.

First-principles review: reused the fixed platform-read permission and transaction lifetime; no additional context type, schema, dependency or policy selector. Removed raw Database imports from the three services. This is partial F4 work, not complete query isolation or RLS.


## Registration read service boundary

Client/resource list and detail reads now require PlatformReadContext. The before-fix regression returned 200 after authority revocation and lacked no-store (`/private/tmp/id-registration-context-before.log`). The expanded matrix checks revocation, copied/expired contexts, tenant substitution and response caching across all seven adopted fleet directory routes. Existing registration tests retain pagination, redaction and revision checks. Focused suite: 215 tests pass.

First-principles review: kept the existing registration shared locks but removed redundant detail-service transactions. The issued context already owns the transaction, so no new context kind, permission selector, schema or dependency was needed. Test adapters use the production factory; type annotations now reference the implementation namespace. This remains partial F4 work; resource ownership and the exact-pair evaluator are not implemented by this change.

Registration boundary gates pass: 1,347 ID tests, 10,136 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-registration-context-{focused,tests,typecheck,lint,web,build}.log`. The full foundation remains active; no production deployment or migration occurred.


## SSO connectivity configuration boundary

Before-fix regression returned 200 after platform authority revocation and lacked no-store (`/private/tmp/id-sso-snapshot-before.log`). The endpoint loader now requires the existing PlatformReadContext; the HTTP handler awaits completion before invoking the database-free probe. Only issuer and discoveryEndpoint cross that boundary. Existing platform-only permission, 404 provider_not_found and network safety checks remain unchanged. Focused verification: 68 tests, 547 assertions pass, including live/copied/expired/tenant context checks and actual local discovery/JWKS fetches observing zero active read transactions.

First-principles review removed the network unit tests' fake database and moved missing-provider coverage to the existing real-database HTTP test. No new snapshot brand or policy abstraction is needed: this value contains endpoints, not authority or a database handle. The snapshot is intentionally point-in-time, not a promise of unchanged configuration throughout network I/O.

The full ID suite passed 1,347 tests / 10,147 assertions with 100% line/function coverage. Typecheck caught the test's unsupported app fetch option; the corrected test observes global fetch without changing app configuration. The final observer test passes (3 tests / 61 assertions; `/private/tmp/id-sso-snapshot-observer.log`). Typecheck, lint and 62 web tests pass.

SSO snapshot gates pass: full ID coverage (1,347 tests, 10,147 assertions, 100% line/function coverage), final observer regression (3 tests, 61 assertions), typecheck, lint, 62 web tests, production build and git diff --check. Logs: `/private/tmp/id-sso-snapshot-{focused,tests,observer,typecheck,lint,web,build}.log`. No schema or public response-body change; no deployment. The full foundation remains unfinished.


## Global users command service boundary

Five services formerly accepted raw Executor plus Actor: disableUser, enableUser, retireUserEmail, revokeUserSession and revokeUserSessions. They now validate an issued PlatformUsersContext. The journal's current-authority callback returns the mutation runner; context issuance happens only when the journal calls it, so historical replay never creates an unused context. Contexts expire on callback success/failure. Read, tenant, raw-executor, copied and expired handles are rejected in new integration tests. Existing service rollback tests and HTTP replay/current-authority tests remain intact. Focused verification: 115 tests / 765 assertions pass.

First-principles review removed the five inner service transactions; the journal or explicit test helper already owns the transaction. Reused the fixed platform:users permission without adding a configurable policy selector. Actor attribution remains separately supplied internally; global erasure and other write-service contexts remain unfinished. No API response or schema change.

Global users command gates pass: 1,349 ID tests / 10,183 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-users-command-{focused,tests,typecheck,lint,web,build}.log`. No production migration or deployment. Full F0–F7 completion remains unproven and the goal stays active.


## Command authorisation lifetime correction

Review found the platform users factory returned a reusable runner: although each issued context expired, a saved runner could issue another context later. The journal now takes an optional explicit authority release callback and invokes it in finally after authorisation, covering execution, replay, conflict and failures. The platform users runner is single-use and closes on journal exit. The same owner cleanup releases tenant member contexts on replay, which previously skipped their mutation callback cleanup. Tests capture actual authorisations across commit/replay/conflict/failed mutation and reject later reuse; a tenant replay regression checks both captured contexts expire. First focused run: 98 tests / 784 assertions pass; full suite includes the additional tenant regression.

First-principles review: lifecycle belongs to the existing transaction owner. An explicit cleanup callback avoids inspecting arbitrary authority objects or adding timers, AsyncLocalStorage or a context broker. Internal non-journal callers must own the transaction and close authorisation in finally. Existing read contexts already own their transaction/callback lifetime. No schema or public API change.

Command lifetime gates pass: 1,351 ID tests / 10,201 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-command-lifetime-{focused,tests,typecheck,lint,web,build}.log`. The full foundation remains active and unfinished.


## Platform write context and organisation lifecycle

All platform write HTTP callbacks now receive an issued PlatformWriteContext. Organisation creation/update/disable/enable/erasure and global user erasure validate it directly. Remaining client/resource/domain/group/entitlement/SSO write services still receive an explicitly extracted executor; their direct adoption remains pending. Current platform:write is checked before replay/execution. Tests reject users/read/tenant/copied/expired/raw handles and prove write contexts cannot substitute for users authority. Focused verification: 137 tests / 855 assertions pass, including service audit rollback, organisation revision/replay and actual machine grant lock ordering.

First-principles review shares command lifetime machinery across two fixed scopes, with runtime access discrimination rather than interchangeable authority. Removed the superseded authorizePlatformCommand wrapper and six inner transactions. Internal grant-ordering fixtures now use the real write-context factory. No additional schema, permissions or public response changes.

Platform write slice gates pass: 1,352 ID tests / 10,242 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-platform-write-{focused,tests,typecheck,lint,web,build}.log`. No production migration or deployment. The full foundation remains active.


## Domain and SSO write service boundary

Four domain operations and SSO put/delete now require PlatformWriteContext directly. The shared domain status helper validates the context; its public enable/disable wrappers pass the context through. HTTP handlers no longer extract a raw executor for these services. Existing organisation/domain/provider locks, secret preservation, redaction, revision and journal semantics remain. The expanded context matrix rejects copied/expired/raw/read/users/tenant handles. Focused verification: 121 tests / 772 assertions pass.

First-principles review removes five inner transaction blocks (three domain implementations, two SSO implementations), keeping the existing shared status helper and command factory. No new context type, permission or schema. An initial broad text edit removed two audit-call closing lines; the syntax was corrected, the invalid test process was explicitly stopped, and the focused suite rerun. Formatter used the existing apps/web binary after root bunx could not resolve one. Typecheck and lint pass.

Domain/SSO write gates pass: 1,352 ID tests / 10,278 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-federation-write-{focused,tests,typecheck,lint,web,build}.log`. No deployment or migration. The full foundation remains active and unfinished.


## Group, assignment and entitlement write boundary

Seven group/assignment and five entitlement entry points require PlatformWriteContext directly. Public enable/disable functions delegate to their context-checked status helper. HTTP handlers no longer extract an executor. The context rejection matrix covers all twelve calls; existing tests retain foreign references, revoked membership, immutable assignment/revision, noops, rollback and historical replay checks. Focused verification: 232 tests / 1,780 assertions pass. Typecheck and lint pass.

First-principles review removed ten inner transaction blocks and retained shared status helpers and lock order. A test-only platformWriteService adapter reduces repeated fixture signatures while using the real context factory and transaction; it adds no production bypass. Two membership-revocation fixture calls were updated after typecheck identified their raw executor arguments. No new permission, schema or public response shape. Exact-pair capability policy, tenant write delegation and RLS remain unfinished.

Group/assignment/entitlement write gates pass: 1,352 ID tests / 10,350 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-access-write-{focused,tests,typecheck,lint,web,build}.log`. No production migration or deployment. The full foundation remains active.


## Client and resource write service boundary

Nine client and five resource operations now require PlatformWriteContext directly; no administrative handler extracts platform.tx for service mutation calls. Expanded context rejection tests cover all 38 platform-write operations alongside the five users commands; tenant member commands retain their separate context. Token, runtime-role, admin and issuance-ordering fixtures use the real test context adapter. Existing lock/identity/secret/replay behaviour remains.

The initial focused suite passed 247 tests and failed the client creation context test: input validation ran before the guard. Moving context validation first fixes this; the corrected service/context subset passes 17 tests / 458 assertions. Typecheck and lint pass. Full suite results follow.

First-principles review removed twelve inner transaction blocks, retained the shared disabled-state helpers and used the existing context/fixture adapter. Protected-resource validation also follows context validation. This completes administrative mutation service context adoption (38 platform-write + 5 platform-users + 3 tenant-member), not query contexts/RLS, actor binding, capability/pair policy or operational release proof. No schema or API response change.

Registration write gates pass: 1,352 ID tests / 10,434 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-registration-write-{focused,corrected,tests,typecheck,lint,web,build}.log`. The initial focused failure is superseded by the corrected subset and full passing suite. No production migration or deployment. Full foundation remains active.


## Targeted tenant RLS

Generated migration 0025 enables RLS on groups, group_members and entitlements. Validated service factories install transaction-local read/write scopes; bootstrap uses its established tenant UUID. Policy-user and bound-platform root reads are read-only, own a transaction/savepoint and restore enclosing settings. Runtime-role startup rejects disabled RLS. The isolation inventory names all table exclusions and remaining query/context/capability work.

Initial runtime proof exposed a one-connection fixture barrier deadlock; its two-party test now has a two-connection pool and failure cleanup. The HTTP fixture needed an actual tenant group, and foreign tenant routes retain their existing 404 projection. Human middleware now has a broker transaction; one-shot revocation hooks explicitly skip it so they continue testing post-middleware service authority. Corrected focused proof: 110 tests / 805 assertions pass; final pool-reuse and full-suite results follow.

First-principles review: two policies per protected table, three transaction-local settings, existing context owners and narrow broker wrappers. No blanket Better Auth RLS, new tenant database, separate policy service or session-level setting. Policy-user RLS limits membership organisations; the existing evaluator still decides effective grants. This does not defend against arbitrary SQL execution capable of setting trusted context. Only the disposable test database was migrated.


Final RLS review removed unnecessary protected-table authority from global user/session commands: their database scope is now `none`. Real-runtime tests prove those commands see no protected rows and an actual HTTP user disable still succeeds. Two simultaneously checked-out backend connections prove pool reuse clears context; nested broker success and rollback both restore the enclosing tenant.

RLS slice gates pass: 1,355 ID tests / 10,521 assertions, 100% line/function coverage; final runtime-only verification 6 tests / 113 assertions; 62 web tests; typecheck, lint, production build and git diff --check. The focused command exits 1 solely because repository-wide coverage thresholds apply to a one-file run; all six tests pass, and the full coverage command exits 0. Logs: `/private/tmp/id-rls-{tests,final-runtime,typecheck,lint,web,build}.log`. Only the disposable test database was migrated. Query context typing, actor binding, capability/pair policy, tenant-bound user grants, complete writer ordering, audit effects and production rehearsal remain unfinished. F0–F7 and the active goal are not complete.


## Global user/session actor binding

The five platform-users command services now read their audit actor from the issued command context. Removed their separate Actor argument and the corresponding HTTP callback argument. Platform command contexts snapshot the authenticated principal identity at authorisation and freeze the actor assembled for execution. Only request ID, operation ID, IP and user agent are copied from execution metadata; metadata cannot replace actor type or ID. The existing journal supplies operation correlation. Platform-write contexts also carry this actor, but their services still accept the old separate actor argument; their migration and tenant-member actor binding remain unfinished.

Focused verification: 25 tests / 479 assertions pass, including actor freezing, identity/metadata mutation, service rollback and journal replay. Three initial service assertions expected an arbitrary user actor although their helper authorises root; corrected fixtures now expect root. HTTP replay assertions verify the actual platform administrator's identity on retained events. Full repository gate results follow.

First-principles review removes duplicated actor plumbing at the five service boundaries and reuses the existing command context/runner. The metadata copy is allowlisted rather than spread, including at runtime when a structurally wider object is supplied. No new permissions, database schema or HTTP response contract. This is partial actor adoption, not completion of F4/F7.


Global user/session actor gates pass: 1,356 ID tests / 10,534 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-actor-{focused,corrected,tests,typecheck,lint,web,build}.log`. The initial fixture expectation failures are superseded by the corrected focused suite and full suite. Existing HTTP user/session tests validate both human and machine actor identity; replay tests validate the retained administrator identity. No migration or deployment. Remaining 38 platform-write and three tenant-member service actor arguments are explicitly not yet migrated; F0–F7 remain active and unfinished.


## Platform-write actor binding

All 38 platform-write service entry points now derive their actor from the authorised context. Removed separate Actor parameters from those services, shared status helpers and HTTP mutation callbacks. Together with the five platform-users commands this binds 43 of 46 administrative service actors; the three tenant-member commands remain pending. Test adapters accept request/operation metadata while their authorised root identity determines the audit actor. Raw, copied, expired and wrong-scope context rejection remains covered.

Focused evidence: 166 tests / 1,568 assertions pass across service, replay and issuance-ordering tests. The frozen-identity regression now exercises both platform command factories, including forged actor fields in metadata and mutation of the caller object after authorisation. Initial typecheck identified two fixture metadata literals carrying obsolete actor fields; those fields were deleted. Full gate results follow.

First-principles review keeps the existing shared context factory and audit helpers. Removing actor arguments also removes duplicate identity choices from service callers; no new service, permission, schema or runtime mechanism is needed. Existing rollback tests still inject invalid request metadata into the actual audit write. Tenant context actor binding and the other foundation requirements remain unfinished.


Platform-write actor gates pass: 1,357 ID tests / 10,538 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-write-actor-{focused,tests,typecheck,lint,web,build}.log`. Existing HTTP audit assertions cover actual human and machine identity for all migrated route families. The targeted suite's exit 1 reflects repository-wide coverage thresholds; its 166 tests all pass, and the full coverage command exits 0. No migration or deployment. Three tenant-member actor boundaries and the remaining F0–F7 requirements remain unfinished; the goal remains active.


## Tenant-member actor binding

Member window updates, removal and reinstatement now derive actor identity from their issued tenant command context. This completes actor adoption for all 46 administrative mutation services. Tenant authorisation returns a single-use runner: current authority and the organisation lock precede journal replay; only a new mutation creates the frozen actor-bearing context. Journal cleanup closes the runner on replay, success, conflict or failure. Removed the old context-release/callback exports. Platform and tenant factories share the small allowlisted commandActor constructor.

First-principles review removed three redundant member-service transactions and their separate Actor parameters. The journal or explicit test owner already encloses the writes and audit facts. Kept the tenant's distinct authority and organisation lock; no configurable permission factory or ambient context was introduced. The remaining internal HTTP journal callback receives request/operation metadata, while public command callbacks receive only their issued context. No database or public API change.

Focused verification: 113 tests / 998 assertions pass, including actual retained audit identity after forged metadata, immutable identity snapshots, single-use runner rejection, expired/copied contexts, replay cleanup, audit rollback, tenant boundary and SSO revocation checks. Typecheck initially identified remaining federation fixture calls and a test edit syntax error; these were corrected before the passing focused run. Full gates follow. Query typing, capability/pair evaluation, tenant-bound user grants, complete writer ordering and operational foundation gates remain unfinished.


Tenant actor gates pass: 1,358 ID tests / 10,543 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-tenant-actor-{focused,tests,typecheck,lint,web,build}.log`. The focused command exits 1 only because whole-repository coverage thresholds apply; all 113 focused tests pass and the full coverage command exits 0. Existing member HTTP audit assertions verify both human and machine caller identity. All 46 administrative service actor boundaries are now migrated. No migration or deployment. The full foundation goal remains active: query boundaries, capability/pair policy, tenant-bound user grants, complete concurrency/audit effects and operational release proof remain incomplete.


## Bounded administrative lock waits

executeOperation now sets transaction-local lock_timeout to two seconds before current-authority validation or mutation. PostgreSQL lock-timeout SQLSTATE 55P03 maps through the existing problem mapper to 503 database_busy with retryable true and Retry-After: 1. All 46 journalled commands inherit this bound, including authorisation before replay. A blocked duplicate may therefore return database_busy before operation_in_progress; current authority still precedes disclosure of a stored result.

Focused proof: 47 tests / 242 assertions pass. Real PostgreSQL advisory locks test timeouts during authorisation and after mutation/audit writes, empty rolled-back journals/evidence/domain writes, authority cleanup, same-key retry/replay and restoration of lock_timeout on a single reused connection. An HTTP row-lock test verifies the actual 503/header/body, no retained receipt or mutation audit, and successful same-key retry followed by replay. The public guide and generated OpenAPI document the response.

First-principles review uses one SET LOCAL at the existing transaction owner and the existing database error mapper. No retry loop, queue, deadline framework or session-level setting. The limit applies to each lock wait, not total request duration or pool acquisition; it does not prove all grant writers share the required lock order. No migration or deployment. Full gates follow.


Administrative lock gates pass: 1,362 ID tests / 10,575 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. OpenAPI files were regenerated through the exporter and drift tests pass. Logs: `/private/tmp/id-command-lock-{focused,tests,typecheck,lint,web,build,openapi}.log`. The focused command's exit 1 reflects whole-repository coverage thresholds; all 47 focused tests pass, and full coverage exits 0. No migration or deployment. This completes the journal's per-lock wait bound, not total request deadlines, complete writer ordering or the remaining F0–F7 release requirements. The goal remains active.


## Resource ownership and classification

Migration 0026 adds platform_shared/tenant_owned classification and a nullable owner UUID, with an exact shape CHECK, owner index/FK RESTRICT and immutable classification/owner trigger. Existing global rows and omitted creation classification remain platform_shared. Administrative creation accepts explicit private ownership; incompatible shapes fail validation, and PATCH rejects ownership fields. Responses and allowlisted audit snapshots include ownership. A private resource's owner cannot be erased while that resource exists.

A database trigger rejects cross-tenant private-resource entitlement insert/update, including direct SQL. Machine issuance checks the locked resource owner against the already authenticated/current machine tenant before the provider issues a token. Compatibility links remain independent of tenant authority, preserving the intended future use of shared login clients under a selected user tenant. User OAuth remains closed and capability/pair policy remains unfinished.

Focused verification: 102 tests / 515 assertions pass across identity constraints, the installed provider, resource HTTP administration and replay. Raw SQL proves private assignment isolation and immutable ownership; real provider calls prove own-tenant issuance and foreign denial despite a link. Initial fixture failures came from passing unexecuted Drizzle builders to rejection assertions; all were corrected. Typecheck also exposed the preceding lock-response header's widened string literal. Adding `as const` fixes it; the prior lock-slice typecheck-pass statement is superseded by this finding and the corrected passing typecheck. No production migration; only the disposable schema was rebuilt.

First-principles review adds two columns, one shape constraint, an owner reference, and narrow database guards. Uses the existing issuance lock and service command boundary; no new policy service, mutable ownership transfer or per-tenant database. The backfill preserves the previous global resource semantics but requires an operator inventory review before production cutover. Existing workers must be quiesced so they cannot ignore the newly supported private-ownership request fields. Full repository gates follow.


The initial full ownership run passed 1,364 tests and failed the two schema-contract snapshots. Their expected columns, owner FK/shape CHECK and index were updated against migration 0026 and actual PostgreSQL definitions. The full gate is being rerun. The corrected resource API also rejects ownership PATCH fields and records owner/classification in its committed audit snapshot.


Final ownership review found a retry-compatibility issue before completion: schema defaults would add new fields to the canonical input of historical shared-resource creation commands. The route now omits shared ownership defaults from the fingerprint while retaining private ownership fields. A receipt written with the pre-ownership command input is recovered under both implicit and explicit shared requests; a private request with that key conflicts. Focused replay proof: 6 tests / 67 assertions pass. Historical replay bodies remain unchanged and may omit the newer ownership fields; GET returns current state. Final full gates are rerunning after this fix.


Resource ownership validation is complete for this slice: full runtime suite 1,367 tests / 10,611 assertions passes with 100% line/function coverage. The final metadata-only adjustment permits omitted ownership fields in historical mutation responses while current list/detail schemas require them; generated-contract inspection and 85 final resource/replay/OpenAPI tests / 417 assertions pass. Final typecheck, lint, production build, 62 web tests and git diff --check pass. The earlier OpenAPI header type error is corrected and final typecheck exited 0. Logs: `/private/tmp/id-resource-owner-{tests,final-contract,typecheck,lint,web,build,legacy-replay,openapi}.log`. Focused exits 1 are the repository-wide coverage threshold, not failed tests. Migration 0026 was rebuilt/applied only on the disposable test database. Production inventory review and coordinated cutover remain required; no deployment occurred. Capability ceilings, exact-pair/user-grant policy, query contexts, full writer/effect evidence and F7 operational proof remain unfinished. The full goal remains active.


## Exact client/resource assignment groundwork

Migration 0027 permits client-only, resource-only and exact pair targets while retaining non-empty targets, existing foreign keys, private-resource ownership guards and NULLS NOT DISTINCT uniqueness. Platform entitlement creation accepts pairs. Member access emits distinct client_resource targets (id is the client ID, resource is explicit); target access matches both nullable fields exactly, and the service validates both references. HTTP forwarding retains both inputs. Inventory filters retain their broader configured-row meaning.

Direct administrative grants and bound-platform writer detection already require clientId IS NULL; tests now prove pair scopes cannot enter those paths. New query tests separate two clients, two resources, another tenant, client-only and resource-only grants while unioning only matching organisation/group/member pair sources. HTTP proof creates a pair, checks its committed audit fields, reads it by both target fields and rejects a missing resource reference. Initial failures were obsolete both-target rejection expectations and the resulting extra schema fixture row; corrected focused suite passes 134 tests / 1,003 assertions.

First-principles review derives target kind from existing columns, avoiding another persisted discriminator. Replaced duplicated target-selection SQL with exact comparisons of both fields; a tuple map key prevents pair collisions. No wildcard pair semantics or user OAuth opening. Capability ceilings, common issuance evaluator, approved conversion of non-admin resource-only assignments and tenant-bound user grants remain unfinished. Only the disposable database received migration 0027. Old readers must be quiesced before pair data is introduced. Full gates follow.


Exact-pair slice gates pass: 1,369 ID tests / 10,625 assertions, 100% line/function coverage; final private-resource cross-check 13 tests / 67 assertions; 62 web tests; final typecheck, lint, production build and git diff --check. Generated OpenAPI drift checks pass. The private cross-check additionally proves both rejection of a foreign pair and acceptance of the owning tenant's pair. Logs: `/private/tmp/id-pair-{focused,corrected,tests,private,typecheck,lint,web,build,openapi}.log`. Focused exit 1 is the whole-repository coverage threshold; reported focused tests pass and the full coverage command exits 0. No production migration or deployment. F5 remains partial: capability ceilings, common issuance evaluation and approved legacy resource-only conversion remain unfinished, along with the other F0–F7 release requirements. The full goal remains active.


## Active global user in effective policy

The effectiveGrants, hasPlatformWriter, memberAccess and targetAccess queries now join only active global users. Session admission already checked this state, but query-level policy and root writer detection previously omitted it. Inert/disabled users therefore receive no effective target projection and cannot keep root locked merely through retained memberships/assignments. Re-activation restores query eligibility subject to all remaining membership/grant conditions; it does not recreate revoked sessions or memberships.

Focused proof: 28 tests / 218 assertions pass. Query regressions exercise active → inert → disabled → active for resource, client and pair targets and bound-platform writer detection. The actual root HTTP integration proves the sole disabled writer no longer locks root and re-enabling them locks it again. Access fixtures that intend active people now explicitly seed active users instead of relying on the inert creation default.

First-principles review adds the missing user-state condition to existing SQL joins. It keeps session admission and current-command checks because those establish separate authentication/lifetime guarantees. No new policy abstraction, schema, permission or token protocol. This fixes a shared prerequisite; capability ceilings, the full evaluator and writer ordering remain unfinished. Full gates follow.


The initial full active-user run passed 1,370 tests and failed one HTTP expectation that explicitly included disabledUser in effective access. The expectation now excludes that inactive person, and the same test asserts their member endpoint returns effective false with no targets. The full suite is rerunning; this corrects the expected policy rather than reactivating the disabled fixture.


Active-user policy gates pass: 1,371 ID tests / 10,660 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. Logs: `/private/tmp/id-active-user-{focused,initial-tests,tests,typecheck,lint,web,build}.log`. The initial HTTP expectation failure is superseded by the corrected passing full suite. Focused exit 1 reflects whole-repository coverage thresholds; all 28 focused tests passed and full coverage exits 0. No API schema change, migration or deployment. Capability ceilings, full issuance evaluation, query contexts and the remaining concurrency/operational requirements remain unfinished; the full goal stays active.


## Foundation plan reconciliation

Re-read the feedback and current assignment/access, grant, machine-provider, operation, membership and isolation code. Corrected stale current-state rows in the remaining execution plan and the isolation inventory: exact pairs, immutable private/shared resources, administrative service contexts and targeted RLS already exist. Added the proposed capability enforcement contract, per-grant-kind scope separation, platform-only capability writes, bootstrap/data review, evaluator evidence, writer ordering and explicit release acceptance. Historical slice notes remain historical. No capability implementation or runtime behaviour changed in this planning pass; the complete foundation goal remains active.


## Machine capability implementation

Migration 0028 adds immutable organisation/target/grant-kind capability rows, scope/window checks, private/machine ownership guards, server-controlled revisions and RLS with platform-only writes. Machine issuance now requires an effective exact capability; scope defaults and explicit requests obey client/resource/capability intersection. A shared capability query also constrains online admin principal/current command authority. Successful issuance records the approving capability identity/revision, scope/window and evaluation time. Three platform commands create/update/remove machine ceilings with existing contexts, journal recovery, ETags and before/after audit. The mutation inventory now contains 49 commands. Client/resource erasure requires explicit capability removal. User/direct-session capability evaluation and administration remain unfinished.

Before-change regression: registration plus compatibility returned 200 where missing capability should return 400 (`/private/tmp/id-capability-before.log`). Explicit fixture approvals replace implicit registration authority. Initial full runs found obsolete fixtures, a path inventory and one test import; these were corrected. The stale query-builder unit duplicate was removed in favour of real signed-token/default-dependency integration coverage, which also tests lowercase bearer authentication and current ceiling removal.

A fresh-schema full run passed 1,434 tests / 11,091 assertions, 100% line/function coverage, with typecheck/lint and 62 web tests. Further first-principles review reproduced expiry after transaction start: PostgreSQL transaction-time now() admitted an expired capability. The shared effective-window predicate now uses statement_timestamp(), so the policy statement after issuance locks evaluates current windows and audit records the same instant. `/private/tmp/id-capability-expiry-before.log` records the failing 200; the after log passes all 15 lock tests. Full gates after this correction are running.

The design uses existing organisation locks rather than per-assignment locks, existing command recovery rather than a second framework, and explicit platform approvals rather than a migration fallback. Capability RLS uses a separate platform-write rule because the existing tenant-write rule would permit ceiling escalation. No production migration or deployment occurred. Reviewed approvals, runtime permission provisioning, worker cutover, direct/user policy, full effects and operational release proof remain required; F0–F7 and the goal remain incomplete.


Machine capability slice final gates pass: 1,435 ID tests / 11,093 assertions, 100% line/function coverage; 62 web tests; typecheck, lint, production build and git diff --check. The disposable schema was rebuilt from migrations, including 0028. Generated OpenAPI drift tests pass and 78 local documentation links resolve. Final logs: `/private/tmp/id-capability-verified-{tests,typecheck,lint}.log`, `/private/tmp/id-capability-{migrate,web,build,openapi}.log`; expiry proof: `/private/tmp/id-capability-expiry-{before,after}.log`. All test/build handles are terminal. No production migration, deployment or commit. Direct-admin/user ceilings, trusted user grants, complete shared policy/effect/ordering proof and operational release gates remain unfinished; the full goal remains active.


## Direct administrator capability slice

Human effective grants and platform-writer detection now intersect effective resource-only assignments, an effective admin_session capability and the enabled resource vocabulary. Existing active user/member/organisation/group/window checks remain. Bootstrap provisions the bound platform ceiling once and audits its actual row; restrictions survive restart. Migration 0029 prevents reserved ceiling deletion. The three existing capability commands support machine and direct admin kinds; unsupported user OAuth kinds remain closed. Tenant approvals are explicit. Capability PATCH rejects loss of the currently effective last platform writer, but all-writer protection and natural expiry handling remain unfinished.

The first full suite found three obsolete fixtures/expectations: runtime RLS tenants needed explicit approval and unrelated-resource access fixtures could no longer assert direct admin authority. Corrected tests include a positive control distinguishing a direct platform-write assignment from a pair. A subsequent run passed 1,442 tests / 11,140 assertions before the bootstrap audit snapshot was enriched. The next run found one exact audit expectation missing the new snapshot; corrected and final full verification is running. No production migration or deployment. Documentation now distinguishes implemented direct-admin ceilings from the still-unfinished tenant-bound user policy and release proof.

First-principles review reuses the existing capability model, SQL joins, organisation policy lock and journal. Removed obsolete binding helper and unused fixture groups. No separate policy service, workflow engine, event store or additional target discriminator. Full F0–F7 remains active and incomplete.


Final verification qualification: the final full suite reports 1,441 pass / 1 fail, 11,139 assertions and 100% line/function coverage (`/private/tmp/id-admin-capability-verified-tests.log`). The failure is the concurrent user retry: the server logs `timeout exceeded when trying to connect`, returned as HTTP 500. The isolated suite then passes all four tests / 73 assertions (`/private/tmp/id-admin-capability-user-retry.log`; focused exit 1 reflects whole-repository coverage thresholds). This does not supersede the failed full gate. The pool uses configured connectionTimeoutMillis in db/client.ts; problemHandler maps this unclassified error to 500. Pool exhaustion/error semantics and a clean full rerun remain required. No timeout was relaxed and no assertion was weakened to accept 500.

The bootstrap audit expectation is corrected and passes in the final full run. Final typecheck and lint pass; 62 web tests pass; 78 local documentation links resolve. Production build is running separately after the database processes ended. No production migration, deployment or commit occurred. The whole foundation remains incomplete.


Production build exits 0 (`/private/tmp/id-admin-capability-build.log`); git diff --check passes. All test/build processes from this slice are terminal. The failed full-suite connection-timeout gate remains open despite the passing isolated retry suite. Next work must preserve this evidence and resolve it alongside the remaining foundation requirements.


## Connection exhaustion and safe command retry

Previous goal turn classification: progress. It completed administrator-capability evidence/docs and exposed the connection-timeout failure. This turn reproduced the failure deterministically with an exhausted local pool. The pinned pg-pool implementation emits exact un-coded checkout/connection timeout messages; the shared problem mapper now recognises raw checkout errors and Drizzle-wrapped query errors as retryable 503 database_busy. Better Auth replaces session lookup failures with an internal API error, so the default principal dependency maps that case to a distinct authentication_unavailable without claiming a database cause. Unrelated exceptions retain their existing handling. Both responses carry Retry-After: 1.

Tests occupy every connection before session lookup and after broker authentication/before command checkout. Both paths deny admission without a new journal receipt or user mutation; release permits a same-key command and subsequent replay. The pre-existing concurrent user retry fixture now has two connections rather than queueing both requests through one. No timeout was increased and 500 is not accepted as success.

Focused proof: 6 integration tests / 89 assertions and 46 unit tests / 194 assertions pass. The earlier deterministic run returned500; a second run exposed Better Auth error translation, now covered separately. Typecheck/lint pass; OpenAPI is regenerated and declares administrative 503 responses, including reads. Public guide and README describe the error distinctions and retry contract. Full-suite verification is running.

First-principles review keeps existing transaction ownership and recovery, with one shared unavailable-problem constructor. No pool wrapper, ambient failure state, retry daemon or new command framework. Exact driver-message matching is deliberately narrow because this installed pg-pool exposes no timeout code; real pool tests guard that compatibility. This does not complete deployment capacity limits, the user-grant foundation or F0–F7.

Additional observation retained for follow-up: Better Auth's default session error logging can include query parameters in its diagnostic output on database failure. Tests use fixture credentials only, but production logging needs a secret-safe provider logger before release. Do not claim the response redaction fixes upstream logging.


Connection-exhaustion full suite passes: 1,447 ID tests / 11,181 assertions, 100% line/function coverage (`/private/tmp/id-pool-tests.log`). This supersedes the prior failed full gate for the timeout regression; the explicit exhausted-pool cases and concurrent two-connection retry pass in the full run. Typecheck, lint and 62 web tests pass, generated OpenAPI drift checks pass, and git diff --check passes. Build is running separately after the test process exited0. No migration or deployment. Provider diagnostic log redaction and the rest of F0–F7 remain unfinished.


Connection-exhaustion slice final gates: production build exits0 (`/private/tmp/id-pool-build.log`), 58 checked local documentation links resolve and git diff --check passes. Together with the full 1,447-test ID suite, typecheck/lint and 62 web tests, this slice is verified. All test/build handles are terminal. No migration, deployment or commit. The complete foundation goal remains active; next priorities are secret-safe provider logging and trusted tenant-bound user grant policy, followed by the remaining writer/effect/operational release proof.


## Secret-safe authentication diagnostics

Previous goal turn classification: progress; it corrected the exhausted-pool error contract and passed all gates. The current slice reproduces the provider logging leak using the real session lookup outage. The configured Better Auth logger now drops free-form messages and argument objects entirely, retaining only severity and the fixed provider_diagnostic event. Warn/error diagnostics remain visible on stderr; debug/info/success remain below the configured warn threshold. Unexpected HTTP errors retain requestId plus a fixed unexpected_error event, without exception name/message/stack/cause.

The test first failed because the installed provider printed a Drizzle query and its parameters. The corrected real outage test asserts the complete emitted structure, while a configured logger test supplies fixture secrets and objects whose getters throw if inspected. It asserts that only two bounded warn/error facts are emitted. Existing unexpected-error response tests also assert the complete redacted log. Focused checks: 6 integration tests / 91 assertions and 14 unit tests / 73 assertions pass. Full suite is running.

First-principles review deletes payloads instead of trying to recognise every token/password format with redaction regexes. Uses Better Auth's supported logger option and the existing HTTP handler; no new logging library, recursive scrubber or ambient context. This reduces diagnostic detail intentionally. These lines are operational signals, not durable audit evidence. Request correlation remains in HTTP diagnostics and audit; provider diagnostic lines do not claim request attribution. Request metadata, retained audit content and third-party/infrastructure logging remain separate contracts. No schema/API change or migration.


User-grant seam reconnaissance (source evidence, not runtime proof): the installed provider's OAuthTokenIssueParams accepts referenceId, sessionId, authTime, originalResources and a refreshToken row. The existing oauth_refresh_tokens schema stores referenceId, authorizationCodeId, resources and authTime, but has no enforced membership/tenant relationship. Installed introspect-C6P1zrTr.mjs renewal code takes referenceId/sessionId/authTime from the stored refresh row. Critically, revoked-token rotation replay may return before createUserTokens, so an issuance-only extension cannot establish current-authority replay. The next provider proof must cover original-context persistence and policy admission before cached refresh replay, not only successful minting. User OAuth remains publicly closed. Do not treat an arbitrary referenceId or current browser organisation as verified tenant authority.


Logging slice full verification passes: 1,448 ID tests / 11,184 assertions, 100% line/function coverage (`/private/tmp/id-log-tests.log`); typecheck, lint and 62 web tests pass. The configured provider logger and real exhausted-pool test both pass in the full suite. Build is running separately after the test process ended. No API schema change, migration or deployment. This closes the reproduced free-form provider/HTTP diagnostic leak, not all metadata/log infrastructure policy or remaining F0–F7 work.


Logging slice final gate: production build exits0 (`/private/tmp/id-log-build.log`; web reused its unchanged build, ID bundled the changed runtime). git diff --check passes. All test/build processes are terminal. No migration, deployment or commit. The full goal remains active. Next implementation proof must bind trusted user tenant/membership context across authorisation-code redemption and refresh, including policy checks before cached refresh rotation replay.


## User OAuth provider boundary proof

Previous goal turn classification: progress; diagnostic payload removal and all repository gates were completed. This slice adds a test-only integration of the installed provider's native authorisation-code and refresh endpoints inside the existing transaction adapter. It uses a real SSO-established browser cookie from the administrative fixture, PKCE S256, client Basic authentication, a server-selected membership reference and the actual native rotation cache. Production user OAuth remains closed; no runtime wrapper or grant storage has been added.

Focused evidence: 2 tests / 51 assertions pass (`/private/tmp/id-user-seam-final.log`). Code redemption and refresh retain referenceId, sessionId, authTime, authorizationCodeId and resources. A request-supplied organisation and a changed browser active organisation do not switch the stored reference. A duplicate refresh returns the native cached response without calling issuance claims; a preflight membership check denies that same replay after membership revocation. Issuance rejection rolls back an extension write and code consumption for the Basic-authenticated test client. The wrapper preserves native response headers; its initial nested invocation accidentally serialised the endpoint's headers/response envelope, caught and corrected before the passing test. Typecheck/lint pass after correcting fixture types.

An initial resource vocabulary containing only proof:read produced an initial refresh token but no refresh rotation, because the provider stores the filtered scope set and checks offline_access on renewal. The passing rotation proof explicitly retains offline_access in that fixture vocabulary. This is an integration constraint, not the production solution: the common policy still needs separate login/renewal admission and resource scope ceilings, without leaking login scopes into resource authority.

The test wrapper is deliberately not production code. Inspection of installed provider code shows additional release requirements: private_key_jwt assertion consumption uses the supplied adapter, so putting authentication inside the grant transaction would roll back its replay marker on rejection unless handled separately; native invalid refresh-family cleanup keys by client/user rather than tenant/grant; native cleanup triggered by a rejected code or refresh must not be undone by an issuance rollback. The Basic test does not establish those guarantees. Do not promote the wrapper or open user grants until supported integration proves authentication once, persistent denial effects and tenant-local family revocation. Claim hooks alone cannot enforce cached replay.

First-principles review adds executable provider evidence before a new production grant table or generic hook framework. Uses the supported endpoint and transaction API, with no package modification or protocol cryptography fork. The proof's preflight checks one membership prerequisite, not the complete evaluator. Next work must solve the independent authentication/denial boundary, persist trusted tenant/membership/client/resource provenance, and establish separate grant-kind scope semantics. F0/F4/F5 remain incomplete.


User-grant seam verification: full ID suite passes 1,450 tests / 11,234 assertions with 100% line/function coverage (`/private/tmp/id-user-seam-tests.log`). The final focused test adds explicit user identity and non-null authorisation-code identity checks: 2 pass / 55 assertions (`/private/tmp/id-user-seam-identity-final.log`); its exit1 reflects whole-repository coverage thresholds. Typecheck, lint and 62 web tests pass. Build is running separately after the database test processes ended. No runtime grant opening, production schema change, migration or deployment. The proved seam remains limited to Basic-authenticated mechanics; independent assertion/denial effects, tenant-local families, scope separation and full trusted policy remain unfinished.


User-grant proof final gate: production build exits0 (`/private/tmp/id-user-seam-build.log`; unchanged web build reused), and git diff --check passes. All test/build handles are terminal. The previous goal turn is progress, not completion: provider behaviour is now executable evidence and identifies the required security integration boundaries. F0–F7 remain active. No migration, deployment or commit.


## Native client authentication and independent assertion consumption

Previous goal turn classification: progress; it established native code/refresh mechanics and identified assertion rollback as a missing guarantee. The new native-client-authentication helper calls the supported provider authentication API before the grant transaction. Its request-local adapter records only successfully committed assertion-create results. During the native endpoint's own verification it acknowledges the exact same create input once; other writes use the transaction adapter. No additional connection pool, global replay bypass, custom signature validation or provider fork is introduced. Native signature/client validation still runs again against current configuration; only the duplicate marker insertion is acknowledged.

The helper refuses an already-active Better Auth transaction using the core's exported transaction-context inspection API. Its callback lifetime closes on success or error, clears recorded markers, and rejects late use. Unknown assertion-create requests cannot acquire a marker through this binding. Public-client authentication is delegated to the provider with requireCredentials:false; the native user-code and PKCE checks still apply. This is an integration helper, not a public OAuth grant opening or the complete tenant policy.

Executable evidence: the initial test showed zero retained markers after a rejected private-key grant (`/private/tmp/id-native-assertion-before.log`). Corrected focused proof passes 4 tests / 114 assertions (`/private/tmp/id-native-assertion-public.log`) using a one-connection pool. It covers retained assertion markers after issuance rollback, replay rejection, concurrent reuse with one policy evaluation, fresh-assertion recovery, key removal between authentication and native validation, unknown-marker rejection, expired bindings, nested-transaction rejection, and public clients rejected for missing/wrong PKCE before valid proof succeeds. The native invalid-key response is 401; the test preserves that protocol result. Typecheck/lint and 62 web tests pass; full-suite verification is running.

First-principles review uses one small callback-lifetime map and the provider's existing adapter seam. It avoids a second pool and therefore the deadlock/liveness problem of trying to autocommit assertions while already holding the grant connection. No marker is acknowledged across requests. The exact-input comparison is local to the installed provider's repeated create call, not a new durable request fingerprint scheme. The supported SDK and native validator remain the owners of cryptography and protocol checks.

Remaining user-grant requirements: security revocations triggered by rejected code/refresh requests must commit rather than be rolled back; refresh families must be tenant/grant-local; renewal admission must remain distinct from resource-token scopes; trusted grant provenance and the full capability/pair evaluator must be persisted and enforced. The current test wrapper still rolls back native code consumption and must not be promoted unchanged. Public user OAuth remains closed, and F0–F7 remain incomplete.


Verification correction: the first full assertion run failed with two fixture hooks exceeding fifteen minutes, a later duplicate fixture row, and an SSO timestamp-order assertion. The process exited; only the guarded disposable database was rebuilt. A fresh sequential run is active under a temporary caffeinate process to prevent idle sleep while the command runs. No test timeout was relaxed. The long hook pauses are observed evidence, not proof of their environmental cause.

The timestamp failure exposed an independent clock-source defect: insert defaults used PostgreSQL time but the shared ORM update default used application Date. A deterministic skewed-clock test reproduced an update timestamp in year2000 after a current database insert (`/private/tmp/id-clock-before.log`). The shared automatic update default now uses statement_timestamp(); the focused test passes 2 tests / 16 assertions (`/private/tmp/id-clock-after.log`). This is an ORM write-default change, not a database migration. Explicit provider-supplied timestamps and wall-clock monotonicity remain distinct; revisions establish configuration ordering.

Next denial-effect seam to prove: installed createUserTokens calls customTokenResponseFields after protocol/resource validation and before refresh/access-token writes. A narrowly owned issuance savepoint may let recognised protocol-denial cleanup commit while rolling back failed issuance, with authentication markers already independent. This is source reconnaissance only. It must cover native code-replay cleanup, policy/signing/audit failure and refresh replay; no production use is justified until those outcomes are demonstrated. Tenant-local family storage and renewal/resource scope separation remain required.


Verified closure of the native assertion slice: the fresh sequential full ID run passes 1,453 tests / 11,299 assertions with 100% line and function coverage (`/private/tmp/id-native-assertion-verified-tests.log`). Typecheck, lint, build and 62 web tests pass. The disposable schema rebuild and temporary idle-sleep prevention do not establish the cause of the earlier long hook pauses. No production migration or deployment occurred.

Final review corrected a test-only assumption: native cached refresh replay preserves the token material but recalculates `expires_in`. The proof advances the application clock by two seconds, restores it in `finally`, checks identical token fields and a strictly smaller non-negative lifetime, and confirms no extra claims evaluation. The final focused run passes 4 tests / 116 assertions (`/private/tmp/id-native-assertion-final-replay.log`); its exit status reflects the repository-wide coverage threshold when running one file. The full-suite count above precedes this test-only correction. No runtime change followed that full run.

First-principles conclusion: retain the small native authentication adapter and database-clock fix; neither needs another service, pool, migration or policy framework. The user-grant wrapper remains proof code. Next work must establish which rejected-request security effects commit, tenant-local refresh families, trusted immutable grant context and separate renewal/resource scopes before production user grants can open. F0–F7 remain incomplete.


## Authorisation-code replay revocation proof

The previous slice made verified progress. The next regression demonstrated that wrapping the native endpoint in a transaction which throws on every rejection undoes required security effects: replaying a consumed authorisation code returned `invalid_grant`, but its refresh token still redeemed successfully (`/private/tmp/id-code-replay-before.log`, expected 400, received 200).

The [native integration proof](apps/id/src/auth/user-grant-boundary.integration.test.ts) now returns a rejection outcome from the transaction and throws it only after commit for the pinned provider's pre-issuance authorisation-code `invalid_grant` path. Its JWT claims hook records entry into issuance using the actual transaction adapter. Errors after that boundary still throw inside the transaction. This is deliberately limited to the test configuration, whose only custom claims hook is the access-token hook; it is not a general boundary for opaque tokens or arbitrary ID-token hooks, and it is not production user OAuth support.

The corrected proof checks that code replay removes the affected grant's refresh tokens, does not re-run claims, and leaves a different grant usable. Separate fault cases verify that both an issuance error named `invalid_grant` and an unexpected error roll back extension writes and refresh-token state. Existing assertion replay, native client revalidation, public PKCE and membership-before-cache checks remain green. Focused result: 6 tests / 179 assertions (`/private/tmp/id-code-replay-final.log`); the focused command exits non-zero only because it does not meet the repository-wide coverage threshold. Typecheck and lint pass. Full-suite results are recorded separately once terminal.

First-principles review: a generic commit-on-4xx rule is unsafe; error names alone cannot distinguish protocol cleanup from failed issuance. Keep the production route closed and retain the narrow proof instead of exporting this wrapper as a security abstraction. No provider fork, new connection pool, generic workflow or schema change is justified by this result.

Remaining gates: the installed provider catches authorisation-code cleanup errors internally (`revokeTokensIssuedForAuthorizationCode` in its distributed source), so a normal `invalid_grant` response does not itself prove successful cleanup. Production integration must detect failed cleanup and prove its failure response and retry behaviour. Tenant-local refresh family revocation, issuance savepoint semantics, trusted grant context, separate renewal/resource scopes and complete policy/audit enforcement remain unfinished. An already-issued JWT's offline rejection is also not proved by deleting refresh rows.


Full verification of the code-replay proof: 1,455 ID tests / 11,365 assertions pass with 100% line/function coverage (`/private/tmp/id-code-replay-full.log`, 112.65 seconds). Typecheck and lint pass. The full run is terminal and no database test remains active. This is progress on F0/F4, not completion of the foundation.

Final gates: build and all 62 web tests pass (`/private/tmp/id-code-replay-build.log`, `/private/tmp/id-code-replay-web.log`). No application runtime, schema, OpenAPI contract or production route was changed in this slice. The next implementation requirement is fail-closed detection of native cleanup errors, followed by tenant-local refresh families and the complete grant boundary.


## Detecting native revocation cleanup failure

The native code-replay proof now injects failures in each deletion step: access-token deletion and refresh-token deletion, using both an adapter exception and a real PostgreSQL division-by-zero error. The original wrapper returned ordinary 400 `invalid_grant` despite failed cleanup (`/private/tmp/id-cleanup-before.log`, expected 503, received 400).

A small [native cleanup guard](apps/id/src/auth/native-token-cleanup.ts) records deletion failure independently of the provider's catch blocks. The test wrapper invokes it inside the existing grant transaction. Failure overrides the native outcome with 503 `temporarily_unavailable` and `Retry-After: 1`; throwing inside the transaction rolls back partial deletion. No error-message parsing, retry worker, new pool or provider fork is involved.

The regression seeds a correlated opaque access row beside the JWT grant's native refresh row, proving that failure of the second deletion restores the first row too. It checks unchanged refresh rows, no claims evaluation, and successful cleanup when the same consumed code is retried after the fault clears. The subsequent refresh attempt is rejected. Focused verification passes 7 tests / 279 assertions (`/private/tmp/id-cleanup-after.log`). Full gates are recorded after completion.

Limits: this guard is integration groundwork exercised by the isolated native proof; production user OAuth remains closed. A cleanup outage does not magically revoke existing credentials: the failed transaction retains those rows until successful retry. The durable grant revocation state, online enforcement and tenant-local refresh family therefore remain necessary. Offline JWT rejection is not established here. Next work should move to that persistent grant boundary rather than accumulate independent protocol wrappers.

First-principles review retains one request-local failure flag and uses the existing transaction. There is no need to store exception payloads, add a generic operation framework or build background cleanup before a durable grant model exists. The native provider still owns protocol and cryptographic verification.


Review correction: the first full cleanup suite passed 1,456 tests / 11,465 assertions, but lint rejected throwing from `finally`. The guard now captures the native success/error outcome explicitly, checks the deletion-failure flag first, then returns or rethrows the original outcome. No exception payload is retained beyond this call. Typecheck and lint pass on the corrected helper; a fresh full suite is running. The earlier green test run alone was not treated as slice completion.


Final cleanup-guard verification: the corrected helper passes 1,456 ID tests / 11,464 assertions with 100% line/function coverage (`/private/tmp/id-cleanup-verified-full.log`, 116.39 seconds). Typecheck, lint, build and all 62 web tests pass; git diff --check is clean. All test/build processes are terminal. No production migration, route opening or deployment occurred. The next implementation remains persistent tenant-bound grant authority and family isolation; F0–F7 are incomplete.


## Persistent tenant-bound grant context

Migration 0030 and [grant context schema](apps/id/src/db/schema/grant-contexts.ts) introduce one Answerable-owned record per user authorisation. The native provider carries its UUID in `referenceId` through code redemption and refresh rotation. Tenant, membership, user, immutable client/resource instances, authentication session/time, requested scope envelope and absolute expiry are immutable. Only initial revocation can change the row; revocation cannot be cleared or retimed. These are live authority records, not audit history: parent erasure cascades, while the authentication session UUID is retained without a live-session foreign key.

The database checks membership/user/tenant agreement, a matching unexpired authentication session, enabled client and registered scope ceiling, plus resource visibility to the selected tenant. These checks are prerequisites, not full permission admission. The scope field is explicitly `requestedScopes`; it is not an approved resource-token scope set. The native test's 30-day context expiry matches its refresh configuration assumption and is not a newly approved production retention policy.

The [resource grant lookup](apps/id/src/db/queries/grant-contexts.ts) requires the stored UUID, exact client/resource, unrevoked/unexpired context, effective membership and active user/tenant/client/resource. The isolated native proof uses it before cached refresh replay and during access-token claims. It no longer interprets a provider reference as a membership UUID. A shared client can hold grants for different tenants of the same person; client ownership is not used as the user's tenant.

Executable evidence: 34 focused tests / 572 assertions pass across native integration, schema contracts and migration idempotency (`/private/tmp/id-grant-context-verified.log`). The native cases prove distinct A/B contexts for one user/session/client, revoked A cached replay denied before claims while B refresh succeeds, irreversible revocation, rejected identity/scope/expiry rewrites, rejected forged tenant/session/auth-time inserts, expired lookup denial and exact client/resource matching. The column, constraint, index and migration inventories include the new table. Typecheck and lint pass; full results follow after completion.

First-principles review uses one context record referenced by provider state, not copied tenant fields in every rotation. It keeps requested scope provenance separate from approval, and background grant authority separate from the live browser session. It does not add a policy language, workflow engine or new connection pool. The database identity checks do not prove complete concurrent policy ordering; the production issuance transaction must establish that ordering with existing writers.

Remaining work before production user grants: grant creation through the trusted production tenant-selection boundary and complete capability/pair evaluator; context-scoped native refresh-family cleanup; automatic irreversible context revocation by membership/user/tenant/client/resource lifecycle writers with complete audit effects; user-grant locking and concurrent writer tests; broker/query isolation and runtime-role proof for this new table; scope separation and operational migration/recovery proof. `grant_contexts` currently has no RLS policy and its lookup accepts an executor; the existing four-table isolation claim is unchanged. No automatic backfill converts legacy unbound provider references. User OAuth remains closed. The provider proof creates contexts during its server-selected post-login callback; production creation/lifecycle cleanup is not yet wired.


Final persistent-context gate: 1,458 ID tests / 11,555 assertions pass with 100% line/function coverage (`/private/tmp/id-grant-context-full.log`, 115.35 seconds). Typecheck, lint, build and all 62 web tests pass; git diff --check is clean. Migration 0030 was applied only to the guarded disposable database. All processes are terminal. No production migration, deployment or user OAuth opening occurred. Next work is context-scoped family invalidation and lifecycle revocation with audit effects, followed by production evaluator/creation and broker isolation. F0–F7 remain incomplete.


## Grant-scoped native refresh invalidation

The previous persistent-context slice made verified progress. The next native regression reproduced missing durable revocation after refresh-token reuse outside the configured grace interval: the response was `invalid_grant`, but the context's `revokedAt` remained null (`/private/tmp/id-family-before.log`). The installed provider identifies a refresh family by client/user; that boundary is too broad for one user's separate tenant grants.

The [native refresh-family boundary](apps/id/src/auth/native-refresh-family.ts) scopes refresh-family reads and deletion by the stored grant UUID. It recognises the pinned provider's client/user invalidation lookup, records context revocation, then opens a savepoint for cleanup. A successful native invalid-grant outcome commits revocation and cleanup. Cleanup failure restores token rows to the savepoint but commits the preceding revocation; the request returns its failure outside the transaction. The proof locks the stored grant row before current-context preflight, including cached refresh. Existing native client authentication and signature checks remain intact.

Focused proof passes 12 tests / 463 assertions (`/private/tmp/id-family-verified.log`). Reuse in A revokes A's context and deletes A's refresh rows while B remains usable for the same user/client. Adapter and real PostgreSQL cleanup failures return retryable 503, preserve the revoked context and restore partial token deletion. Both old and rotated A refresh tokens are then denied before claims despite retained rows. A simulated provider success after invalidation cannot return token material. All earlier client assertion, PKCE, immutable context and code-replay proofs remain green.

First-principles review keeps one existing transaction, one grant row lock and one cleanup savepoint. It adds no queue, secondary pool or policy language. A static import replaces an unnecessary dynamic import. The context lookup now explicitly declares its nullable return type; runtime tests already established missing context returns null, but inferred TypeScript indexing had hidden that possibility.

Integration limits: this adapter boundary is deliberately pinned to the installed provider's invalidation query shape and must be re-proved on provider upgrades. It is exercised by the native test wrapper, not yet production user OAuth. This slice does not establish every lifecycle writer's ordering, fresh authentication, capability/pair policy, or complete grant/denial audit. Cleanup failure leaves inert token rows; bounded later cleanup/retention remains an operational requirement. Code-replay cleanup still needs to revoke persistent context too. User OAuth remains closed, and F0–F7 remain incomplete.

Final gate: 1,461 ID tests / 11,659 assertions pass with 100% line/function coverage (`/private/tmp/id-family-full.log`, 113.19 seconds). Typecheck, lint, build and all 62 web tests pass; git diff --check is clean. All processes are terminal. No migration or production route change occurred in this slice.


## Membership lifecycle revokes persistent grants

The previous native-family slice made verified progress. A service regression then reproduced a remaining lifecycle gap: membership removal left its persistent grant context unrevoked (`/private/tmp/id-member-grants-before.log`). Checking membership status alone would let such a context become effective again after reinstatement.

The production [member removal service](apps/id/src/services/members.ts) now calls [tenant-local grant revocation](apps/id/src/db/queries/grant-contexts.ts) inside the existing member command transaction. The update matches both organisation and membership, touches only unrevoked contexts and returns their actual IDs. Those IDs appear as `effects.revokedGrantContexts` in the same audit event. Newly revoked contexts count as an applied effect; a new-key command with no remaining changes remains a no-op. Existing revocation timestamps are preserved. No token material is added to audit facts.

Focused evidence passes 21 tests / 583 assertions (`/private/tmp/id-member-grants-verified.log`). Service tests prove irreversible A revocation with same-user B preservation, unchanged earlier revocation time, exact newly changed audit IDs, no-op empty effects, and membership/context rollback on audit failure followed by successful retry. The native integration proof calls the production removal and reinstatement services: old cached and rotated A refresh tokens stay denied before claims, B refresh succeeds and the global browser session remains present.

First-principles review reuses the existing transaction, command context and audit envelope. One update scoped to the member avoids a new lifecycle abstraction or generic job framework. Its target is bounded to one membership; the number of contexts is not yet capped, so full large-offboarding limits remain a release requirement. Retained provider token rows become ineffective through the context; this change does not claim physical token deletion or immediate offline JWT rejection.

Still incomplete: global user, organisation, client/resource and session lifecycle context revocation; code-replay context revocation; full issuance/writer concurrency proof; bounded cleanup and retention; production user-grant creation, complete capability/pair policy and broker isolation. Production user OAuth remains closed. Member HTTP commands retain their existing journal contracts; this slice adds no route or public request schema.


Final member-grant lifecycle gate: 1,464 ID tests / 11,715 assertions pass with 100% line/function coverage (`/private/tmp/id-member-grants-full.log`, 116.19 seconds). Typecheck, lint, build with updated static docs and all 62 web tests pass; git diff --check is clean. All processes are terminal. No migration, new route or production deployment occurred. Next work is the remaining lifecycle writers and code-replay context revocation with actual audit effects, followed by the unresolved policy, concurrency and broker-isolation gates. F0–F7 remain incomplete.


## Global user grant lifecycle and erasure evidence

The previous member-lifecycle slice made verified progress. New service regressions reproduced two remaining gaps: global disable left persistent contexts unrevoked, and user erasure removed contexts without recording their IDs (`/private/tmp/id-user-grants-before.log`). Erasure can also remove another user's context through a client whose `userId` foreign key points at the erased user; tracing only contexts directly owned by the target would miss that cascade.

The production [user lifecycle service](apps/id/src/services/users.ts) now revokes unrevoked contexts across all the target user's tenants and records their IDs/organisation IDs with the existing session and token effects. Enable does not alter revoked contexts. A disable command reconciles remaining contexts even if the user is already disabled; its applied/no-op result includes actual context effects. Previously revoked rows are not retimed.

Before erasing a user, [context deletion](apps/id/src/db/queries/grant-contexts.ts) explicitly deletes and returns contexts either belonging to that user or referencing a client owned by that user. The audit captures each deleted context's UUID, tenant UUID and affected user UUID. Another user's independent client/context remains. This completes the grant-context portion of erasure evidence, not every other erasure cascade or affected-subject history index.

Focused evidence: 22 tests / 610 assertions pass (`/private/tmp/id-user-grants-verified.log`). Service tests cover multi-tenant revocation, other-user preservation, re-enable without context restoration, no-op empty effects, already-disabled reconciliation, owned-client cascade evidence, and audit-failure rollback for disable and erasure. Native integration proves cached and rotated A grants and B grants stay denied before claims after global disable/re-enable, and browser sessions are removed. Typecheck passes; full gates follow after completion.

First-principles review reuses the existing platform command contexts, transaction and audit envelope. Two explicit database operations cover user revocation and the actual erasure cascade; there is no generic lifecycle engine or background workflow. Context data carries UUIDs only in new audit facts. The number of affected rows is not yet bounded; large-operation evidence limits remain open.

Remaining requirements: organisation/client/resource/session context lifecycle; code-replay context revocation; indirect affected-user/tenant audit-history indexing; complete concurrent writer ordering and durable token/grant audit; production user-grant evaluator/creation and broker isolation; bounded cleanup and release rehearsal. Production user OAuth stays closed. This slice adds no route, migration or public request schema.


Final global user-context gate: 1,469 ID tests / 11,772 assertions pass with 100% line/function coverage (`/private/tmp/id-user-grants-full.log`, 133.64 seconds). Typecheck, lint, build with updated static docs and all 62 web tests pass; git diff --check is clean. All processes are terminal. No migration, new route or production deployment occurred. Indirect subject history indexing is confirmed missing in the existing audit trigger and remains explicit next work alongside the remaining lifecycle writers. F0–F7 remain incomplete.


## Indirect grant-erasure audit history

The previous global-lifecycle slice made verified progress. A new regression reproduced the missing link from an owned-client erasure event to another affected user's history (`/private/tmp/id-affected-history-before.log`). The event already contained the deleted grant/user/tenant IDs; the existing subject trigger indexed only actors, targets and direct member/session relationships.

Migration 0031 extends the existing `capture_audit_subjects` function. It adds one global `affected` user reference per distinct user ID recorded in version-one, successful, global `user.erased` effects. It neither consults live identities nor changes the event payload. A duplicate effect for the same person produces no duplicate history item. Non-array/malformed user references, other actions/targets/outcomes, tenant-scoped envelopes and legacy schema-version-zero effects are not treated as this contract.

The migration also indexes qualifying existing effects with conflict-safe insertion. These references retain `recorded` provenance because their identities were explicitly captured in the committed event; they are not reconstructed from surviving relationships. Missing historical IDs are not invented. The function retains the existing privileged trigger path and blocked direct execution; no new privileged entry point or table is introduced.

Focused evidence passes 54 tests / 395 assertions (`/private/tmp/id-affected-history-focused.log`). The actual user-erasure service test now finds the global event through the indirectly affected user's history and continues to find it after that user is removed. Database tests cover deduplication, envelope boundaries, malformed optional facts, and a populated upgrade from the prior function followed by a second migration application without rewriting events. Runtime-role tests prove protected subject insertion through the trigger. HTTP tests prove platform readers can find the indirect event while tenant history omits it and tenant access to user history returns 403. The final full run also includes the explicit legacy-version exclusion check added after the focused run.

First-principles review extends the existing index rather than adding a second audit store or scanning JSON on every read. Global subject references use no arbitrary tenant assignment: one person can have effects in multiple organisations, and the existing subject key has one affected-person row per event. Full global payloads remain platform-only. Tenant-specific projections of those payloads are not implemented.

Remaining work: organisation/client/resource/session context lifecycle, code-replay context revocation, complete audit effect coverage, concurrent writer ordering, production user-grant evaluator/creation and broker isolation, bounded cleanup and release rehearsal. User OAuth remains closed. Migration 0031 was applied only to the guarded disposable database; production inventory and rollout remain unperformed.


Final indirect-history gate: 1,473 ID tests / 11,792 assertions pass with 100% line/function coverage (`/private/tmp/id-affected-history-full.log`, 127.51 seconds). Typecheck, lint, build with updated static docs and all 62 web tests pass; git diff --check is clean. All processes are terminal. Migration 0031 was applied only to the disposable test database. No new public route or production deployment occurred. Remaining lifecycle writers and broader release requirements stay open; F0–F7 are incomplete.


## Organisation grant-context lifecycle

The previous indirect-history slice made verified progress. Organisation regressions reproduced unrevoked stored contexts after disable, missing context IDs in erasure audit, and a no-op result despite remaining contexts on an already-disabled organisation (`/private/tmp/id-org-grants-before.log`).

The production [organisation service](apps/id/src/services/organizations.ts) now revokes contexts by their immutable tenant ID, not by client ownership. Returned IDs and affected user IDs are included beside existing machine-token effects. Re-enable does not restore contexts. Erasure explicitly returns deleted tenant context IDs/users before the parent deletion, preserving audit evidence. All changes share the existing platform command transaction; audit failure restores the context and organisation state. Existing client/foreign-key erasure guards remain in force.

Focused lifecycle evidence passes 24 tests / 647 assertions (`/private/tmp/id-org-grants-verified.log`). Service cases cover A/B preservation, global session survival, exact audit effects, no-op/reconciliation behaviour, re-enable and failure rollback. The native case proves B can refresh while A is disabled even though the client belongs to A, and that cached/rotated A tokens remain denied after re-enable.

First-principles review found a test-harness weakness: the new native scenario failed when run alone because a prior scenario created its second-tenant membership (`/private/tmp/id-org-independent-before.log`). The native file now creates and closes a fresh fixture per scenario, resets its injected faults/counters, and seeds the shared user's second membership explicitly. This removes test-order dependence rather than adding a workaround to the scenario. The isolated case now passes 1 test / 70 assertions (`/private/tmp/id-org-independent-after.log`). It still uses one database connection per fixture. The final full run validates all scenarios with this isolation.

The implementation adds no lifecycle framework, job queue or new public route. Queries retain explicit tenant targets and return actual effects. Complete bounded offboarding, client/resource/session lifecycle, code-replay context revocation, affected-user indexing for all new effect shapes, concurrency ordering and production user-grant policy/broker isolation remain unfinished. User OAuth stays closed. No migration was required for this slice.


Final organisation-context gate: 1,477 ID tests / 12,282 assertions pass with 100% line/function coverage (`/private/tmp/id-org-grants-full.log`, 181.61 seconds with independent native fixtures). Typecheck, lint, build with updated static docs and all 62 web tests pass; git diff --check is clean. All processes are terminal. No migration, new route or production deployment occurred. Client/resource/session lifecycle, code-replay context revocation and the broader release requirements remain open. F0–F7 are incomplete.


## Administrative session grant-context revocation

The previous organisation-lifecycle slice made verified progress. New regressions showed that single-session revocation left its persistent contexts active and revoke-all reported no work when browser sessions/tokens were gone but unrevoked contexts remained (`/private/tmp/id-session-grants-before.log`). Existing native token revocation correctly ran before single-session deletion; the missing piece was persistent context authority.

The production [session service](apps/id/src/services/sessions.ts) now revokes contexts by immutable authentication-session ID plus user ID before deleting a single session. It audits returned context IDs and tenant IDs. Revoke-all reuses user-wide context revocation, including contexts whose browser session has already disappeared, and counts those effects in its applied/no-op result. The public `revoked` count remains the number of sessions deleted; zero does not imply no security effect. Session, token, context and audit changes remain in one transaction.

Focused evidence passes 24 tests / 1,186 assertions (`/private/tmp/id-session-grants-verified.log`). Service tests prove origin-specific revocation with other-session/other-user preservation, exact audit IDs, zero-session context-only work, repeat no-op and audit-failure rollback. Independent native scenarios prove both administrative modes block cached and rotated refresh across the affected authentication session's tenant grants before claims run. A test callback was made explicitly async to discard the two services' different return types; no runtime contract changed for that correction.

First-principles review reuses the existing user-wide revocation query for revoke-all and adds only the origin-specific query for a single session. It introduces no new lifecycle engine, worker or table. The immutable authentication-session reference remains useful after session deletion. These are explicit administrative security commands; this slice does not wire normal browser logout or natural session expiry to grant revocation.

Remaining work: client/resource lifecycle, code-replay context revocation, complete affected-subject indexing and effect coverage, concurrent writer proof, production user-grant evaluator/creation and broker isolation, bounded cleanup and release rehearsal. Production user OAuth remains closed. No migration or public request schema change was required.


Final administrative session-context gate: 1,482 ID tests / 12,426 assertions pass with 100% line/function coverage (`/private/tmp/id-session-grants-full.log`, 140.28 seconds). Typecheck, lint, build with updated static docs (17.084 seconds) and all 62 web tests pass; git diff --check is clean. No migration, new route or production deployment occurred. Client/resource lifecycle, code-replay context revocation and broader foundation requirements remain open. F0–F7 are incomplete.


## Resource grant-context lifecycle

Resource disable previously left persistent contexts unrevoked, and resource erasure silently cascaded them away. New regressions reproduced both gaps and an already-disabled resource's incorrect no-op (`/private/tmp/id-resource-grants-before.log`: three failing tests). The resource service now revokes by immutable resource UUID, includes context effects in applied/no-op classification, and explicitly returns deleted contexts before erasure. Audit facts include actual context, tenant and user IDs. Re-enable never clears revocation. Existing erasure guards remain unchanged. Resource audit events remain platform-scoped; multi-tenant effects are not exposed through a tenant audit filter.

First-principles review keeps the existing resource lock and transaction and adds two explicit queries; no workflow, worker, table or generic lifecycle abstraction is needed. Native user OAuth is still test-only. Complete client lifecycle requires protecting foreign-tenant effects from the client owner's audit view; full evaluator, broker isolation, affected-user indexing, concurrent writer ordering, bounded cleanup and operational release proof remain unfinished. No migration or public schema change is required.


Resource lifecycle verification: the full ID suite passes 1,488 tests / 12,515 assertions with 100% line/function coverage (`/private/tmp/id-resource-grants-full.log`, 168.10 seconds). This includes five service regressions for tenant/resource scope, reconciliation, erasure effects and audit rollback, plus native cached/rotated refresh denial after disable/re-enable in both tenants. The initial native test fixture reference was corrected to `fixture.environment`; the successful full run contains that correction. Resource API descriptions now state the context-only applied result and non-restoration on enable. OpenAPI was regenerated with the exporter; focused contract verification passes 12 tests / 1,812 assertions (`/private/tmp/id-resource-grants-contract.log`; focused runs still exit 1 against the repository-wide coverage threshold). No public request/response schema changed.


Final resource-context gate: build passes in 20.181 seconds, with updated static docs and generated API descriptions. Typecheck, lint, all 62 web tests and git diff --check pass. The full ID and focused contract gates above are terminal. No migration or production deployment occurred. The foundation goal remains active; F0–F7 are incomplete.


## Client disable and erasure grant effects

New regressions reproduced persistent contexts surviving client disable, context erasure without an effect manifest, and an already-disabled client's incorrect no-op (`/private/tmp/id-client-grants-before.log`: three failing tests). The client service now revokes/deletes contexts by immutable client instance UUID. Disable reconciles remaining token/context effects even when the flag already matches; re-enable does not restore contexts. Erasure captures context IDs before the client cascade and retains the entitlement/capability guards. Other clients and browser sessions remain intact.

A shared client's owner is not authority to read other tenants' grant identities. The existing owner-scoped lifecycle event is preserved and links a separate platform-only `client.grants_revoked` or `client.grants_erased` event through `grantEffectsEventId`. That event carries actual context/tenant/user IDs and immutable client instance ID, with the same actor and operation metadata. No extra effect event is written when no contexts change. Both events and mutations share the existing command transaction. The owner event does not contain the foreign tenant's context/user/tenant IDs. No generic event router, new table, worker or workflow engine was added.

Focused verification passes 34 tests / 1,444 assertions (`/private/tmp/id-client-grants-focused.log`): tenant-history and platform-history service readers, origin-specific context scope, disable/re-enable, reconciliation/no-op, erasure and audit failure. A database trigger deliberately fails the owner event after confirming the platform effect event exists; the command rolls back both events, client state and contexts. Native integration proves cached and rotated refresh from both tenants are denied after disable/re-enable. Typecheck and lint pass. OpenAPI descriptions were regenerated to document non-restoration and context/token-only applied results.

First-principles review uses a single small effect-audit helper for the two real callers. A second event is required by the different visibility boundaries; placing the manifest in the existing owner event would disclose foreign authority. There is no per-tenant fan-out and no framework abstraction. Remaining work includes credential/configuration change semantics for user grants, code-replay revocation, complete affected-user subject indexing for these effect shapes, bounded manifests/cleanup, production evaluator and broker isolation, concurrent writer proof and release rehearsal. Production user OAuth remains closed. No migration or public request/response schema change was needed.


Final client disable/erasure gate: 1,496 ID tests / 12,628 assertions pass with 100% line/function coverage (`/private/tmp/id-client-grants-full.log`, 144.12 seconds). Typecheck, lint, build with updated static docs (18.337 seconds), all 62 web tests and git diff --check pass. OpenAPI was regenerated through its exporter before the full run; drift checks pass. All processes are terminal. No migration or production deployment occurred. Credential/configuration semantics, affected-subject indexing for the new effect shapes and broader F0–F7 requirements remain incomplete; the goal stays active.


## Lifecycle grant effects in per-user history

Six regressions reproduced missing subject references for `client.grants_revoked`, `client.grants_erased`, `resource.disabled`, `resource.erased`, `organization.disabled` and `organization.erased` (`/private/tmp/id-lifecycle-subjects-before.log`: six failures). Migration 0032 extends the existing `capture_audit_subjects` function and performs a repeatable backfill using only explicit recorded user IDs. The action, target type, event scope, schema version and success outcome must match a known contract. Organisation events additionally require the target ID to equal their tenant ID. Malformed effect arrays, wrong fields and unsupported versions are ignored. Duplicate user references collapse to one affected subject per event. Existing user-erasure indexing remains intact.

The same event organisation is retained on each reference: client/resource multi-tenant effects remain global, while organisation effects retain their own tenant. Existing audit readers keep their permission checks. No live identity lookup is used for these effect references, and no original payload or timestamp is rewritten. Public execute permission on the capture function remains revoked; the existing protected audit trigger handles inserts from the restricted runtime role.

Focused verification passes 45 tests / 351 assertions (`/private/tmp/id-lifecycle-subjects-verified.log`). It includes all six valid contracts and incompatible envelopes, populated migration run twice, runtime-role inserts, and actual client/resource/organisation lifecycle records queried after user deletion. A missing test import was corrected before the successful focused run. The old migration regression now saves and restores its current function definition inside its transaction, preventing it from leaving migration 0031 installed over 0032.

First-principles review adds no table, new database function, event dispatcher or recursive JSON reader. The small explicit selector is repeated for live capture and backfill, with tests proving parity; a new privileged helper solely to deduplicate migration SQL is unnecessary. Migration 0032 was applied only to the disposable test database. Named identity recovery, full policy/effect evidence, bounded manifests, credential/configuration and native code-replay semantics, concurrency, broker isolation and release rehearsal remain unfinished.


Final lifecycle-subject gate: 1,504 ID tests / 12,659 assertions pass with 100% line/function coverage (`/private/tmp/id-lifecycle-subjects-full.log`, 165.49 seconds). Typecheck, lint, build with updated static docs (19.135 seconds), all 62 web tests and git diff --check pass. Migration 0032 was applied only to the disposable test database; no production deployment occurred. There is no public API schema change. All processes are terminal. Credential/configuration semantics, native code-replay invalidation and broader F0–F7 requirements remain incomplete; the goal stays active.


## Secret rotation revokes existing user grants

The regression reproduced a successful refresh using an old tenant grant with the newly rotated client secret (`/private/tmp/id-rotation-grants-before.log`: native response 200 instead of invalid_grant, and unrevoked context rows). The existing client epoch protected online machine validation but the persistent user context lookup did not carry that epoch. Secret rotation now revokes existing client token rows and immutable client-instance contexts in the same transaction as the credential/version change. It reuses the platform-only `client.grants_revoked` manifest and owner-visible event link; migration 0032 already indexes these affected users.

Focused verification passes 44 tests / 1,623 assertions (`/private/tmp/id-rotation-grants-focused.log`). It proves cross-tenant context revocation, preservation of other clients/browser sessions, audit failure rollback, secret exclusion from audit data, rejection of cached and rotated refresh with the new credential, rejection of the old credential, and successful fresh authorisation. The HTTP journal regression creates a fresh context after rotation and proves replay recovers the same secret without revoking that context, advancing the version or adding a second effect event. The two original audit events share the operation ID.

First-principles review reuses the existing token/context queries and effect-audit helper; no migration, table, abstraction or new recovery mechanism is needed. Documentation and generated OpenAPI now distinguish a new rotation from replay. This closes shared-secret rotation semantics for stored contexts, not arbitrary client configuration/JWK changes, the complete production user policy, native code-replay invalidation or concurrency ordering. Offline JWT consumers retain their documented expiry/online-verification limitation. Production user OAuth remains closed.


Final secret-rotation context gate: 1,508 ID tests / 12,789 assertions pass with 100% line/function coverage (`/private/tmp/id-rotation-grants-full.log`, 158.14 seconds). Typecheck, lint, build with updated static docs (18.545 seconds), all 62 web tests and git diff --check pass. OpenAPI was regenerated through its exporter before the full run. All processes are terminal. No migration or production deployment occurred. General client configuration/JWK semantics, native code-replay invalidation and the broader F0–F7 requirements remain incomplete; the goal stays active.


## Persistent authorization-code replay revocation

The pinned provider's `revokeTokensIssuedForAuthorizationCode` only deletes rows by authorization-code ID and catches deletion errors. Three regressions reproduced contexts remaining active after replay, cleanup failure and deletion of all native token rows (`/private/tmp/id-code-context-before.log`). Token rows therefore cannot be the only durable link from an issued code to its authority context, particularly for JWT-only issuance.

Migration 0033 adds nullable, non-empty, unique `grant_contexts.authorization_code_id`. The existing provenance trigger allows one-time binding and prohibits clearing/replacing a bound hash or binding a revoked context. `bindGrantCode` runs inside successful native issuance's transaction after context admission; refresh does not rebind it. The proof explicitly uses hashed native token storage and verifies equality with the provider's stored code ID. Failed issuance rolls back its binding with the other writes. Existing contexts remain unbound; no historical identity is inferred from optional token rows. Production creation/migration admission still needs its final reviewed policy before opening user OAuth.

`withNativeCodeReplay` observes the pinned provider's exact code-cleanup query. It revokes only contexts matching the native code hash and authenticated client instance, then opens a cleanup savepoint. Native token deletion is also scoped to that authenticated client. Cleanup failures roll back partial deletion while retaining the authoritative context revocation; unexpected provider success cannot release token material. The wrapper returns the error for the outer transaction to commit the barrier before throwing. No raw code, new workflow or token-family table is introduced. This remains a pinned-provider integration seam, not the completed production user policy.

Focused evidence: 48 schema/native tests pass (`/private/tmp/id-code-context-verified.log`), followed by 25 native tests including JWT-only issuance (`/private/tmp/id-code-context-native.log`). New cases cover immutable/unique/non-empty binding, missing/revoked contexts, absent token rows, foreign authenticated client isolation, unexpected success and cleanup rollback. Existing expiry-fixture cloning was corrected to omit the now-unique code binding; a misplaced test input property was removed before successful typecheck. Final changes additionally make the preserved replay grant belong to tenant B and verify rollback of a non-token adapter deletion after the cleanup savepoint. The full gate below verifies those final changes.

First-principles review keeps the code hash on the existing grant context instead of adding a separate code-to-grant table or deriving authority from disposable token rows. Native protocol recognition stays explicit in the code and refresh helpers; a generic adapter framework would hide their different boundaries. Broader configuration/JWK semantics, user-grant policy/denial audit, complete concurrency, broker isolation, bounded cleanup and operational release proof remain unfinished.


Final native code-context gate: 1,513 ID tests / 13,028 assertions pass with 100% line/function coverage, including `native-code-replay.ts` (`/private/tmp/id-code-context-full.log`, 145.95 seconds). Typecheck, lint, build with updated static docs (20.049 seconds), all 62 web tests and git diff --check pass. Migration 0033 was applied only to the disposable test database. No public route/schema change or production deployment occurred. All processes are terminal. The native integration remains proof for production adoption; full user policy/denial evidence, configuration/JWK semantics, concurrent writer ordering, broker isolation and broader F0–F7 requirements remain incomplete.


## User resource policy in native issuance and cached refresh

Two regressions reproduced native issuance succeeding without an exact pair assignment and cached refresh returning tokens after its renewal capability was removed (`/private/tmp/id-user-policy-before.log`). The proof now explicitly seeds separate login, code-pair and renewal capabilities plus login/pair assignments for both tenants. No registration helper automatically grants this authority, and public capability administration still supports only machine/direct-admin kinds.

`userResourcePolicy` resolves the stored grant reference and authenticated client, then uses the existing scoped broker read with explicit tenant/member/user/instance joins. One SQL statement loads active identity/context state, client grant types, compatible/private resource constraints, effective capabilities and applicable organisation/member/group assignments. All source windows share `statement_timestamp()` and the statement snapshot. Login identity scopes are evaluated separately from resource scopes; resource requests intersect client vocabulary, resource vocabulary, exact pair capability, pair assignments and original grant scopes. Refresh additionally requires its exact renewal capability. Missing, disabled, future/expired, wrong-target or insufficient authority denies. Denials carry bounded internal reasons; the native proof exposes generic protocol errors.

The returned decision includes membership windows/revision, contributing capability and assignment IDs/revisions/windows, and group-membership/group revisions where relevant. Both native issuance admission and refresh preflight call the policy, including before cached token recovery. `grantScopes` is also used by the production machine path; it preserves the existing rule that defaults use the intersection and explicit overreach is rejected rather than silently narrowed. The existing direct-admin SQL policy is not yet replaced by this decision contract.

Focused verification initially passes 55 tests / 2,163 assertions (`/private/tmp/id-user-policy-verified.log`), including real restricted-role evaluation, two-tenant preservation, renewal rechecks and target/scope substitution denials. Further focused checks pass for group-membership evidence and client grant-type configuration (`/private/tmp/id-user-policy-group.log`). The first full run exposed an incorrect test setup: native authorize already rejects a missing compatibility link before code issuance. The regression now authorises first, removes the link, then proves policy/refresh denial of that existing grant. The final full gate records the corrected scenario and null-vocabulary coverage.

First-principles review keeps existing entitlement matching, scoped reads and capability tables. The new shared scope helper replaces the machine's duplicate intersection code; there is no policy language or new schema. Consistent reads are not proof of serialisation against concurrent writers. Public adoption remains gated on lock ordering, final protocol/resource scope integration (the native fixture's offline_access vocabulary workaround remains), production user capability/creation paths and decision audit, honest access projections, bounded evidence and release rehearsal. Production user OAuth remains closed.


### User-capability administration

Platform capability commands now support client-only `authorization_code` login ceilings, exact client/resource `authorization_code` ceilings and exact-pair `refresh_token` ceilings. User approvals validate registered grant kinds and client scopes, separate identity from resource scopes, and retain private-resource ownership checks. Shared user clients may be approved independently in multiple tenants; machine ownership rules are unchanged. Existing journal, revision and audit paths are reused. Suspension/removal remain available after registration narrows. The native integration fixture now creates its approvals through the production service. No migration is required; OpenAPI is regenerated. Public user OAuth remains closed; production context creation, final scope integration, common decisions, concurrency and release evidence remain unfinished.


User-capability verification: the full ID suite passes 1,532 tests, zero failures and 13,681 assertions with 100% line/function coverage (`/private/tmp/id-user-cap-full.log`, 190.40 seconds). Focused HTTP/native tests pass 101 tests / 2,499 assertions. Typecheck, lint, generated OpenAPI and 62 web tests pass. First-principles review removed obsolete user-kind rejection branches and native fixture capability inserts, reusing production service validation rather than introducing a new framework or migration. Production user OAuth, common policy integration, concurrency, complete effect evidence and operational release gates remain unfinished.

Build verification completed: both packages pass in 19.209 seconds (`/private/tmp/id-user-cap-build.log`). `git diff --check` passes. The full foundation goal remains active and incomplete.


### Native grant provenance creation

The native user proof now calls `auth/create-resource-grant.ts` with the callback's actual user, session and requested scopes. It no longer reads a fixture-selected session or stores the full registered scope maximum. One insert-select derives tenant, client/resource instances and authentication time from current stored rows, validates effective membership/active identities/session expiry/client grant support/resource ownership and scope containment, and uses database time with a server-supplied lifetime. The existing immutable-context trigger still guards insertion. The restricted-runtime test uses the same creation function. No migration is added.

This is a provenance boundary, not authentication by possession of IDs. Public user OAuth remains closed. The native fixture still selects a member and supplies client/resource request context; signed request continuation, real tenant selection/consent, production policy integration, writer ordering and grant-creation audit/retention remain unfinished. First-principles review removed the redundant supplied organisation and three fixture lookups rather than adding another context store. Focused native tests pass 41 tests / 2,192 assertions; the restricted-runtime suite passes 10 tests / 132 assertions.


Grant-provenance full verification: `/private/tmp/id-grant-create-full.log` records 1,534 passing ID tests, zero failures, 13,708 assertions and 100% line/function coverage (184.65 seconds). Typecheck, lint and all 62 web tests pass. Public API schemas are unchanged; no OpenAPI regeneration or migration is required for this slice, and full drift checks pass. Build verification passes for both packages in 18.38 seconds (`/private/tmp/id-grant-create-build.log`). `git diff --check` passes. The full foundation objective remains incomplete.


Grant creation now also requires the exact client/resource compatibility link in its insert-select. A restricted-role regression first reproduced context insertion after unlinking; it now denies without adding a row. This closes a creation prerequisite, not a demonstrated token bypass: issuance already checked the link. The native creation tests separately exercise expired membership/session, disabled user/tenant/client/resource, unsupported client grant type, missing client scopes and foreign private resources. First-principles review added one join to the existing statement; no separate preflight query, cache, permission store or migration. Concurrent unlink/creation ordering and trusted production request/consent integration remain incomplete.


Exact-pair creation verification: `/private/tmp/id-grant-pair-full.log` records 1,543 passing ID tests, zero failures, 13,979 assertions and 100% line/function coverage (218.05 seconds). Focused native/runtime tests pass 51 tests / 2,326 assertions; the additional creation-prerequisite matrix passes 9 tests / 270 assertions. Its initial disabled-tenant fixture failed the existing disabled-at constraint and was corrected before the passing runs. Typecheck, lint and all 62 web tests pass. No migration or public contract change; full OpenAPI drift checks pass. Build passes for both packages in 17.407 seconds (`/private/tmp/id-grant-pair-build.log`); `git diff --check` passes. F0–F7 remain incomplete.


### Native tenant policy locking

`auth/lock-resource-grant-policy.ts` acquires a shared lock on the organisation derived from the stored grant and authenticated client, using the active native adapter transaction. User code issuance takes it before evaluating policy; refresh takes it before the grant-family row lock and before cached-response admission. Existing tenant policy writers take the organisation exclusively. Lock waits are bounded to two seconds, with the same OAuth 503/Retry-After mapping as production machines through `auth/grant-error.ts`. This is a per-lock bound, not a total request deadline.

The initial native race reproduced capability removal committing while an already-approved issuance was paused. Tests now cover issuance-first for code and refresh, revocation-first for code, retryable contention without code consumption, and the actual restricted-role adapter. First-principles review reused the existing tenant row and extracted the previous machine error mapping; no lock table, distributed lock, queue or schema change. Global user/client/resource dependencies, creation/unlink ordering, other writer race scenarios, production user OAuth and complete operational release proof remain unfinished.


Tenant-lock verification: the full ID suite passes 1,547 tests, zero failures and 14,164 assertions with 100% line/function coverage (`/private/tmp/id-user-lock-full.log`, 192.52 seconds). The focused native/machine/runtime suites pass 79 tests / 2,837 assertions. Typecheck, lint and all 62 web tests pass. The initial concurrent capability-removal regression failed before the tenant lock; after the fix, issuance-first code/refresh, revocation-first code and timeout/code-retry tests pass. Build verification passes for both packages in 18.905 seconds (`/private/tmp/id-user-lock-build.log`); `git diff --check` passes. No migration or public API shape change was introduced; full contract drift checks pass. The foundation remains incomplete.


### Native client/resource policy locking

The tenant lock helper is now `auth/lock-resource-grant-policy.ts`. It locks organisation → client → resource in the native transaction before the refresh-family lock. Existing client/resource configuration services lock those same target rows, so scope changes, unlinking and disable cannot commit while an approved issuance is paused. The callback avoids repeating locks already acquired by refresh preflight. Global user ordering, creation races and other writer/credential semantics remain unfinished.

Six initial code/refresh races reproduced scope changes and unlinking committing before issuance. The expanded tests cover capability changes, client/resource scope changes, unlink and client/resource disable: issuance-first for code and refresh, writer-first for code, plus existing timeout recovery and restricted-runtime coverage. Disabled clients fail later authentication with 401; other removed grant authority returns 400 on refresh. First-principles review extends the existing lock order rather than adding locks on individual assignments or new state, and removes redundant refresh lock queries. No migration or public API change.


Target-lock verification: `/private/tmp/id-user-target-lock-full.log` records 1,564 passing ID tests, zero failures, 14,878 assertions and 100% line/function coverage (210.37 seconds). The focused race matrix passes 19 tests / 806 assertions; the final tenant/client/resource timeout suite passes 3 tests / 138 assertions. Typecheck, lint and all 62 web tests pass. Initial lifecycle matrix setup was corrected to supply the required resource environment and to expect authentication-level 401 after client disable; the final full run has no failures. First-principles review removed redundant refresh locks, reused the existing target row locks and retained one error mapper. No migration or public API change; full drift checks pass. Build verification passes for both packages in 16.831 seconds (`/private/tmp/id-user-target-lock-build.log`); `git diff --check` passes. The full foundation objective remains incomplete.


### Global user ordering for native grants

The grant lock helper now reads immutable context/owner IDs, locks the grant subject and any client-owner user in UUID order, then organisation → client → resource, before the grant-family row. Global user lifecycle services already take an exclusive user lock. This prevents user disable from committing between an approved policy read and code binding, and prevents erasure of another user's client from deleting the context while its grant transaction holds the client. Current policy is read after all locks.

The initial global-disable race completed disable before the paused issuance, which subsequently failed code binding. Native tests now cover both global-disable commit orders and user-lock timeout/retry. The separate client-owner erasure test uses the native adapter transaction and performs the real code-binding write before committing; it is a lock/cascade proof, not a complete user-owned-client OAuth flow. It verifies actual context/client deletion after release. Focused tests pass 4 tests / 156 assertions. First-principles review removed the redundant organisation join from identity discovery; no new state, migration or lock service. Grant creation, session/SSO writers, remaining credential and multi-operation races, production consent/tenant selection and release proof remain unfinished.


Global-user locking verification: after correcting a dangling join left by the cleanup edit, typecheck passed and the disposable schema was rebuilt from migrations. The native/runtime suites pass 85 tests / 3,650 assertions. The final full run (`/private/tmp/id-user-global-lock-final-full.log`) passes 1,568 tests, zero failures, 15,034 assertions and 100% line/function coverage in 203.03 seconds. The earlier `/private/tmp/id-user-global-lock-full.log` was interrupted after detecting the malformed query and is not passing evidence. Lint, typecheck and all 62 web tests pass. Build passes for both packages in 18.105 seconds (`/private/tmp/id-user-global-lock-build.log`); `git diff --check` passes. No migration or public API change was introduced; full drift checks pass. F0–F7 remain incomplete.


### Administrative session revocation races

The existing single-session and all-session services already take the user lock before session/token/context changes. The native proof now tests those commands and global disable against code and refresh issuance, plus writer-first code issuance. All nine race cases pass (378 assertions), and subsequent refresh is denied. No additional production lock or service change was needed.

A native sign-out test separately deletes the browser session, preserves the context's authentication origin, and successfully exercises cached and fresh refresh. Later administrative all-session revocation reports zero deleted sessions but a real change, and denies both refresh paths before claims run (61 assertions). This is not a complete production user OAuth flow; it proves the existing browser-session/delegation distinction. First-principles review retained the shared user barrier and avoided coupling grant validity to a live browser session. Creation, SSO/credential and multi-operation races, full audit guarantees and production release gates remain unfinished.


Session-race verification: the full ID suite passes 1,576 tests, zero failures and 15,395 assertions with 100% line/function coverage (`/private/tmp/id-session-race-full.log`, 213.56 seconds). The focused race matrix passes 9 tests / 378 assertions; native sign-out/delegation proof passes 1 test / 61 assertions. Typecheck, lint and all 62 web tests pass. No production code, schema or public API change was necessary; first-principles review retained the existing user lock and independent delegation semantics. Build verification passes for both packages in 21.109 seconds (`/private/tmp/id-session-race-build.log`); `git diff --check` passes. Creation, SSO/credential and multi-operation races, complete audit/isolation, production OAuth and operational release gates remain unfinished.

## Creation lock ordering: verified slice

Reproduced a stale compatibility-link read: creation waited for unlink to commit, then inserted a context anyway (`/private/tmp/id-create-unlink-red.log`). Creation now owns a transaction/savepoint, resolves immutable ownership, shares the issuance target lock order and re-reads admission after locking. Separate-connection tests prove both unlink commit orders; a third proves timeout, no partial context and retry. The restricted-runtime and native integration suite passed 95 tests / 4,073 assertions before the separate timeout test passed 1 test / 32 assertions.

Final gates: 1,579 ID tests, zero failures, 15,488 assertions, 100% line/function coverage (`/private/tmp/id-create-lock-final-full.log`, 273.51 seconds); typecheck, lint, 62 web tests and build pass (`/private/tmp/id-create-lock-{typecheck,lint,web,build}.log`). The first full attempt (`/private/tmp/id-create-lock-full.log`) timed out in a fixture and then lost database connections; it was interrupted with exit 130. No surviving test process remained, container readiness and host-driver connectivity were verified, and the subsequent full run passed. Do not cite the interrupted run as green.

First-principles review shared the existing row-lock order instead of duplicating it, removed a redundant timeout-setting call and removed identical adjacent historical documentation paragraphs. No new state, schema, workflow or public API was added. README, foundation specification and release decision plan reflect the narrower evidence. Remaining creation writers, SSO/credential and multi-operation races, complete audit/isolation, trusted production OAuth admission and operational release proof remain unfinished. Next: creation versus member/user/tenant/session lifecycle and client-owner erasure in both commit orders.

## Creation versus lifecycle writers

The native fixture now exercises both commit orders for unlink, membership removal, user/tenant disable, single/all-session revocation, client/resource disable and client-owner erasure through production services. PostgreSQL blocking is observed on separate connections. Writer-first denies creation with no context. Creation-first allows the context to commit, after which lifecycle writers revoke it or owner erasure deletes it; unlink leaves a context that current policy denies. Owner erasure uses a dedicated user-owned client and proves provenance/cascade behaviour, not full token admission for that client. Focused matrix: 18 tests / 567 assertions (`/private/tmp/id-create-lifecycle-focused.log`).

First-principles review found the existing sorted user → organisation → client → resource lock order sufficient for these cases. No production code, schema or public API change was needed. The existing two commit-order tests were expanded as a matrix rather than copied into separate harnesses. Creation provenance and final issuance authority remain distinct; credential/configuration tests must assert the intended behaviour of each change, not assume every write forbids all new contexts.

Final gates: 1,595 ID tests, zero failures, 15,994 assertions and 100% line/function coverage (`/private/tmp/id-create-lifecycle-full.log`, 263.02 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-create-lifecycle-{typecheck,lint,web,build}.log`); `git diff --check` passes. README, foundation specification and release decision plan describe this evidence. Remaining configuration/credential/SSO and multi-operation races, production OAuth admission, full audit/isolation, bounded operations and release rehearsal remain unfinished. F0–F7 remain open.

## Credential concurrency and the SSO provenance gap

Four native code/refresh race cases pass 195 assertions (`/private/tmp/id-credential-race-final-focused.log`). Rotation waits for approved issuance, revokes its resulting context and prevents refresh with the new secret. Rotation after initial client authentication denies before claims; code revalidation returns 401 invalid_client, while refresh preflight returns 400 invalid_grant. Retained refresh rows are revoked, not deleted. New credentials cannot restore old grants but can redeem fresh authorisation. Initial test assumptions about uniform error codes and deleted rows were corrected against the actual preflight and token-revocation implementation; no production change was justified.

Final gates: 1,599 ID tests, zero failures, 16,189 assertions and 100% line/function coverage (`/private/tmp/id-credential-race-full.log`, 265.20 seconds); typecheck, lint, 62 web tests and build pass (`/private/tmp/id-credential-race-{typecheck,lint,web,build}.log`). The full run started only after the focused process was terminal. An earlier launch was rejected by automatic approval review over possible overlap; a process listing confirmed no surviving test process before the successful serial sequence. `git diff --check` passes.

Next substantive implementation: immutable tenant/provider authentication provenance and SSO-change invalidation. Current sessions have no such origin, and provider configuration services do not revoke it. Revoking existing grants alone would leave stale authentication able to create new contexts; global user-session deletion would affect unrelated tenants. The pinned SSO callback already checks its accepted provider reference within the native transaction. Prove and reuse the supported session-hook/adapter seam for provenance instead of forking protocol handling. Full SSO, production OAuth admission, audit/isolation, bounded operations and release rehearsal remain open; F0–F7 are incomplete.

## Native SSO origin capture

Migration 0034 adds immutable session origin (organisation UUID, provider instance UUID and provider revision), plus guards against changing session ID, user or creation time. Complete origin must match a current provider at insertion and survives provider deletion. Legacy sessions remain unattributed. The supported native resolver/session before-create adapter bridge captures origin once inside the transaction, rejects a missing provider before identity writes, strips caller-supplied origin and fails closed on a native callback without provenance. A failed origin insert rolls back new user/account/session rows.

Runtime responses hide the three fields. The native OpenAPI generator ignored returned:false, so the public projection now honours that metadata; snapshots were regenerated through the exporter. Unit auth stubs were updated to include their options interface. Focused native/schema proof passes 50 tests / 305 assertions; native/contract proof passes 28 tests / 107 assertions; corrected application/contract tests pass 21 tests / 126 assertions. Earlier full attempts respectively exposed OpenAPI drift and the stale unit stub and are not green baselines.

Full ID verification passes 1,603 tests, zero failures, 16,217 assertions and 100% line/function coverage (`/private/tmp/id-sso-origin-complete-full.log`, 283.47 seconds). Typecheck, lint and 62 web tests pass (`/private/tmp/id-sso-origin-{typecheck,lint,web}.log`). Build passes for both packages in 16.479 seconds (`/private/tmp/id-sso-origin-build.log`); `git diff --check` passes. Migration was applied only to the guarded disposable database. No production migration or deployment occurred.

First-principles review retained one transaction-scoped hand-off and three evidence fields, with no new authorisation store or epoch counter. It moved transaction validation before identity writes, protected the attested session's subject/time and kept hidden fields out of the public contract. Capture alone is not revocation: grant admission and provider-change invalidation remain unfinished, including restricted-role native SSO proof. Next prerequisite: ordinary provider row-lock updates must not advance configuration revisions. The pinned provider locks via a same-value providerId update; generic updated_at/revision handling currently treats it as a change. Prove and fix that before using revision equality for admission.

Separate confirmed F6 gap: the native better-call router prints raw non-API exceptions through its SERVER_ERROR fallback after the configured Better Auth logger returns. SSO write-fault tests expose this path. Sanitised provider logger output therefore does not prove complete callback error sanitisation; add a sanitised native error boundary. Tenant-specific versus cross-membership SSO policy was asked asynchronously; origin capture is independent of that decision. Full F0–F7 remain incomplete.


## Stable SSO revisions and native error handling: verified

Native regressions reproduced ordinary login advancing provider revision and a failed origin insert exposing the raw router fallback. Migration 0035 now preserves SSO revision/timestamp on same-value locking updates; real edits and forged-revision rejection retain proof. The native API error hook preserves protocol errors and throws fixed authentication_unavailable for unexpected exceptions. Real SSO fault injection proves fixed HTTP 500 JSON, one sanitised diagnostic and full user/account/session rollback. Focused: 29 pass / 136 assertions.

Full gates: 1,605 ID tests, zero failures, 16,235 assertions, 100% line/function coverage (`/private/tmp/id-sso-revision-full.log`, 336.16 seconds); typecheck, lint, 62 web tests and build pass (`/private/tmp/id-sso-revision-{typecheck,lint,web,build}.log`, build 25.917 seconds). Migration applied only to the disposable database; no production deployment. README, schema/foundation contracts, sign-in guide and release/evidence reports updated. First-principles review retained an SSO-specific trigger because other configuration timestamp touches intentionally signal related-row changes; no second counter or provider fork. Historical notes above describing these two gaps are superseded by this result.

Next: enforce authentication origin in tenant admission and provider-change invalidation, including callback races, A/B preservation, audit rollback and actual restricted-role SSO. Origin capture and stable revision alone do not revoke authority. Tenant-specific versus cross-membership authentication policy remains unresolved; no silent policy cutover. Full F0–F7 remain incomplete. See the execution report for code-linked evidence.


## SSO changes revoke existing tenant grants: verified

Previous goal turn classification: progress. It repaired ordinary-login revision changes and sanitised the native error fallback. This turn reproduced missing SSO grant revocation/effects (four failing regressions), then missing affected-user audit attribution (three failing regressions). The SSO service now reuses organisation locking and tenant-wide context revocation for creation, real replacement and deletion. No-op configuration preserves grants. Configuration, actual affected IDs, successful audit and journal receipt remain atomic; restoration never revives old grants. Migration 0036 adds explicit durable SSO affected-user subjects with recorded-only backfill.

Focused service/HTTP/native proof: 15 pass / 344 assertions (`/private/tmp/id-sso-grants-focused.log`). Three native modes deny A cached/rotated refresh before claims and preserve B refresh and the global session. Nine separate-connection races pass 381 assertions (`/private/tmp/id-sso-grants-races.log`), covering writer-first code denial and issuance-first code/refresh with observed PostgreSQL blocking. Audit failure rolls back provider and contexts; affected-user history survives erasure.

Full gates: 1,624 ID tests, zero failures, 16,856 assertions, 100% line/function coverage (`/private/tmp/id-sso-grants-full.log`, 293.40 seconds); typecheck, lint, 62 web tests and build pass (`/private/tmp/id-sso-grants-{typecheck,lint,web,build}.log`, build 16.625 seconds). OpenAPI regenerated. README, schema/foundation contracts, onboarding guide and reports updated; mutation totals corrected to 49 and seven PATCH routes from generated OpenAPI. `git diff --check` passes. Migration only on the disposable database; no production deployment.

First-principles review retained existing locks/query/transaction and extended existing race matrices; no counter, workflow or second grant store. Remaining: new grant and direct-admin admission using authentication origin; callback/configuration races; restricted-runtime native SSO; policy for tenant-specific versus cross-membership authentication. Raw SQL/provider-internal changes do not invoke service revocation, and stale browser authentication can still create new authority until admission is fixed. Next slice must address that boundary, not treat revoking existing contexts as complete SSO invalidation. Full F0–F7 remain unfinished.


## Restricted SSO callback boundary: verified

Previous turn: progress. This turn moved the federation HTTP suite to a real restricted login and reproduced secret-only rotation being accepted during token exchange. Native fingerprinting excludes the secret. The adapter now observes the first provider revision in request-local async context; native resolution compares it under the provider transaction before identity writes and returns SSO_PROVIDER_CHANGED on mismatch/missing evidence. Overlapping callbacks remain independent. No secret, new epoch, migration or protocol fork.

Focused: 33 pass / 139 assertions (`/private/tmp/id-sso-runtime-verified.log`). Full: 1,630 pass, zero fail, 16,892 assertions, 100% line/function coverage (`/private/tmp/id-sso-runtime-full.log`, 282.61 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-sso-runtime-{typecheck,lint,web,build}.log`, build 21.28 seconds); diff check passes. Docs and generated OpenAPI updated. No production deployment. See the execution report’s restricted-native-SSO section for code evidence and earlier failing probes.


## Callback-first provider ordering: verified

Previous turn: progress. Three new native restricted-login races prove provider update/delete/recreate waits for the callback transaction, which commits its original origin. A test-only advisory barrier is removed after every settled run. The first run exhausted the owner fixture's one-connection pool; control/writer/observer now use three owner connections while native auth retains one restricted connection. Corrected focused proof: three pass / 33 assertions.

Full gates: 1,633 pass, zero fail, 16,925 assertions, 100% line/function coverage (`/private/tmp/id-sso-callback-first-full.log`, 258.77 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-sso-callback-first-{typecheck,lint,web,build}.log`, build 274 ms with one cached task). Diff check passes. Test/docs changes only; no migration or deployment. Code-linked evidence is in the execution report.


## Process recovery verification in progress

Previous goal turn: progress. It revised the evidence-backed release plan and exposed that the latest full run was failing rather than presenting an older green baseline as current proof.

A process inspection confirmed no surviving test workers. The resumed serial full run passed 1,639 ID tests / 17,195 assertions with 100% line/function coverage (`/private/tmp/id-foundation-resumed-full.log`, 367.24 seconds); the earlier failing run remains recorded and its cause is not established. Typecheck, lint and 62 web tests pass. First-principles review retained the existing transaction/journal and identified a missing practical recovery assertion: the returned secret must work. Both rotation crash cases now reject the old credential, mint with the recovered credential and use that token on an authenticated administrative read. Focused: six passes / 280 assertions; typecheck/lint pass. Final full run is active in `/private/tmp/id-crash-credential-full.log`; build follows only after it exits.

The tenant SSO trust decision remains pending. This slice does not implement new admission policy, production user OAuth, complete audit/isolation, or release rehearsal. F0–F7 remain unfinished.


Process recovery slice verified: final full suite 1,639 pass / zero fail / 17,205 assertions, 100% line/function coverage (`/private/tmp/id-crash-credential-full.log`, 357.41 seconds). Typecheck/lint pass; web 62 pass; build two successful packages, one cached, 308 ms. Earlier failures remain recorded with no asserted root cause. Exact previously reported orphan runtime role is absent. First-principles outcome: retain the existing journal, add real credential-use assertions to its six-case crash matrix, and simplify the foundation document's current-status summary. No production code/schema/API/deployment change. Pending tenant SSO decision and remaining F0–F7 requirements keep the goal active.


## Access query context boundary: verified

Previous goal turn: progress (completed and verified process-recovery credential proof). A new regression reproduced raw-executor access queries bypassing the service context boundary. Member/target access queries now require existing issued tenant contexts and derive their transaction/tenant. Member-removal audit capture uses its existing command context in the same transaction. New tests cover raw calls, copied/expired/wrong-purpose contexts and the same user's separate A/B memberships under the actual restricted login. The initial restricted fixture defaulted its user to inert; explicitly setting active corrected that fixture without changing policy.

Focused: 47 pass / 468 assertions (`/private/tmp/id-access-context-verified.log`). Full: 1,642 pass / zero fail / 17,229 assertions, 100% line/function coverage (`/private/tmp/id-access-context-full.log`, 326.36 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-access-context-{typecheck,lint,web,build}.log`); build two successful packages, one cached, 275 ms. `git diff --check` passes. Documentation and isolation inventory updated. No schema, public API or deployment change.

First-principles review removes independent tenant/executor parameters and reuses existing guards. Service validation remains before existence lookups; query validation protects independent calls. No new registry, wrapper framework or policy engine. The views remain assignment projections: full policy explanations, other raw query families, grant-context isolation, tenant authentication policy and F0–F7 release proof remain incomplete. Continue those boundaries; do not treat this family as complete query adoption or mark the goal achieved.


## Membership query contexts: verified

Previous goal turn: progress (closed access-query context adoption). This slice reproduced raw membership-query access, then required existing tenant/platform contexts at all eight query entry points. Tenant queries derive their executor/organisation; read purposes cannot mutate. Configuration locking follows command context, replacing the caller-controlled flag. Group/entitlement assignment checks now read only membership status under platform-write authority. Native federation and RLS policies are unchanged.

Focused: 50 tests / 558 assertions (`/private/tmp/id-member-query-focused.log`, 15.63 seconds). Full: 1,645 pass / zero fail / 17,275 assertions, 100% line/function coverage (`/private/tmp/id-member-query-full.log`, 316.58 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-member-query-{typecheck,lint,web,build}.log`); build two successful packages, one cached, 279 ms. `git diff --check` passes. README, foundation, isolation inventory and evidence/release reports updated. No migration, public API change or deployment.

First-principles review removed independently supplied tenant/executor arguments, a locking flag and unused user/group projections. Existing guards and transactions suffice; no new registry or framework. Other query families, complete policy explanations, grant-context isolation, production user OAuth, tenant SSO trust policy and F0–F7 operational release proof remain unfinished. Keep the full goal active.


## Group and entitlement query contexts: verified

Previous goal turn: progress (closed membership query context adoption). This slice requires issued contexts at all 20 administrative group/assignment/entitlement query entry points. Tenant reads derive organisation/transaction, platform writes retain explicit targets, and fleet reads require platform-read authority. Command lookups replace read-controlled locking flags. Bootstrap performs two direct inserts in its existing scoped transaction. The test-only addGroupMember helper moved out of production.

Focused: 39 tests / 721 assertions (`/private/tmp/id-policy-query-verified.log`, 10.78 seconds). The initial raw-call regression accepted the handle and returned an empty result; it demonstrated a missing internal context check, not an external data leak. Two old nonexistent-tenant read expectations now correctly expect context creation to fail with 404. Direct misuse covers raw/copy/expiry/wrong-purpose callers for every query. Restricted-login tests prove A/B group/assignment/entitlement isolation, reject tenant writes and allow explicit platform fleet reads.

Full: 1,648 pass / zero fail / 17,496 assertions, 100% line/function coverage (`/private/tmp/id-policy-query-full.log`, 361.32 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-policy-query-{typecheck,lint,web,build}.log`); build two successful packages, one cached, 355 ms. `git diff --check` passes. README, foundation, isolation inventory and evidence/release reports updated. No schema, RLS policy, public API, permission semantics, federation or deployment change.

First-principles review deleted unused production helper code, obsolete locking flags and redundant guards in read services. Write-service guards remain before locks/mutations. Existing context registries and transactions suffice. Other raw query families, full policy explanations, grant-context broker isolation, production user OAuth, pending tenant SSO trust policy and F0–F7 operational release proof remain unfinished. Keep the goal active.


## Retained history reader boundaries: verified

Previous goal turn: progress (closed group/entitlement query contexts). This slice reproduced raw audit-query acceptance and now requires existing contexts for platform, user-history and tenant-history readers. Tenant history derives its organisation and overrides forged caller filters. The common raw SQL reader is private. The raw operation-status projection also became private beside its current-authority/actor-bound callers, removing its exported caller-controlled visibility descriptor.

Focused: 76 tests / 532 assertions (`/private/tmp/id-history-query-focused.log`, 14.37 seconds). Direct misuse tests cover raw/copy/expiry/purpose/tenant substitution. Actual restricted-login proof preserves A/B/global facts after deleting live user/tenant rows; authorised root-issued tenant history sees its own facts and platform user history sees all UUID-linked facts. This does not restore deleted members' authority or recover names. Migration tests retain their same-transaction backfill evidence under issued platform contexts.

Full: 1,651 pass / zero fail / 17,522 assertions, 100% line/function coverage (`/private/tmp/id-history-query-full.log`, 315.34 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-history-query-{typecheck,lint,web,build}.log`); build two successful packages, one cached, 768 ms. `git diff --check` passes. Documentation and inventories updated. No schema, RLS, public API or deployment change.

First-principles review made the status query private instead of adding a new context registry, reused one audit SQL reader and removed a redundant platform-reader service guard. Audit writes remain a transaction primitive for bootstrap, commands and native protocol callbacks. Remaining raw query families, complete audit/effects and policy explanations, grant-context isolation, production user OAuth, pending tenant SSO trust policy and F0–F7 operational release proof remain unfinished. Keep the full goal active.


## Summary query contexts and exact-pair reporting

Two failing regressions reproduced an unscoped raw summary read and a response that dropped the resource from exact-pair entitlement counts (`/private/tmp/id-summary-query-before.log`). Fleet summary/statistics now require issued platform read contexts. Organisation summary/statistics require issued directory contexts and derive their tenant and transaction. The shared raw statistics query is private; redundant service guards were removed. No new context registry, schema or RLS policy was necessary.

Exact-pair targets retain `kind: client_resource`, client `id` and `resource`, alongside existing standalone targets. HTTP schema and generated OpenAPI reflect the change. Counts describe stored rows, including disabled/out-of-window assignments, not an authorisation decision. The staff runbook documents this distinction.

Focused proof: 114 pass, zero fail, 671 assertions across four files (`/private/tmp/id-summary-query-focused.log`, 8.36 seconds). Tests cover raw/copied/expired/wrong-purpose contexts, exact-pair and standalone projection, actual HTTP schema preservation, and A/B counts/statistics through the restricted database login. Full gate verification is complete for this slice; see the terminal results below. Other query/broker boundaries, full policy explanations, production user OAuth, the pending tenant SSO trust decision and operational release proof remain unfinished.


The first full summary run failed: 1,654 pass, two fail and one error (`/private/tmp/id-summary-query-full.log`, 365.47 seconds). Removing the service guard exposed inconsistent query failure timing: async `platformSummary` returned a rejected promise while synchronous `platformSignInStats` threw before `Promise.all` attached rejection handlers. The unhandled rejection disrupted the adjacent SSO probe test. Both statistics entry points now use async functions, matching the summary readers. The existing fleet context-lifetime tests and summary tests pass together: 12 pass, zero fail, 107 assertions (`/private/tmp/id-summary-query-recovery-focused.log`, 13.76 seconds). A full rerun is required; this failed run is retained as evidence.


Summary slice final verification: 1,656 ID tests pass, zero fail, 17,559 assertions, 100% line/function coverage (`/private/tmp/id-summary-query-full-2.log`, 422.74 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-summary-query-{typecheck,lint,web,build}.log`); build two successful packages, neither cached, 24.758 seconds. Generated OpenAPI export succeeds and git diff --check passes. The failed first full run remains recorded; consistent async query rejection fixes its unhandled promise. Existing context registries and one private statistics query suffice; duplicate service guards were removed. No migration, new RLS policy or deployment. Other query/broker isolation, complete policy explanations, production user OAuth and the remaining F0–F7 gates remain unfinished. Keep the full goal active.


## Domain query boundary: implementation and focused evidence

Previous goal turn: progress, verified summary query contexts and pair reporting. This slice reproduced raw database acceptance by domain listing (`/private/tmp/id-domain-query-before.log`), then required existing contexts for all six domain query entry points. Platform-write context guards create/status/delete/locked lookup; tenant-directory guards listing; tenant-member-access guards the new boolean domain acceptance query used by diagnosis. The global routing identity lookup, unused unlocked detail query and public lock flag were deleted. Fixture row assertions remain test-only. No new registry, schema, RLS policy, public API or deployment change.

Focused final result: 137 pass, zero fail, 990 assertions over eight files (`/private/tmp/id-domain-query-focused-2.log`, 15.09 seconds). Coverage includes raw/copy/expiry/purpose misuse, real restricted-login A/B read/write isolation, domain uniqueness/reassignment/disabled-organisation constraints, lifecycle audit rollback and HTTP diagnostic authority/redaction. The earlier focused run also passed before removal of the unused helper; typecheck caught and resolved fixture import/readonly-array errors. Typecheck, lint and 62 web tests pass. The full serial ID gate has completed; final evidence follows.

First-principles review favoured removal of global routing data over returning it and redacting later. No new authorisation subsystem is needed. Domain tables remain outside RLS; issued query contexts constrain application calls, not arbitrary SQL by a compromised runtime process. Other broker/query boundaries and complete F0–F7 operational and user-OAuth proof remain unfinished.


Domain slice final gates: 1,659 pass, zero fail, 17,669 assertions, 100% line/function coverage (`/private/tmp/id-domain-query-full.log`, 292.16 seconds). Typecheck, lint, all 62 web tests and production build pass (`/private/tmp/id-domain-query-{typecheck,lint,web,build}.log`); build two successful packages, one cached, 242 ms. `git diff --check` passes. README, foundation, isolation inventory and execution/release reports are updated. No schema or public contract change required OpenAPI regeneration. First-principles deletion removed the global routing lookup, unused unlocked production detail reader and public lock flag. Remaining query/broker isolation, complete policy explanations, production user OAuth, pending tenant SSO trust policy and F0–F7 operational release proof remain unfinished. Keep the goal active.


Previous goal turn: progress, verified all domain query contexts and removed global routing data from diagnosis. Current slice closes SSO configuration query contexts and narrows read projections.


## SSO configuration query authority and projections

All seven database-facing SSO query entry points now require issued contexts. Create/update/delete and full configuration lookup require platform-write context; the lookup always locks. Tenant directory reads return the existing redacted provider shape inside the query boundary. Member-access diagnosis selects only the issuer. Platform connectivity reads select only the issuer and JSON discovery endpoint; the external probe still runs after the read transaction closes. Copied, expired, raw and differently scoped contexts are rejected.

The exported raw full-configuration reader and caller-controlled lock flag are removed. The private shared row query supports the command and directory paths; directory redaction still occurs in the server process after the database read, so this is not a database column-privilege claim. Diagnosis and connectivity no longer load the full secret-bearing configuration. The existing serialiser/redactor and context factories are reused, and duplicate read-service guards are removed. No new registry, schema, RLS policy, public response shape or federation callback behaviour is introduced. The SSO provider table remains available to the native authentication adapter; this does not complete broker isolation or the pending tenant SSO trust contract.

Regression evidence: `/private/tmp/id-sso-query-before.log` reproduces raw configuration query acceptance. Focused tests pass 111/111 with 1,041 assertions over seven files (`/private/tmp/id-sso-query-focused.log`, 13.21 seconds). Coverage includes context purpose/lifetime/provenance, absent/legacy-null configuration, redaction, A/B projections under the restricted login, authorised command secret access, HTTP SSO lifecycle and the existing probe transaction-lifetime regression. Full gate results are recorded in the execution evidence after completion.

First-principles review reused the existing contexts and serialiser/redactor, removed the raw reader/lock flag and redundant read-service guards. The context distinction follows actual callers; no general policy framework or new registry is needed. Full suite has passed for this slice; the full goal remains incomplete.


SSO query slice final gates: 1,663 pass, zero fail, 17,818 assertions, 100% line/function coverage (`/private/tmp/id-sso-query-full.log`, 334.05 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-sso-query-{typecheck,lint,web,build}.log`); build two successful packages, one cached, 273 ms. `git diff --check` passes. README, foundation, isolation inventory and execution/release reports updated. No schema, RLS, API or deployment change. First-principles review removed the raw exported configuration reader, public lock flag and duplicate service read guards; it reused the existing context registries and serialiser/redactor. Other query/broker boundaries, full policy explanations, production user OAuth, pending tenant SSO trust policy and complete F0–F7 operational release proof remain unfinished. Keep the goal active.


## Organisation query authority and internal locking

All ten administrative organisation query entry points require issued contexts. Fleet listing requires platform-read; detail derives its tenant from directory context; diagnosis selects only id/slug/status under member-access context; retained history checks only existence under history context. Create/update/status/delete/client-count/locked lookup require platform-write. Raw, copied, expired and wrong-purpose contexts are rejected. History can check an erased organisation without granting live directory access.

The exported raw organisation reader is deleted. The low-level lock moved to `db/organization-lock.ts` as an explicit internal primitive for tenant authority establishment, operation-status authority checks and protocol transactions. These callers cannot require an already-issued context before they establish authority. Administrative services use the guarded command lookup. The primitive still accepts an executor and is not a standalone authorisation boundary. Native machine claim construction now selects only status and authorisation version from its authenticated adapter transaction; its preceding lock protocol is unchanged.

Client creation now acquires the organisation command lock before checking ownership existence. A barrier test proves it waits for concurrent erasure, then returns 404/not_found after erasure commits, without client or audit effects. This prevents a stale non-locking existence check from reaching a foreign-key failure instead of the documented missing-owner result. Other organisation command consumers retain their lock order.

First-principles review reuses the existing contexts and lock implementation, removes the raw directory reader and duplicate service/helper guards, and narrows diagnosis/history/machine reads. No new context registry, schema, RLS policy, public API or deployment is introduced. Remaining query/broker boundaries, complete policy explanations, production user OAuth and F0–F7 operational proof remain unfinished.


Previous goal turn: progress, verified SSO query contexts and purpose-specific projections. Organisation regression `/private/tmp/id-organization-query-before.log` reproduced raw database access. Final focused result: 149 pass, zero fail, 1,140 assertions (`/private/tmp/id-organization-query-focused-2.log`, 17.62 seconds). The first focused run failed because its machine-client race fixture had empty scopes and failed validation before reaching the lock; corrected to a valid scope. Typecheck fixture inference and lint unused-binding errors were fixed. The full ID gate passed for this slice; terminal evidence follows.


Organisation query slice final gates: 1,667 pass, zero fail, 18,014 assertions, 100% line/function coverage (`/private/tmp/id-organization-query-full.log`, 356.76 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-organization-query-{typecheck,lint,web,build}.log`); build two successful packages, one cached, 318 ms. `git diff --check` passes. README, foundation, isolation inventory and execution/release reports updated. No schema, RLS, public API or deployment change. First-principles review removed the raw full organisation reader and duplicate guards, retained the necessary authority-establishment lock primitive, and narrowed diagnosis/history/machine projections. Other query/broker boundaries, common policy explanations, production user OAuth, pending tenant SSO trust policy and complete F0–F7 operational release proof remain unfinished. Keep the goal active.


Previous goal turn: progress, verified organisation query authority and client-creation/erasure ordering.


## Global user and session query authority

All eleven database-facing user/session query entry points now require issued platform contexts. Global user list/detail/existence and session list require platform-read. Status changes, email retirement, session lookup/deletion and user-session ID deletion require platform-users. User erasure requires platform-write. The shared user row lock accepts only the two issued command contexts that use it; permission to lock does not authorise the other command family's mutations. Tenant contexts, raw executors, copied contexts and expired contexts are rejected.

The unused production `findUserByEmail` helper is deleted; tests needing it compose the existing platform directory queries in one authorised transaction. The unused count-only `deleteUserSessions` wrapper is deleted. `deleteUserSessionIds` now accepts one user ID, matching every production caller, and returns exact sorted affected session IDs. There is no unneeded bulk array interface or empty-array bypass. Global revocation remains explicitly global; tenant membership removal is a different operation. Existing lifecycle effects, audit facts and lock ordering are preserved.

First-principles review reuses the existing context registries and removes duplicate read-service guards and unused helpers. No broker exception is needed for these query modules: their only non-administrative import is the pure retired-email formatting helper. Tables and native authentication adapter access remain unchanged; context guards constrain application query calls, not arbitrary runtime SQL. No schema, RLS, public API or deployment change.

Evidence: `/private/tmp/id-user-session-query-before.log` reproduces raw user and session listing. Focused tests pass 143/143 with 1,154 assertions (`/private/tmp/id-user-session-query-focused.log`, 13.15 seconds). Direct tests cover all eleven entry points, including null/raw/copy/expiry and wrong platform/tenant purposes. Restricted-login proof shows both memberships in a legitimately global user view, omits upstream/session credentials, rejects foreign-user session IDs and preserves another user's sessions during global revocation. Full gate results are recorded below after completion. Remaining query/broker boundaries, common policy explanations, production user OAuth and F0–F7 operational proof stay open.

Typecheck's new test array inference error was corrected with an explicit string array. Typecheck, lint and 62 web tests pass; the full ID suite also passed, as recorded below. Keep the full goal active.


Global user/session query final gates: 1,671 pass, zero fail, 18,184 assertions, 100% line/function coverage (`/private/tmp/id-user-session-query-full.log`, 293.20 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-user-session-query-{typecheck,lint,web,build}.log`); build two successful packages, one cached, 229 ms. `git diff --check` passes. README, foundation, isolation inventory and execution/release reports updated. No schema, RLS, public API or deployment change. First-principles review removed the unused full email lookup and count-only session deletion wrapper, narrowed session deletion to one user, and removed redundant service read guards. Existing context registries and command authority distinctions remain. Other query/broker boundaries, common policy explanations, production user OAuth, pending tenant SSO trust policy and complete F0–F7 operational release proof remain unfinished. Keep the goal active.


Previous goal turn: progress, verified global user/session query authority and removed unused helpers.


## Resource query authority and private-target concealment

All twelve resource query entry points now require issued contexts. Fleet listing, resource detail and linked-client listing require platform-read; policy validation and mutation/erasure checks require platform-write; tenant target-access lookup requires directory context and selects only an ID. The tenant query filters shared resources or resources owned by the context's organisation. A foreign private resource and a missing resource both produce 404 for tenant target-access requests, including exact client/resource pairs. Platform readers querying a tenant's access follow the same target boundary; their separate fleet inventory still includes all resources.

The initial HTTP regression reproduced 200 for a foreign private resource, disclosing existence (`/private/tmp/id-resource-query-before.log`). It also reproduced raw resource listing. The unrestricted resource finder is deleted. Protocol locking is an explicit internal primitive in `db/resource-lock.ts`; machine issuance audit selects only resource identity/revision/scope fields within its already-locked authenticated transaction. Administrative readers/commands use guarded shared/exclusive lookup functions. Capability and entitlement scope validation keep the resource locked while checking its ceiling. Existing serialisation and lifecycle locks remain; no new registry, schema or RLS policy is introduced.

Focused tests: 137 pass, zero fail, 1,213 assertions across eight files (`/private/tmp/id-resource-query-focused.log`, 13.55 seconds). They cover all twelve query entry points, context provenance/purpose/lifetime, restricted-login shared/A-private/B-private projections, complete platform inventory and HTTP foreign/missing/own/shared targets through both tenant and platform readers. The route's not_found description and public guide are updated; OpenAPI is regenerated through the exporter. A nullable assertion type error in the test was resolved with separate visible/hidden assertions. Full gate results are recorded after completion.

First-principles review removes the global existence lookup from tenant access and the exported raw resource reader, keeps the necessary native lock primitive and narrows audit projection. OAuth client queries and remaining broker/policy/admission/operational work are still open; this slice does not claim completion of F0–F7.

Typecheck and lint pass; the full ID gate also passed, as recorded below. Keep the full goal active.


Resource query slice final gates: 1,675 pass, zero fail, 18,444 assertions, 100% line/function coverage (`/private/tmp/id-resource-query-full.log`, 347.33 seconds). Typecheck, lint, 62 web tests and build pass (`/private/tmp/id-resource-query-{typecheck,lint,web,build}.log`); build two successful packages, neither cached, 28.777 seconds. OpenAPI was regenerated successfully (`/private/tmp/id-resource-query-openapi.log`); public target-access guidance, README, foundation, isolation inventory and execution/release reports are updated. `git diff --check` passes. First-principles review removed the unrestricted resource finder, narrowed tenant and machine-audit projections, and retained the explicit protocol lock primitive. No schema, RLS or deployment change. OAuth client query adoption, remaining broker boundaries, common policy explanations, production user OAuth, pending tenant SSO trust policy and full F0–F7 operational proof remain unfinished. Keep the goal active.


Client query slice: all fourteen query exports require existing issued contexts; platform/policy SQL excludes secret digests, tenant lookup returns only ID, and commands retain locked rows. Deleted the raw administrative finder and duplicate read redaction. Explicit protocol lock/principal primitives remain; no claim of completed broker isolation. Focused: 204 pass, 0 fail, 1,596 assertions, nine files, 19.39s. Typecheck/lint/web pass. Full gate result follows in the execution report. Continue remaining F0–F7 work; keep the goal active.


Client query final verification: 1,678 ID tests pass with zero failures, 18,741 assertions and 100% line/function coverage (`/private/tmp/id-client-query-full.log`, 339.70 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-client-query-{typecheck,lint,web}.log`). Build passes for both packages, one cached (`/private/tmp/id-client-query-build.log`, 526 ms). `git diff --check` passes. Next independent work: inspect capability/grant/token query authority, distinguishing protocol scope from tenant and global lifecycle commands. Do not mechanically move raw queries and claim isolation. F0–F7 remain incomplete; goal active.


Revocation query slice: four token queries require platform command contexts; member grant revocation derives its tenant from issued member context. Removed unused batch-token API and empty-list branch. Restricted-login proof covers preservation across tenants/members and distinct global token scopes. Focused final: 65 pass, 0 fail, 878 assertions, 26.14s. Earlier fixture failures retained, final session provenance fixed without weakening constraints. Typecheck/lint/web pass; full suite pending. Full F0–F7 goal remains active.


Revocation final verification: 1,681 ID tests pass with zero failures, 18,915 assertions and 100% line/function coverage (`/private/tmp/id-revocation-query-full.log`, 380.87 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-revocation-query-{typecheck,lint,web}.log`). Build passes for both packages, one cached (`/private/tmp/id-revocation-query-build.log`, 271 ms). `git diff --check` passes. Full F0–F7 goal remains active. Next: guard the nine remaining administrative grant-context mutation helpers and review removal of the test-only effective-grant production reader.


Grant query slice: all ten administrative grant mutation exports now require existing issued contexts. Nine remaining raw mutations converted; unused `findEffectiveResourceGrant` deleted. Native test uses real policy with positive/negative cases; lifecycle tests inspect persisted revocation/rollback. Direct provenance/purpose/lifetime and restricted-login exact effect checks added. No schema/RLS/API change. Focused/full verification pending; F0–F7 goal remains active.


Grant query final verification: 1,683 ID tests pass with zero failures, 19,232 assertions and 100% line/function coverage (`/private/tmp/id-grant-query-full.log`, 339.24 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-grant-query-{typecheck,lint,web}.log`). Build passes for both packages, one cached (`/private/tmp/id-grant-query-build.log`, 311 ms). `git diff --check` passes. All ten administrative grant mutation exports now require contexts; the unused effective-grant reader is removed. Native policy tests replace its expiry/mismatch assertions; restricted fixtures separate tenant and client ownership. Next: review broker/policy boundaries and the common evaluator/explanation gap, retaining the pending tenant SSO trust decision and full F0–F7 release scope. Goal remains active.


Human command policy slice: reproduced capability revocation overtaking an admitted command, plus expiry during a policy wait. Administrative grant reads now hold discovered organisation/admin-resource locks and filter to locked organisations; command expiry is rechecked with database time. Actual shared-lock upgrade deadlock receives retryable503 and same-key recovery, with no partial receipt/audit or repeated effects. Focused162pass/1512assert/20.06s. Typecheck/lint/web pass. Full gates pending; preserve full F0–F7 scope and active goal.


Human command policy final verification: 1,690 ID tests pass with zero failures, 19,446 assertions and 100% line/function coverage (`/private/tmp/id-command-policy-full.log`, 336.78 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-command-policy-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-command-policy-build.log`, 20.088 seconds). `git diff --check` passes. Actual races and expiry denial are fixed; deadlock recovery is tested with real journal/audit effects. Next: inspect machine/root authority ordering, including whether bearer expiry remains available for rechecking after waits. That expiry concern is a candidate for proof, not an established finding. Preserve common evaluator/explanation, pending SSO trust, production user OAuth and the remaining F0–F7 requirements. Goal remains active.


Machine command policy slice: reproduced missing-exp acceptance, capability revocation overtaking admitted command, and HTTP admission after lookup-time expiry. Principal lookup now holds organisation/client/resource locks; verified exp retained and checked after lookup and before command/replay. Focused81/1282assert and finalprincipal34/126assert pass. Typecheck/lint/web pass. Controlled-clock expiry-during-PostgreSQL-wait proof is explicit, not wall-clock lifetime proof. Full gates pending; goal active, root ordering and other F0–F7 work open.


Machine administrative policy final verification: 1,698 ID tests pass with zero failures, 19,707 assertions and 100% line/function coverage (`/private/tmp/id-machine-policy-full.log`, 322.56 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-machine-policy-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-machine-policy-build.log`, 16.993 seconds). `git diff --check` passes. Principal lookup holds organisation/client/resource locks; signed exp is mandatory and retained for post-wait admission checks. No new schema/cache/registry. Next: root admission ordering and the common permission evaluator/explanation work; preserve pending tenant trust, production OAuth and operational release requirements. Full F0–F7 goal remains active.


Root ordering slice: reproduced first-writer activation overtaking an admitted root command. hasPlatformWriter now locks bound organisation FOR UPDATE, platform membership users and resource FOR SHARE before evaluation. Exclusive organisation locking protects new-member FK insertions; no new lock state. Focused117/1352assert and finalroot8/300assert pass; unrelated tenant writes and break-glass preserved. Typecheck/lint/web pass. Full suite pending. Next substantive requirement: common permission evaluator/explanation contract; retain tenant trust decisions, production OAuth and operational release scope. Goal active.


Root ordering final verification: 1,706 ID tests pass with zero failures, 20,007 assertions and 100% line/function coverage (`/private/tmp/id-root-policy-full.log`, 307.19 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-root-policy-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-root-policy-build.log`, 17.426 seconds). `git diff --check` passes. Existing bound organisation/user/resource row locks and membership foreign keys cover the verified activation cases; explicit break-glass and unrelated-tenant writes remain available. Next substantive requirement: unify permission evaluation/explanations across issuance and access views. Preserve tenant SSO trust decisions, production user OAuth and remaining F0–F7 audit/lifecycle/operational proof. Goal remains active.


**Exact-pair access explanations.** Member and target access now attach `permission` to client/resource pairs using the same SQL fact projection and evaluator as native user-resource grant policy. Top-level scopes remain assigned scopes; approved resource scopes and source/revision evidence are separate. Denied pairs remain diagnosable and paginated. Each query uses one SQL statement for assignment and policy windows, under its existing tenant context; native issuance still validates its immutable grant context and original scope ceiling. This is authorization-code permission, not authentication, consent or refresh approval. Client-only/direct-administrator explanations, complete lifecycle evidence and the remaining F0–F7 requirements remain open. Public user OAuth stays closed.

Pair permission slice verification is in progress. Before/focused logs and scope limitations are in the execution report. Full F0–F7 goal remains active.


Exact-pair permission final gates: 1,709 ID tests pass with zero failures, 20,105 assertions and 100% line/function coverage (`/private/tmp/id-access-policy-full.log`, 360.73 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-access-policy-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-access-policy-build.log`, 20.978 seconds). `git diff --check` passes. Public descriptions and generated OpenAPI distinguish assigned scopes from approved pair scopes; denied assignments remain visible and paginated. The same SQL fact projection and evaluator feed native grant policy and both access queries, while native provenance/original-scope checks remain mandatory. No migration, RLS change, cache or policy service. First-principles review deleted duplicate native eligibility/compatibility checks. The first focused run exposed implementation/fixture errors; the corrected final focused run passes 17 tests/510 assertions/8.99 seconds. Existing native grant, lifecycle, audit and crash recovery gates pass in the full suite.

Next: client-only login and direct-administrator permission explanations, preserving actual authority calculations and command locks; then complete decision/audit convergence. Retain pending tenant SSO trust and identity retention choices, production user OAuth, lifecycle/capacity/operational requirements and full F0–F7 scope. Goal remains active.


**All member target explanations.** Every access target now includes permission: client-only login admission, exact-pair authorization-code permission or resource-only direct administration. `auth/member-permission.ts` shares facts and explicit calculations with native user-resource policy, administrative grant lookup and root writer detection. Existing authority locks remain in place. Top-level scopes remain assignments; approved scopes and contributing source/revision evidence are separate. The existing capability constraint enforces the bound admin resource, so no new binding registry/check is added. Diagnostics do not establish authentication, consent or refresh approval. Common decision/audit metadata, complete lifecycle evidence, tenant SSO trust, production OAuth and other F0–F7 gates remain unfinished.

Current slice: 64 focused plus four restricted/native/HTTP tests pass; full gates pending. See the execution report. Keep the full F0–F7 goal active.


Login/direct-administrator final gates: 1,712 ID tests pass with zero failures, 20,197 assertions and 100% line/function coverage (`/private/tmp/id-single-policy-full.log`, 289.79 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-single-policy-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-single-policy-build.log`, 18.829 seconds). `git diff --check` passes. Public docs and generated OpenAPI require a permission decision for every target, while retaining assigned scopes separately. Client-only admission is shared with native resource grants; direct administration is shared with `effectiveGrants` and `hasPlatformWriter`. Removed duplicate administrator SQL, the old resource-specific module, a redundant binding check already enforced by migration 0028, and superseded pair-only README/spec status text. No new schema, RLS rule, cache or policy service. Focused command-race checks and actual restricted-login shared-user A/B checks pass; the full suite preserves native grant, lifecycle, audit and crash recovery evidence. No production release or capacity guarantee follows from these local results.

Next substantive work: finish the typed decision/audit contract across member and machine consumers, preserving immutable tenant/client/resource/subject identity, grant-kind semantics, reasons, source revisions and historical audit versions. Current permission summaries are not a completed production issuance/audit contract. Retain pending tenant SSO trust/identity retention decisions, production user OAuth, lifecycle bounds and all remaining F0–F7 operational proof. Goal remains active.


**Machine decision/audit binding.** Machine permission evaluation now returns the approved identity/scope decision and source snapshot from one SQL statement. Issuance compares its generated JWT with that decision before committing success or releasing a token. Version 2 `oauth.token.issued` records retain the decision under `data.decision`; version 1 history remains unchanged. Omitted requested scopes are recorded as null, distinct from the approved intersection. Removed duplicate caller-supplied client scope ceilings, post-mint client/resource reads and repeated audit parameters. No schema migration or new policy service. This completes the machine success-path binding, not the common member/denial decision contract, production user OAuth or remaining F0–F7 release gates.

Machine success-path snapshot and five native fault cases are implemented; focused tests pass. Full gates pending. See execution report; retain the full F0–F7 goal.


Machine decision/audit final gates: 1,718 ID tests pass with zero failures, 20,236 assertions and 100% line/function coverage (`/private/tmp/id-decision-audit-full.log`, 345.51 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-decision-audit-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-decision-audit-build.log`, 17.292 seconds). `git diff --check` passes. OpenAPI was regenerated through the exporter; public docs explain payload versions, omitted scopes, snapshot fields and failure behaviour. All five native fault-injection cases (tenant, client instance, both versions and scope mutation) deny token release and roll back the transaction; corrected requests succeed. Legacy/current tenant history and restricted-login issuance pass. No schema migration, new service or cache is introduced.

Code evidence: [decision snapshot](/Users/anthonyriera/code/answerable/apps/id/src/auth/machine-capability.ts:7), [single-statement facts](/Users/anthonyriera/code/answerable/apps/id/src/db/queries/capabilities.ts:12), [signed-claim agreement and versioned audit](/Users/anthonyriera/code/answerable/apps/id/src/auth/machine-audit.ts:20), [native fault matrix](/Users/anthonyriera/code/answerable/apps/id/src/auth/machine-audit.integration.test.ts:302), and [retained history](/Users/anthonyriera/code/answerable/apps/id/src/auth/machine-audit.integration.test.ts:344).

Next substantive work: member decision identity metadata and denied/failed outcome evidence, converging at the actual native/admin consumers without copying untrusted request identity into audit authority. Preserve tenant resource visibility when enriching access explanations. The machine helper still throws on denied capability/scope checks; this slice does not claim a completed denial decision contract. Production user OAuth, tenant SSO trust/identity retention choices, complete lifecycle/capacity bounds and all remaining F0–F7 operational proofs remain open. Goal stays active.


**Authenticated machine rejection slice verified.** 1,723 ID tests pass with zero failures, 20,292 assertions and 100% line/function coverage (`/private/tmp/id-machine-denial-final-full.log`, 268.40 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-machine-denial-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-machine-denial-build.log`, 17.029 seconds). Rejection facts retain only provider-verified client/owner identity and safe reasons/stages. Audit outages preserve the original error and emit a safe signal; wrong credentials never produce tenant attribution. The first full run caught adapter hooks executing twice; rejection auditing now uses a plain database transaction, and all existing rollback assertions pass unchanged. Full denied-policy and pre-authentication evidence, member metadata, production user OAuth and all remaining F0–F7 gates remain open. See [execution evidence](/Users/anthonyriera/code/answerable/reports/answerable-id-foundation-execution-plan.md#authenticated-machine-rejection-evidence). Goal remains active.


**Member decision identity verified.** 1,723 ID tests pass with zero failures, 20,296 assertions and 100% line/function coverage (`/private/tmp/id-member-decision-full.log`, 275.61 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-member-decision-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-member-decision-build.log`, 17.156 seconds). The shared evaluator returns trusted identity/configuration snapshots and keeps public denials coarse. Duplicate scope projections were deleted; no new table or policy service. Native/restricted checks preserve stored tenant/request semantics. The attempted foreign-private assignment fixture was rejected by an existing constraint and removed; it was not a reproduced vulnerability. Existing member-removal access audit snapshots inherit approved metadata, while production user-token audit integration remains open. See [execution evidence](/Users/anthonyriera/code/answerable/reports/answerable-id-foundation-execution-plan.md#member-decision-identity-and-public-denial-projection). All remaining F0–F7 requirements and pending tenant-trust/retention decisions remain active.


**Incoming request boundaries verified.** 1,730 ID tests pass with zero failures, 20,339 assertions and 100% line/function coverage (`/private/tmp/id-request-boundaries-final-full.log`, 299.33 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-request-boundaries-{typecheck,lint,web}.log`). Build passes for both packages, neither cached (`/private/tmp/id-request-boundaries-build.log`, 19.583 seconds). Bodies are limited to 256 KiB and five seconds of application acquisition before CORS, provider or command work. Real HTTP/TCP and in-process checks cover size, streaming, false lengths, deadline delivery, cancellation failure/stall and preflight. Unusable correlation IDs become consistent UUIDv7; failed SSO callbacks leave unverified provider/IP attribution null. The native limit alone missed streamed input; a transform abort did not release a stalled wait; the first TCP probe waited for EOF despite a complete response. Those failures and the corrected proof are retained in the [execution report](/Users/anthonyriera/code/answerable/reports/answerable-id-foundation-execution-plan.md#incoming-request-size-acquisition-deadline-and-attribution). No claim of cluster rate/concurrency limits, verified session IPs, database execution deadlines or production readiness. All remaining F0–F7 requirements and pending trust/retention decisions remain active.
# Upstream custody research checkpoint

**Storage seam proof verified:** 1,732 ID tests pass, zero fail, 20,414 assertions, 100% line/function coverage (`/private/tmp/id-account-storage-seam-full.log`, 285.95 seconds). Typecheck, lint and 62 web tests pass; build passes both packages with one cached (`/private/tmp/id-account-storage-seam-build.log`, 264 milliseconds). Focused adapter/native SSO tests pass 37/37 with 246 assertions. Supported field transforms preserve all three token fields through actual adapter operations and restricted native first/repeat SSO; rollback, null import and corrupt-ciphertext refusal are tested. A new adapter wrapper is unnecessary for the proven paths. Production key configuration, versioned storage, repeat-safe conversion and restore remain next; no production encryption claim. Goal active. See reports/answerable-id-upstream-token-custody.md and the current execution report.

Completed a read-only installed-provider probe (eight checks) and code-backed implementation contract in reports/answerable-id-upstream-token-custody.md. Corrected the schema/foundation claim that encryptOAuthTokens alone protects all upstream tokens. The actual flag leaves ID tokens unchanged and includes a legacy plaintext compatibility path. Runtime, migrations, production keys and database rows were untouched. Prior 1,730-test repository results remain the last implementation gate, not a fresh run for this documentation/probe change. F0–F7 and the execution goal remain active; implement and prove the storage boundary next.


**Upstream cutover simplification:** migration 0037 replaces unused-token conversion with one-time audited retirement. Focused tests pass 46/46 with 330 assertions, including actual SIGKILL/runner recovery and native SSO/session preservation. No production data changed. Full gates pending; external-consumer, representative-volume, backup/restore and key-custody gates remain. See the custody and execution reports. The full F0–F7 goal remains active.


Upstream cutover full rerun passes 1,745 tests, zero failures, 20,576 assertions and 100% line/function coverage (`/private/tmp/id-upstream-cutover-final-full.log`, 305.96 seconds). Typecheck, lint and 62 web tests pass. Initial full run had one existing runtime health assertion failure (400 after rejected uploads); three correctly timed isolated repeats and the unchanged full rerun passed. The cause remains unexplained, recorded in the custody report. Build passes both packages, neither cached (`/private/tmp/id-upstream-cutover-build.log`, 18.925 seconds). `git diff --check` passes. No production data or keys changed. Remaining custody, trust/retention and F0–F7 gates stay active.


**Transport investigation progress:** a durable local-only probe reproduces the earlier health 400 without DB/auth/timeouts. Across 500 sequences, pooled Bun produced 63 failures; explicit close 72; higher native cap plus close 311; fresh Bun and independent Node clients each zero. Failed health requests did not reach the application handler. Close/raised-cap experiments were reverted; no runtime fix is claimed. See reports/answerable-id-request-reuse.md and its recorded JSON. Next: exact wire/socket evidence and a supported remedy. Full foundation remains active.


**Rejected-upload framing proved:** independent Node parser records 31/500 Bun 1.3.1 sequences with HPE_INVALID_CHUNK_SIZE and GET at byte zero while awaiting a chunk size. The corrected fixture keeps only the rejected streamed upload out of the client pool; 500 pooled subsequent health requests pass, and the existing runtime test passes five repetitions (25 cases/140 assertions). No production limit, middleware, retry or runtime-version change. Full gates pending. Prefer deleting the invalid client-reuse assumption over adding server workarounds. Next custody work remains actual backup/restore; all remaining F0–F7 gates stay active. See reports/answerable-id-request-reuse.md.


**Framing correction final gates:** 1,745 ID tests pass, zero failures, 20,575 assertions and 100% line/function coverage (`/private/tmp/id-body-framing-full.log`, 307.76 seconds). Typecheck, lint and all 62 web tests pass (`/private/tmp/id-body-framing-{typecheck,lint,web}.log`). Build passes both packages, one cached (`/private/tmp/id-body-framing-build.log`, 279 milliseconds). `git diff --check` passes. The change is limited to the rejected-upload test client's pooling and reproducible diagnostic evidence; production runtime behaviour is unchanged. No deployment or database/key provisioning occurred. Next: actual populated backup/restore with separately retained keys, plus the remaining F0–F7 requirements. Goal remains active.


**Populated custody restore proof:** new guarded test:restore uses existing Compose PostgreSQL pg_dump/pg_restore, closes DB pools, deletes the fixture identity, restores its full snapshot, and validates mixed-version encrypted values through the restricted production adapter. Missing either key refuses reads; both keys recover expected tokens. Old cookie and fresh native SSO preserve identity. Exact migration receipts survive and runner replay preserves post-cutover tokens. Compose/app fixture UUID check prevents targeting a different similarly named DB. CI runs after coverage. Two local runs, typecheck and lint pass; full gates pending. Same-cluster proof only: role reconstruction, production backup systems, post-snapshot revocations/receipts and capacity remain open. No new production backup framework.


**Restore rehearsal final gates:** `test:restore` passes before the full suite and again in CI order after it (`/private/tmp/id-upstream-restore-final.log`, `/private/tmp/id-upstream-restore-ci-order.log`). All 1,745 ID tests pass, zero failures, 20,576 assertions and 100% line/function coverage (`/private/tmp/id-upstream-restore-full.log`, 314.46 seconds). Typecheck, lint and 62 web tests pass (`/private/tmp/id-upstream-restore-final-typecheck.log`, `/private/tmp/id-upstream-restore-lint.log`, `/private/tmp/id-upstream-restore-web.log`). Build passes both packages, one cached (`/private/tmp/id-upstream-restore-build.log`, 261 milliseconds). `git diff --check` passes. This is local disposable-database evidence; no production data or key provisioning changed. F0–F7 remain incomplete. Next: explicit restored operation-reservation/replay and revocation evidence; production role/key reconstruction and remaining tenant trust/retention/release work remain open.


**Restored administrative and tenant effects:** expanded the existing guarded test:restore proof with real HTTP client creation/rotation and tenant-B member removal before backup. Exact operation/results/audit/subjects/identifier/client/member/context snapshots survive restoration. Retained replay keys under a new active key recover identical secrets; absent keys return 503, changed input 409, bad authentication 401, and purged response 410 while the permanent reservation remains. A later A context stays active through rotation replay. Native SSO denies revoked B without a session while A still works. Rehearsal/typecheck/lint pass; full gates pending. Fixture typo state/status was corrected without runtime changes. No new schema or recovery service. Post-snapshot/replacement-host/production gates remain.


**Restored command final gates:** extended `test:restore` passes both before and after the full suite (`/private/tmp/id-restored-commands-final.log`, `/private/tmp/id-restored-commands-ci-order.log`). All 1,745 ID tests pass, zero failures, 20,576 assertions and 100% line/function coverage (`/private/tmp/id-restored-commands-full.log`, 343.78 seconds). Typecheck, lint and 62 web tests pass (`/private/tmp/id-restored-commands-final-typecheck.log`, `/private/tmp/id-restored-commands-lint.log`, `/private/tmp/id-restored-commands-web.log`). Build passes both packages, one cached (`/private/tmp/id-restored-commands-build.log`, 206 milliseconds). `git diff --check` passes. No production schema, command behaviour, data or keys changed. F0–F7 remain incomplete. Next: replacement-cluster restore/role reconstruction and explicit post-snapshot recovery requirements, while preserving all remaining tenant-trust, retention, OAuth and operational gates.


**Fresh-cluster restore:** replaced the in-place rehearsal with an ephemeral PostgreSQL target using the exact source image, tmpfs data and random loopback port. System identifiers differ; source runtime role is absent, recreated through production provisioning, and its old password is refused (28P01) before new credentials work. All custody, replay, audit and A/B/native SSO assertions pass against target. First failure was an assertion against a Drizzle wrapper message, corrected to the PG error code. Both failed/successful runs removed the target container. No new runtime service or schema, no second restore mode. Full gates pending; remote deployment/other key families/post-snapshot reconciliation remain open.


**Fresh-cluster final gates:** the corrected rehearsal and final CI-order rehearsal pass (`/private/tmp/id-replacement-restore-second.log`, `/private/tmp/id-replacement-restore-ci-order.log`). All 1,745 ID tests pass, zero failures, 20,575 assertions and 100% line/function coverage (`/private/tmp/id-replacement-restore-full.log`, 325.53 seconds). Typecheck, lint and 62 web tests pass (`/private/tmp/id-replacement-restore-final-typecheck.log`, `/private/tmp/id-replacement-restore-lint.log`, `/private/tmp/id-replacement-restore-web.log`). Build passes both packages, one cached (`/private/tmp/id-replacement-restore-build.log`, 233 milliseconds). `git diff --check` passes. Docker inspection confirms no rehearsal container remained after the final run. No production schema, runtime behaviour, database or deployment keys changed; F0–F7 remain incomplete. Next: remaining boot/key-family recovery and post-snapshot requirements, plus unfinished tenant authentication, public user OAuth, audit/lifecycle and operational gates. Keep the full goal active.


**Restored runtime slice:** actual production-mode startup/HTTP proof and unified startup failure cleanup are implemented. Eight focused runtime tests and the corrected restore rehearsal pass. Full coverage is confirmed live, so no overlapping database work or build was started. Typecheck/lint and 62 web tests pass. See the custody report section on restored production startup for regression details, limitations and first-principles review. The preceding planning turn made progress by correcting the release decision plan against current code. F0–F7 remain active.


**Startup and retention recovery verification:** all 1,748 ID tests pass, zero failures, 20,584 assertions and 100% line/function coverage (`/private/tmp/id-restored-boot-full.log`, 317.98 seconds). Typecheck, lint and 62 web tests pass; build passes both packages, one cached (`/private/tmp/id-restored-boot-build.log`, 252 milliseconds). The later retention addition changes only the standalone restore verifier: its final complete rehearsal, typecheck and lint pass (`/private/tmp/id-restored-retention-final.log`, `/private/tmp/id-restored-retention-final-typecheck.log`, `/private/tmp/id-restored-retention-final-lint.log`). The restore script is a separate gate and is not counted as Bun unit tests. First-principles review retained the existing startup catch, retention SQL and role provisioner, and removed duplicate snapshot query expressions. No production deployment/data/keys or new schema. Next: signing-key restore and post-snapshot recovery contracts, alongside unfinished tenant admission, public user OAuth, complete audit/lifecycle evidence and operational bounds. F0–F7 remain active.


**Signing restore investigation:** added a real reachable machine-token fixture and independent JOSE verification to the restore rehearsal. Initial fixture cleanup was blocked by a capability FK and masked the underlying assertion; cleanup now removes only fixture-tenant capabilities before clients. The underlying startup snapshot mismatch was a legitimate admin-resource revision increment from linking the fixture; replaced that with a separate tenant-owned resource. A mistyped tenant_private enum was rejected by the existing ownership constraint and corrected to tenant_owned. Missing signing-decryption custody returns the existing safe 500 authentication_unavailable contract, not the guessed 503 used only for contention/audit storage; corrected the test expectation after reading production error handling. No production contract or key implementation changed. Final signing/cleanup proof remains pending.


**Signing restore progress:** corrected rehearsal passes (`/private/tmp/id-restored-signing-fifth.log`); wrong BETTER_AUTH_SECRET fails closed, exact JWKS survives, old/new tokens verify independently with original public key, new issuance is audited. No production behaviour changed. Initial cleanup leftovers were removed by exact synthetic IDs/role; dedicated resource avoids unrelated platform revisions. Full repository gates are running serially with database work. First-principles review retained provider signing and existing restore command; no custom key manager. All F0–F7 requirements remain active.


**Signing recovery final gates:** corrected and CI-order restore rehearsals pass (`/private/tmp/id-restored-signing-fifth.log`, `/private/tmp/id-restored-signing-ci-order.log`). All 1,748 ID tests pass, zero failures, 20,584 assertions and 100% line/function coverage (`/private/tmp/id-restored-signing-full.log`, 322.98 seconds). Typecheck, lint and 62 web tests pass (`/private/tmp/id-restored-signing-verified-typecheck.log`, `/private/tmp/id-restored-signing-verified-lint.log`, `/private/tmp/id-restored-signing-web.log`). Build passes both packages, one cached (`/private/tmp/id-restored-signing-build.log`, 258 milliseconds). `git diff --check` passes; Docker inspection shows no remaining rehearsal container. This slice changes the restore verifier and fixture cleanup, not production signing, schema, deployment data or keys. First-principles review retains the provider signer and one restore command, with a dedicated tenant resource and shared old/new verification. F0–F7 remain incomplete. Next: managed signing/application-secret rotation and production delivery, post-snapshot reconciliation, unfinished tenant authentication/user OAuth, audit/lifecycle and operational bounds.


**Application secret rotation:** preceding signing-recovery turn was progress. Added strict, secret-safe native BETTER_AUTH_SECRETS parsing and passed it through production auth. Environment regression fails before/passes after; native integration proves staged cookie continuity, promoted-cookie refusal, retained legacy/new signing custody and missing-version failure. No custom crypto, cookie fallback or new schema. Promotion must be coordinated with fresh sign-in; singular legacy secret remains required. Focused 13 tests/112 assertions pass (whole-suite coverage exit 1), typecheck passes. Full gates pending. Complete F0–F7 scope remains active.


**Rotation fixture isolation correction:** the first full run exposed encrypted signing-key state leaking from the new rotation fixture into default-secret provider tests (`/private/tmp/id-auth-rotation-full.log`). The identified runner was terminated with SIGTERM (143); this run is failed evidence, not a complete test result. The fixture now removes the JWKS state it owns before closing. Focused rotation/environment/machine-provider checks pass 22 tests/184 assertions (`/private/tmp/id-auth-rotation-isolation.log`, whole-suite coverage exit 1). Production key handling was not relaxed. The complete corrected suite is running.


**Application-secret rotation verification:** 1,750 ID tests pass, zero failures, 20,648 assertions and 100% line/function coverage (`/private/tmp/id-auth-rotation-verified-full.log`, 319.42 seconds). After that full run, the same integration test gained fresh native SSO/old-replica cookie assertions; all 22 focused rotation/environment/machine-provider tests pass with 189 assertions (`/private/tmp/id-auth-rotation-final-focused.log`, 2.53 seconds; exit 1 is the whole-suite coverage threshold). No production source changed after the full run. Final typecheck/lint and 62 web tests pass (`/private/tmp/id-auth-rotation-verified-typecheck.log`, `/private/tmp/id-auth-rotation-verified-lint.log`, `/private/tmp/id-auth-rotation-web.log`). Build passes both packages, one cached (`/private/tmp/id-auth-rotation-build.log`, 219 milliseconds). Complete restore rehearsal passes (`/private/tmp/id-auth-rotation-restore.log`); Docker inspection confirms cleanup. `git diff --check` passes. Optional native ring configuration adds no custom crypto, schema or public route. Legacy secret retirement, production delivery, managed signing rotation, tenant admission, audit/lifecycle/capacity and remaining F0–F7 gates remain open.


**Statement deadline slice:** prior goal turn was progress. Reproduced absent server-side timeout, implemented validated 10-second default using pg connection startup settings, reused existing 503 mappings for 57014. Restricted-login cancellation, command rollback/same-key replay and machine policy timeout/recovery pass alongside env/problem tests: 31 cases/179 assertions. Corrected fixture scope and unique-key mistakes; root MDX formatter invocation corrected to web cwd. Typecheck/lint/62 web tests pass; full suite running. No timer race, workflow or schema. Total-operation bounds, tenant admission, capacity and all remaining F0–F7 scope remain active.


**Statement-deadline final gates:** 1,755 ID tests pass, zero failures, 20,684 assertions and 100% line/function coverage (`/private/tmp/id-statement-full.log`, 314.04 seconds). Typecheck, lint and 62 web tests pass (`/private/tmp/id-statement-final-typecheck.log`, `/private/tmp/id-statement-lint.log`, `/private/tmp/id-statement-web.log`). Build passes both packages, neither cached (`/private/tmp/id-statement-build.log`, 18.703 seconds). Complete restore rehearsal passes (`/private/tmp/id-statement-restore.log`); Docker inspection confirms no leftover target. OpenAPI was generated through the repository exporter. `git diff --check` and local report-link checks pass. First-principles review retained PostgreSQL cancellation and existing retry handlers; no app timer, worker or schema was added. These are per-statement limits, not total-transaction/HTTP limits or tenant admission/capacity guarantees. F0–F7 remain incomplete.


**Admission slice:** previous goal turn was progress. Implemented one per-app active-handler counter with no queue, configurable default 64, pre-auth/body refusal, bodyless liveness exemption and finally release after handler completion. Real HTTP abort keeps its slot occupied until work finishes. Corrected raw-response correlation loss; focused body test needed standard 15-second test timeout. Five final admission/contract cases and 43 broader focused checks pass. OpenAPI preserves existing JSON 503 responses alongside text overload. First-principles review removed an arbitrary upper config cap; no queue/timer/library was introduced. Full gates running; tenant fairness/cross-replica limits, capacity metrics, pending product contracts and all F0–F7 work remain active.


**Admission final gates:** 1,761 ID tests pass, zero failures, 20,773 assertions and 100% line/function coverage (`/private/tmp/id-admission-full.log`, 341.04 seconds). Typecheck, lint and 62 web tests pass (`/private/tmp/id-admission-verified-typecheck.log`, `/private/tmp/id-admission-lint.log`, `/private/tmp/id-admission-web.log`). Build passes both packages, neither cached (`/private/tmp/id-admission-build.log`, 20.932 seconds). Complete restore rehearsal passes (`/private/tmp/id-admission-restore.log`); Docker inspection confirms cleanup. OpenAPI was regenerated; `git diff --check` passes. First-principles review retained one counter/finally boundary, removed an arbitrary config ceiling and preserved existing JSON error contracts. No queue, timer, library or schema was added. This is instance admission only; tenant fairness, shared replica limits, metrics/capacity, total operation bounds and remaining F0–F7 requirements stay open.


### Shared machine issuance admission — verification in progress

The previous planning turn changed the release report; this execution adds a concrete F6 control. The valid pre-change regression admitted a third signing transaction for one owner (200 instead of 503). A small PostgreSQL transaction-lock helper now admits two, using the locked verified owner and no requested tenant identity. Independent restricted pools prove same-owner client aggregation, forged-tenant refusal, B progress and release on commit/rollback. Focused admission/race tests pass 17/17; typecheck/lint and web tests pass. Full ID coverage and subsequent build/restore are pending. Initial fixture scope mistakes and obsolete metadata fields were corrected; the timed-out initial test process was explicitly interrupted before restarting. First-principles review retained the existing transaction and fixed slots, adding no queue, lease, schema or configuration machinery. Overall tenant fairness, initial authentication/pool capacity, production admission and remaining F0–F7 work are still open.


Shared machine admission verification complete: 1,763 ID tests / 20,796 assertions / 100% line and function coverage; typecheck, lint, 62 web tests, both uncached builds and the full fresh-cluster restore all pass. Evidence: `/private/tmp/id-tenant-admission-{full,typecheck,lint,web,build,restore}.log`. OpenAPI regenerated and diff whitespace checked. Removed the exact inactive interrupted-test role `id_test_capacity_334fcb70cdfb4a7abfc301b04cae384a`; no restore container remains. No production changes. Fixed two-slot issuance admission is implemented; overall fairness, pre-authentication capacity, production user admission and all remaining F0–F7 requirements remain active.


### Unverified IP attribution — verification in progress

Previous goal turn made verified implementation progress on shared machine admission. Current source review found direct forwarding-header trust in administrative actor/root audit and native session propagation into sign-in/sign-out evidence. Removed those readers, configured the supported empty IP-header list and normalised new session IP to null; legacy values are not copied to new audit. Kept provider IP tracking enabled because the pinned native limiter explicitly bypasses checks when tracking is disabled. Focused regressions pass 188 tests/965 assertions, including restricted-role SSO and actual native rate limiting with isolated storage. Typecheck/lint/web gates pass; full suite and build/restore are pending. Initial assertion failures (missing property versus undefined) and a test options typing issue were corrected. First-principles review adds no proxy parser, config toggle, schema or service. Trusted ingress and distributed abuse limits remain unfinished.


IP attribution verification complete: full ID suite 1,764 pass / zero fail / 20,820 assertions / 100% line and function coverage (313.94s). Typecheck, lint, 62 web tests, both uncached builds (19.616s) and fresh-cluster restore pass. Logs: `/private/tmp/id-ip-boundary-{full,typecheck,lint,web,build,restore}.log`. Generated API schema descriptions and operator/public guidance are updated. Diff whitespace passes; restore target cleanup confirmed. No historical rewrite, migration or production change. This fixes false attribution; verified ingress and deployment-wide rate controls remain required alongside the rest of F0–F7.


### Bounded authentication audit input — verification in progress

Previous goal turn made verified progress by removing false IP attribution. Current callback audit regression reproduced retained arbitrary error descriptions/reasons and oversized metadata (four failures). Version 2 now removes free-form descriptions and uses recognised categories or sso_callback_failed. Local federation rejection codes have one typed vocabulary. A single 512-character printable-ASCII validator protects HTTP/native-session user-agent metadata and rechecks legacy values on sign-out. Focused 58 tests/325 assertions pass; final version assertions run in the full suite. Boundary probe adds trailing newline/carriage-return checks and passes. Typecheck/lint/web pass; full ID/build/restore pending. First-principles review deletes fields instead of adding a redaction engine, and corrects the schema guide's stale tenant-erasure statement. No schema, dependency or production changes; remaining F0–F7 work stays active.


Bounded audit input verified: full rerun passes 1,769 tests / 20,874 assertions / 100% line and function coverage (316.03s), plus typecheck, lint, 62 web tests, both uncached builds (19.415s) and complete fresh-cluster restore. Logs: `/private/tmp/id-audit-input-verified-full.log` and `/private/tmp/id-audit-input-{typecheck,lint,web,build,restore}.log`. First full run's one stale application assertion is retained in `id-audit-input-full.log`; the corrected assertion requires version 2 and absence of free-form data. Documentation/OpenAPI updated, diff whitespace passes and restore cleanup confirmed. No migration or production change. This bounds selected stored audit metadata; total transport/rate limits, complete outcome evidence, production admission and remaining F0–F7 work stay active.


### Last platform membership writer — verification in progress

Previous goal turn made verified audit-input progress. Current source review found that the existing last-writer capability guard did not cover membership removal/window changes. New restricted HTTP regressions reproduce loss of the only writer and two removals both returning 204. The membership context now checks before/after using persisted platform/resource identity and the existing evaluator under the already-held organisation lock. Focused member tests pass 81 cases; expanded restricted proof passes six cases/197 assertions covering rollback, same-key recovery/replay, past/future windows, concurrent removals, unrelated tenant progress and writerless repair. Typecheck/lint/web pass; full ID/build/restore pending. First-principles review introduces no schema, global tenant lock, generic invariant framework or duplicate policy. Remaining global-user/group/entitlement writers, freshness and other F0–F7 work stay open.


Last-platform-membership guard verified: 1,775 ID tests / zero failures / 21,070 assertions / 100% line and function coverage (318.63s), plus typecheck, lint, 62 web tests, both uncached builds (17.763s) and complete fresh-cluster restore. Logs: `/private/tmp/id-last-member-{full,typecheck,lint,web,build,restore}.log`. OpenAPI and documentation updated; diff whitespace and restore-container cleanup verified. No schema or production change. The membership gap is closed under the existing transaction/lock; remaining global-user/group/entitlement writers and the rest of F0–F7 stay active.


### Global-user last-platform-writer protection — verification in progress

Five corrected regressions reproduce the missing global disable/erase guard, including concurrent loss and late membership during a user lock wait. Initial fixture omitted application IDs and was corrected. Reuse existing user lock, persisted platform binding and writer evaluator with excluded-target permission; NOWAIT platform lock avoids inverse-order waiting, existing503 handles retry. Unrelated users avoid the platform lock. Focused90tests/941assertions pass; initial NOWAIT boolean type corrected to literaltrue. Full suite and final gates pending. No schema/general guard framework. Other authority writers and remaining F0–F7 work stay active.


Global-user last-writer guard verified: 1,781 ID tests / zero failures / 21,274 assertions / 100% line and function coverage (317.45s). Final Drizzle lock config additionally passes 31 focused cases/459 assertions (14.39s), typecheck and lint. Web62/204, both uncached builds17.03s and complete fresh-cluster restore pass. Logs: `/private/tmp/id-last-user-full.log`, `id-last-user-final-focused.log`, `id-last-user-typecheck-final.log`, `id-last-user-final-lint.log`, `id-last-user-web.log`, `id-last-user-build-verified.log`, `id-last-user-restore.log`. Initial sandboxed build failed because Turbopack compiler port binding was denied; permitted rerun passes with no code workaround. Documentation/OpenAPI updated; diff whitespace and restore-container cleanup verified. No schema or production change. Other authority-changing writers and remaining F0–F7 work stay active; product tenant-trust and named-erasure-retention choices remain unresolved.


### Group/assignment/entitlement last-writer guard — verification in progress

Previous goal turn made verified global-user progress. Current corrected regressions reproduce ten unsafe mutations and mixed concurrent group/entitlement loss (eleven failures). Initial fixture transfer violated immutable group assignment identity; recreated rows now use new IDs. Shared platform-writer helper extracts the membership check and protects existing org-locked group/assignment/entitlement mutations. No schema/lock service/framework; other tenants skip evaluation and writerless state can recover. Corrected focused32tests/1039assertions pass. Added explicit unrelated group progress and writerless group-enable cases for full suite. Full gates/restore pending. Remaining resource/organisation writers, product decisions and F0–F7 remain active.


Group/assignment/entitlement writer protection verified: full ID suite1,794pass / zero failures /21,744assertions /100%line+function coverage (322.29s); typecheck, lint, web62/204, both uncached builds18.817s and complete fresh-cluster restore pass. Logs: `/private/tmp/id-last-policy-{full,typecheck-final,lint,web,build,restore}.log`. OpenAPI/operator/public/schema/foundation docs and mutation inventory updated; diff whitespace and temporary-container cleanup confirmed. First-principles review shares the old membership check rather than adding schema/framework/duplicated policy. Initial fixtures incorrectly attempted identity transfer; correctedbefore11fails, correctedfocused32pass/1039assertions, fullincludes unrelated-tenant progress and writerless recovery. Remaining resource/organisation writers, trust/retention decisions and F0–F7 remain active.


### Platform organisation/resource authority — verification in progress

Previous goal turn made verified group/assignment/entitlement progress. Three HTTP regressions reproduce org disable, admin scope narrowing and configured-name resource protection gaps. Shared org-first writer check now protects org disable and bound scope writes; resource disable/erase use persisted UUID and remove environment parameters/callers. Delete obsolete name-only fixture, keep stronger restrictedHTTP proof. Focused53tests/1217assertions pass; deterministic lock ordering, unrelated-target progress and disabled-platform recovery3tests/94assertions pass. Full suite/final gates/restore pending. No schema/framework/newpolicy; expiry, freshness, audit completeness and remaining F0–F7 remain active.


Platform organisation/resource authority verified: full ID1,800pass /zero failures /21,947assertions /100%line+function coverage (326.91s); typecheck, lint, web62/204, both uncached builds17.077s and complete fresh-cluster restore pass. Logs `/private/tmp/id-last-system-{full,typecheck-final,lint,web,build,restore}.log`. OpenAPI and public/operator/schema/foundation docs updated; whitespace and temporary-container cleanup verified. Three initial runtime regressions are retained; focused53/1217 and extra lock/recovery3/94 pass. Removed obsolete environment plumbing/name-only fixture rather than introducing new policy or schema. Org-first bound-resource lock ordering is directly observed. Current writer protection does not prevent time expiry, prove IdP availability/freshness or complete audit/admission and other F0–F7 release work. Goal remains active.


### Group erasure effects and retained users — verification in progress

Previous goal turn made verified organisation/resource progress. Current source/regressions expose unrecorded group cascades and missing affected-user history. Existing deletion query now returns explicit deleted assignments/entitlements under parent locks; group.erasedv2 stores allowlisted-field manifests and migration0038 extends the existing subject function for exact tenant/group/user references. No newtable/trigger/workflow; no invented version1backfill. Initial rollback snapshot incorrectly counted root-access facts outside the transaction; corrected to domain events/subjects. Correctedfocused21/272 passes, includingA/Bshareduser preservation, history afterusererase, malformedcontractrefusal, replay and failure rollback/retry. Disposable schema migrated. Full gates/restore pending. Effect manifests are sources, not permissionunion/remote revocation; largeworkload bounds and other F0–F7 remainactive.


Group-erasure evidence verified: migrated disposable schema, full ID1,803pass /zero failures /22,052assertions /100%line+function coverage (355.89s), typecheck, lint, web62/204, both uncached builds17.504s and complete fresh-cluster restore pass. Logs `/private/tmp/id-group-evidence-{migrate,full,typecheck-final,lint,web,build,restore}.log`. Documentation/OpenAPI updated; whitespace and temporary-container cleanup confirmed. Migration0038 must precede version2writers; no version1effect backfill is invented. Initial domain snapshot errors included outside-transaction root access facts; correctedfocused21/272 passes. First-principles review uses actual deleted rows and existing subject function, no newtable/trigger/workflow. Other lifecycle evidence, measured largeoperationbounds, product decisions and allremaining F0–F7 obligations stayactive.


### Assignment history versus global user erasure — verification in progress

Previous goal turn made verified group-erasure progress. New source investigation suspected group_member audit lookup could lose identity during concurrent user erasure. Both observed restrictedHTTP commit orders disprove that gap: assignment row lock holds the parent cascade until audit/receipt commit; reverseorder returns404withoutremoval. Kept production implementation/schema unchanged, added meaningful concurrency/regression proof. Focused5tests/174assertions pass. Full gates/restore pending. Other group/entitlement/global-lifecycle audit manifests and F0–F7 stayopen; no newproduct decision assumed.


Assignment/user-erasure ordering verified: full ID1,805pass /zero failures /22,122assertions /100%line+function coverage (352.19s); finalfocused5/174(5.22s), typecheck, lint, web62/204, both uncached builds18.737s and complete fresh-cluster restore pass. Logs `/private/tmp/id-assignment-history-{full,final-focused,typecheck-final,final-lint,web,build,restore}.log`. Initial test-only Hono return type was too narrow; actual returntype plus simpler allSettled cleanup fixes it. Evidence/docs updated; whitespace and temporary-container cleanup confirmed. First-principles review disproved suspectedrace and kept production code/schema unchanged. Nextsourcefinding: direct member entitlement events lack an affected-user subject contract; reproduce before implementation. Full F0–F7 scope remainsactive.


### Direct entitlement history — verification in progress

Previous planning turn changed the release hand-off but did not execute code. Current restricted-role regression reproduces eight missing affected-user history entries. Migration0039 extends existing version1subject capture; no application changes/eventversion/table/trigger. Focused5tests pass with observed both erasure orders, malformedcontract refusal, shared-userA/Bpreservation, history after erasure, subject-failure rollback and samekeyreplay. First-principles review rejected redundant application lookup/version2payload because existing child locks protect transactional identity capture. Fullgates/restore pending; broader lifecycle/productionadmission/F0–F7 stayactive.


Direct entitlement history verified: full ID1,810pass /zero failures /22,325assertions /100%line+function coverage (345.02s); focused5/202(4.74s), typecheck, lint, web62/204, both uncached builds18.241s and complete fresh-cluster restore pass. Logs `/private/tmp/id-ent-audit-{before,migrate,final-focused,full,typecheck-final,lint,web,build,restore}.log`. Migration0039 only extends existing insertion-time subject capture; no service/payload/table changes or guessed backfill. Initial wrong-directory MDX formatting failed; corrected fromapps/web. Documentation and mutationinventory updated. Firstprinciples review deleted planned extra eventversion/applicationlookup. Broader group/orgimpact, effective-accessmanifests, productionadmission, operationalproof and allremaining F0–F7 requirements remainactive.


### Group status policy evidence — verification in progress

Previous turn completed the direct-entitlement subject correction with allgates. New restrictedHTTP regression reproduces groupstatus events missing policy sources and affecteduserhistory. Implemented version2enable/disable snapshots through issuedcommandquery under parent+childsharelocks; reuse existing deletion fieldprojections and subjectfunction via migration0040. Noops stayv1/skipsnapshot. Focused15/577 and expandedHTTP10/362 pass, including exactsources, shareduserA/B, both observed erasureorders, subjectfailure rollback/retry and versioncontractrefusal. Fullgates/restore pending. Sourceevidence is not effectiveaccessdelta or remoterevocation; measuredgroupbounds, entitlementfanout and remainingF0–F7 stayactive.


Group status evidence verified: full ID1,815pass /zero failures /22,524assertions /100%line+function coverage (352.28s); expandedHTTP10/362(9.63s), policy/query/service15/577, finaltypecheck/lint, web62/204, bothuncachedbuilds22.085s and completefreshclusterrestore pass. API-description change follows fullsuite; generatedsnapshots2/13pass afterward. Logs `/private/tmp/id-group-status-{before,migrate,focused,races,full,openapi-export,openapi-test,typecheck-final,lint-final,web,build,restore}.log`. Docs/inventories updated, diffwhitespace and temporarycontainercleanup verified. Migration0040 onlytest/restoredDB. Firstprinciples review reuses projections/function/context and retains childlocks justified by observedraceproof; no newtable/framework or noopsnapshot. Nextsourcefinding: group/organisation entitlement commands lack historical audience capture. FullF0–F7 remainsactive.


### Broad entitlement audience — verification in progress

Previous turn completed groupstatus evidence with allgates. Current restrictedHTTP reproduces both broadprincipal historygaps. Version2changed events now retain member/optionalgroupassignment sources via issuedcontext+same-tenant queries+sharelocks. Migration0041 indexes explicit matchingaudiences; no backfill. Direct/noop contracts remainv1, broadnoops skipsnapshot. Expanded14tests/616assertions and focused8/543 pass, including sixobservederasureorders, both broadrollback/recovery cases, noops/replay, rawcontractrefusal and post-erasurehistory. Simplifiedhelperderives identity fromcontext+typedstate and removes redundantargs/nonnullassertion. Initialdbgeneratewrongcwd failed withoutgenerating; correctedapps/id. Fullgates/restore pending. Audience is noteffectivepermissiondelta; measuredbounds and remainingF0–F7 stayactive.


First audience fullsuite:1,822pass/2fail. Both failures are old management personalhistory predicates permitting only direct targets; new required tenant entitlementfact is correctlyreturned. Corrected assertions require exact own-tenant entitlementID/schema/action/audience and still exclude outsidergrant. Focused/fullrerun pending; failurelog retained `/private/tmp/id-ent-audience-full.log`.


Broad entitlement audience verified: correctedfull ID1,824pass /zero failures /22,961assertions /100%line+function coverage (376.33s); correctedfocused19/958(17.35s), expandedHTTP14/616, finaltypecheck/lint, web62/204, bothuncachedbuilds18.842s and completefreshclusterrestore pass. Logs `/private/tmp/id-ent-audience-{before,migrate,focused,expanded,full,corrected-focused,corrected-full,final-typecheck,final-lint,web,build,restore}.log`. Firstfull2fail were outdated direct-target-only personalhistory assertions, corrected to exacttenant-entitlement/audience forhuman+machine. Typecheckthenrequired explicit IDarraytype inthatassertion; finalpasses. Migration0041 onlytest/restoredDB; OpenAPI/docs/inventoriesupdated, diffwhitespace/containercleanupverified. Firstprinciples review removes redundant auditidentityargs/nonnullassertion; no newstore/framework/per-usereventstream. Next measure audience/lifecyclebounds; effectivepermissiondeltas, productiontenantadmission and allremainingF0–F7 stayactive.


### Audit workload measurement — verification in progress

Previous turn completed broadaudience evidence and allgates. Added guarded standalone measurement with actualHTTP/restrictedrole, synthetic100/1k/10kmembers, Bprobes, RSS/timer/pool observations and exactaudience/idempotent-recovery assertions. Three10k/eight-A-command runs each refuseB503; latercodes database_busy, observed4busyconnections+5queuedcheckouts. AllAoriginalkeyretries succeedexactlyonce, noaudiencetruncation. Root-authorisedtargetworkload/oneinstance/fourpool, notpublictenantattack orproductioncapacity. Initialkeyargtype narrowUUIDinferencefixedstring. No runtimepolicy/timeouts/capschanged; report/rawdatasets retained. Fullgates/restore pending. Nextdeterministic blocker/Bprogress proofbeforeadmissionfix; F0–F7 remainsactive.


Audit workload measurement verified: three guarded runs/70observations preserve exactaudiences and samekeyrecovery but each10k/eight-command burst refusesB503; latercode database_busy and sampled4busyconnections/5queuedcheckouts. Rawdatasets/report saved, no productionpolicy orlimitschanged. Full ID1,824pass/zero failures/22,961assertions/100%line+function coverage(355.87s); typecheck/lint, web62/204, bothuncachedbuilds19.88s and completefreshclusterrestore pass. Logs `/private/tmp/id-audit-load-{measure,repeat,pool,typecheck-final,lint,web,full,build,restore}.log`. Temporarymeasurementroles count0, restorecontainers absent, diffwhitespacepass. Firstprinciples rejected timeout/poolincreases, arbitrarymembercap and genericworkflowengine; demonstratedgaprequires deterministic blocker/Bprogress regression and minimal compatible admissionfix. Authentication/replica/workloadlimits and allremainingF0–F7 stayactive.


### Tenant command admission — completion gates running

The prior planning turn updated authoritative completion requirements; implementation resumes with the unfinished command gate. Restricted-role blocker tests reproduced B refusal. The gate now bounds two target-tenant commands per actual runtime pool before journal checkout, shares across Drizzle aliases and releases on rollback/replay. Capturing validated replay configuration fixes the TS2345 callback narrowing failure without a cast. The follow-up workload has 39 observations, zero B failures, full audit audiences and original-key recovery; largest-burst B maximum 40.30 ms. This does not cover authentication/global work or deployment fairness. Typecheck/lint/web pass; full ID, build and restore completion remain pending. No schema or production database changes. First-principles review retains existing authority locks/journal and adds no queue, setting or persistent state. Overall F0–F7 remains active.


Tenant command admission verified: full ID suite 1,827 pass / zero failures / 23,123 assertions / 100% line and function coverage (348.22 s); typecheck/lint/web 62 tests pass; both uncached builds pass (17.848 s); complete fresh-cluster restore passes and temporary container cleanup is confirmed. Logs `/private/tmp/id-command-admission-final-{full,typecheck,lint,web,build,restore}.log`. Generated OpenAPI and management/operator/foundation/isolation documents updated. First-principles review retains only the pool-bound active counter, existing retry error and finally release; no queue, schema, general limiter framework or extra setting. The measured workload improves but authentication/checkout, global work, supported cardinality and deployment-wide fairness remain open. Continue full F0–F7; do not mark the overall goal complete.


### Organisation erasure evidence — gates running

Previous goal turn completed command-admission code and gates (progress). Source inspection found organisation erasure cascades configuration without a full manifest or member-without-grant attribution. Corrected regression reproduces v1 payload gap; earlier fixture ID/principal errors are not reproduction evidence. Explicit DELETE RETURNING now captures child policy rows; UPDATE RETURNING captures actual cleared active-organisation session selections while preserving session authentication origin. Version 2 event and migration0042 retain member/session user subjects; old events are not guessed. Restricted-role proofs include A/B, post-erasure HTTP history, rollback/replay, malformed contracts and both real global-user erasure orders. Five dedicated tests/201 assertions pass. First-principles review deleted a redundant query export: existing deleteOrganization returns effects, using existing transaction/context/locks/event index. Full gates running; separate guarded measurement now includes organisation erasure/replay and will run after the DB suite. All broader F0–F7 requirements remain active.


Organisation erasure evidence verified: full ID suite 1,832 pass / zero failures / 23,325 assertions / 100% line and function coverage (355.16 s). Final typecheck/lint after script change, web 62/204, both uncached builds (17.591 s), and complete fresh-cluster restore pass. Logs `/private/tmp/id-org-erasure-final-{full,web,build,restore}.log` and `/private/tmp/id-org-erasure-complete-{typecheck,lint}.log`. Temporary role count zero and no restore container; diff whitespace passes. Guarded workload `reports/id-org-erasure-workload-evidence.json`: 45 observations, zero B failures; 10k membership erasure 693.36 ms, 2,493,297-byte event, all10k user subjects, replay7.72 ms. Shape is sparse after group erasure, not a dense-tenant production limit. Docs/OpenAPI/schema/inventories updated. Migration0042 applied to disposable database only. First-principles review removed redundant query helper, retains existing deletion path with actual effect returns and one transactional event/index. Next requirements remain effective-permission/lifecycle completeness, tenant trust/retention decisions, overall capacity, production migration/key/consumer proof. Goal remains active; this slice does not close F0–F7.


### Organisation grant tenant projection correction — gates running

Previous goal turn made code/schema/test/documentation progress. Follow-up user-erasure source review found the new organisation version-two subject consumer expects tenant IDs, while the actual deleted-grant producer omitted them. Removed-member subjects masked the mismatch. A valid real stored-grant regression fails on the missing organisation UUID; initial invalid grant fixture is not reproduction evidence. Added the existing stored organizationId column to RETURNING; no validator relaxation, new query or migration. Focused19 tests/666 assertions pass, full gates running. Global user-erasure catalogue and secondary cascades recorded in execution report for the next implementation; not claimed complete. First-principles fix is the one producer column. Full F0–F7 remains active.


Stored organisation grant tenant projection verified: full ID1,833 pass / zero failures /23,356 assertions /100% line and function coverage (347.43s); typecheck/lint, web62/204, both uncached builds17.17s and complete fresh-cluster restore pass. Logs `/private/tmp/id-org-grant-tenant-{full,typecheck,lint,web,build,restore}.log`. Temporary organisation-audit roles count0, restore containers absent, diff whitespace passes. The real deleted-grant regression prevents membership subject capture from masking a malformed grant projection. OpenAPI and relevant docs updated. First-principles review keeps only the missing stored column, strict consumer checks and regression; no schema/fallback/new framework. Global-user erasure's direct and secondary cascade catalogue is recorded as the next implementation boundary. Overall goal remains active.


### Global user-erasure manifest — full gates running

Previous goal turn corrected the stored tenant projection with code/test/documentation progress. New restricted-role regression reproduces missing global erasure manifest. Existing deleteUser now captures actual direct/member/owned-client/refresh child deletions, surviving token session-reference clearing and SSO attribution revision changes; returns ID/effects instead of a full profile. Version2 plus migration0043 retains other affected users from explicit grant/token/consent arrays. No account linkage identifiers or credentials retained. Original focused15/201 passes; external-reference test assumptions corrected from400 to existing409/reference_violation. Corrected4/178 passes; both real owned-client/user erasure orders pass (6/244 before final invalid-contract tests). Raw effect arrays are isolated to avoid masking missing subject capture; surviving rows are checked unchanged except intended fields. First-principles deleted redundant session lookup/full-profile return, uses existing query/context/transaction/index, no new framework. Full ID gates running; typecheck/lint/web pass. Broader F0–F7 remains active.


Global user-erasure manifest verified: full ID1,840 pass / zero failures /23,644 assertions /100% line and function coverage (362.67s); typecheck/lint, web62/204, both uncached builds16.831s and complete fresh-cluster restore pass. Logs `/private/tmp/id-user-erasure-final-{full,typecheck,lint,web,build,restore}.log`. Temporary user-audit role count0, restore containers absent, diff whitespace passes. Migration0043 applied only to disposable DB. Final seven-case file includes actual producer-array isolation, malformed contract refusal, surviving-row preservation, all-effect rollback/replay and both owned-client/user erasure orders; other existing user/tenant races pass. Docs/design/schema/inventories/OpenAPI updated. First-principles retains one existing deletion path, safe RETURNING projections, joined exact session references and one snapshot map; removes full profile return/redundant session subquery. Complete effective-permission/lifecycle audit, representative global-erasure capacity, tenant authentication and production release proof remain active F0–F7 requirements. Do not mark overall goal complete.


### Machine rejection evidence — verification in progress

The previous planning turn established the production/test-only user issuance boundary and identified discarded machine denial decisions. Corrected native regressions reproduce three missing-evidence failures; an earlier legacy fixture version edit is excluded from reproduction evidence. Existing machineCapability now returns denied results, provider retains the result before rollback and maps the same public errors, and the rejection writer emits version 2 with decision or explicit null. Rejected raw scope/target values remain excluded; no-match evidence is explicitly minimal. Focused26/200, typecheck/lint and web62/204 pass. Full ID coverage is running. No schema/new queries/framework. First-principles review retains one evaluator and existing independent rejection transaction. Docs/OpenAPI updated. F0–F7 remains active.


Machine rejection decision evidence verified: full ID 1,842 pass / zero failures /23,659 assertions /100% line and function coverage (363.68s); typecheck/lint, web62/204, both uncached builds17.217s and complete fresh-cluster restore pass. Logs `/private/tmp/id-machine-denial-{full,final-typecheck,lint,web,build,restore}.log`. Version-two events retain safe evaluator decisions through issuance failure without querying policy after rollback; pre-evaluation failures retain null. Legacy history, scope secrecy, foreign-target exclusion, claim-mismatch rollback and audit-outage denial are verified. First-principles review keeps one evaluator and existing rejection transaction, with no schema/new query/framework. No production database changes. Full F0–F7 remains active, including user issuance/admission, complete effective-permission audit, capacity and real release/recovery proof.


### Membership access observations — gates running

Previous goal turn implemented and verified machine denial decisions (progress). Current source review found membership window/reinstatement evidence lacked access observations. Four restricted-role regressions reproduce missing version-two contracts. Existing writers now reuse memberAccess before/after, preserve transaction/replay/rollback, and record version 2 for window/reinstatement/unchanged reinstatement. No new query, lock topology, schema or policy evaluator. Statement observations explicitly are not isolated causal deltas because windows/endpoint configuration can change. Focused13/264 passes after isolating Bun nested matcher mutation; added assigned/approved scope distinction to full coverage run. Typecheck/lint/web62/204 pass, full ID is running. Docs/OpenAPI updated. First-principles keeps existing projection and accurate semantics; complete effective permission, capacity and F0–F7 remain active.


Membership access observations verified: full ID 1,846 pass /zero failures /23,822 assertions /100% line and function coverage (360.38s); typecheck/lint, web62/204, both uncached builds16.303s and complete fresh-cluster restore pass. Logs `/private/tmp/id-member-observations-{full,typecheck,lint,web,build,restore}.log`. No migration or production database change. Version-two window/reinstatement observations retain existing evaluator results, distinguish assigned/approved scopes, prove organisation-wide regained access without reconstructed direct grants, and survive later erasure. Restricted-role rollback/replay/recovery, same-user A/B and session preservation pass. First-principles keeps existing projection/transaction and observational semantics rather than adding broad locks or another policy engine. Complete causal permission evidence, representative cardinality limits, production admission/user OAuth and full F0–F7 remain active.


### Dense member access — query correction and gates running

Previous goal turn implemented membership access observations with full gates (progress). The new guarded density workload proves 1,000 assignments per member hit the ten-second statement deadline, including all eight isolated same-key recoveries. Inspection found full policy-source arrays projected once per entitlement, then deduplicated in JS. Existing memberAccess now groups targets before fact projection and deletes the JS map, preserving evaluator/source/order/tenant contracts. Focused20/425 passes. Two corrected workload runs recover/replay all24 members across10/100/1000 assignments; 1,000-source HTTP recovery98–122ms. Explicit final checks retain all sources, one event and one revision. B has no refusals; latency observations are not limits. Full gates running; default harness branch will be verified serially after suite. No schema/newframework/cache/truncation. Broader F0–F7 remains active.


Dense member-access correction verified: full ID1,846 pass /zero failures /23,822 assertions /100% line+function coverage365.01s; typecheck/lint, web62/204, both uncached builds17.802s and complete fresh-cluster restore pass. Logs `/private/tmp/id-member-load-{full,final-typecheck,lint,web,build,restore}.log`. Before42 observations demonstrate persistent1,000-assignment failure; corrected48+48 observations complete/replay all commands, with24 explicit per-member checks preserving complete sources and one event/revision. Default audience mode45 observations also passes with no B refusals. Workload roles count0, restore containers absent, diff whitespace passes. First-principles removes repeated database projection and JS map rather than truncating, increasing deadlines or adding cache/workflow. No schema or production DB changes. Broader F0–F7 remains active; no general production capacity claim.


### Client-resource audit isolation — gates running

Previous goal turn fixed measured duplicate policy projection and passed all gates (progress). Source review found link/unlink audit events attributed foreign private resource identifiers to client-owner tenants. Restricted HTTP regression proves actual disclosure; seven initial cases fail. A shared version-two writer retains resource instance/classification/owner and places foreign/unknown links in platform-only history. Tenant history filters unsupported/legacy link versions before pagination; platform history remains complete. Focused45/557 passes; expanded version0/1/3 and action-family checks included in full run. First-principles uses one writer projection/read predicate, existing guarded resource lock and no history rewrite/schema/tenant event copies. Product-policy questions for SSO trust and named erasure retention were asked while independent work continues; no answer inferred. Broader F0–F7 remains active.


Client-resource audit visibility verified: full ID1,853 pass /zero failures /24,120 assertions /100% line+function coverage407.30s; typecheck/lint, web62/204, both uncached builds17.819s and complete fresh-cluster restore pass. Logs `/private/tmp/id-link-audit-{full,final-typecheck,lint,web,build,restore}.log`. Seven restricted-role tests cover actual foreign-identifier disclosure, all link/no-op/unlink/replay paths, legacy/unrecognised0/1/3 action versions, safe retained history after target erasure, missing targets and rollback/recovery. Link-audit roles count0, restore containers absent, diff whitespace passes. No migration/history rewrite/production DB change. First-principles keeps one shared writer and one pre-pagination read predicate. Product decisions asked remain unanswered; no dependent policy assumed. Overall F0–F7 remains active.

### Native user scope contract — focused proof

Confirmed the pinned provider carries the same complete allowed scope set in response/access JWT/refresh rows for the existing fixture, while the shared evaluator returns resource-only decision.scopes after separate login checks. Added six assertions to the existing native flow; focused118/5264 passes in100.08s, root typecheck/lint and diff whitespace pass. Logs /private/tmp/id-native-scope-{contract,typecheck,lint}.log. Updated release/execution plans with the required separate comparison and remaining filtering/narrowing cases. No production code/schema/route change; prior full gates were not rerun or relabelled. Source review found the existing0026 database guard already rejects foreign-private entitlement assignments; no redundant filter. First-principles retains existing primitives. Product trust/retention decisions remain unanswered, overall F0–F7 active.

### Native filtering and narrowing — verified integration constraints

Previous goal turn was progress (scope assertions and contract). Added two native regressions proving resource filtering changes code token kinds before claims policy, and refresh narrowing rejects widening without mutating rows, permits retry, then stops replacement refresh issuance after offline_access is removed. Entire native file120 pass/5369 assertions/zero failures101.33s; root typecheck/lint and diff whitespace pass. Logs /private/tmp/id-scope-narrowing-{full-file,typecheck,lint}.log. Release/execution plans now require pre-filter original code policy, current refresh-token ceiling and explicit token-kind semantics. No production changes or new replay state; full build/restore/coverage not rerun for test-only proof. First-principles preserves native enforcement rather than duplicating it. Trust/retention decisions still unresolved; broader F0–F7 goal active.

### Membership removal observations — verification running

Previous scope-contract turn was progress. Restricted-role regressions now reproduce missing after.access/version2 on removal (3pass2fail). Production removal reuses memberAccess after local revocation, and all member events use version2 so the action-version switch is deleted. Tests cover surviving organisation-wide assignment, ineffective removed/no-op access, subject-failure rollback of membership/assignments/receipt and same-key recovery/replay. Focused90/833, typecheck/lint and web pass. Full suite running; API regeneration/build/restore follow serially. Existing grant tests cover B preservation and local revocation rollback. Docs updated, no migration. Goal remains active.

Membership removal observations verified: full ID1856/24276/zero failures/100% line+function coverage378.22s; typecheck/lint, web62/204, both uncached builds17.033s, regenerated API2/13 and complete fresh-cluster restore pass. Logs /private/tmp/id-removal-observation-{full,typecheck,final-lint,web,build,restore,contract}.log. Temporary member-audit roles0, restore containers absent, diff whitespace passes. Current release/execution evidence updated. First-principles removes action-version switch and reuses one access projection/transaction; no schema or production DB changes. Broader production user admission, full lifecycle audit/capacity/release proof and F0–F7 remain active.

### Global-user erasure density — measured

Previous removal-audit turn was progress. Extended the existing guarded restricted-HTTP workload harness with user-erasure mode (eight global users, two tenants each,10/100/1000 group assignments and distinct direct-resource grants per membership). Corrected development-only duplicate-target seed and target-vs-affected subject assertion before retaining two completed runs. Each retains30 observations/27 explicit final checks; all24 erasures and replays pass with exact manifests, no residual target rows, one event and unrelated-member/group preservation. Bprobes0 failures; poolwaiting5 and variable latency prevent a general fairness/limit claim. Largestevent~1.27MB. Existing audience45 and member48 observation modes pass. Final typecheck/lint and diff whitespace pass; production untouched, prior fullgates not relabelled. Reports/README updated. First-principles reuses harness and rejects unnecessary workflow/truncation. Remaining independent dimensions include owned-client cascades/manytenant identities; F0–F7 goal active.

### Query boundary reconciliation — verified source and runtime evidence

Previous density measurement was progress. Current review inventories123 exported db/queries function declarations:113 context-taking entry points begin with runtime guards, six take raw database handles (explicit broker/audit/health/SQL construction), four are pure transformations/expression constructors. Inspected production callers establishing ownership/scope/session authority. Existing context/restricted-role suites27pass/1201 assertions5.28s; /private/tmp/id-query-boundary-audit.log. Updated source artifact, isolation inventory and execution report, replacing stale general raw-query backlog. No runtime/schema/API change and no added wrapper. Concrete remaining broker grant-context isolation and target-tenant authentication admission remain open; full F0–F7 active. This review establishes the narrower administrative conversion status, not all isolation completion.

### Grant-context integration trace — baseline verified

Previous query reconciliation was progress. Traced grant insertion/discovery/code binding/code reuse/refresh family/admin revocation under actual current scopes. Existing restricted-runtime test permits unscoped grant reads; platform-users commands use scope none. A blanket new RLS policy could silently stop revocation. Added atomic integration matrix covering production transaction scope, policies, admin scopes, startup/migration/restore and required failure/race tests. Selected native cleanup/replay8pass/517 assertions8.60s,112filtered, /private/tmp/id-grant-isolation-baseline.log. No partial migration/runtime change; first-principles rejects copying generic tenant policies. Asked tenant trust and erasure retention decisions again via async tool; no response inferred. F0–F7 active; no blocked/completed status claimed.

### Unattributed machine authentication attempts — verified

Previous grant-isolation trace was progress. F6 review found native client authentication outside rejection auditing. Reproduced missing credential event (16pass1fail); moved authentication inside the existing catch boundary, preserving its autocommit marker semantics. Version3 platform-only events use fixed system actor and null tenant/client/decision, no claimed identity or raw request/exception details. Switched to Better Auth isAPIError to recognise native Better Call credential denials. Added audit-outage preservation, unknown storage failure, tenant-history exclusion and real restricted-login subject checks. Corrected older zero-event test and version type; initial full run deliberately interrupted, corrected run authoritative. Full1858/24299/100%line+function385.91s, typecheck/lint, web62/204, both uncached builds20.226s, generated API consistency and fresh-cluster restore pass. Logs /private/tmp/id-machine-auth-attempt-{full,final-typecheck,final-lint,web,build,restore}.log. Temporary roles0, restorecontainersabsent, diffwhitespacepass. Docs/API/release evidence updated. First-principles keeps one boundary/writer/safe signal, no schema/identitylookup. Earlier parsing/route/rate-limit outcomes, audit-write capacity, tenanttrust/retention and full F0–F7 remain open; goal active.

### Already-disabled user reconciliation — full rerun in progress

Effect review found a correctness prerequisite before token-ID manifests: disable skipped sessions/tokens if status already disabled and no live grant context existed. Three seeded residual cases reproduced false noops. Removed the shortcut; calculate applied/noop from actual effects while preserving unchanged user timestamps. New restricted HTTP test distinguishes old-key replay/new-key reconciliation and verifies audit-failure rollback/recovery. Focused18/226, typecheck/lint/web pass. First full run encountered unrelated-path lifecycle/hook timeouts (retained /private/tmp/id-disable-reconcile-timeout-full.log); no processes/DB waits/test fault constraints remained, and affected plus changed files52/1376 passed in34.80s. Cause unconfirmed; full rerun uses unchanged limits. No migration/audit version change. Token-ID evidence remains open, fullgoal active.

Already-disabled user reconciliation verified: full rerun1862pass/24358 assertions/zero failures/100%line+function457.09s; typecheck/lint, web62/204, both uncached builds18.523s, generated API consistency and fresh-cluster restore pass. Logs /private/tmp/id-disable-reconcile-{full,typecheck,lint,web,build,restore}.log. Initial timeout run retained separately; affected52/1376 and full rerun passed with originallimits, root cause unconfirmed. No temporary reconciliation/last-writer roles or restorecontainers, diffwhitespacepass. Docs/API/release evidence updated. First-principles removes conditional cleanup and uses actual effects for applied/noop, preserves state timestamps and old-key replay. No schema/prodDBchange. Actual token-ID manifests, broker/admission product decisions and broader F0–F7 remain open; goalactive.


## 2026-09-10 — exact token revocation evidence

- Completed safe token-row manifests from UPDATE RETURNING for user/session/client revocation. User/session event contracts are v2. Client-wide token effects join the existing platform-only grant effect event; owner events retain counts and a link. Migration 0044 preserves explicit affected-user subjects without historical inference.
- Added five restricted-role HTTP tests for user disable, single/all session revocation, client disable and secret rotation. Exact effects, subject-fault atomic rollback, same-key recovery, tenant-owner exclusion and post-erasure UUID history pass. Existing replay/new-command separation is preserved.
- Initial query contract failed twice before the implementation. Corrected nullable test projection, fixture-only DDL/root-actor assumptions and four older client-event expectations. First full result: 1,863 pass/4 outdated-assertion failures, 418.14s; retained in `/private/tmp/id-token-manifest-count-assertions-full.log`. Targeted correction run: 136 pass/1,063 assertions, 11.94s, `/private/tmp/id-token-manifest-count-recheck.log`.
- Final: 1,867 pass/0 fail/24,692 assertions/100% line and function coverage, 507.88s. Root typecheck/lint pass; web 62/204 pass; both uncached builds pass, 23.561s; complete fresh-cluster restore passes; regenerated API consistency passes in the full suite. Logs `/private/tmp/id-token-manifest-{full,typecheck,lint,web,build,restore}.log`.
- First-principles review reused update results, existing journal transaction and global effect event. Removed a duplicate query-test assertion. No extra token lookup, worker, event table, credential snapshot or inferred identity. Local row changes are not proof of downstream termination. Documented exact standalone client-erasure cascade gap and next acceptance criteria.
- Updated ID README, foundation specification, mutation inventory, public management docs, generated OpenAPI and execution/release reports. Only the disposable database was migrated; no production deployment/commit. F0–F7 and unanswered SSO-trust/retention choices remain open; goal stays active.


## 2026-09-10 — standalone client erasure and refresh-cascade ordering

- Previous goal turn was verified progress (token-revocation manifests, passing gates). This turn captures actual standalone client-erasure token/consent/link cascades in platform-only v2 history; owner events retain counts and a link. Migration 0045 preserves explicit affected-user subjects without historical inference.
- Initial restricted HTTP regression: missing indirect-user subject allowed erasure through a subject fault (`/private/tmp/id-client-erasure-before.log`). First implementation then reproduced a lost manifest entry for a cross-client token committed while refresh deletion waited (`id-client-erasure-race-before.log`). Fixed by locking refresh parents in a separate statement before access deletion, under the command's existing client lock. Bounded SQL subquery avoids a per-token parameter list.
- Restricted tests cover exact deleted IDs, already-revoked rows, indirect cross-client cascades, unrelated users/sessions/rows preserved, owner exclusion, secret omission, subject/owner-audit rollback, same-key recovery and post-erasure UUID history. Versioned subject tests reject malformed/unsupported contracts. Race observer uses fresh PostgreSQL activity snapshots; fixed a test-only Response/Promise type mismatch.
- Focused evidence: 140/859 lifecycle tests/assertions; lock/query group 18/472; subjects 10/56; final restricted HTTP 2/102. Logs `/private/tmp/id-client-erasure-{focused,locked,subjects,final-focused}.log`.
- Final ID suite: 1,870 pass, zero fail, 24,798 assertions, 100% line/function coverage, 372.54s. Typecheck/lint pass; web 62/204 pass; both uncached builds pass, 16.395s; complete fresh-cluster restore passes; generated OpenAPI consistency passes inside full suite. Logs `/private/tmp/id-client-erasure-{full,typecheck,lint,web,build,restore}.log`.
- First-principles review kept one deletion query, transaction and audit mechanism with a discriminated payload; added only the parent lock required by the reproduced race. No new table, queue, worker, credential copy or recursive identity inference. Updated README, specification, mutation inventory, public docs, generated API and execution/release reports.
- Next evidenced investigation: global-user deletion captures owned-client/refresh cascades without equivalent explicit parent locking. Source pattern documented; global-user race not yet reproduced. Test before changing lock order. Only disposable DB migrated; no production deployment/commit. Goal and wider F0–F7 requirements remain active; SSO-trust and named-person-retention decisions remain unanswered.


## 2026-09-10 — global user erasure parent ordering

- Previous goal turn was verified progress on standalone client erasure. Reproduced four global-user manifest omissions: owned-client child, refresh child, session-reference clearing and membership assignment committed while erasure waited on a parent (`/private/tmp/id-user-cascade-before.log`). A corrected-fixture control bypassing only the added lock stage again failed all four, 128 assertions (`id-user-cascade-without-locks.log`). Invalid grant authentication and a missing fixture variable were corrected in intermediate tests; they were fixture failures.
- Lock owned clients, memberships, sessions and affected refresh parents before any grant/cascade capture. First-principles review deleted the new exported locking helper and moved locking plus grant-context deletion into the existing user deletion query. Service consumes the returned manifests, reducing caller coordination. No new schema, event version, queue, table, worker or per-row SQL parameter list. Existing user FOR UPDATE remains the service precondition.
- Final focused group 17 pass/627 assertions, including restricted races, existing erasure audit/rollback tests and query-authority checks (`id-user-cascade-final-focused.log`). Regenerated query AST evidence remains 123 exports/113 guarded context-taking declarations; static inventory is not runtime proof.
- Existing dense-erasure dimension: 30 observations/27 checks; 24 erasures and all replays succeed, zero B-probe failures. Bursts 267/141/546ms at 10/100/1,000 assignments/direct grants per membership; B maxima 188/88/403ms; largest complete audit 1,268,829 bytes. Evidence `reports/id-user-erasure-parent-lock-density.json`, log `/private/tmp/id-user-cascade-density.log`. Two memberships per user; not large parent counts, token fan-out or production capacity.
- Final ID suite: 1,874 pass, zero fail, 24,952 assertions, 100% line/function coverage, 440.57s. Typecheck/lint pass; web 62/204 pass; both uncached builds pass, 18.345s; complete fresh-cluster restore passes; regenerated OpenAPI consistency passes in full suite. Logs `/private/tmp/id-user-cascade-{full,typecheck,lint,web,build,restore}.log`.
- Updated README, foundation spec, mutation/isolation evidence, public management docs, generated API, execution/release and workload reports. No production database/deployment/commit. Re-presented the two product questions asynchronously: first-release tenant SSO trust and named-person retention after erasure. No answer inferred. Broader F0–F7 objective remains active with independent work still available.


### Public authentication admission: completed verification

- Previous planning turn changed the authoritative release sequence; this continuation completed verification of the existing authentication-admission implementation. No running database test process remained before starting the final suite.
- Shared actual-pool limiter bounds public /auth handlers before body/credential work. Restricted regressions retain B reads while rejection audits are blocked, including app aliases and two pools. This is a handler bound, not a dedicated connection reservation or admin-route/cluster fairness guarantee.
- First full run had 11 federation fixture failures: intentional overlapping callbacks exceeded the fixture's one-connection admission limit and queued claims contaminated later cases. The overlap case alone now uses three connections; issuer reset prevents cross-test contamination and cleanup awaits the callback. Focused federation/admission rerun 42/42, 345 assertions.
- First-principles review retained the existing limiter and refusal/finally semantics; no additional queue, pool, lease table or configuration. Pool identity and actual pool maximum control sharing. No further production simplification justified.
- Final full suite 1,879 pass, zero failures, 25,067 assertions, 100% line/function coverage (452.96s). Typecheck/lint and web 62/204 pass. Both uncached builds pass (20.685s); fresh-cluster restore passes; OpenAPI drift checks pass in full suite. Logs /private/tmp/id-auth-admission-final-{full,typecheck,lint,web,build,restore}.log.
- Next independent finding: actual administrative JWKS resolver with paused synthetic getJwks and eight unknown-key calls performs eight simultaneous initial loads, sixteen total. Source/probe scope recorded in findings.md. No JWKS production change yet. Tenant trust/retention decisions and remaining F0–F7 remain open.


### Administrative JWKS concurrency: implemented and verified

- Previous turn completed admission verification and produced the JWKS amplification finding: progress. Inspected the actual resolver and pinned provider adapter before changing code.
- Added failing concurrent cold/cached missing-key and failed-store regressions (34 pass/2 fail before fix, /private/tmp/id-jwks-before.log). Shared one temporary in-flight promise, avoided immediate reload after a fresh miss, and reused an already refreshed set. Existing five-minute expiry and immediate cached-miss rotation remain.
- Added actual signed-token rotation proof: eight verifications share refresh while old cached tokens still verify. Failure rejects all waiting callers, then subsequent load recovers. Focused 37 pass/149 assertions/518ms. Immediate-completion probe: eight cold calls→one load; eight cached misses→one refresh.
- First-principles review retained existing cache/policy and added no key-indexed state, timers, cooldowns, configuration, queues or provider fork. Verification results are never shared. Sequential missing-key requests and separate resolvers remain outside this guarantee; this is not complete abuse protection.
- Final verification: 1,882 ID tests pass, zero failures, 25,090 assertions and 100% line/function coverage (533.39s). Typecheck/lint pass; 62 web tests/204 assertions pass; both uncached builds pass (52.174s); the complete synthetic fresh-cluster restore passes. Generated OpenAPI consistency passes inside the full suite; this slice changes no API shape or migration. Logs: `/private/tmp/id-jwks-{full,typecheck,lint,web,build,restore}.log`. Focused tests pass 37/37 with 149 assertions (`/private/tmp/id-jwks-focused-final.log`, 518ms). An additional immediate-load probe reports one load for eight cold requests and one refresh for eight cached misses, covering fast completion as well as paused loads. This closes the reproduced concurrent amplification only; sequential invalid traffic, distinct resolver instances and overall admin admission remain outside the guarantee.
- Updated operator README, foundation specification, public management guide, release/execution reports and progress. No production migration, deployment or commit. Full F0–F7 objective remains active.


### Grant-context RLS and restricted native proof

- Previous goal turn fixed/tested JWKS concurrency: progress. Re-inspected grant admission, native code/refresh helpers, query contexts and runtime-role configuration. Re-presented tenant SSO trust and named-identity retention questions asynchronously; no answer inferred. Independent isolation work continued.
- New restricted native-flow regression reproduced unscoped grant disclosure (0 pass/1 fail, 40 assertions, /private/tmp/id-broker-rls-before.log). Native provider/admission fixture now uses a real restricted login and one-connection pool; owner access is limited to explicit setup/inspection.
- Generated/applied migration 0046 only in the disposable database. Added purpose-specific direct grant row policies; startup requires the fifth RLS table. Global platform-users commands now use explicit scope. Creation scopes user/session; native proof installs client scope after authentication. Existing provenance/immutability and policy locks remain.
- First-principles review reused/renamed the scope helper to withDatabaseScope instead of adding another helper/registry. Clear/restore client/session alongside mode/tenant/subject, including nested rollback and pooled reuse. No per-tenant roles/databases, policy engine, new broker pool or authority cache. Query inventory remains 123 exports/113 context-guarded declarations.
- First restricted-native run 118 pass/3 fail: direct helper tests lacked client scope, now supplied explicitly. Scope test's lazy-query promise matcher corrected to execute the query and require SQLSTATE 42501. Added A/B, wrong client/user/session, scoped mutation, restoration and startup-with-disabled-RLS checks.
- Final verification: 1,885 ID tests pass, zero failures, 25,186 assertions and 100% line/function coverage (620.22s). Typecheck/lint pass; 62 web tests/204 assertions pass; both uncached builds pass (40.644s); the synthetic fresh-cluster restore passes, including retained grant state, rebuilt runtime permissions and startup checks. Generated OpenAPI consistency passes in the full suite; API shapes are unchanged. Logs: `/private/tmp/id-broker-rls-{full,typecheck,lint,web,build,restore}.log`. The focused native/runtime/context set passed 151 tests/6,204 assertions (230.35s). Temporary native broker login roles and restore containers are absent; diff whitespace checks pass. Migration 0046 was applied only to the disposable test database.
- Updated operator/schema/foundation/public management docs, mutation/isolation inventories, query AST artifact and release/execution evidence. Mixed writer versions are unsupported: stop old platform-users writers before applying grant RLS. No production migration, deploy or commit. Settings remain trusted application code; parent-table cascades and arbitrary runtime SQL are not secured by direct grant RLS. Public user OAuth, selected-tenant trust, production issuance audit and broader F0–F7 remain open.


### Administrative refusal audit outages: verified

- Previous goal turn was a verified wait: polled the live full-suite session and reported scope at the user's request. Resumed that same session without restarting it; terminal result is successful.
- Corrected restricted-role regressions reproduced 500 replacing existing 403/404 during denial-audit insertion failure. Two refusal paths now use one outcome-restricted writer. Successful root admission and local mutation auditing remain mandatory.
- Actual user, foreign-target, locked-root and machine refusals preserve response/challenge, emit exactly one fixed request-ID signal, leave domain rows unchanged and resume recording after storage recovery. Root-success control proves no operation/receipt during audit outage, then same-key recovery and replay.
- First-principles review keeps the existing audit writer and two call sites, with no retry queue, new event schema or success-path exception suppression. No further production change is justified by these tests.
- Final verification: 1,890 ID tests pass, zero failures, 25,378 assertions and 100% line/function coverage (572.89s). Typecheck/lint pass; 62 web tests/204 assertions pass; both uncached builds pass (36.562s); the synthetic fresh-cluster restore passes. Generated OpenAPI consistency passes in the full suite. Logs: `/private/tmp/id-denial-audit-{full,final-typecheck,lint,web,build,restore}.log`. Focused verification passed 54 tests/438 assertions before the final explicit baseline-code/challenge assertions, which are covered by the full run. Temporary denial-audit roles are absent. No migration or API shape change; no production database change or deployment.
- Reduced the working task checklist from 634 to 41 lines; preserved the original contents unchanged under reports/answerable-id-task-history.md. Corrected stale README status and linked the current checklist. Historical notes do not introduce new release requirements.
- Remaining priority is the existing tenant authentication/production OAuth contract, followed by the other F0–F7 acceptance requirements. Tenant-trust and named-identity retention answers remain pending; no approval or duration is inferred. The full goal remains active.


### Production consent integration investigation

- Previous goal turn completed denial-audit verification and consolidated the checklist: progress. Re-inspected actual production provider/allowlist, native callback declarations and federation identity resolution. Re-presented the tenant-trust choice asynchronously; no answer inferred.
- Existing native fixture skips interactive consent. A temporary restricted-role probe enabled it and reproduced consent acceptance returning to consent with two additional grant rows (1 pass/33 assertions, 1.68s). A stable per-flow reference control produced a code and redeemed it into the expected grant-bound JWT (corrected final 1 pass/44 assertions, 1.58s). Intermediate control failed only on an out-of-scope final assertion, after successful issuance.
- Retained sanitised evidence in reports/id-interactive-consent-evidence.json and the temporary final reproducer under /private/tmp. Removed the temporary copied test from the repository. No production/test implementation changed; the previously verified 1,890-test state is preserved. No full-suite rerun is warranted for this report-only change.
- Updated the existing release integration contract with stable per-flow binding, actual callback inputs and required consent/concurrent-flow coverage. First-principles review rejects copying the fixture's mutable selected member and per-callback creation, and rejects process-local memoisation as production state. Reuse the native signed continuation and existing grant contexts where their contracts permit it.
- Tenant trust remains required input for dependent admission; named identity retention and real deployment/consumer evidence also remain open. No new opportunistic hardening requirement or completed foundation claim.


### Mutation inventory reconciled with current source

- Previous goal turn executed interactive-consent probes and changed the next integration contract: progress. Re-inspected current checklist, registered mutation handlers and actual lifecycle audit producers instead of repeating the full suite.
- All 49 administrative mutation call sites use existing journal wrappers. Fresh route/OpenAPI contract tests pass 2 tests, zero failures and 1,780 assertions (330ms), logged at /private/tmp/id-route-contract-reconciliation.log. This is declaration/source evidence, not a new runtime replay proof.
- Corrected the inventory's stale claims that member contexts, organisation/group/user erasure manifests, exact token IDs and callback-origin protection were missing. Added the previously omitted capability audit row. Remaining items now distinguish production flow, product decisions and deployment/consumer proof from implemented local effects.
- First-principles review removes obsolete adoption work rather than implementing a second boundary or rerunning unchanged tests. No production code, migration or test source changed; the previous full 1,890-test verification remains the latest implementation gate. No test process remains running.
- Next production admission still requires the requested tenant-trust answer. Identity retention and actual deployment/consumer evidence remain dependencies for their respective work. Do not substitute additional speculative hardening for these decisions or count this documentation correction as production OAuth completion.


### Dependency checkpoint: execution blocked

- The same unanswered tenant-trust dependency has persisted through the consent-probe turn, mutation-inventory reconciliation turn and this checkpoint. The preceding turn made documentation/contract-check progress; it was not a running-process wait.
- Independent consent investigation and current mutation-inventory reconciliation are finished. No test handle remains live. The current full implementation verification remains 1,890 passing ID tests; repeating unchanged tests cannot resolve a product decision.
- Dependent production admission requires the tenant-trust answer. Identity-history implementation requires the retained fields/readers/duration decision. Representative release certification requires the intended deployment and real tenant/consumer evidence. Do not invent these inputs or substitute further opportunistic hardening for them.
- Mark execution blocked, not complete. Resume the existing objective when the missing input arrives, starting with production tenant admission and the stable consent-flow contract. All implementation, history and evidence remain in the worktree.


### User-directed final migration consolidation

- User confirms the platform has never shipped to production and explicitly requests one clean migration after implementation is done.
- Added final consolidation to the current checklist and replaced the canonical production-upgrade assumptions with a fresh-install contract. The release plan now identifies older cutover/conversion notes as superseded.
- Final cleanup must retain all custom SQL security invariants, produce one initial migration plus matching snapshot/journal, remove obsolete upgrade-only machinery, update docs/tests, and pass fresh-install/repeated-startup/role/schema/restore and repository gates.
- No migration files or databases changed in this turn: the requested timing is after implementation. Tenant-trust and retention decisions remain unresolved; this new fact removes the legacy-production migration assumption, not those product dependencies.


### Approved tenant trust and soft deletion

- User explicitly chose own-tenant SSO with verified identity binding and UUID-based audit history, then specified `deletedAt` soft deletion. Physical cleanup jobs at a future configurable age are deferred.
- Updated the current checklist and coordination contract; removed those product questions as blockers. Historical physical-erasure/retention proposals are superseded. Retained soft-deleted profiles remain identifying data, not anonymised records.
- T1 remains the only implementation/DB owner while finishing initiation-state verification. T3 is assigned a read-only revision of its deletion plan and interface mapping; no concurrent schema or database mutation is authorised.
- Required deletion semantics include ordinary-read exclusion, immediate loss of eligibility, preserved revocation/tenant boundaries, reserved identifiers and idempotent audit/command handling. Do not build deferred cleanup jobs or apply deletedAt mechanically to protocol-consumption state.


### Coordinated SSO initiation fix integrated

- T1 completed with a clean worktree and local commit 9bb7c62. Coordinator reviewed the production diff and native regression assertions, then cherry-picked it as 372b066 onto codex/id-foundation-completion.
- Raw full log confirms 1,904 ID tests / zero failures /25,455 assertions /100% line+function coverage in445.02s. Other gates and scope are in reports/id-sso-initiation-revision.md. Integrated application trees exactly match the tested worker commit; no duplicate full run.
- Next exclusive writer/DB owner is T3 for soft deletion. Reviewed plan adjustment preserves explicit membership reinstatement as a revocation operation; soft deletion must not silently eliminate that existing feature. No auto resurrection through SSO or linking.
