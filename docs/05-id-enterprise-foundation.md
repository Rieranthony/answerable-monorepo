---
title: Enterprise ID foundation
description: Tenant isolation, token identity, idempotency and audit specification, implementation progress and release gates.
---

# Enterprise ID foundation

> **TL;DR**
>
> - **Decides:** the proposed foundation changes before tenant self-service and user OAuth expansion.
> - **Rule:** authority is tenant-bound, identity is immutable, changes are repeat-safe, and audit evidence survives the live records it describes.
> - **Not here:** billing implementation, a general workflow engine or the rest of the fleet migration.

**Status: implementation in progress; F0–F7 are not complete.** This supplements [the design](03-answerable-id.md) and adds a foundation gate to [the build order](02-plan.md). The [code review](../reports/answerable-id-feedback-review.md) and [executed probes](../reports/answerable-id-feedback-evidence.json) establish the initial defects. Proposed acceptance tests below are not claimed to pass unless specifically recorded here.

**Current implementation:** immutable client/resource identity and permanent reservations; persisted platform bindings; transaction-backed administrative replay and revisions; durable UUID audit subjects; tenant service contexts and selected RLS; tenant capability ceilings and exact-pair assignments; stored tenant grant contexts; native grant/refresh and lifecycle concurrency proofs; immutable SSO origin and callback configuration checks; upstream token encryption with dedicated versioned keys. Process interruption tests exercise creation, secret rotation and erasure through the real HTTP app and restricted database login.

**Still required:** production user OAuth request/consent/tenant selection, the chosen tenant authentication trust rule, complete query isolation and policy explanations, remaining lifecycle/audit effects, fresh authentication and capacity bounds, retention decisions, key/restore/migration rehearsal and real-tenant release proof. Public user OAuth remains closed. UUID attribution after erasure does not establish recovery of a person's name.

The [execution evidence](/Users/anthonyriera/code/answerable/reports/answerable-id-foundation-execution-plan.md) records current gate results and the failed runs they supersede. The numbered contract below describes required behaviour; implementation notes after it are historical slice records. Their “remaining” statements describe that slice and must be checked against the current evidence, not read as a second current-status checklist.

**Current completion sequence:** use the [release decision plan](../reports/answerable-id-release-decision-plan.md#current-completion-plan) for the ordered remaining changes, source evidence, unresolved product contracts and per-slice documentation gates. Command-admission verification and its remaining capacity limits are recorded in the execution report; historical slice results do not replace current gate evidence.

## 1. Scope and design rules

Keep one Bun/Hono service, Better Auth and PostgreSQL. Keep a shared schema and global user IDs. Add ordinary typed services, database constraints and a small operation journal. No separate policy service, event-sourced domain model, database per customer or workflow engine.

The foundation must establish these invariants:

1. A credential cannot gain authority because a name, owner or live database row changes.
2. Every tenant operation has one validated target tenant. Global/platform operations are explicitly different operations.
3. Removing access to A cannot reveal or revoke B's sessions, identity or permissions.
4. Successful upstream authentication cannot restore explicitly revoked tenant membership.
5. Tenant administrators assign only platform-approved capabilities. Registration never grants authority.
6. One logical administrative command has at most one committed effect. Retrying can recover its outcome.
7. A committed security change has a committed audit fact, including relevant prior state and actual local effects.
8. Person and tenant history do not depend on joins to rows that may be erased.
9. User grants remain evidence of that user's authentication; machine grants never impersonate a person.
10. Remote revocation has an explicit time bound. Local database deletion is never represented as proof that all remote access stopped.

**Initial review evidence:** the reviewed baseline had transactions, route allowlists, composite membership/group foreign keys and effective windows, but lacked pair targets and operation replay. The historical probes reproduced client-token reassignment/revival, lost person-history retrieval, repeated rotation and missing token audit. Preserve the existing protections while addressing those gaps. See [schema](../apps/id/src/db/schema/authorization.ts), [principal resolution](../apps/id/src/http/principal.ts), [client services](../apps/id/src/services/clients.ts), [audit query](../apps/id/src/db/queries/audit.ts).

## 2. Tenant and identity boundaries

### Ownership model

| Record                               | Proposed boundary                                                                        | Rule                                                                                                     |
| ------------------------------------ | ---------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| User and verified upstream account   | Global identity, broker-managed                                                          | Stable user UUID; no email/name linking; no tenant-wide global directory search                          |
| Membership                           | Exactly one organisation and user                                                        | Stable membership UUID; explicit active/revoked state; effective window; no implicit reinstatement       |
| Group, assignment and access ceiling | Exactly one organisation                                                                 | Composite tenant foreign keys; tenant-prefixed indexes/uniqueness where relevant                         |
| OAuth client                         | Immutable owner organisation when owned; explicitly unowned public registrations allowed | Machine credentials require an owner; shared user-login clients remain usable only through tenant policy |
| Resource                             | Explicit platform-shared or tenant-owned classification                                  | Ownership is distinct from permission to use; cross-tenant use of a private resource denied              |
| Authorization/refresh context        | Exactly one organisation, client and subject kind                                        | Never infer tenant from mutable browser state at refresh                                                 |
| Audit and operation record           | Durable tenant or explicit platform scope                                                | No cascading deletion of evidence; no cross-tenant response replay                                       |

A globally shared Circle client or public external tool does not make all its users part of the client's owner organisation. **Machine tenant comes from immutable ownership; user tenant comes from verified authentication plus an effective membership selected for that grant.** Each token carries exactly one tenant. A user's memberships are never merged into a multi-tenant token.

Use organisation UUIDs for authority, not slugs. The platform organisation is anchored by a persisted immutable system binding established at initial bootstrap. Changing its display name or configuration cannot designate another organisation as the platform. Reserve system resource/group identities. Bootstrap repairs only records whose immutable binding and expected owner match; otherwise fail with a specific conflict. Do not adopt an arbitrary existing record with the expected name.

### Tenant administration and the global browser session

Introduce a typed `TenantContext` created only after authentication and authorization. Tenant services and queries require it; accepting an optional organisation filter is not a tenant boundary. Platform-wide queries have separate names and require `PlatformContext`. Root is an explicit principal, never a missing-context fallback.

**Implemented global users command boundary:** disable, enable and retire-email user services and both global session revocation services require a PlatformUsersContext. Current platform:users is checked before replay/execution; context lifetime covers only a new mutation callback. The runner is single-use and journal exit closes authorisation on replay, success or failure. The journal transaction owns mutation/audit/result without an inner service transaction. Client/resource writes, group/assignment and entitlement writes, domain create/disable/enable/delete, SSO put/delete, organisation create/update/disable/enable/erase and global user erasure now require a distinct PlatformWriteContext with current platform:write. Both context types share single-use/journal cleanup, but cannot substitute for one another. All 38 platform-write, five platform-users and three tenant-member administrative mutation service entry points now require issued contexts. All 46 administrative mutation services now derive audit actors from their issued command context; principal identity is frozen and request/operation metadata cannot override it. Query-layer context adoption and complete concurrent writer ordering remain unfinished. Targeted RLS now covers groups, group_members and entitlements (migration 0025); new access ceilings are not built yet.

**Implemented member boundary:** member window update, removal and reinstatement require the authorised `TenantMemberContext` directly. Its private runtime registry rejects reconstructed contexts; the command callback expires its context on both success and failure. These services reuse the organisation lock acquired before current-authority validation. Member list/detail and configuration reads now use separate scoped contexts, shared organisation locks and current-authority validation inside the read transaction. Configuration authority does not authorise directory/profile reads; read contexts cannot authorise writes and expire on callback completion. Access views also require these contexts: member access uses platform:read or org:users, while target access uses platform:read or org:read. Their services reject copied, expired and differently scoped contexts. Organisation detail/summary, redacted SSO provider reads, group list/detail, group member list/detail, domain listing and tenant entitlement list/detail also require the directory context and transaction authority check. Sign-in diagnosis also uses a validated read context and tenant-bound membership lookup; it omits global accounts, retired-email state and foreign routing identities. Its authentication_required result avoids claiming an unlinked email is globally unused or will sign in. Organisation audit reads require a distinct history context with current platform:read or org:read authority; it permits erased tenants without relaxing live-data contexts. The service checks retained evidence before returning history for a missing organisation. Operation audit reads also recheck authority inside their transaction and derive actor-versus-platform visibility from that current result, preserving retained receipts after erasure. SSO connectivity configuration snapshots, client/resource list and detail, fleet summary, organisation and fleet entitlement lists, global user list/detail, session listing, fleet-wide and user audit history now require a separate issued platform read context with current platform:read authority; retained erased-user evidence remains readable. This adoption does not cover remaining broker services or query-layer context types; targeted RLS now covers the three access tables, while resource ownership/classification now has immutable database constraints and private machine/assignment isolation, while the exact-pair access evaluator remains unfinished. Internal command context creation must remain inside a transaction and use its single-use runner. The journal finally block closes tenant authorisation when replay skips execution; mutation contexts expire on callback exit.

Tenant reads expose a member projection, not the complete global user/account record. Tenant administrators cannot erase the global user, enumerate other memberships or operate on another tenant's credentials. Platform authority remains separately scoped and audited; an `answerable` organisation claim is not a universal bypass in resource servers. Staff access to tenant data must pass an explicit support/platform rule for that target tenant.

**Fix existing session routes first.** The unsafe `listMemberSessions`/`revokeMemberSessions` aliases are now removed, along with the organisation summary's global-session count. The old routes return 404; platform user-session routes retain their explicit global authority. Organisation disable now preserves global sessions/unbound user grants and revokes owned machine access-token rows; migration 0017 adds a monotonic organisation epoch so pre-disable machine tokens do not revive at the admin API after re-enable. Replace tenant session views with tenant authorization/delegation views. A global browser login can remain valid for B after A is revoked. Global sign-out/disable remains an explicit platform or user-owned operation. Do not use `sessions.active_organization_id` as proof that a global session belongs to A. [Current session services](../apps/id/src/services/sessions.ts), [organisation disable](../apps/id/src/services/organizations.ts).

Introduce durable tenant binding on authorization/refresh state before opening user grants: organisation ID, membership ID for users, immutable client ID, authentication evidence reference, grant purpose, resource and scopes. Prefer provider-owned state with declared server-only fields if it supports the required invariant; otherwise one Answerable `grant_contexts` table keyed by the provider's immutable grant/reference ID. Do not maintain two independently authoritative copies. Background delegation remains distinct from the browser session.

### Membership lifecycle and admission

**Implemented portion:** migration 0016 adds retained active/revoked membership state, revocation time and immutable membership identity. Removal deletes direct assignments and group memberships with pre-removal access and deleted-row audit evidence. SSO rejects revoked exact/imported identities. Explicit reinstatement preserves the UUID/window without restoring removed assignments. Tests compare access in another membership of the same person. Tenant grant/session cleanup and complete concurrent grant/revocation ordering remain unfinished; member journal adoption and revision preconditions are implemented. Previously deleted memberships require reviewed reconciliation before cutover.

Change tenant removal to revoke membership, preserving its identity and recording the actor/reason/time. Strip tenant-local group assignments and direct grants as specified by the command, and revoke A's authorization/refresh contexts. Reinstatement is explicit and does not restore old credentials or silently restore removed assignments.

Check revoked membership in federation resolution before the SSO plugin can provision it. Preserve a denial marker when erasing a membership while its user remains. Full global erasure removes identity linkage; a later upstream login is a new enrolment unless an explicitly retained admission block says otherwise. Document that distinction rather than promising erasure and indefinite identity recognition simultaneously.

Source-derived risk requiring a regression test: the installed SSO plugin creates a member when no row exists, while the current remove service deletes the row. Test valid SSO immediately after revocation/removal. [Federation resolver](../apps/id/src/services/federation.ts), [member removal](../apps/id/src/services/members.ts), installed `@better-auth/sso` 1.7.2 `assignOrganization`.

Preserve `(issuer, sub)` / `(tid, oid)` bindings and current fail-closed email collisions. A person can have multiple memberships without automatic linking of different upstream accounts. Explicit cross-provider linking would require proof of both identities and its own future design; this change does not introduce it.

### Database isolation

Retain shared PostgreSQL tables. Inventory every tenant table, query, unique key, foreign key and cascade. Enforce same-tenant relationships at the database and service layers; test reads, aggregates, exports and error responses as well as writes. Normalise inaccessible foreign references to non-disclosing errors. Global uniqueness checks must not reveal another tenant's record details.

Use separate migration and runtime database roles. The runtime role is not a superuser/table owner and cannot alter schema or update/delete audit facts. Add targeted row-level security to Answerable-owned tenant configuration tables (`groups`, `group_members`, entitlements and new access ceilings), using transaction-local validated tenant/platform context. Missing context denies access; no session-level context that can leak through a pooled connection. Test broker/policy reads and bootstrap with explicit narrow context, including pool reuse, rollback and concurrent tenants.

Do not blanket-enable RLS on Better Auth's global/session/federation tables and hope its queries work. Their broker access remains an explicit reviewed boundary. Initial implementation must prove the targeted coverage against the installed adapter; any table excluded must be named with its service/constraint isolation tests. RLS guards missing filters, not compromised application code capable of choosing trusted context. PostgreSQL owners and `BYPASSRLS` roles have special bypass behaviour, so tests must use the production runtime role. [PostgreSQL 16 RLS](https://www.postgresql.org/docs/16/ddl-rowsecurity.html).

## 3. Immutable credentials and token policy

**Decision: remove in-place client ownership transfer.** A move creates a new client/credential under the new owner and retires the old client. Public `client_id` values and security-sensitive resource identifiers are permanently reserved after retirement. Use a small identifier-reservation table if hard deletion is retained; do not preserve secret-bearing client rows merely to reserve a name. Existing mutable-owner API becomes a documented conflict/deprecated operation, not an alias that silently transfers authority.

Every access token must bind issuer, audience, token type, expiry, public client ID, immutable client instance ID, organisation UUID and subject kind. User tokens also bind the user/membership and authenticated grant context. Define claim names once in the shared verifier contract. Machine tokens have no user subject. Treat slugs and display fields as descriptive.

Add a client authorization version. Increment it for retirement, disable and explicit revoke-all; secret rotation revokes the previous version by default. Re-enable never revives old tokens. The admin API checks current owner, immutable instance, version, client-resource compatibility, active organisation and current scope ceiling. **Requiring the new claims is intentional:** reject pre-foundation tokens and reissue machine credentials/tokens during cutover. Do not provide a legacy acceptance path that preserves the reproduced defect.

The shared resource verifier checks signed tenant identity and local tenant routing, including resources shared across tenants. A shared audience is not a shared data namespace. Local offline verification cannot observe version changes immediately: short lifetimes bound remote access, while the admin API performs online current-state checks. Any resource requiring immediate revocation must use a defined online check or acknowledged invalidation mechanism. State this limitation explicitly. JWT validation follows the established issuer/audience/type rules. [RFC 9068](https://www.rfc-editor.org/rfc/rfc9068.html).

### One policy, explicit intersections

Introduce a platform-managed `organization_capabilities` table: the maximum targets, grant types and scopes available to an organisation. Entitlements become assignments beneath that ceiling. Tenant admins may narrow/add assignments only within it; they cannot alter the ceiling, global catalog, client ownership or platform scopes. Removing a ceiling immediately makes dependent assignments ineffective, even if they remain as historical/configuration rows.

Support three target shapes:

| Shape             | Meaning                                                         | Required fields                                                              |
| ----------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| Client only       | User may sign into/use this application                         | `clientId`, no resource                                                      |
| Client + resource | User may obtain a token for this resource through this client   | Both fields; exact pair, no wildcard                                         |
| Resource only     | Direct session administration of the reserved ID admin resource | Reserved admin resource only; cannot mean unrestricted OAuth resource access |

Resource-only grants outside the reserved admin surface must be explicitly converted to approved pairs before user OAuth opens. They are not silently expanded to every linked client. A resource-only administration permission must never satisfy a user OAuth resource request.

Within one exact target and tenant, assignments from organisation/group/member are additive, with effective windows. Across different targets/tenants, scopes never combine. No deny-precedence language. Tenant restrictions are represented by the available exact pairs and assignment removal. This solves “OmniChat → Microsoft 365 allowed; OpenCode → Microsoft 365 denied” without modelling arbitrary graphs.

Client-only scopes describe application login; they are not resource scopes. The client-login assignment is an admission check when evaluating a resource request, not another unrelated scope set to union into that token. Resource scope ceilings and pair assignments decide the resource token's scopes.

For user resource requests require: active user + effective member + allowed client-login assignment + compatible client/resource + platform tenant ceiling + matching pair assignment + consent/protocol evidence. Effective scopes are the intersection of requested scopes, client ceiling, resource scope vocabulary, tenant capability scopes and applicable assignment scopes. For machine grants substitute immutable owner/client credentials and the machine-specific tenant capability for user membership/assignment. Grant types are explicitly allowed by the capability; refresh re-evaluates the original tenant/pair and cannot switch tenants or widen authority.

For direct admin sessions preserve the six current scopes through reserved admin capabilities. Platform-only scopes can only be delegated from platform-managed ceilings/bindings; tenant assignment APIs cannot bootstrap their own privilege. Define whether a caller may delegate each admin scope and protect the last effective platform administrator.

Return a structured internal policy decision: allowed/denied, reason, tenant, client, resource, granted scopes, contributing capability/entitlement IDs and policy version. Reuse it for issuance, access views and audit evidence. Keep public denial information deliberately limited.

**Implemented member decision identity:** the shared evaluator carries trusted tenant/user/member/client/resource identities, configuration versions/ceilings, grant kind and requested scopes from the same statement as its policy sources. Client login, exact-pair access and direct administration retain distinct null target semantics. Internal denials for located contexts retain available effective sources; successful evidence lists contributing sources. Public access denials remain limited to allowed/reason. An unknown or mismatched native grant context does not acquire identity from request parameters. Production user-issuance audit adoption and complete token outcome coverage remain unfinished.

### Provider integration proof

Before depending on hooks, write a focused integration test proving that the installed Better Auth plugin exposes the **authenticated** client and grant context, supports rejection before usable token release, and allows grant state/audit to commit consistently. Its `customAccessTokenClaims` signature alone is insufficient: it exposes user/reference/scopes/resources/metadata, not a verified client and transaction handle. Client-supplied metadata is never an authority source.

Use supported custom plugin hooks and the transaction-bound adapter; never edit `node_modules` or hand-roll OAuth cryptography. If the seam cannot provide the guarantees, keep the affected grant closed and specify the smallest supported adapter/upstream extension. This is an implementation gate with a concrete proof, not a reason to redesign the entire provider.

## 4. Idempotency and concurrency contract

**Implemented primitive:** migration 0010 adds immutable completed command reservations. `executeOperation` serialises duplicate keys with a transaction advisory lock, checks current authority before replay, compares canonical validated input, and commits the mutation, its audit writes and the result reference together. A concurrent duplicate receives retryable `operation_in_progress`; a changed input receives non-retryable `idempotency_key_reused`. Failed transactions leave no reservation. The runtime cannot update, delete or truncate completed keys. Reference-only commands store no response body. Encrypted recovery is now supported through a separate ciphertext row and an immutable expiry; client creation and secret rotation now require the key and recover their original responses for 24 hours. Client PATCH also requires If-Match and recovers its original response for seven days. Every remaining client mutation now requires the key with seven-day recovery, including bodyless erasure/unlink replay. Enable/disable and link/unlink record noops when the desired state already holds; owner verification remains immutable. Resource create/patch/enable/disable/erase commands also use seven-day recovery, and resource PATCH requires If-Match. Member update/removal/reinstatement also use tenant-scoped keys and seven-day recovery. Domain creation/lifecycle/deletion also use platform-scoped keys and seven-day recovery. All 46 administrative mutations are now connected to the journal. Operation-status reads now expose reference-only projections, restricted to the same tenant actor or platform audit authority. The replay helper now enforces 24-hour secret and seven-day ordinary windows, operation-bound encryption and retained-key refusal after expiry. Dedicated key configuration now validates at environment loading. A restricted maintenance login can purge expired ciphertext in audited batches while retaining the keys. Production key provisioning/job scheduling, full effect manifests and tenant/authority ordering remain required. Callers must supply immutable actor/scope identity, normalise domain sets/defaults and implement transactional authority checks; the helper does not infer those semantics.

**Decision: a PostgreSQL operation journal for administrative mutations.** `x-request-id` stays per-attempt correlation. `Idempotency-Key` identifies the logical command. Require it on supported write/erase routes after a documented API transition; do not apply generic response replay to OAuth authorization codes or refresh rotation.

An `admin_operations` row holds immutable operation ID, actor instance, tenant/platform scope, operation name, key digest, canonical request fingerprint, outcome/status, result reference, timestamps and replay expiry. A unique constraint on actor instance + authority scope + operation name + key digest arbitrates concurrent duplicates. Target IDs and all semantic fields belong in the fingerprint, so reusing the same key for a different target conflicts. Canonicalise validated values, including default values, dates, scope-set order, and omitted versus explicit-null semantics; exclude request ID and transport noise.

Provide an operation-status read by immutable operation ID, scoped to the same actor/tenant or explicit platform audit authority. It reports committed/no-op/failed status, result references and replay availability without exposing secret values. Errors have stable codes and an explicit retry classification; clients do not guess from HTTP 409 alone.

The transaction reserves the key, locks relevant state, rechecks current authority, performs the mutation, inserts audit facts, stores the result and commits. No network call occurs inside that transaction. A competing duplicate waits only to a bounded lock timeout and then receives a documented retryable response; a crashed uncommitted request rolls back its reservation and changes together. A committed request whose HTTP response was lost is replayed from the journal. Do not leave a permanently committed `running` row for a single-transaction operation.

| Situation                                        | Proposed response                                                                                   |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------- |
| Same key, same validated input, still authorised | Original status/body or documented result representation; replay indicator; no second domain effect |
| Same key, different input/target                 | 409 `idempotency_key_reused`                                                                        |
| Concurrent original still executing              | 409 `operation_in_progress` with retry guidance                                                     |
| Desired state already reached under a new key    | 200/204 with a recorded no-op outcome, rather than an “already disabled” error                      |
| Existing entity has a different owner/identity   | 409 `ownership_conflict`; never adopt it                                                            |
| Stale revision on a configuration patch          | 412 `revision_mismatch`                                                                             |
| Replay payload expired                           | 410 `operation_result_expired`; result reference where permitted; never rerun the effect            |
| Database/dependency failure before commit        | Retryable 503; no successful operation/audit fact                                                   |

Authorize before replay and scope errors so other actors/tenants cannot discover stored outcomes. Revoked callers cannot recover secrets. For deletion replay, use retained operation/subject identity rather than requiring the deleted live row. Record HTTP attempts separately from the single domain operation; an `admin.root_request` remains an admission event, not proof that the mutation succeeded.

**Replay retention proposal:** ordinary response payloads seven days; secret-bearing payloads at most 24 hours, encrypted using a dedicated versioned key outside PostgreSQL. Retain a minimal completed-key/fingerprint/result-ID reservation after payload purge so an old key never executes again. No token, password or plaintext secret in audit JSON. A lost secret after its recovery window requires a new deliberate rotation, not replay of the original command. Purging operation reservations is a contract change and must not happen in a generic cleanup job. Volumes are administrative, not one operation row per API read or inference token.

**Implemented for client and resource PATCH:** migrations 0014–0015 cover both endpoint rows and visible links, stale writes return 412, and unchanged patches record a noop without a revision bump. Other configuration remains unfinished.

Add numeric revisions to mutable configuration. Require `If-Match` for patch/replace operations where stale writes can silently overwrite newer configuration; evaluate replay before stale-revision checks for an already committed identical request. Row locks serialize local mutations but do not replace client-visible concurrency preconditions.

Use a consistent lock order for global user, organisation, client/resource, membership and assignment rows. Recheck authorization inside the transaction that mutates security state; a middleware decision from before revocation is insufficient. Start with organisation-level serialization of tenant security changes, short bounded transactions and indexed queries. Do not hold an organisation lock over all token signing work without measuring it; use a common shared/exclusive locking or revision-validation protocol so issuance and revocation have a tested commit order.

Exactly-once HTTP delivery is not promised. The contract is **one committed local effect per retained logical operation key, recoverable results, and explicit retry rules**. HTTP method idempotence alone does not provide this result-recovery contract. [HTTP semantics](https://www.rfc-editor.org/rfc/rfc9110.html#section-9.2.2).

## 5. Durable audit evidence

Keep mutable domain tables and append-only audit facts; do not rebuild the product from an event stream.

Extend `audit_events` with a versioned envelope: operation ID, actor kind and immutable ID, durable tenant/platform scope, action, target type/ID, outcome/reason, trusted occurrence time, request correlation and policy version. Remove the tenant FK behaviour that nulls historical attribution. Store a durable tenant UUID independent of live organisation existence; the writer validates it at event creation.

Add `audit_event_subjects(event_id, entity_type, entity_id, relationship, organisation_id)` for related users, members, clients and affected entities. These references do not cascade with live records. Index by tenant/entity/time. A person-history query uses those references, never current membership joins. An erased UUID with retained events is queryable by an authorised auditor; an unknown UUID returns an empty history or the documented historical-subject response.

Use typed event payloads with allowlisted before/after values, actor intent and actual effects. Capture effective access before revocation: target pairs, scopes, grant source IDs, membership/group windows and the policy version used. Include actual revoked grant/session IDs and counts. Large manifests become multiple bounded audit effect records linked to the same operation, inserted in the same transaction; never silently truncate evidence. Secrets, bearer tokens, upstream refresh tokens and raw provider configuration are excluded.

Distinguish `applied`, `noop`, `denied`, `failed` and `pending external delivery` meanings rather than labelling every request successful. A no-op has its own operation outcome but no fabricated change. If audit insertion fails, a local security mutation and any token/grant state must not commit or become usable. Authentication/provider hooks require their own transaction/failure tests; current service transaction tests do not prove hook atomicity.

Log every reachable token grant outcome with immutable client/tenant/subject/resource/scopes and decision reason. Store token identifier/digest only if necessary for correlation, never the token. A successful audit record means the grant committed, not that the network response reached the caller. A crash after commit remains explainable. Failed-attempt audit outages emit an operational failure signal while authorization remains denied; do not allow an audit outage to turn a denial into success.

**Implemented authenticated machine attempts:** the reachable machine endpoint records version 2 `oauth.token.rejected` after the issuance transaction exits with an error. Attribution uses the provider-verified immutable client instance/public ID/owner. The decision returned by the locked evaluator survives transaction rollback in the request context and is recorded without querying policy again: approved evidence for subsequent issuance failure, trusted ceiling/revision/capability evidence for scope denial, or a minimal no-eligible-capability decision. Pre-evaluation failures carry `data.decision: null`. The minimal decision does not diagnose missing versus disabled/expired policy; failed request scopes and untrusted targets are omitted. Approved requested scopes and trusted target configuration can appear. Stage/outcome distinguish denial from failure independently of policy approval, which alone does not establish commit. Audit outages preserve rejection and emit a safe signal. Version 1 records are unchanged. Version 3 covers failed client authentication inside the handler. Earlier parsing/route/rate-limit refusals and production user-grant outcome coverage remain unfinished; no unauthenticated tenant attribution or durable-outage guarantee is claimed.

### Erasure and identifying a person

Permanent UUID correlation is mandatory. A restricted encrypted identity record is the recommended additional capability if auditors must recover a person's name/upstream identity after operational erasure. Keep it separate from ordinary audit facts and tenant-visible projections, with separate read permission, audited access, an explicit retention period and actual deletion/key lifecycle. Do not copy identifying fields into every immutable event.

**Product decision before real erasure:** choose UUID-only history or the restricted identity record, and set its retention duration. The user has been asked; planning does not assume an answer. Until configured, the erase API must not promise both full identity removal and indefinite named attribution. Tenant erasure affects tenant-local identity evidence; global identity erasure remains platform-controlled and accounts for other memberships.

Enforce append-only application permissions through the runtime DB role. Retention maintenance uses a separate narrowly privileged path and is audited. Backups and a restore rehearsal belong to release readiness. External immutable audit export can be added when an operational/compliance consumer requires it; application append-only permissions alone are not a tamper-proof archive.

## 6. Interruption, external effects and money

Single-database commands use the journal and transaction above. Restarting requires no checkpoint engine because either the command committed or it did not.

For a large offboarding operation, first commit the authoritative tenant/user denial barrier, operation identity and bounded access-evidence snapshot using a consistent revision/locking strategy. Cleanup cannot re-enable access. If its size requires batching, persist one operation-specific cursor, deterministic item keys and completion state; restart from that cursor. Only report completion after every required local effect and evidence item is reconciled. Set explicit size/timeout limits in the first implementation; never silently process only the first page.

At the first implemented external effect (for example back-channel logout), add one transactional outbox table and a small PostgreSQL worker. Commit intent with the domain operation; deliver at least once with a stable effect ID, bounded retries, lease expiry and redelivery after crashes. Consumers deduplicate that ID. “Delivered” and “acknowledged” are distinct; an outbox does not provide exactly-once remote execution. No DBOS/BullMQ/Kafka dependency is required for this foundation.

No money ledger is built in ID now. The architecture rule is that money-bearing operations live with the owning product's ledger and use a unique business-operation key atomically with local balance/access changes. Remote charges need provider idempotency and reconciliation; never hold a database transaction around a payment request. ID grants identity/access, not financial truth. A future billing-to-access integration must use durable idempotent events, not a naked HTTP callback that retries a grant blindly.

## 7. Enterprise and adversarial operation

Address concrete surfaces before public tenant writes:

- Keep the Better Auth allowlist and generated per-route negative tests. Extend route metadata with tenant/platform authority, replay policy, concurrency precondition and expected audit outcome; fail inventory tests when a mutation omits them.
- Domain claims remain platform-managed until proof-of-control verification exists. Bind challenges to tenant/domain/operation with expiry and single-use consumption; prevent races and same-name adoption. Prove SSO issuer/domain ownership before activation. Provider changes invalidate affected authentication/grant contexts.
- Implement bounded request sizes, pagination, execution/lock timeouts and tenant/client/IP rate limits for relevant public routes. Use trusted ingress-derived IPs; caller-supplied forwarding/request headers are not trusted audit identity. Tenant quotas must prevent a noisy tenant exhausting the shared pool. Rate-limit storage must match the number of replicas; do not claim cluster limits from process memory.
- Encrypt every retained upstream OAuth token field before real logins. Supported account transforms now protect all three fields with a dedicated versioned key ring and reject plaintext/legacy unversioned reads. Native first/repeat SSO, missing-key rollback and retained-key rotation are tested. Complete the [custody contract](../reports/answerable-id-upstream-token-custody.md): migration 0037 now retires pre-cutover upstream credentials atomically with durable audit and a migration receipt; real process interruption, replay and fresh SSO are tested. External-consumer review, representative-volume rehearsal, backup restore and production key provisioning remain open. Retain the separate signing-key and operation-replay custody milestones.
- Audit privilege, SSO, credential and erasure changes. Require fresh authentication for human high-impact operations with a documented freshness bound; machines require explicit scopes. Confirmation text is an intent check, not authentication or authorization.
- Publish metrics for failed audit writes, rejected grants after revocation, tenant denial spikes, operation conflicts/timeouts, pool saturation and unfinished external effects. Test failure recovery and restore, not only happy-path uptime.

**Implemented request-body boundary:** the runtime and application enforce a 256 KiB body ceiling; the application counts actual streamed bytes and gives body acquisition five seconds before provider/command work. Tests exposed the native streamed-body gap and an ineffective transform-abort approach; a direct reader now enforces the deadline without timing out a running mutation. Invalid correlation headers become bounded generated IDs consistently; failed SSO callbacks do not attribute caller-supplied provider/IP values. These controls do not establish database execution deadlines, per-tenant/client/IP or cluster limits, trusted ingress across all session metadata, or durable transport-failure audit. Those F6 requirements remain open.

No claim of resisting every AI-driven attack follows from these controls. Automated callers and attackers exercise the same bounded interfaces; authorization, tenant isolation and replay rules must hold regardless of who generates requests. OAuth protections remain grounded in [the OAuth security BCP](https://www.rfc-editor.org/rfc/rfc9700.html).

## 8. Implementation sequence and proof

The [remaining execution plan](../reports/answerable-id-foundation-execution-plan.md) maps the current working tree to ordered packages, acceptance criteria and documentation requirements. It distinguishes implemented primitives from unfinished adoption and release proof; the original review remains historical evidence.

The [mutation inventory](../reports/answerable-id-mutation-inventory.md) lists all current administrative writes, their authority and remaining replay/revision/audit work. Route contract tests pin the no missing replay contracts and no missing replacement preconditions. These inventory assertions do not establish runtime replay or complete audit evidence.

Deliver reviewable slices. Start each behaviour change with a failing test; update its documentation in the same slice. Do not open another grant type or tenant mutation surface midway through the foundation.

| Slice | Work and main files                                                                                                                                 | Required evidence                                                                                                                         |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| F0    | Promote report probes to regression tests; add tenant session/offboarding/SSO rejoin tests; provider integration proof in `auth/*`                  | Reproduce each defect; demonstrate authenticated grant context and transaction boundary using the installed provider                      |
| F1    | Immutable client/system bindings, reserved identifiers, token claim/version verification; `bootstrap`, client services/schema, principal middleware | Old tokens fail after disable/re-enable, retirement, attempted transfer and client-ID reuse; wrong instance/owner/version/resource denied |
| F2    | Durable audit envelope/subjects and operation journal primitives; DB roles and migrations                                                           | Audit survives erasure; local effect+audit+journal rollback/commit together; runtime role cannot alter audit facts                        |
| F3    | Apply replay/revisions to every admin mutation; first client creation/rotation, then all route families                                             | Concurrent duplicate, mismatched payload, stale revision, response loss, expired secret recovery, actor/tenant isolation, deletion replay |
| F4    | Tenant contexts, revoked memberships, tenant grant contexts, scoped session APIs, targeted RLS                                                      | A/B shared-user tests; A cannot read/revoke B; SSO cannot restore revoked A; pool context cannot leak; global disable remains explicit    |
| F5    | Capability ceiling, paired assignments, grant evaluator and truthful access views                                                                   | Pair truth table; scope/tenant intersections; tenant self-escalation denied; policy and audit match; removed ceiling denies renewal       |
| F6    | Complete token/auth audit hooks, bounded offboarding, public-route limits and key/retention readiness                                               | No usable token without committed decision evidence; grant-vs-revocation races; interrupted cleanup resumes; audit/DB outages fail closed |
| F7    | Migration rehearsal, public contract/docs, production-role tests, review and release gate                                                           | All foundation acceptance tests and repository gates pass; no unresolved data conversion/retention/integration blocker                    |

F1 has its necessary minimal audit assertions; F2 standardises the full envelope. F0 must prove the integration seam before F1 relies on it. F4/F5 are completed before tenant self-service or user OAuth is enabled. Outbox delivery and ledger rules become implementation gates when their first consumers ship, not speculative infrastructure in F2.

**Mandatory scenario matrix:** two unrelated tenants; one shared user with A+B memberships; staff with platform authority; tenant-owned and shared resources; a shared user-login client; separate machine clients; unowned external registration; disabled/revoked/expired entities; same display name with different immutable IDs. Run each against service and HTTP boundaries, with raw SQL constraint tests where applicable. Include concurrent grant/revoke, duplicate commands, process termination before/after commit, pool reuse and restored database state.

Run the repository gates: root `bun run typecheck`, `bun run lint`, `bun run build`, `bun --filter web test`, `bun --filter @answerable/id test:coverage` after disposable test migrations. Update the catalog/migration snapshots deliberately. Coverage remains required, but the above invariants and fault tests define correctness.

## 9. Initial migration and recovery

**Confirmed by the user:** Answerable ID has never shipped to production. The numbered development migrations are temporary implementation history, not a deployed compatibility contract. No legacy production import, backfill or mixed-version maintenance cutover is required for this first release. Historical implementation notes below describing those procedures are superseded by this decision.

**Final implementation step — not yet:** consolidate the complete final schema into exactly one initial SQL migration, with one matching Drizzle snapshot and journal entry. Preserve database invariants that schema generation alone may omit: functions, triggers, identifier reservations, audit-subject capture, immutability, RLS policies and privilege restrictions. Generate from the final schema and incorporate required custom SQL deliberately; do not merely concatenate the development sequence of creates, alters and historical data conversions.

Remove the intermediate migrations/snapshots and obsolete upgrade-only branches, migration receipts and tests where they have no fresh-install or ongoing runtime purpose. Keep ongoing security controls and meaningful regression tests. Update the operator README, schema/design contracts, scripts and tests that reference the old chain. Historical reports remain evidence of development, not instructions for a new installation.

Prove installation into an empty disposable database, safe repeated migration/startup, schema consistency, bootstrap bindings and restricted runtime privileges. Run the repository gates and the fresh-cluster restore rehearsal against the consolidated baseline. Do not describe this cleanup as complete until exactly one migration remains and these checks pass.

Recovery remains a first-release requirement. Retained keys, identifier reservations, command receipts and revocation barriers must survive backup/restore. A restored snapshot can predate later security changes; reconcile those changes before serving traffic rather than claiming that a successful database restore alone proves safe recovery. Once the platform ships, subsequent changes will require ordinary forward migrations.

## 10. Documentation is part of completion

Each slice updates facts only when implemented. Until then use “Not yet.” and link this specification. The report remains historical evidence, not the current reference.

| Document                                                                         | Required update                                                                                                                                                                                    |
| -------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/03-answerable-id.md`                                                       | Token/tenant ownership, immutable client moves, paired policy, operation/audit guarantees, global versus tenant lifecycle; reconcile inherited Node/CDN wording with existing Bun/no-CDN decisions |
| `docs/04-answerable-id-schema.md`                                                | Actual tables, ownership, constraints, cascade changes, audit subject references, revisions and operation retention; implemented/deferred distinction                                              |
| `docs/02-plan.md`                                                                | Foundation slice status, release gates, resolved decisions; do not mark a slice complete before tests/docs land                                                                                    |
| Root and `apps/id/README.md`                                                     | Actual shipped capability, bootstrap binding, runtime roles/keys, reset/recovery commands and compatibility changes                                                                                |
| `apps/web/content/docs/id/manage.mdx`, `onboard.mdx`, `sign-in.mdx`, `index.mdx` | Tenant-local lifecycle, verified enrolment, replay/error semantics and shipped boundaries                                                                                                          |
| New public guides when implemented                                               | Idempotent administration, access policy, audit/erasure and tenant lifecycle; cURL examples first, error/action tables, migration and recovery examples                                            |
| Generated OpenAPI                                                                | Header requirements, stable problem codes, response replay/revision semantics, route metadata; regenerate with `openapi:export`, never hand-edit snapshots                                         |
| Runbooks                                                                         | Failed/unknown operation recovery, lost secret, key rotation, tenant offboarding, audit outage, backup restore and compatibility cutover                                                           |

For public documentation, read [the repository documentation principles](../AGENTS.md) and [Lee Robinson's guidance](https://leerob.com/docs). Preserve existing anchors and check inbound links. Add title/description, British spelling and bidirectional guide/API links. Keep static rendering, Markdown URLs/content negotiation, `/llms.txt`, `/llms-full.txt`, navigation and mobile accessibility working. A slice is incomplete when code and published behaviour disagree.

## 11. Decisions remaining before implementation release

The architectural defaults above are concrete proposals, not a menu of competing stacks. Remaining checks are bounded:

- **Product:** UUID-only erasure history versus restricted identifying evidence, and its retention duration. This gates real erasure, not writing the plan.
- **Integration:** prove verified provider context and atomic grant/audit behaviour with the installed version; prove targeted RLS adapter boundaries. A failed proof keeps the affected capability closed.
- **Deployment:** inventory actual production state/consumers; choose concrete limiter capacity, transaction/operation size limits, retention maintenance and recovery objectives from that deployment. Rehearse them before cutover.

The foundation is complete when the invariants have executable evidence and the relevant documentation matches the implementation. It is not complete merely because new tables exist or line coverage is 100%.

**Implemented slice: organisation machine epoch and global-state isolation.** Migration 0017 and the organisation service remove global user/session revocation from organisation disable. The issuer, admin verifier and operation authority bind/check `organization_authorization_version`; stored machine revocations and before/after epochs are audited atomically. This does not complete F4: tenant user-grant contexts, tenant session route replacement and all-writer lock ordering remain required. Cutover must reissue existing machine JWTs, including tokens without the new claim. Offline consumers cannot infer current epochs from a signed token alone.

**Implemented slice: remove cross-tenant browser-session aliases.** Tenant membership no longer provides a route to list or revoke the person's global sessions. The same global activity count is removed from organisation summaries. This is the initial deletion step, not completion of tenant grant/delegation views: no trusted user grant context exists yet, and the user OAuth grant remains closed. The required replacement must derive from actual verified tenant-bound grants; do not infer it from activeOrganizationId or client ownership.

**Implemented slice: machine lifecycle commit ordering.** The authentication adapter now exposes its active SQL transaction through a short-lived object mapping. Machine identity evaluation holds organisation-then-client shared locks and rereads current state before emitting claims. Organisation/client disable uses the same rows exclusively. Barrier tests observe PostgreSQL blockers for issuance-first ordering and rejection/rollback for disable-first ordering; shared readers overlap and another tenant can progress. Existing provider tests still prove signing/policy rollback and persistence of consumed client assertions. This does not complete the common F4 protocol: resource/capability/assignment ordering, bounded waits, user grant contexts and production lock-duration/load measurement remain required. JWT access tokens are not stored by the current provider, so validity checks rely on signed versions plus current online state, with the existing offline expiry boundary.

**Implemented slice: bounded machine issuance lock waits.** Issuance applies transaction-local PostgreSQL `lock_timeout = '2s'`. Lock timeout errors roll back and return OAuth `503 temporarily_unavailable` with `Retry-After: 1`; other failures are not relabelled as contention. Integration tests prove no transaction marker survives, a retry succeeds after the blocking writer releases, and no timeout setting leaks to any pooled connection. The bound is per lock wait, not an end-to-end request deadline. Client assertions remain consumed outside issuance and must be regenerated for retries. Administrative transaction waits, resource/capability ordering and total request budgets remain separate unfinished work.

**Implemented slice: resource policy reads and deletion ordering.** Issuance now acquires organisation, client and resource shared locks before invoking provider policy reads. It checks the authenticated client instance/version, then reloads current client configuration through the adapter before evaluating default/requested scopes. Resource scopes/lifetime are read under the resource lock. Tests cover committed client/resource scope changes, blocked policy updates, later-token TTL and unlinking. Migration 0018 requires explicit unlink-before-resource-erasure through a restrictive FK and service conflict; this deletes the reverse resource-to-client cascade path. Client erasure keeps its own link cascade. Capability/assignment policy, the remaining administrative lock protocol, user grant contexts and production load measurement remain open.

**Implemented slice: successful machine issuance audit.** Signing is followed by an allowlisted oauth.token.issued audit insert before transaction commit. Audit failure rolls back policy/extension writes and prevents a successful token response. Tests cover real runtime-role insertion, secret/metadata exclusion, stable identity/scope/revision evidence, new jtis under repeated request correlation, and failure recovery. Configuration revisions are evidence of current registration state, not a substitute for the still-unfinished capability policy decision/version. Denied grants, user grants, complete contribution evidence and operational monitoring remain required. A committed issuance event does not assert delivery or remote consumption.

**Implemented slice: current member command authority.** The three reachable member mutations now enter `authorizeTenantMemberCommand`, whose SQL transaction locks the target organisation and checks current session/user or client claims and scopes before mutation. The callback receives the validated organisation and transaction. Real HTTP tests revoke the acting membership after middleware admission and require update/removal/reinstatement to fail without a successful mutation fact. Tenant machine org:users access remains tenant-bound. This does not complete F4 or F3: reads and direct internal service APIs still accept organisation IDs, RLS remains pending; member journal/revisions are now implemented, and the full platform/client/resource writer ordering and bounded command waits remain required.

**Implemented slice: member operation replay.** The journal now owns the member command transaction and invokes the tenant authoriser before replay or mutation. Update/removal/reinstatement require operation keys, scope receipts to tenant and actor, recover ordinary responses for seven days and attach their one successful mutation fact to the operation. New-key unchanged requests record noops; input mismatches and expired recovery never repeat effects. Tests cover all three routes, concurrent removal, authority revoked between middleware and replay, missing keys and purged recovery data. Full tenant service boundaries and bounded administrative waits remain incomplete; member revisions are now implemented.

**Implemented slice: member configuration revisions.** Migration 0019 reuses the server-controlled configuration revision trigger for memberships. getMemberConfiguration reads stable membership fields with an ETag under member-administration authority; profile/group/effective-access projections do not receive that validator. PATCH requires If-Match and locks the membership before comparing it; same-key committed replay precedes the stale check. Window audit includes before/after configuration. Direct revision edits are rejected, noops preserve the revision and removal/reinstatement invalidate old preconditions. No cascade of user/group display changes is required to maintain a membership validator.

**Implemented slice: domain command replay.** All four domain mutations use the existing platform command journal with seven-day recovery. Canonical input includes organisation/domain identity; creation normalises hostnames before fingerprinting and never adopts conflicting assignments. Lifecycle noops preserve row timestamps and record unchanged audit facts; replay emits no second domain fact. Domain status/deletion snapshot the locked row into allowlisted before/after data. Tests prove creation/lifecycle/deletion replay, noops, audit-failure rollback and root setup with configured replay encryption. Domain ownership verification, tenant write delegation and the complete common writer protocol remain separate unfinished work.

### Implemented slice: organisation command recovery

All five organisation mutations now use platform-scoped journal receipts and seven-day encrypted recovery, including historical results after erasure. New-key unchanged PATCH/enable/disable records a noop and preserves timestamps; no-op disable does not advance the epoch or repeat machine revocation. Current platform authority is checked before replay. Audit captures allowlisted configuration before/after, and only a change flag for arbitrary metadata. Organisation revision preconditions are now implemented; full cascade manifests and complete tenant user-grant cleanup remain unfinished. The mutation inventory now has no missing replay contracts and no missing replacement preconditions.

### Implemented slice: organisation configuration revisions

Migration 0020 adds a positive server-controlled organisation revision using the existing trigger. Stored-row getOrganization supplies its strong ETag. PATCH requires If-Match and an operation key, checks the expected instance/revision under the organisation lock, and includes it in the fingerprint. Identical replay precedes the old precondition; unchanged patches preserve the revision. Lifecycle and raw metadata changes invalidate older tags. Complete tenant contexts/RLS and erasure manifests remain separate unfinished requirements.

### Implemented slice: keyed request fingerprints

Before integrating submitted SSO secrets, new encrypted journal commands now use versioned HMAC-SHA-256 fingerprints bound to command identity and canonical input. The existing replay key ring supplies a domain-separated MAC key; rotation verifies with the original key ID. Existing SHA-256 receipts remain supported without being rewritten. Internal reference-only calls without encryption retain SHA-256 and must not accept secrets. Expired/purged results return 410 before fingerprint verification, preserving completed-key reservations after key retirement; live missing-key verification returns 503. SSO PUT/delete journal adoption and conditional creation/replacement preconditions are now implemented.

### Implemented slice: SSO command recovery

SSO PUT/delete now use platform-scoped journal receipts with keyed fingerprints and seven-day encrypted redacted responses. PUT normalises hostnames/defaults/scope sets, preserves omitted secrets on new execution, and records noop for equivalent effective configuration without advancing timestamps. Redacted before/after settings and credential-change flags share the mutation/receipt transaction. Historical PUT replay cannot overwrite newer credentials, and historical delete replay cannot delete a recreated provider. SSO replacement preconditions are now implemented; full in-flight grant invalidation and tenant write delegation remain unfinished. The inventory now has no missing replay contracts and no missing replacement preconditions.

### Implemented slice: SSO creation/replacement preconditions

Migration 0021 reuses the configuration revision guard for provider rows, including secret changes. Redacted GET/PUT responses include revision and ETag. PUT requires exactly one precondition: If-None-Match: * for absence, or If-Match for an existing immutable provider instance/revision. The journal fingerprints that expectation and recovers committed results before evaluating it under organisation/provider locks. Old tags fail after deletion/recreation. No additional endpoint or revision subsystem is introduced. Federation/grant invalidation and complete writer ordering remain separate requirements.

### Implemented slice: group command recovery

All seven group mutations now use platform journal receipts, keyed fingerprints and seven-day response recovery. Group configuration/lifecycle and assignment-window noops skip writes. Group/assignment before/after state is recorded with the operation; replay of removal cannot remove a new assignment. Directory-managed groups and revoked memberships retain their admission restrictions. Group PATCH and membership-window PUT preconditions are now implemented; complete cascade manifests and full writer ordering remain unfinished. The inventory has no remaining replay contracts and no replacement-precondition gaps.

### Implemented slice: group configuration revisions

Migration 0022 adds the server-controlled group revision and reuses protect_configuration_revision. Stored-row getGroup supplies the strong ETag. PATCH requires If-Match, fingerprints the expected immutable instance/revision, and checks under the existing organisation/group locks. Historical replay precedes this check. Noops preserve revisions; lifecycle and raw row changes advance them. Individual assignment windows require their own identity/revision treatment because their composite key can be reused after removal; this slice does not version them through unrelated group configuration changes.

### Implemented slice: group assignment identity prerequisite

Migration 0023 gives each group assignment an immutable UUID and server-controlled revision while retaining composite pair uniqueness and tenant foreign keys. Populated-row migration and update/recreation regressions prove existing windows are preserved and a recreated pair gets a new instance. Mutation responses and audit snapshots include these fields. HTTP creation/replacement preconditions and an exact stored-assignment read are now implemented; no replacement-precondition gaps and no replay contracts remain. This avoids using a parent group revision for unrelated assignment edits.

### Implemented slice: conditional assignment writes

getGroupMember returns only stored assignment fields and a strong instance/revision ETag, with org:read/platform:read isolation. PUT requires expected absence (If-None-Match: *) or the current assignment ETag (If-Match). The shared SSO/assignment header parser rejects missing/conflicting/malformed conditions. Expected state is fingerprinted, then checked under existing organisation/group/assignment locks after committed replay. Noops preserve revisions. Tests cover stale replacements, competing creation/update commands, historical recovery and rejected old tags after deletion/recreation. This completes this HTTP precondition, not tenant write delegation or full policy locking.

### Implemented slice: entitlement command recovery

All five entitlement mutations require keys and use platform journal recovery. Scope-set and date normalisation feeds the fingerprint; current authority precedes replay. New-key unchanged state records noop and preserves timestamps. Row locks protect allowlisted before/after audit snapshots, and failures roll back the mutation and journal reservation. Tests cover historical results after removal, changed-input conflicts, noops, concurrent retry, audit rollback and authority revoked after middleware. Entitlement PATCH revisions are now implemented; global user command recovery is now implemented. Capability ceilings/pairs and common policy writer ordering are separate unfinished requirements.

### Implemented slice: entitlement revisions

Migration 0024 reuses the database configuration revision guard. GET supplies the stored-row ETag and PATCH fingerprints its required If-Match expectation. The journal recovers committed results before checking the old revision under existing locks. Noops preserve revisions; lifecycle/raw changes invalidate tags, and old instance tags fail against recreated principal/target grants. The route contract inventory now has no missing replacement preconditions and no missing replay contracts. Full tenant contexts/RLS, capability/pair policy and common writer ordering remain unfinished.

### Implemented slice: global session recovery

Both session revocation commands now require keys and recheck platform:users inside the platform journal. Historical recovery does not revoke later sessions. Audit records actual deleted session IDs and token counts without credentials; revoke-all targets the user. Empty new commands record noop, while token-only effects count as applied. Tests cover historical recovery, later-session preservation, erased-user recovery, rollback, concurrent retry and users-only authority revoked after middleware. Global user commands now have recovery contracts. The later migration 0044 update records exact local token effects for these commands. Tenant grant isolation and production issuance/revocation ordering remain unfinished.

### Implemented slice: global user recovery

The four user commands now use the platform journal, preserving platform:users for lifecycle and platform:write for erasure. New-key lifecycle noops preserve timestamps; inert/retired-email blockers remain. New facts use limited UUID/lifecycle snapshots instead of names/email copies, with actual deleted session IDs and token counts on disable. Historical profile responses are encrypted for seven days and can survive operational erasure; old audit facts and backup/key lifecycle still require the explicit retention decision before release. Tests cover historical recovery after erasure, noops, rollback/concurrency, audit redaction and both authority requirements revoked before replay. The current 46-mutation inventory has no missing replay or replacement-precondition contracts. This is not completion of F0–F7: complete cascade/token effects, tenant contexts/RLS, capability/pair policy, fresh-authentication and operational release proof remain unfinished.

### Implemented slice: own receipt inspection

getMyOperationStatus adds an actor-bound, reference-only read for callers with current administrative authority in the receipt's platform or tenant. The actor is derived from the authenticated principal; foreign actors and unsupported contexts are never returned. Users-only callers can inspect their own results without platform audit access. Current authority is checked inside the read transaction; mutation replay still requires its original command scope. Tests cover user/client/root identity, tenant scope mismatch, no grants, revoked membership, private field exclusion and the unchanged audit-wide boundary. The OpenAPI metadata lists shared scope alternatives rather than falsely requiring a fixed read scope on self routes. Full reader contexts/RLS and concurrent grant-writer ordering remain unfinished.

**Targeted tenant RLS.** Migration 0025 protects groups, group_members and entitlements using transaction-local tenant/platform read/write scopes. Missing scope denies access; broker grants are limited to the authenticated user's membership organisations, and root-lock checks to the immutable platform organisation. Bootstrap uses its established tenant UUID. Broker savepoints restore enclosing scope. Runtime startup rejects disabled RLS. See [the isolation inventory](../reports/answerable-id-isolation-inventory.md) for exclusions and rollout limits. Query context types, access ceilings and production migration rehearsal remain unfinished.

**Implemented administrative lock bound:** all journalled commands set a transaction-local two-second limit per database lock wait before current-authority checks. A PostgreSQL lock timeout returns `503 database_busy`, `retryable: true` and `Retry-After: 1`; the failed transaction leaves no new receipt or committed mutation/audit effects. Retry with the same key and input. This also applies before replay, so blocked authority can produce this response before duplicate detection. Connection acquisition and total request duration are separate bounds. Complete writer ordering and operational load/recovery proof remain unfinished.

**Implemented resource ownership boundary:** migration 0026 adds immutable classification/owner, shape checks and owner-erasure restriction. Existing global resources are explicitly backfilled as platform-shared; private administrative creation is supported and ownership PATCH is rejected. Private entitlement assignments cannot cross tenants, including raw SQL writes. Machine issuance checks the locked resource against the authenticated owner tenant before provider issuance. New responses and audit snapshots include classification/owner. This does not complete capability ceilings, exact-pair user policy or user-grant contexts; production inventory review/cutover proof remain required.

**Implemented exact-pair groundwork:** migration 0027 permits explicit client/resource pairs. Creation, audit snapshots, member projections and target access preserve both fields. Client-only and resource-only access queries exclude pairs; pair scopes cannot satisfy direct admin grants or root-lockout writer detection. Existing resource-only rows remain for migration. Capability ceilings, approved non-admin resource-only conversion and the common user-grant evaluator are not yet implemented; user OAuth remains closed. Quiesce old readers before introducing pair rows.

**Implemented active-user prerequisite:** effective grant, target access and platform-writer queries exclude inert/disabled users. The root HTTP test proves disabling the sole effective writer restores root availability and re-enabling them locks root. Existing session and command-authority checks remain in place. This does not complete concurrent writer ordering or the capability/user-grant evaluator.

**Implemented machine capability slice:** migration 0028 adds platform-only capability writes, tenant reads, immutable target/kind, scope/window checks, ownership guards and revisions. Three platform commands create/update/remove machine ceilings through journalled contexts; there are now 49 administrative mutations. Machine issuance intersects requested/default scopes with client/resource/capability ceilings and audits its approving capability. Online admin principal and command checks use the same effective-capability query. Capability writers share the organisation policy barrier with issuance. User/direct-session capability evaluation, tenant delegation, complete common policy/effect evidence and release rehearsal remain unfinished. Existing production machines require reviewed explicit approvals; no automatic backfill widens authority.

**Implemented direct administrator ceiling:** direct session grants and platform-writer detection intersect resource-only assignments, an effective `admin_session` capability and the enabled resource vocabulary. Pair and machine permissions cannot substitute. Bootstrap seeds only the immutable bound platform ceiling and preserves existing restrictions; migration 0029 prevents its deletion. Tenant ceilings require explicit platform approval. Existing journalled capability commands support this grant kind. Capability changes cannot remove the currently effective last platform writer; complete authority-writer protection, future expiry handling and shared user-grant evaluation remain unfinished. See the [remaining execution plan](../reports/answerable-id-foundation-execution-plan.md#direct-administrator-capability-enforcement) for evidence and release order.

**Implemented administrative exhaustion responses:** configured database checkout timeouts return retryable 503 database_busy, while provider session lookup internal failures return authentication_unavailable without inferring their cause. Both carry Retry-After: 1. Deterministic real-pool tests prove denial before a new command receipt/mutation and same-key recovery after capacity returns. Total request budgets, production capacity proof and secret-safe provider diagnostic logging remain unfinished.

**Implemented diagnostic payload boundary:** the production Better Auth configuration uses its supported logger hook to retain only fixed provider_diagnostic events and severity. Free-form messages and arguments are discarded rather than heuristically scrubbed. The unexpected HTTP error handler retains only request ID and a fixed event. A real session-lookup outage and configured logger test prove the payload boundary. This is not completion of operational monitoring, request metadata policy or durable denial/grant audit coverage.

**Partial user provider proof:** [native endpoint integration tests](../apps/id/src/auth/user-grant-boundary.integration.test.ts) demonstrate code/refresh context persistence, membership preflight before cached refresh replay, and transaction rollback for Basic-authenticated issuance. They are not production enforcement. The installed provider's filtered offline_access/rotation interaction, assertion-marker persistence, denied-grant revocation effects and tenant-local refresh family boundaries remain prerequisites. User grants stay closed until those are resolved and the complete capability/pair policy is wired. See [the execution report](../reports/answerable-id-foundation-execution-plan.md#user-oauth-provider-boundary-proof).

**Implemented native authentication helper:** [withNativeClientAuthentication](../apps/id/src/auth/native-client-authentication.ts) commits provider authentication before the grant transaction and permits the native verifier to acknowledge its exact committed assertion marker once within the same callback. Signatures and current client configuration are still verified by the provider. The binding expires at callback completion and rejects nested authentication transactions. [Native user-grant tests](../apps/id/src/auth/user-grant-boundary.integration.test.ts) cover private-key replay/rollback/concurrency, key removal, public-client PKCE and a one-connection pool. This closes the assertion-consumption integration gap in the proof; production user grants, persistent denial effects, tenant-local families and complete scope/policy enforcement remain unfinished.

**Partial code-replay denial proof:** the native JWT integration test now commits pre-issuance authorisation-code rejection cleanup before returning `invalid_grant`, while rejecting issuance errors inside the transaction. It proves affected refresh tokens stop working and another grant survives. The boundary is test-only and configuration-specific; provider-swallowed cleanup failures, tenant-local refresh families and full production grant/audit enforcement remain unresolved. See the [execution evidence](../reports/answerable-id-foundation-execution-plan.md#authorisation-code-replay-revocation-proof).

**Partial cleanup failure proof:** the [native cleanup guard](../apps/id/src/auth/native-token-cleanup.ts) detects deletion failures even when the provider catches them. The native JWT proof returns retryable 503 and restores partial deletion on both adapter and PostgreSQL errors; retry completes cleanup. A durable revocation barrier and tenant-local refresh families are still required, and production user grants remain closed.

**Partial persistent grant context:** migration 0030 adds immutable tenant/membership/user/client/resource and authentication provenance plus irreversible revocation. The native provider proof carries this context UUID through code/refresh state and checks it before cached replay. One user's A/B grants are independently revocable. Production creation, lifecycle writer revocation, native family scoping, complete evaluator, concurrency ordering and broker isolation remain unfinished; see [execution evidence](../reports/answerable-id-foundation-execution-plan.md#persistent-tenant-bound-grant-context).

**Partial native family invalidation:** the [refresh boundary](../apps/id/src/auth/native-refresh-family.ts) narrows the provider's client/user family to a grant UUID. In the proof, revocation commits even when cleanup fails and token rows are restored; later refresh is denied before claims. Same-user tenant B remains usable after A's reuse. Production wiring, complete writer ordering, code-replay context revocation, audit and bounded cleanup remain unfinished.

**Implemented member-context revocation:** member removal updates only that tenant/member's unrevoked contexts and audits returned IDs atomically with membership and assignment effects. Reinstatement does not revive them. Service and native refresh proofs cover same-user tenant isolation, cached/rotated tokens and audit rollback. Other lifecycle writers, full concurrency ordering, cleanup and production user-grant policy remain unfinished.

**Implemented global user-context lifecycle:** disable irreversibly revokes that user's contexts across tenants; reconciliation of an already-disabled user still counts remaining context effects. Erasure explicitly captures deleted context IDs, tenants and affected users, including the owned-client cascade. Native refresh and service tests cover re-enable, other-user preservation and audit rollback. Other lifecycle writers, indirect history indexing, concurrency and full production user-grant policy remain unfinished.

**Implemented indirect erasure history:** migration 0031 links recorded grant-erasure effects to affected users, including qualifying existing global events. Service, populated-migration, runtime-role and HTTP tests cover retention, deduplication and tenant privacy. Missing historical IDs are not inferred. Other cascade effects and tenant-specific projections of global events remain incomplete.

**Implemented organisation-context lifecycle:** disable irreversibly revokes its tenant-bound contexts; erasure captures deleted context/user IDs. Service/native tests prove rollback, reconciliation, no revival after re-enable, and B refresh while client-owner A is disabled. Native scenarios now run with independent fixtures. Client/resource/session writers, complete audit indexing, concurrency and production user-grant policy remain unfinished.

**Implemented administrative session-context revocation:** single-session and revoke-all commands now include persistent contexts with actual audit IDs and atomic rollback. Revoke-all remains effective after the original browser session disappears. Native tests cover cached and rotated tokens across tenant grants; other-session isolation is tested at the service boundary. Normal logout remains distinct. Client/resource writers and the broader policy/audit/concurrency gates remain incomplete.

**Resource context lifecycle:** disabling a resource now irreversibly revokes its stored contexts across tenants; repeated disable reconciles remaining contexts, and erasure captures actual deleted context/tenant/user IDs in platform audit evidence before cascade. Resource changes and effects share one transaction; enable does not restore contexts. Other resources and global browser sessions are preserved. Production user OAuth, client-context lifecycle, complete affected-subject indexing and concurrency/release proof remain unfinished.

**Client disable/erasure context effects:** immutable client-instance queries now revoke/delete stored contexts across tenants, include remaining effects in repeated-disable applied/no-op semantics, and never restore contexts on enable. The owner-scoped lifecycle event links a separate platform-only manifest of context/tenant/user IDs; both commit atomically. Seven service cases and the native refresh case cover tenant visibility, rollback and cross-tenant scope. Credential/configuration semantics, broader affected-subject indexing, concurrency, bounded effects and production user-grant policy remain incomplete.

**Lifecycle affected-user history:** migration 0032 extends the existing audit-subject capture/backfill for successful version-one client grant-effect, resource disable/erasure and organisation disable/erasure contracts. Only explicit recorded user IDs are indexed, with existing event scope preserved. Records remain queryable after operational erasure; named identity recovery is not implied. Malformed contracts are excluded and repeated backfill does not rewrite events. Full audit/policy evidence and broader release gates remain incomplete.

**Shared-secret rotation:** a new rotation now atomically revokes stored client tokens and immutable user-grant contexts across tenants, with the existing platform-only effect manifest and owner event link. Replaying its idempotency key recovers the original secret without revoking later grants. Other clients and browser sessions survive; a fresh authorisation with the new credential is required for a new user grant. Broader configuration/JWK policy, code-replay invalidation, concurrent writer proof and production user OAuth remain incomplete.

**Native code replay:** migration 0033 adds a unique one-time code-hash binding to the existing grant context. The native proof commits it during successful issuance, then revokes that context on code replay even without token rows. Cleanup failure preserves revocation while restoring partial deletions; foreign-client cleanup is scoped out. Raw codes are not stored. Production user OAuth stays closed; unbound legacy contexts, final policy/denial semantics and complete concurrency/isolation/operational gates still need review and implementation.

**User-resource policy proof:** native issuance and cached-refresh preflight now evaluate login admission, exact pair assignment/capability, renewal permission and original scope ceilings from the stored grant context. One scoped SQL statement evaluates windows and returns source/revision evidence, including group membership. Production machines share the scope intersection helper. User OAuth and user-capability management remain closed; full concurrent-writer ordering, scope integration, production decision audit, common admin/access projection adoption and operational release proof remain incomplete. Query entry points still accept raw executors under trusted callers.

### User-capability administration

Platform capability commands now support client-only `authorization_code` login ceilings, exact client/resource `authorization_code` ceilings and exact-pair `refresh_token` ceilings. User approvals validate registered grant kinds and client scopes, separate identity from resource scopes, and retain private-resource ownership checks. Shared user clients may be approved independently in multiple tenants; machine ownership rules are unchanged. Existing journal, revision and audit paths are reused. Suspension/removal remain available after registration narrows. The native integration fixture now creates its approvals through the production service. No migration is required; OpenAPI is regenerated. Public user OAuth remains closed; production context creation, final scope integration, common decisions, concurrency and release evidence remain unfinished.

### Native grant provenance creation

The native user proof now calls `auth/create-resource-grant.ts` with the callback's actual user, session and requested scopes. It no longer reads a fixture-selected session or stores the full registered scope maximum. One insert-select derives tenant, client/resource instances and authentication time from current stored rows, validates effective membership/active identities/session expiry/client grant support/resource ownership and scope containment, and uses database time with a server-supplied lifetime. The existing immutable-context trigger still guards insertion. The restricted-runtime test uses the same creation function. No migration is added.

This is a provenance boundary, not authentication by possession of IDs. Public user OAuth remains closed. The native fixture still selects a member and supplies client/resource request context; signed request continuation, real tenant selection/consent, production policy integration, writer ordering and grant-creation audit/retention remain unfinished. First-principles review removed the redundant supplied organisation and three fixture lookups rather than adding another context store. Focused native tests pass 41 tests / 2,192 assertions; the restricted-runtime suite passes 10 tests / 132 assertions.

Grant creation now also requires the exact client/resource compatibility link in its insert-select. A restricted-role regression first reproduced context insertion after unlinking; it now denies without adding a row. This closes a creation prerequisite, not a demonstrated token bypass: issuance already checked the link. The native creation tests separately exercise expired membership/session, disabled user/tenant/client/resource, unsupported client grant type, missing client scopes and foreign private resources. First-principles review added one join to the existing statement; no separate preflight query, cache, permission store or migration. Concurrent unlink/creation ordering and trusted production request/consent integration remain incomplete.

### Native tenant policy locking

`auth/lock-resource-grant-policy.ts` acquires a shared lock on the organisation derived from the stored grant and authenticated client, using the active native adapter transaction. User code issuance takes it before evaluating policy; refresh takes it before the grant-family row lock and before cached-response admission. Existing tenant policy writers take the organisation exclusively. Lock waits are bounded to two seconds, with the same OAuth 503/Retry-After mapping as production machines through `auth/grant-error.ts`. This is a per-lock bound, not a total request deadline.

The initial native race reproduced capability removal committing while an already-approved issuance was paused. Tests now cover issuance-first for code and refresh, revocation-first for code, retryable contention without code consumption, and the actual restricted-role adapter. First-principles review reused the existing tenant row and extracted the previous machine error mapping; no lock table, distributed lock, queue or schema change. Global user/client/resource dependencies, creation/unlink ordering, other writer race scenarios, production user OAuth and complete operational release proof remain unfinished.

### Native client/resource policy locking

The tenant lock helper is now `auth/lock-resource-grant-policy.ts`. It locks organisation → client → resource in the native transaction before the refresh-family lock. Existing client/resource configuration services lock those same target rows, so scope changes, unlinking and disable cannot commit while an approved issuance is paused. The callback avoids repeating locks already acquired by refresh preflight. Global user ordering, creation races and other writer/credential semantics remain unfinished.

Six initial code/refresh races reproduced scope changes and unlinking committing before issuance. The expanded tests cover capability changes, client/resource scope changes, unlink and client/resource disable: issuance-first for code and refresh, writer-first for code, plus existing timeout recovery and restricted-runtime coverage. Disabled clients fail later authentication with 401; other removed grant authority returns 400 on refresh. First-principles review extends the existing lock order rather than adding locks on individual assignments or new state, and removes redundant refresh lock queries. No migration or public API change.

### Global user ordering for native grants

The grant lock helper now reads immutable context/owner IDs, locks the grant subject and any client-owner user in UUID order, then organisation → client → resource, before the grant-family row. Global user lifecycle services already take an exclusive user lock. This prevents user disable from committing between an approved policy read and code binding, and prevents erasure of another user's client from deleting the context while its grant transaction holds the client. Current policy is read after all locks.

The initial global-disable race completed disable before the paused issuance, which subsequently failed code binding. Native tests now cover both global-disable commit orders and user-lock timeout/retry. The separate client-owner erasure test uses the native adapter transaction and performs the real code-binding write before committing; it is a lock/cascade proof, not a complete user-owned-client OAuth flow. It verifies actual context/client deletion after release. Focused tests pass 4 tests / 156 assertions. First-principles review removed the redundant organisation join from identity discovery; no new state, migration or lock service. Grant creation, session/SSO writers, remaining credential and multi-operation races, production consent/tenant selection and release proof remain unfinished.

### Administrative session revocation races

The existing single-session and all-session services already take the user lock before session/token/context changes. The native proof now tests those commands and global disable against code and refresh issuance, plus writer-first code issuance. All nine race cases pass (378 assertions), and subsequent refresh is denied. No additional production lock or service change was needed.

A native sign-out test separately deletes the browser session, preserves the context's authentication origin, and successfully exercises cached and fresh refresh. Later administrative all-session revocation reports zero deleted sessions but a real change, and denies both refresh paths before claims run (61 assertions). This is not a complete production user OAuth flow; it proves the existing browser-session/delegation distinction. First-principles review retained the shared user barrier and avoided coupling grant validity to a live browser session. Creation, SSO/credential and multi-operation races, full audit guarantees and production release gates remain unfinished.

## Grant creation and concurrent unlink

The creation regression reproduced an insertion after a concurrent unlink committed: the insertion statement had read the old compatibility link before waiting on the provenance trigger. Creation now owns a transaction (a savepoint when already inside one), resolves immutable ownership and acquires the same sorted user → organisation → client → resource locks as issuance. The insertion then reads admission state in a new statement after those locks. Both callers share `auth/lock-resource-grant-policy.ts`; no new state or migration is required.

Separate-connection tests observe actual PostgreSQL blocking in both commit orders for unlink, membership removal, user/tenant disable, single/all-session revocation, client/resource disable and client-owner erasure. Writer-first denies creation without a context row. Creation-first commits the context, then the writer revokes it (or deletes it through owner erasure); unlink leaves it stored but current policy denies it. These are stored-context and policy-decision proofs, not a complete production OAuth flow. The owner-erasure case creates a user-owned client for provenance/cascade testing and does not claim complete token admission for that client. The expanded matrix passes 18 tests and 567 assertions. Remaining configuration/SSO/credential and multi-operation races, complete audit/isolation, production consent/tenant selection and release rehearsal are still unfinished.

First-principles review keeps the existing row locks and insert-select, shares their ordering instead of duplicating it, and removes a redundant timeout-setting call. The database provenance trigger remains defence in depth; raw inserts are not a supported authentication boundary.

## Secret rotation during native issuance

The native proof covers code and refresh issuance on both sides of rotation. Issuance-first holds the client policy lock until commit; rotation then revokes the resulting grant, and the new credential cannot refresh it. Rotation after initial client authentication denies before claims: code redemption returns `401 invalid_client`; refresh preflight returns `400 invalid_grant` for its revoked context. Refresh rows are retained with revocation timestamps. A fresh code obtained after rotation can redeem with the new secret. These four cases pass 195 assertions; existing production code was sufficient.

This is not complete SSO invalidation. `services/sso-providers.ts` currently changes/deletes provider configuration without invalidating authentication or grant contexts. Migration 0034 now records immutable tenant/provider authentication origin on native SSO sessions; admission and invalidation based on that origin remain unfinished. Deleting all sessions of a user shared by A and B would break tenant isolation, while revoking existing A grants alone would leave stale authentication available for creating new grants. Production admission must bind trusted authentication origin and enforce the provider-change boundary; a mutable selected organisation or possession of IDs is insufficient. Provider/credential and complete production OAuth release gates remain open.

## Native SSO session origin

Migration 0034 adds nullable `authentication_organization_id`, `authentication_provider_id` and `authentication_provider_revision` to sessions. All three must be absent or complete, with a positive revision. Attributed inserts must match the current provider; updates cannot change session identity, user, creation time or origin. Provider deletion preserves this historical origin. Existing sessions remain unattributed; no backfill guesses their source.

`auth/sso-origin.ts` captures current stored provider identity inside the supported native SSO transaction and passes it once, through that transaction's adapter, to the session before-create hook. Transaction validation precedes identity writes. Native SSO callback creation fails closed if origin is missing. Fields are server-controlled and hidden from the session response. Rejected resolution, missing providers, caller-supplied origin, write failure and immutable updates have regression coverage; a failed native session insert rolls back new user/account/session rows.

Session origin capture does not yet enforce tenant admission. The SSO configuration service now irreversibly revokes existing tenant grant contexts on creation, replacement and deletion, with exact affected IDs in the same audit transaction. Unchanged configuration preserves grants. Other tenants and the global browser session remain intact; restoring configuration does not restore revoked grants. Stale browser authentication can still create new contexts until the admission check is implemented. Provider revision is historical configuration evidence, not an enforced authentication epoch. Migration 0035 preserves revision and timestamp for the native provider’s same-value row-lock update; actual stored configuration changes still advance revision. Native discovery may separately change real configuration. Finish the semantic change boundary and trusted grant admission before opening user OAuth. Tenant-specific SSO versus cross-membership authentication policy remains to be settled before enforcing that boundary.

**SSO callback configuration changes.** The HTTP handler keeps the first provider revision read by the native adapter in request-local memory. Native resolution compares it with the locked provider before identity writes. Missing evidence or a changed revision returns `SSO_PROVIDER_CHANGED`; callers start sign-in again. This includes client-secret changes omitted by the native provider fingerprint. No secret, additional epoch or persistent request record is stored. Direct internal callback invocation without that HTTP evidence fails closed. Restricted-login tests cover secret rotation, deletion, record replacement, unchanged configuration and overlapping callbacks. Callback-first update/delete/recreate tests prove the writer waits until the native transaction commits. The browser session then survives with its original provider identity/revision, even if that provider has been deleted or replaced. This historical origin does not establish current tenant authority; checking it on new admission remains unfinished.

**Process interruption evidence.** The real HTTP app now has separate-process SIGKILL/restart tests for organisation creation, client-secret rotation and organisation erasure with member/group cascades, using the restricted database login and a test root principal. Before-commit termination preserves original target state with no committed command success/receipt; a fresh process may execute the same key. After-commit termination loses the HTTP response but a fresh process recovers the original encrypted result without repeating effects. In both rotation cases, the restarted service rejects the old credential, issues a machine token using the recovered secret and accepts that token for an administrative read. Independently recorded root-attempt events are not command success facts and need not roll back. This is local process proof for those routes; production recovery keys, backup restoration and operational readiness still need rehearsal.

**Access query contexts.** Member and target access projections now require existing issued tenant contexts at the query entry point; raw executors and independently supplied tenant UUIDs are no longer accepted. Offboarding captures its prior assignment projection through the existing member command context in the same transaction. Restricted-login tests cover one user's separate A/B memberships, while context tests reject copied, expired and wrong-purpose readers. No public API, schema or RLS policy change. These remain assignment projections; complete policy explanations and other query boundaries are unfinished.

**Membership query contexts.** Administrative membership queries now require existing issued tenant contexts, including configuration reads and all membership/assignment mutations. Configuration reads take a row lock only in a command context; callers no longer supply a lock flag. Group/entitlement assignment checks use a separate platform-write lookup returning only membership status. Copied, expired and wrong-purpose contexts are rejected; actual restricted-login tests cover a shared user's A/B memberships and foreign-write rejection. Native federation and the membership table's RLS exclusion are unchanged. No schema or public API change; other query boundaries and complete policy explanations remain unfinished.

**Group and entitlement query contexts.** Administrative group/assignment and entitlement queries now require existing tenant or platform contexts; fleet-wide entitlement queries require platform-read authority. Read callers cannot supply a tenant UUID or locking flag. Command lookups take the row lock. Bootstrap keeps its two system inserts inside its existing scoped transaction; the old test-only group-member insert helper is no longer production code. Restricted-login tests prove A/B reads and reject tenant writes. No schema, RLS policy, permission semantics or public API change. Other query families and complete release proof remain unfinished.

**Audit reader contexts.** Platform, user-history and tenant-history queries now require existing issued contexts at the query entry point. Tenant history always derives organisation from the context, including after live-row erasure; caller filters cannot select another tenant. The raw operation-status projection is private to its authorising service. Restricted-login tests retain A/B/global UUID-linked facts after deleting the live user and tenant rows, with tenant and platform visibility kept distinct. Audit writes remain a transaction-level primitive for commands, bootstrap and protocol callbacks. No RLS, schema or public API change; named recovery and complete audit/release guarantees remain unfinished.

## Summary query boundary and exact target reporting

Fleet and organisation summary query entry points now require the existing platform-read and tenant-directory contexts respectively. Sign-in statistics expose separate guarded fleet/tenant functions over one private SQL query. Raw handles, copied/expired contexts and wrong-purpose contexts are rejected. Services pass these contexts through without redundant checks. No new registry, schema or RLS policy is introduced.

Organisation summaries previously grouped by client and resource in SQL but discarded the resource in the response whenever a client was present. Exact pairs now report `kind: client_resource`, client `id` and `resource`; standalone entries retain their existing shape. The route schema and generated OpenAPI contract match. These are stored assignment counts, including disabled/out-of-window rows, not an effective-access decision. Query, HTTP and restricted-login A/B tests cover the boundary and projection. Full gate results belong to the current execution evidence. Other query/broker boundaries, common policy explanations and the production user OAuth release gate remain unfinished.

## Domain query authority and tenant-local routing checks

All six production domain query entry points now require issued contexts. Domain create/status/delete and the locked command lookup require platform-write context; domain listing requires tenant-directory context; sign-in diagnosis uses a tenant-member-access context to ask only whether that tenant accepts the domain. The query predicates include the context's organisation, active domain and active organisation, with existing domain normalisation. It returns a boolean, never a foreign organisation ID or slug.

The previous global `findOrganizationByDomain` lookup had only one production caller: diagnosis loaded a foreign route and then suppressed it in the response. That lookup is deleted. The unlocked domain-detail helper was only needed by test fixtures and is also removed from production; fixture assertions inspect rows directly. The command lookup always locks, so no caller-controlled lock flag or shared raw lookup is needed. Existing context factories, transactions and constraints suffice; no schema, RLS, federation routing or public API change is introduced.

Direct query tests exercise raw/copied/expired/wrong-purpose contexts. Restricted-login A/B tests verify independent domain lists and routing booleans, foreign-ID write refusal and disabled-domain denial. Existing HTTP diagnosis tests retain their foreign-user/routing redaction and current-authority checks. This closes the domain query family only. Other query/broker boundaries, complete policy explanations and the remaining F0–F7 release gates remain open.

## SSO configuration query authority and projections

All seven database-facing SSO query entry points now require issued contexts. Create/update/delete and full configuration lookup require platform-write context; the lookup always locks. Tenant directory reads return the existing redacted provider shape inside the query boundary. Member-access diagnosis selects only the issuer. Platform connectivity reads select only the issuer and JSON discovery endpoint; the external probe still runs after the read transaction closes. Copied, expired, raw and differently scoped contexts are rejected.

The exported raw full-configuration reader and caller-controlled lock flag are removed. The private shared row query supports the command and directory paths; directory redaction still occurs in the server process after the database read, so this is not a database column-privilege claim. Diagnosis and connectivity no longer load the full secret-bearing configuration. The existing serialiser/redactor and context factories are reused, and duplicate read-service guards are removed. No new registry, schema, RLS policy, public response shape or federation callback behaviour is introduced. The SSO provider table remains available to the native authentication adapter; this does not complete broker isolation or the pending tenant SSO trust contract.

Regression evidence: `/private/tmp/id-sso-query-before.log` reproduces raw configuration query acceptance. Focused tests pass 111/111 with 1,041 assertions over seven files (`/private/tmp/id-sso-query-focused.log`, 13.21 seconds). Coverage includes context purpose/lifetime/provenance, absent/legacy-null configuration, redaction, A/B projections under the restricted login, authorised command secret access, HTTP SSO lifecycle and the existing probe transaction-lifetime regression. Full gate results are recorded in the execution evidence after completion.

## Organisation query authority and internal locking

All ten administrative organisation query entry points require issued contexts. Fleet listing requires platform-read; detail derives its tenant from directory context; diagnosis selects only id/slug/status under member-access context; retained history checks only existence under history context. Create/update/status/delete/client-count/locked lookup require platform-write. Raw, copied, expired and wrong-purpose contexts are rejected. History can check an erased organisation without granting live directory access.

The exported raw organisation reader is deleted. The low-level lock moved to `db/organization-lock.ts` as an explicit internal primitive for tenant authority establishment, operation-status authority checks and protocol transactions. These callers cannot require an already-issued context before they establish authority. Administrative services use the guarded command lookup. The primitive still accepts an executor and is not a standalone authorisation boundary. Native machine claim construction now selects only status and authorisation version from its authenticated adapter transaction; its preceding lock protocol is unchanged.

Client creation now acquires the organisation command lock before checking ownership existence. A barrier test proves it waits for concurrent erasure, then returns 404/not_found after erasure commits, without client or audit effects. This prevents a stale non-locking existence check from reaching a foreign-key failure instead of the documented missing-owner result. Other organisation command consumers retain their lock order.

First-principles review reuses the existing contexts and lock implementation, removes the raw directory reader and duplicate service/helper guards, and narrows diagnosis/history/machine reads. No new context registry, schema, RLS policy, public API or deployment is introduced. Remaining query/broker boundaries, complete policy explanations, production user OAuth and F0–F7 operational proof remain unfinished.

## Global user and session query authority

All eleven database-facing user/session query entry points now require issued platform contexts. Global user list/detail/existence and session list require platform-read. Status changes, email retirement, session lookup/deletion and user-session ID deletion require platform-users. User erasure requires platform-write. The shared user row lock accepts only the two issued command contexts that use it; permission to lock does not authorise the other command family's mutations. Tenant contexts, raw executors, copied contexts and expired contexts are rejected.

The unused production `findUserByEmail` helper is deleted; tests needing it compose the existing platform directory queries in one authorised transaction. The unused count-only `deleteUserSessions` wrapper is deleted. `deleteUserSessionIds` now accepts one user ID, matching every production caller, and returns exact sorted affected session IDs. There is no unneeded bulk array interface or empty-array bypass. Global revocation remains explicitly global; tenant membership removal is a different operation. Existing lifecycle effects, audit facts and lock ordering are preserved.

First-principles review reuses the existing context registries and removes duplicate read-service guards and unused helpers. No broker exception is needed for these query modules: their only non-administrative import is the pure retired-email formatting helper. Tables and native authentication adapter access remain unchanged; context guards constrain application query calls, not arbitrary runtime SQL. No schema, RLS, public API or deployment change.

Evidence: `/private/tmp/id-user-session-query-before.log` reproduces raw user and session listing. Focused tests pass 143/143 with 1,154 assertions (`/private/tmp/id-user-session-query-focused.log`, 13.15 seconds). Direct tests cover all eleven entry points, including null/raw/copy/expiry and wrong platform/tenant purposes. Restricted-login proof shows both memberships in a legitimately global user view, omits upstream/session credentials, rejects foreign-user session IDs and preserves another user's sessions during global revocation. Full gate results are recorded below after completion. Remaining query/broker boundaries, common policy explanations, production user OAuth and F0–F7 operational proof stay open.

## Resource query authority and private-target concealment

All twelve resource query entry points now require issued contexts. Fleet listing, resource detail and linked-client listing require platform-read; policy validation and mutation/erasure checks require platform-write; tenant target-access lookup requires directory context and selects only an ID. The tenant query filters shared resources or resources owned by the context's organisation. A foreign private resource and a missing resource both produce 404 for tenant target-access requests, including exact client/resource pairs. Platform readers querying a tenant's access follow the same target boundary; their separate fleet inventory still includes all resources.

The initial HTTP regression reproduced 200 for a foreign private resource, disclosing existence (`/private/tmp/id-resource-query-before.log`). It also reproduced raw resource listing. The unrestricted resource finder is deleted. Protocol locking is an explicit internal primitive in `db/resource-lock.ts`; machine issuance audit selects only resource identity/revision/scope fields within its already-locked authenticated transaction. Administrative readers/commands use guarded shared/exclusive lookup functions. Capability and entitlement scope validation keep the resource locked while checking its ceiling. Existing serialisation and lifecycle locks remain; no new registry, schema or RLS policy is introduced.

Focused tests: 137 pass, zero fail, 1,213 assertions across eight files (`/private/tmp/id-resource-query-focused.log`, 13.55 seconds). They cover all twelve query entry points, context provenance/purpose/lifetime, restricted-login shared/A-private/B-private projections, complete platform inventory and HTTP foreign/missing/own/shared targets through both tenant and platform readers. The route's not_found description and public guide are updated; OpenAPI is regenerated through the exporter. A nullable assertion type error in the test was resolved with separate visible/hidden assertions. Full gate results are recorded after completion.

First-principles review removes the global existence lookup from tenant access and the exported raw resource reader, keeps the necessary native lock primitive and narrows audit projection. OAuth client queries and remaining broker/policy/admission/operational work are still open; this slice does not claim completion of F0–F7.

**Client query implementation:** issued contexts now protect all fourteen client query entry points. Read/policy projections exclude secret digests in SQL; tenant existence checks reveal only registered IDs, preserving shared-client semantics. Commands use the existing locked full row. Principal resolution and native protocol locking remain explicit internal primitives, not proof of complete broker isolation. The execution report records regression and restricted-login evidence. No schema, RLS or public API change was required.

**Revocation query boundary.** Token revocation requires existing issued command contexts: platform-users for one user/session, platform-write for one client or organisation's machine tokens. Member grant revocation derives its organisation from tenant-member context. Raw/copied/expired/wrong-purpose contexts are rejected. Restricted-login tests preserve other members/tenants and distinguish organisation machine tokens from global user/client/session scope. Unused batch-token support is removed. Remaining grant/broker isolation and operational release proof are unfinished; no public API or schema change.

**Administrative grant mutation boundary.** All ten grant-context query exports require issued commands: tenant-member for local member revocation, platform-users for global user/session revocation, and platform-write for user erasure and organisation/resource/client effects. The unused partial-policy reader is deleted; native expiry tests use the actual user-resource evaluator and lifecycle tests inspect stored effects. Existing locks, transactions and audit effects remain. Remaining broker/admission and operational release work is unfinished.

**Human command policy locks.** Administrative grant reads lock the user's discovered membership organisations in UUID order and the admin resource before evaluating authority. Capability/resource revocation is ordered with admitted human commands; newly discovered outside memberships cannot supply unlocked authority during the same lookup. Session expiry is rechecked using database time after policy waits. Deadlock victims return retryable `503 database_busy`; use the same key/input. Real concurrency tests prove both commit orders, expiry denial and same-key recovery without duplicate effects. Machine/root races, remaining policy/admission and operational release requirements are unfinished.

**Machine command policy locks and expiry.** Principal lookup locks immutable owner organisation, client and resource before reading current authority, within one transaction/savepoint. Service-issued-token tests prove both commit orders for capability/resource/client-disable changes. Admin bearer verification requires `exp`; its verified expiry is retained and checked after principal lookup and before command admission/replay. Expiry during a policy wait returns `invalid_token`; retry with fresh credentials and the original key/input. No new cache, schema or RLS policy. Root ordering and remaining F0–F7 requirements remain open.

**Root admission ordering.** The writer-presence check locks the bound platform organisation, existing membership users and admin resource before evaluating authority. Its exclusive organisation lock also blocks new memberships through their foreign key. Tests prove both commit orders for capability/resource/user/new-member activation; activation-first denies root mutation, while command-first completes before activation. Break-glass and unrelated-tenant writes remain available. No new state, schema or RLS policy. This is not completion of the common policy, admission or operational release requirements.

**All member target explanations.** Every access target now includes permission: client-only login admission, exact-pair authorization-code permission or resource-only direct administration. `auth/member-permission.ts` shares facts and explicit calculations with native user-resource policy, administrative grant lookup and root writer detection. Existing authority locks remain in place. Top-level scopes remain assignments; approved scopes and contributing source/revision evidence are separate. The existing capability constraint enforces the bound admin resource, so no new binding registry/check is added. Diagnostics do not establish authentication, consent or refresh approval. Common decision/audit metadata, complete lifecycle evidence, tenant SSO trust, production OAuth and other F0–F7 gates remain unfinished.

**Machine decision/audit binding.** Machine permission evaluation now returns the approved identity/scope decision and source snapshot from one SQL statement. Issuance compares its generated JWT with that decision before committing success or releasing a token. Version 2 `oauth.token.issued` records retain the decision under `data.decision`; version 1 history remains unchanged. Omitted requested scopes are recorded as null, distinct from the approved intersection. Removed duplicate caller-supplied client scope ceilings, post-mint client/resource reads and repeated audit parameters. No schema migration or new policy service. This completes the machine success-path binding, not the common member/denial decision contract, production user OAuth or remaining F0–F7 release gates.

**Restored startup proof.** The guarded local restore now runs actual production-mode startup in child processes: an owner login is refused before listening, two restricted-role starts preserve persisted platform identity/configuration and recover an existing browser session through HTTP. Startup failure closes its database pool after auth/app construction or listen failure. No schema change. Supplied test configuration and loopback HTTP do not prove production environment distribution, ingress, signing-key custody or post-snapshot reconciliation. See [the evidence](../reports/answerable-id-upstream-token-custody.md#restored-production-startup).

**Restored retention permissions.** The local rehearsal now provisions the existing maintenance role against restored functions/data and proves denied direct reads/writes, atomic rollback on audit failure, and bounded expired-payload removal without losing fresh results or permanent reservations. No schema or runtime change. Production scheduling, signing keys and post-snapshot recovery remain open. See [the retention proof](../reports/answerable-id-upstream-token-custody.md#retention-after-cluster-restoration).

**Restored signing continuity.** The local rehearsal preserves exact JWKS rows, independently verifies original/new machine tokens under the original public key and refuses wrong application-secret custody without replacing keys or recording successful issuance. No runtime/schema change; managed key rotation, production secret distribution, remote consumers and post-snapshot reconciliation remain open. See [the signing proof](../reports/answerable-id-upstream-token-custody.md#signing-keys-after-cluster-restoration).

**Application-secret ring.** Optional provider-native BETTER_AUTH_SECRETS is strictly validated and supplied through production auth configuration. The singular secret remains the legacy fallback. Native integration proves legacy/new signing custody and missing-version refusal; changing the active secret requires fresh browser sign-in in Better Auth 1.7.2. No schema or custom crypto. Coordinate promotion across replicas; legacy retirement and production delivery remain unfinished. See [the rotation contract](../reports/answerable-id-upstream-token-custody.md#application-secret-rotation).

**Statement execution limit.** Service pools now apply PostgreSQL statement_timeout, ten seconds by default with a validated positive DATABASE_STATEMENT_TIMEOUT_MS override. Server cancellation maps to existing retryable administrative/native-grant errors. Restricted-login tests prove rollback of domain/audit/receipt and same-key recovery. No schema change or application timeout race. This is per statement, not a total transaction/HTTP or tenant-concurrency bound. See [the execution evidence](../reports/answerable-id-foundation-execution-plan.md#server-side-statement-deadlines).

**Instance admission.** MAX_CONCURRENT_REQUESTS defaults to 64 active handlers, including body acquisition/authentication. Excess work gets a no-store plain-text 503 with Retry-After before database work; there is no admission queue. Bodyless liveness remains available; readiness participates. Slots survive client disconnect until the handler finishes. No schema or new infrastructure. Real HTTP and published-contract tests pass; tenant fairness, cluster limits and measured capacity remain open. See [the evidence](../reports/answerable-id-foundation-execution-plan.md#bounded-request-admission).

**Shared machine issuance admission.** Two concurrent post-authentication issuance transactions per immutable owner organisation now share PostgreSQL transaction slots across enforcing instances. Restricted-login tests use independent pools, multiple clients of A, forged tenant inputs and a successful B request; commit/rollback release both slots. No queue, lease table or schema change. This does not bound authentication, pool checkout, rejection audit or other flows and does not complete tenant fairness. See [the evidence](../reports/answerable-id-foundation-execution-plan.md#shared-machine-issuance-admission).

**Network attribution boundary.** HTTP administrative audit no longer trusts forwarding headers. New native sessions and sign-in/sign-out events use null IP; legacy session addresses are not promoted into new audit evidence. The supported empty provider IP-header list preserves native fallback rate limiting; disabling IP tracking would bypass it. Public contracts label historical addresses unverified. No schema or proxy framework was added. Verified ingress and distributed/client/IP abuse control remain unfinished; see [the evidence](../reports/answerable-id-foundation-execution-plan.md#unverified-network-attribution-removed).

**Bounded authentication audit input.** Callback rejection version 2 removes free-form upstream descriptions and maps unknown error strings to one fixed category. Locally defined federation codes share their typed vocabulary with the audit classifier. One small validator bounds HTTP/native-session user-agent metadata and rechecks legacy values before new sign-in/sign-out evidence. No schema, redaction engine or new infrastructure. These changes bound selected stored metadata; total header limits, durable transport outcomes and full trusted-ingress/rate controls remain open. See [the evidence](../reports/answerable-id-foundation-execution-plan.md#bounded-authentication-audit-input).

**Platform membership writer invariant.** The issued membership command runner now preserves an existing effective platform writer using the already-held exclusive organisation lock, persisted platform/resource binding and the existing member permission evaluator. Refused removal/window changes roll back domain, audit and receipt; replacement authority permits a same-key retry. Concurrent removals retain one writer. Other tenants skip the global writer evaluation, and an already writerless state can be repaired. No new schema, lock service or policy evaluator. Remaining authority writers, future expiry/freshness and production admission are not covered by this slice. See [the evidence](../reports/answerable-id-foundation-execution-plan.md#last-platform-membership-writer).


**Global-user writer invariant.** User disable and erasure now preserve an existing effective platform writer, including a membership committed while waiting for the user lock. Restricted-role HTTP tests prove rollback, replacement/same-key recovery, replay, concurrent commands and unrelated-user progress. Reuse the existing policy evaluator and user lock; acquire the bound platform lock without waiting to avoid inverted lock-order waits. No schema or general guard framework. Other authority-changing writers, future expiry/freshness and production admission remain open. See [the evidence](../reports/answerable-id-foundation-execution-plan.md#last-platform-writer-during-global-user-changes).


**Group and entitlement writer protection.** Group disable/erasure, assignment removal/window changes and entitlement disable/removal/scope/window changes now preserve an existing effective platform writer. The shared before/after check runs under the existing organisation lock; a refusal rolls back rows, revisions, successful audit effects and the operation receipt. Establish a replacement and retry the same key/input and original precondition. Mixed concurrent restrictions retain one writer; unrelated tenants skip the platform evaluation, and an initially writerless platform can be repaired. Organisation disable and bound-resource scope/lifecycle protection are described below; future expiry and authentication availability remain separate concerns.


**Platform organisation and resource safeguards.** Organisation disable and bound admin-resource scope changes preserve existing effective platform-write authority, returning `409 last_platform_administrator` with transaction rollback when they would remove it. Adding another writer in that same organisation cannot make disabling the whole organisation safe; removing `platform:write` from the shared admin resource vocabulary also affects every writer. Safe scope restrictions retain authority and support ordinary replay. Scope writes acquire the platform lock before the resource lock; unrelated targets avoid it. Resource disable/erasure protection uses the persisted resource UUID, independently of configured names. Recovery can re-enable an already disabled platform organisation. These checks concern current effective permission, not future expiry or upstream availability.


**Group erasure audit (migration 0038).** New `group.erased` events use schema version 2. Alongside group before/after state, `data.effects.removedAssignments` records actual deleted assignment IDs, revisions, tenant/group/member/user UUIDs and validity windows. `removedEntitlements` records actual deleted entitlement IDs/revisions, targets, scopes, status and windows. Expired, disabled and future rows remain part of the evidence. Affected-user UUID history survives later user erasure; another tenant's policy is unaffected. The deletion, event, subject references and operation receipt commit or roll back together. Authorised replay does not repeat them. Apply migration 0038 before deploying the new writer. Older version-one group events lack this complete manifest and are not backfilled from guesses. These are removed policy sources, not a computed proof that every affected user lost all effective access or that remote sessions ended. Maximum supported synchronous deletion size remains a release requirement.


**Assignment history under user erasure.** Restricted-role HTTP concurrency tests prove both commit orders for group-assignment removal and global user erasure. Assignment-first retains its affected-user audit subject before the user cascade can commit; erased-user history and replay remain available. User-first makes the waiting removal return `404` without a false removal event. The existing assignment row lock, cascade and audit transaction provide this ordering; no additional runtime mechanism or schema change is needed.


**Direct member-entitlement history (migration 0039).** New successful version-one entitlement create, update, enable, disable, unchanged and removal events capture the affected user UUID in the existing durable subject index. Capture validates the recorded entitlement/tenant identity and resolves its membership during the command transaction; later history reads do not join live identities. The inserted or locked entitlement prevents concurrent parent erasure from committing its cascade before capture. Erasure-first removal returns 404 without a removal event. Subject-write failure rolls back the domain change and receipt; authorised replay adds no duplicate successful fact. Event payloads and public response shapes are unchanged. Apply migration 0039 to enable capture; earlier missing subjects are not backfilled from current memberships. This covers direct member targets, not complete group/organisation entitlement impact or named identity recovery.


**Group status evidence (migration 0040).** Successful `group.disabled` and `group.enabled` events now use schema version 2. `data.policySources.assignments` records assignment IDs/revisions, tenant/group/member/user UUIDs and validity windows; `data.policySources.entitlements` records entitlement IDs/revisions, targets, scopes, status and windows. The existing before/after group fields show the status transition. These source rows are retained, not deleted, and include ineligible policy. They do not prove that every member lost effective access or that a remote session ended. Source capture holds child share locks through event/receipt commit, ordering concurrent user erasure. Recorded user history survives subsequent erasure. Unchanged commands retain version 1 without a source snapshot. Subject failure rolls back the status, revision and operation receipt; replay does not duplicate evidence. Apply migration 0040 before deploying the new version-two writers. Earlier events are not backfilled with inferred sources. Large-group workload bounds and complete effective-permission deltas remain release work.


**Broad entitlement audience (migration 0041).** Changed organisation- and group-targeted entitlement create/update/enable/disable/removal events use schema version 2. `data.audience` retains the tenant-local membership UUID, user UUID, revision, status and validity window. Group targets also retain the assignment UUID/revision/group UUID/window; organisation targets use `groupAssignment: null`. Source rows are included even when ineligible. This is the recorded audience, not an effective-permission delta or evidence of remote revocation. Capture shares the command transaction and share-locks selected memberships/assignments against concurrent erasure. User history survives deletion through the existing durable subject index. Subject failure rolls back mutation and receipt; replay adds no new successful event. Direct-member events and unchanged commands keep version 1; broad unchanged commands omit the audience snapshot. Apply migration 0041 before deploying the version-two writers. Existing events are not backfilled from later membership. Measured audience/workload bounds and complete permission-decision evidence remain release requirements.


**Measured audit workloads.** The [local workload report](../reports/answerable-id-audit-workloads.md) records 100/1,000/10,000-member synthetic HTTP observations. Three eight-command bursts targeting the largest tenant each caused a second tenant's read to return 503; the later runs identify `database_busy` and observe queued runtime-pool checkouts. Original-key recovery preserves exactly one successful fact per target and all affected-user references. This demonstrates an unresolved shared-capacity gap under the measured single-instance/four-connection topology. It does not set a supported tenant-size limit or prove production capacity. Tenant admission/checkout fairness and measured lifecycle bounds remain release requirements.


**Tenant command admission.** Journalled administrative commands with an `organizationId` path parameter admit at most two outstanding commands per target organisation per runtime database pool. Excess commands return `503 database_busy`, `retryable: true` and `Retry-After: 1` before journal checkout, without a new operation receipt. Retry the same key and input. The bound is shared by app/database wrappers using the same pool; slots release after command completion or rollback. Current authority is still checked inside the admitted transaction. This starts after route authentication and validation; global commands, authentication, reads and other pre-admission work are outside it. It is not a deployment-wide quota or a guarantee of spare capacity with pools of two connections or fewer.


**Organisation erasure evidence (migration 0042).** New `organization.erased` events use schema version 2. `data.effects` records actual `removedMembers`, `removedGroups`, `removedAssignments`, `removedEntitlements`, `removedCapabilities`, `removedDomains`, `removedSsoProviders` and `removedInvitations`. Policy fields include immutable IDs, applicable revisions, status, scope/target and validity windows; provider credentials and invitation email are excluded. `clearedSessionSelections` records session/user UUIDs whose `activeOrganizationId` was cleared. Those browser sessions and their immutable authentication origin remain; this is not session revocation. `deletedGrantContexts` remains alongside the manifest and includes each stored grant ID, user ID and organisation ID. Durable affected-user references come from removed memberships, cleared session selections and deleted grants, so members without issued grants retain history after erasure. The query returns actual deleted/updated rows within the same transaction as the event, subjects and operation receipt. Subject-write failure rolls everything back; authorised same-key replay adds no successful effect. Apply migration 0042 before deploying the version-two writer. Earlier events are not reconstructed from current state. This is local configuration/effect evidence, not a complete effective-permission calculation, named-person retention promise or proof of remote revocation. Supported large-tenant erasure bounds remain release work.


**Global user erasure evidence (migration 0043).** New `user.erased` events use schema version 2. Alongside the existing `deletedGrantContexts`, `data.effects` records `removedMembers`, `removedAssignments`, `removedEntitlements`, `deletedAccounts`, `deletedSessions`, `deletedInvitations`, `deletedClients`, `deletedClientResources`, `deletedAccessTokens`, `deletedRefreshTokens` and `deletedConsents`. The manifest includes direct user effects and cascades through owned clients/refresh tokens, which can affect other users. Membership/policy rows retain tenant IDs, applicable revisions, targets, scopes, status and windows. Account records retain only internal account/user UUIDs; credentials, account linkage identifiers, invitation email and provider configuration are excluded. `clearedAccessTokenSessions` and `clearedRefreshTokenSessions` record surviving token IDs/users/clients with their exact `beforeSessionId` and null `afterSessionId`. Those rows survive; clearing a session reference is not token revocation. `detachedSsoProviders` records provider/tenant IDs and actual before/after user attribution and revision. The existing trigger increments the revision when that attribution is cleared; this event does not claim a separate revocation of that tenant's grants. Affected-user subjects from the explicit grant/token/consent effects survive erasure. External entitlement/capability references to an owned client still return `409 reference_violation` and roll back the operation; resolve them deliberately before retrying the same key. The mutation, effect event, subjects and receipt commit together, and replay adds no successful effect. Apply migration 0043 before deploying the new writer. Older events are not backfilled from current rows. This is local deletion/reference-change evidence, not a complete effective-permission snapshot, named-person retention promise or remote-session revocation proof. Representative global-erasure size/capacity bounds remain release work.

**Membership access observations.** Window changes, removal and reinstatement now emit version 2 events with the existing member-access projection under `before.access` and `after.access`. Both reads share the command transaction and tenant lock, and successful decision evidence carries evaluation time, immutable identity, revisions and approved scopes. Assignment scopes remain distinct from permitted scopes. Reinstatement may regain organisation-wide access without restoring removed direct assignments. These separate statement observations do not isolate causality across time or concurrent endpoint changes and do not prove remote revocation. Changed and unchanged removal/reinstatement events use version 2. Removal retains its actual deleted assignments and revoked grant contexts alongside the access observations; a surviving organisation-wide assignment does not make revoked membership effective. Audit/subject failure rolls back the mutation and receipt; replay does not duplicate the event. No schema migration or historical backfill.

**Dense member access projection.** Member-access queries now group matching assignments by target before projecting policy facts, preserving the existing response and evaluator while removing repeated source arrays. A restricted local one-resource workload that previously timed out at 1,000 group assignments per member now completes and replays with every source retained. This establishes that fixture, not supported production cardinality or latency limits. See [workload evidence](/Users/anthonyriera/code/answerable/reports/answerable-id-audit-workloads.md#dense-member-access-evidence).

**Client-resource audit visibility.** Version-two link/unlink/unchanged events retain resource instance UUID, immutable classification and owner alongside actual link before/after state. Shared targets and the client owner's private targets keep owner attribution; foreign-private and unknown targets are platform-only. Unlink reads the target under the existing client→resource lock order before mutation; a missing target still yields its existing no-op result. Organisation history excludes unrecognised/legacy versions of these three link actions before pagination; the platform endpoint retains all original events. No history rewrite, live-target inference or migration. Deploy the updated readers and writers together; an older serving reader does not enforce this filter. See [restricted HTTP proof](/Users/anthonyriera/code/answerable/apps/id/src/http/admin/client-resource-audit.integration.test.ts).


**Unattributed machine authentication attempts.** The token handler now includes native client authentication in its rejection boundary. New `oauth.token.rejected` version 3 events use system actor `oauth-client-authentication`, null organisation, null authenticated client and null decision. Fixed reasons distinguish credential denial from authentication failure; no supplied client/tenant, resource, scopes, credentials or exception details are retained. Authenticated version 2 events are unchanged. Audit-storage failure keeps the original refusal and emits the existing fixed operational signal. This covers authentication reached inside the machine token handler, not earlier parser/allowlist/rate-limit refusal, and does not establish production audit-write capacity.


**Disabled-user reconciliation.** A new disable command always removes remaining browser sessions and revokes remaining stored tokens and grant contexts, even if the user is already disabled. The operation is applied when any local effect occurs; otherwise it is a no-op. Existing user status timestamps are preserved when status does not change. Replaying an old key returns that command’s original result and does not reconcile later state; use a new key for a new reconciliation. Audit failure rolls back all local effects and the receipt. Token effects now retain exact row IDs as described below; local cleanup is not proof of remote session termination.


**Token revocation evidence (migration 0044).** New `user.disabled`, `user.disable_unchanged`, `session.revoked` and `session.revoked_all` events use schema version 2 and retain `data.revokedTokens.access` and `data.revokedTokens.refresh`. Each entry contains the exact changed token row's `id` and `userId`; machine access tokens have a null user. The existing counts remain. These records come from `UPDATE ... RETURNING`, exclude already-revoked and unrelated rows, and contain no token value, scope or credential. Client disable and secret rotation retain this manifest alongside `grantContexts` in platform-only `client.grants_revoked` version 2. The client owner's event retains counts and `grantEffectsEventId`, excluding individual users and tokens. Migration 0044 captures affected users from that explicit contract so history remains queryable by UUID after live rows are erased. Apply it before the new writer; older events are not backfilled. Mutation, subjects, event and receipt commit together, and historical replay adds no revocation. This proves local stored-token revocation, not termination of downstream sessions or already-issued self-contained tokens. Token fan-out capacity remains release work.


**Client erasure evidence (migration 0045).** Standalone client erasure now captures `deletedAccessTokens`, `deletedRefreshTokens`, `deletedConsents` and `deletedClientResources` under `data.effects` in platform-only `client.grants_erased` version 2, alongside deleted grant contexts. It includes already-revoked records and access tokens reached through a deleted refresh token, even when their client field differs. Safe projections retain row/user/client/session/refresh references and applicable scopes, resources, expiry and revocation state; token values, secrets and consent claim requests are excluded. Refresh parents are locked before the access-token deletion statement so an insert committed while erasure waits is included in its later snapshot. The owning tenant's `client.erased` version 2 contains only effect counts plus `grantEffectsEventId`; foreign resource and user details remain restricted. Migration 0045 preserves affected-user UUID history after erasure without rewriting old events. Apply it before the writer. The command, both events, subjects and receipt remain atomic. This does not delete users or browser sessions, establish named-person retention, prove remote-session termination or establish a supported token fan-out limit.


**Global user erasure ordering.** Before capturing grant or cascade effects, the command now locks the user's owned clients, memberships and sessions in deterministic row order, followed by refresh tokens owned by that user or those clients. The existing user lock prevents new direct children. Separate child-capture statements then include writes committed while parent acquisition waited. Restricted HTTP regressions cover owned-client token/grant insertion, cross-user refresh-token cascades, session-reference clearing on a surviving token, and membership assignment deletion. No event version or schema changes: this corrects completeness of the existing v2 manifests. Parent locks and captured effects roll back with the command; same-key recovery does not repeat erasure. This is local ordering evidence, not production capacity or remote-session termination proof.


**Public authentication admission.** `/auth/*` requests now share a pre-body, pre-credential concurrency bound across app and Drizzle wrappers using the same PostgreSQL pool. The bound is `max(1, actual pool maximum − 1)`: a pool with at least two connections admits fewer authentication requests than its connection count. A one-connection pool still accepts one authentication request. This counts handlers, not individual connection checkouts. Excess requests receive the existing plain-text `503 Service Busy`, `Retry-After: 1`, `Cache-Control: no-store` and request ID; their body is cancelled and the provider is not called. Capacity remains held until the handler and its audit work finish, including after disconnect. Admitted token failures retain their usual rejection audit; requests refused before authentication create no token-attempt event. This does not identify a tenant from an unverified claim, allocate a dedicated database connection, bound admin-route authentication or provide cluster-wide rate limits. Other work can still consume shared connections; legitimate authentication can be refused when this lane is occupied.

**Administrative signing-key lookup.** Each administrative JWT resolver shares one in-flight JWKS load across concurrent verifications. A cold or expired cache loads once and rejects an absent key without immediately loading again. A missing key in a previously cached set still triggers one refresh for rotation; callers reuse a newer set if another verification already refreshed it. Existing cached keys remain usable during that refresh. Failed loads reject their waiting callers and allow a later request to retry. The existing five-minute cache lifetime is unchanged. This bounds concurrent store loads per resolver, not sequential invalid traffic or deployment-wide database use.

**Grant-context isolation (migration 0046).** Direct grant-context access now uses RLS. Unscoped runtime queries see no rows and cannot insert grants. Tenant readers/writers are confined to that organisation; platform readers/writers and global `platform:users` commands retain their required visibility. Only platform-write scope permits direct deletion. Grant creation uses a scope bound to the authenticated user/session; the existing provenance trigger and admission checks still apply. Native token work establishes client scope after client authentication, then uses the existing policy-user scope for evaluation and restores the client scope afterwards. Diagnostic policy-user scope cannot mutate grants. Transaction scopes clear client/session fields on switches and restore all fields across nested success or rollback. These settings are issued by trusted application code; this is not protection against arbitrary SQL by a compromised runtime, and global parent tables/cascades remain explicit exceptions.

The native user-grant integration proof now uses a real restricted login and a one-connection pool, including code/refresh/replay, revocation and failure paths. Public production user OAuth remains closed; the proof wrapper is not the production provider, and selected-tenant SSO trust and successful issuance audit integration remain unfinished.

Deploy migration 0046 and the new scope-aware writers in a coordinated maintenance window, with old writers stopped. An old unscoped global-user command could otherwise miss grant revocation under RLS; mixed versions are unsupported. New production startup refuses a database with grant-context RLS disabled. Do not roll back to an old writer against this schema without a reviewed coordinated database/application rollback. No production migration has been run here.

**Administrative denial audit outages.** If storing an already-decided `admin.denied` or locked-root refusal fails, the request retains its original 403/404 response and authentication challenge. It still does not reach the protected handler. The service emits `admin_denial_audit_unavailable` with the request ID only; no database exception, credential or event payload is logged by this signal. No durable event is claimed for the failed write, and it is not replayed later. Healthy storage continues to record each refusal. Successful root admission and mutation audit writes remain mandatory: their failure still prevents admission or rolls back the command. This changes neither response schemas nor the journal/replay contract, and does not add evidence for administrative authentication failures that were previously unaudited.
