---
title: Answerable ID
description: "One sign-in for every Answerable app and MCP server, backed by your organisation's own directory."
---

## What it does

1. **Identity broker.** Connect your organisation's Microsoft Entra ID or Google Workspace directory once.
2. **Login provider for Answerable apps.** Not yet. Apps will use standard OpenID Connect (OIDC).
3. **Authorization server for hosted MCP servers.** Not yet. MCP servers will use OAuth 2.1.

## Who it is for

**IT admins.** Give admin consent once. You do not need a registration for each app or client secrets to rotate.

**Organisation admins.** Read your organisation's members, groups, access and audit log through the [admin API](/docs/id/manage); changes beyond members: [Not yet.](/docs/id#today-and-next)

**Answerable staff.** Administer organisations, users and access through the same API with platform scopes: [Onboard an organisation](/docs/id/onboard).

**People at client organisations.** Sign in with the work account you already have. Guest and personal accounts are refused.

**Developers.** Not yet. Standard OIDC for apps and OAuth 2.1 for MCP servers come after organisation sign-in.

## The one rule

`sub` is the only identifier to join on. Email is display data.

## Today and next

| Order | Capability                                                                                                                                                 |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Today | Organisation sign-in through your identity provider, sessions, health endpoints, the admin API (platform tier), and machine access with client credentials |
| Next  | OIDC for apps, then entitlements at every grant, then OAuth 2.1 for MCP servers, then self-service                                                         |

This is an order, not a timeline.

## Endpoints

The base URL is `https://id.answerable.org`.

| Endpoint             | Purpose                                     |
| -------------------- | ------------------------------------------- |
| `/auth`              | Better Auth sign-in and session routes      |
| `/auth/oauth2/token` | Machine tokens through `client_credentials` |
| `/api/admin/v1`      | [Admin API](/docs/id/admin-api)             |
| `/healthz`           | Liveness                                    |
| `/readyz`            | Readiness                                   |

Start an organisation sign-in from the [sign-in page](https://answerable.org/login), or read [how sign-in works](/docs/id/sign-in).
